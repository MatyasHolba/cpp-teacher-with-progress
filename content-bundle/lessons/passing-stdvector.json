{
  "index": 215,
  "chapterId": "16",
  "number": "16.4",
  "title": "16.4 — Passing std::vector",
  "url": "https://www.learncpp.com/cpp-tutorial/passing-stdvector/",
  "slug": "passing-stdvector",
  "scrapedAt": "2026-09-15T16:30:27.638Z",
  "totalBlocks": 40,
  "totalCheckpoints": 35,
  "blocks": [
    {
      "id": "passing-stdvector_b0",
      "type": "paragraph",
      "html": "<p>An object of type <code>std::vector</code> can be passed to a function just like any other object. That means if we pass a <code>std::vector</code> by value, an expensive copy will be made. Therefore, we typically pass <code>std::vector</code> by (const) reference to avoid such copies.</p>",
      "preview": "An object of type std::vector can be passed to a function just like any other ob",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b1",
      "type": "paragraph",
      "html": "<p>With a <code>std::vector</code>, the element type is part of the type information of the object. Therefore, when we use a <code>std::vector</code> as a function parameter, we have to explicitly specify the element type:</p>",
      "preview": "With a std::vector, the element type is part of the type information of the obje",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b2",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <vector>\n\nvoid passByRef(const std::vector<int>& arr) // we must explicitly specify <int> here\n{\n    std::cout << arr[0] << '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 };\n    passByRef(primes);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;vector&gt;\n\nvoid passByRef(const std::vector&lt;int&gt;&amp; arr) // we must explicitly specify &lt;int&gt; here\n{\n    std::cout &lt;&lt; arr[0] &lt;&lt; '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 };\n    passByRef(primes);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b3",
      "type": "section",
      "title": "Passing std::vector of different element types",
      "html": "<h3 class=\"cpp-section-heading\">Passing <code>std::vector</code> of different element types</h3>",
      "canCheck": false
    },
    {
      "id": "passing-stdvector_b4",
      "type": "paragraph",
      "html": "<p>Because our <code>passByRef()</code> function expects a <code>std::vector&lt;int&gt;</code>, we are unable to pass vectors with different element types:</p>",
      "preview": "Because our passByRef() function expects a std::vector<int>, we are unable to pa",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b5",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <vector>\n\nvoid passByRef(const std::vector<int>& arr)\n{\n    std::cout << arr[0] << '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 };\n    passByRef(primes);  // ok: this is a std::vector<int>\n\n    std::vector dbl{ 1.1, 2.2, 3.3 };\n    passByRef(dbl); // compile error: std::vector<double> is not convertible to std::vector<int>\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;vector&gt;\n\nvoid passByRef(const std::vector&lt;int&gt;&amp; arr)\n{\n    std::cout &lt;&lt; arr[0] &lt;&lt; '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 };\n    passByRef(primes);  // ok: this is a std::vector&lt;int&gt;\n\n    std::vector dbl{ 1.1, 2.2, 3.3 };\n    passByRef(dbl); // compile error: std::vector&lt;double&gt; is not convertible to std::vector&lt;int&gt;\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b6",
      "type": "paragraph",
      "html": "<p>In C++17 or newer, you might try to use CTAD to solve this problem:</p>",
      "preview": "In C++17 or newer, you might try to use CTAD to solve this problem:",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b7",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <vector>\n\nvoid passByRef(const std::vector& arr) // compile error: CTAD can't be used to infer function parameters\n{\n    std::cout << arr[0] << '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 }; // okay: use CTAD to infer std::vector<int>\n    passByRef(primes);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;vector&gt;\n\nvoid passByRef(const std::vector&amp; arr) // compile error: CTAD can't be used to infer function parameters\n{\n    std::cout &lt;&lt; arr[0] &lt;&lt; '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 }; // okay: use CTAD to infer std::vector&lt;int&gt;\n    passByRef(primes);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b8",
      "type": "paragraph",
      "html": "<p>Although CTAD will work to deduce an vector’s element type from initializers when it is defined, CTAD doesn’t (currently) work with function parameters.</p>",
      "preview": "Although CTAD will work to deduce an vector’s element type from initializers whe",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b9",
      "type": "paragraph",
      "html": "<p>We’ve seen this kind of problem before, where we have overloaded functions that only differ by the parameter type. This is a great place to make use of function templates! We can create a function template that parameterizes the element type, and then C++ will use that function template to instantiate functions with actual types.</p>",
      "preview": "We’ve seen this kind of problem before, where we have overloaded functions that ",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b10",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>We cover function templates in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/function-templates/\">11.6 -- Function templates</a>.</p></div>",
      "preview": "Related contentWe cover function templates in lesson 11.6 -- Function templates.",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b11",
      "type": "paragraph",
      "html": "<p>We can create a function template that uses the same template parameter declaration:</p>",
      "preview": "We can create a function template that uses the same template parameter declarat",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b12",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <vector>\n\ntemplate <typename T>\nvoid passByRef(const std::vector<T>& arr)\n{\n    std::cout << arr[0] << '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 };\n    passByRef(primes); // ok: compiler will instantiate passByRef(const std::vector<int>&)\n\n    std::vector dbl{ 1.1, 2.2, 3.3 };\n    passByRef(dbl);    // ok: compiler will instantiate passByRef(const std::vector<double>&)\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;vector&gt;\n\ntemplate &lt;typename T&gt;\nvoid passByRef(const std::vector&lt;T&gt;&amp; arr)\n{\n    std::cout &lt;&lt; arr[0] &lt;&lt; '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 };\n    passByRef(primes); // ok: compiler will instantiate passByRef(const std::vector&lt;int&gt;&amp;)\n\n    std::vector dbl{ 1.1, 2.2, 3.3 };\n    passByRef(dbl);    // ok: compiler will instantiate passByRef(const std::vector&lt;double&gt;&amp;)\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b13",
      "type": "paragraph",
      "html": "<p>In the above example, we’ve created a single function template named <code>passByRef()</code> that has a parameter of type <code>const std::vector&lt;T&gt;&amp;</code>. <code>T</code> is defined in the template parameter declaration on the previous line: <code>template &lt;typename T</code>. <code>T</code> is a standard type template parameter that allows the caller to specify the element type.</p>",
      "preview": "In the above example, we’ve created a single function template named passByRef()",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b14",
      "type": "paragraph",
      "html": "<p>Therefore, when we call <code>passByRef(primes)</code> from <code>main()</code> (where <code>primes</code> is defined as a <code>std::vector&lt;int&gt;</code>), the compiler will instantiate and call <code>void passByRef(const std::vector&lt;int&gt;&amp; arr)</code>.</p>",
      "preview": "Therefore, when we call passByRef(primes) from main() (where primes is defined a",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b15",
      "type": "paragraph",
      "html": "<p>When we call <code>passByRef(dbl)</code> from <code>main()</code> (where <code>dbl</code> is defined as a <code>std::vector&lt;double&gt;</code>), the compiler will instantiate and call <code>void passByRef(const std::vector&lt;double&gt;&amp; arr)</code>.</p>",
      "preview": "When we call passByRef(dbl) from main() (where dbl is defined as a std::vector<d",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b16",
      "type": "paragraph",
      "html": "<p>Thus, we’ve created a single function template that can instantiate functions to handle <code>std::vector</code> arguments of any element type and length!</p>",
      "preview": "Thus, we’ve created a single function template that can instantiate functions to",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b17",
      "type": "section",
      "title": "Passing a std::vector using a generic template or abbreviated function template",
      "html": "<h3 class=\"cpp-section-heading\">Passing a <code>std::vector</code> using a generic template or abbreviated function template</h3>",
      "canCheck": false
    },
    {
      "id": "passing-stdvector_b18",
      "type": "paragraph",
      "html": "<p>We can also create a function template that will accept any type of object:</p>",
      "preview": "We can also create a function template that will accept any type of object:",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b19",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <vector>\n\ntemplate <typename T>\nvoid passByRef(const T& arr) // will accept any type of object that has an overloaded operator[]\n{\n    std::cout << arr[0] << '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 };\n    passByRef(primes); // ok: compiler will instantiate passByRef(const std::vector<int>&)\n\n    std::vector dbl{ 1.1, 2.2, 3.3 };\n    passByRef(dbl);    // ok: compiler will instantiate passByRef(const std::vector<double>&)\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;vector&gt;\n\ntemplate &lt;typename T&gt;\nvoid passByRef(const T&amp; arr) // will accept any type of object that has an overloaded operator[]\n{\n    std::cout &lt;&lt; arr[0] &lt;&lt; '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 };\n    passByRef(primes); // ok: compiler will instantiate passByRef(const std::vector&lt;int&gt;&amp;)\n\n    std::vector dbl{ 1.1, 2.2, 3.3 };\n    passByRef(dbl);    // ok: compiler will instantiate passByRef(const std::vector&lt;double&gt;&amp;)\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b20",
      "type": "paragraph",
      "html": "<p>In C++20, we can use an abbreviated function template (via an <code>auto</code> parameter) to do the same thing:</p>",
      "preview": "In C++20, we can use an abbreviated function template (via an auto parameter) to",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b21",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <vector>\n\nvoid passByRef(const auto& arr) // abbreviated function template\n{\n    std::cout << arr[0] << '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 };\n    passByRef(primes); // ok: compiler will instantiate passByRef(const std::vector<int>&)\n\n    std::vector dbl{ 1.1, 2.2, 3.3 };\n    passByRef(dbl);    // ok: compiler will instantiate passByRef(const std::vector<double>&)\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;vector&gt;\n\nvoid passByRef(const auto&amp; arr) // abbreviated function template\n{\n    std::cout &lt;&lt; arr[0] &lt;&lt; '\\n';\n}\n\nint main()\n{\n    std::vector primes{ 2, 3, 5, 7, 11 };\n    passByRef(primes); // ok: compiler will instantiate passByRef(const std::vector&lt;int&gt;&amp;)\n\n    std::vector dbl{ 1.1, 2.2, 3.3 };\n    passByRef(dbl);    // ok: compiler will instantiate passByRef(const std::vector&lt;double&gt;&amp;)\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b22",
      "type": "paragraph",
      "html": "<p>Both of these will accept an argument of <em>any</em> type that will compile. This can be desirable when writing functions that we might want to operate on more than just a <code>std::vector</code>. For example, the above functions will also work on a <code>std::array</code>, a <code>std::string</code>, or some other type we may not have even considered.</p>",
      "preview": "Both of these will accept an argument of any type that will compile. This can be",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b23",
      "type": "paragraph",
      "html": "<p>The potential downside of this method is that it may lead to bugs if the function is passed an object of a type that compiles but doesn’t make sense semantically.</p>",
      "preview": "The potential downside of this method is that it may lead to bugs if the functio",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b24",
      "type": "section",
      "title": "Asserting on array length",
      "html": "<h3 class=\"cpp-section-heading\">Asserting on array length</h3>",
      "canCheck": false
    },
    {
      "id": "passing-stdvector_b25",
      "type": "paragraph",
      "html": "<p>Consider the following template function, which is similar to the one presented above:</p>",
      "preview": "Consider the following template function, which is similar to the one presented ",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b26",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <vector>\n\ntemplate <typename T>\nvoid printElement3(const std::vector<T>& arr)\n{\n    std::cout << arr[3] << '\\n';\n}\n\nint main()\n{\n    std::vector arr{ 9, 7, 5, 3, 1 };\n    printElement3(arr);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;vector&gt;\n\ntemplate &lt;typename T&gt;\nvoid printElement3(const std::vector&lt;T&gt;&amp; arr)\n{\n    std::cout &lt;&lt; arr[3] &lt;&lt; '\\n';\n}\n\nint main()\n{\n    std::vector arr{ 9, 7, 5, 3, 1 };\n    printElement3(arr);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b27",
      "type": "paragraph",
      "html": "<p>While <code>printElement3(arr)</code> works fine in this case, there’s a potential bug waiting for a unwary programmer in this program. See it?</p>",
      "preview": "While printElement3(arr) works fine in this case, there’s a potential bug waitin",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b28",
      "type": "paragraph",
      "html": "<p>The above program prints the value of the array element with index 3. This is fine as long as the array has a valid element with index 3. However, the compiler will happily let you pass in arrays where index 3 is out of bounds. For example:</p>",
      "preview": "The above program prints the value of the array element with index 3. This is fi",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b29",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <vector>\n\ntemplate <typename T>\nvoid printElement3(const std::vector<T>& arr)\n{\n    std::cout << arr[3] << '\\n';\n}\n\nint main()\n{\n    std::vector arr{ 9, 7 }; // a 2-element array (valid indexes 0 and 1)\n    printElement3(arr);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;vector&gt;\n\ntemplate &lt;typename T&gt;\nvoid printElement3(const std::vector&lt;T&gt;&amp; arr)\n{\n    std::cout &lt;&lt; arr[3] &lt;&lt; '\\n';\n}\n\nint main()\n{\n    std::vector arr{ 9, 7 }; // a 2-element array (valid indexes 0 and 1)\n    printElement3(arr);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b30",
      "type": "paragraph",
      "html": "<p>This leads to undefined behavior.</p>",
      "preview": "This leads to undefined behavior.",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b31",
      "type": "paragraph",
      "html": "<p>One option here is to assert on <code>arr.size()</code>, which will catch such errors when run in a debug build configuration. Because <code>std::vector::size()</code> is a non-constexpr function, we can only do a runtime assert here.</p>",
      "preview": "One option here is to assert on arr.size(), which will catch such errors when ru",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b32",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Tip</p><p>A better option is to avoid using <code>std::vector</code>in cases where you need to assert on array length. Using a type that supports <code>constexpr</code> arrays (e.g. <code>std::array</code>) is probably a better choice, as you can <code>static_assert</code> on the length of a constexpr array. We cover this in future lesson <a href=\"https://www.learncpp.com/cpp-tutorial/passing-and-returning-stdarray/\">17.3 -- Passing and returning std::array</a>.</p></div>",
      "preview": "TipA better option is to avoid using std::vectorin cases where you need to assert on array length. U",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b33",
      "type": "paragraph",
      "html": "<p>The best option is to avoid writing functions that rely on the user passing in a vector with a minimum length in the first place.</p>",
      "preview": "The best option is to avoid writing functions that rely on the user passing in a",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b34",
      "type": "section",
      "title": "Quiz time",
      "html": "<h3 class=\"cpp-section-heading\">Quiz time</h3>",
      "canCheck": false
    },
    {
      "id": "passing-stdvector_b35",
      "type": "quiz",
      "questionTitle": "Question #1",
      "questionHtml": "Write a function that takes two parameters: a <code>std::vector</code> and an index. If the index is out of bounds, print an error. If the index is in bounds, print the value of the element.",
      "hintHtml": "",
      "solutionHtml": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;vector&gt;\n\n// index needs to be an int, not a std::size_t, otherwise we won't be able to detect if the user passes in a negative index\ntemplate &lt;typename T&gt;\nvoid printElement(const std::vector&lt;T&gt;&amp; arr, int index)\n{\n    if (index &lt; 0 || index &gt;= static_cast&lt;int&gt;(arr.size())) // In C++20, could use std::ssize(arr) to avoid the cast\n        std::cout &lt;&lt; \"Invalid index\\n\";\n    else\n        std::cout &lt;&lt; \"The element has value \" &lt;&lt; arr[static_cast&lt;std::size_t&gt;(index)] &lt;&lt; '\\n';  \n}\n\nint main()\n{\n    std::vector v1 { 0, 1, 2, 3, 4 };\n    printElement(v1, 2);\n    printElement(v1, 5);\n\n    std::vector v2 { 1.1, 2.2, 3.3 };\n    printElement(v2, 0);\n    printElement(v2, -1);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b36",
      "type": "paragraph",
      "html": "<p>The following sample program should compile:</p>",
      "preview": "The following sample program should compile:",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b37",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <vector>\n\n// Write your printElement function here\n\nint main()\n{\n    std::vector v1 { 0, 1, 2, 3, 4 };\n    printElement(v1, 2);\n    printElement(v1, 5);\n\n    std::vector v2 { 1.1, 2.2, 3.3 };\n    printElement(v2, 0);\n    printElement(v2, -1);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;vector&gt;\n\n// Write your printElement function here\n\nint main()\n{\n    std::vector v1 { 0, 1, 2, 3, 4 };\n    printElement(v1, 2);\n    printElement(v1, 5);\n\n    std::vector v2 { 1.1, 2.2, 3.3 };\n    printElement(v2, 0);\n    printElement(v2, -1);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b38",
      "type": "paragraph",
      "html": "<p>and produce the following result:</p>",
      "preview": "and produce the following result:",
      "canCheck": true
    },
    {
      "id": "passing-stdvector_b39",
      "type": "output",
      "text": "The element has value 2\nInvalid index\nThe element has value 1.1\nInvalid index\n",
      "html": "<pre>The element has value 2\nInvalid index\nThe element has value 1.1\nInvalid index\n</pre>",
      "canCheck": false
    }
  ]
}