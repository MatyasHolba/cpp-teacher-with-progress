{
  "index": 155,
  "chapterId": "12",
  "number": "12.6",
  "title": "12.6 — Pass by const lvalue reference",
  "url": "https://www.learncpp.com/cpp-tutorial/pass-by-const-lvalue-reference/",
  "slug": "pass-by-const-lvalue-reference",
  "scrapedAt": "2026-09-15T16:29:56.902Z",
  "totalBlocks": 65,
  "totalCheckpoints": 59,
  "blocks": [
    {
      "id": "pass-by-const-lvalue-reference_b0",
      "type": "paragraph",
      "html": "<p>Unlike a reference to non-const (which can only bind to modifiable lvalues), a reference to const can bind to modifiable lvalues, non-modifiable lvalues, and rvalues. Therefore, if we make a reference parameter const, then it will be able to bind to any type of argument:</p>",
      "preview": "Unlike a reference to non-const (which can only bind to modifiable lvalues), a r",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b1",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nvoid printRef(const int& y) // y is a const reference\n{\n    std::cout << y << '\\n';\n}\n\nint main()\n{\n    int x { 5 };\n    printRef(x);   // ok: x is a modifiable lvalue, y binds to x\n\n    const int z { 5 };\n    printRef(z);   // ok: z is a non-modifiable lvalue, y binds to z\n\n    printRef(5);   // ok: 5 is rvalue literal, y binds to temporary int object\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nvoid printRef(const int&amp; y) // y is a const reference\n{\n    std::cout &lt;&lt; y &lt;&lt; '\\n';\n}\n\nint main()\n{\n    int x { 5 };\n    printRef(x);   // ok: x is a modifiable lvalue, y binds to x\n\n    const int z { 5 };\n    printRef(z);   // ok: z is a non-modifiable lvalue, y binds to z\n\n    printRef(5);   // ok: 5 is rvalue literal, y binds to temporary int object\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b2",
      "type": "paragraph",
      "html": "<p>Passing by const reference offers the same primary benefit as pass by non-const reference (avoiding making a copy of the argument), while also guaranteeing that the function can <em>not</em> change the value being referenced.</p>",
      "preview": "Passing by const reference offers the same primary benefit as pass by non-const ",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b3",
      "type": "paragraph",
      "html": "<p>For example, the following is disallowed, because <code>ref</code> is const:</p>",
      "preview": "For example, the following is disallowed, because ref is const:",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b4",
      "type": "code",
      "language": "cpp",
      "code": "void addOne(const int& ref)\n{\n    ++ref; // not allowed: ref is const\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">void addOne(const int&amp; ref)\n{\n    ++ref; // not allowed: ref is const\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b5",
      "type": "paragraph",
      "html": "<p>In most cases, we don’t want our functions modifying the value of arguments.</p>",
      "preview": "In most cases, we don’t want our functions modifying the value of arguments.",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b6",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Favor passing by const reference over passing by non-const reference unless you have a specific reason to do otherwise (e.g. the function needs to change the value of an argument).</p></div>",
      "preview": "Best practiceFavor passing by const reference over passing by non-const reference unless you have a ",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b7",
      "type": "paragraph",
      "html": "<p>Now we can understand the motivation for allowing const lvalue references to bind to rvalues: without that capability, there would be no way to pass literals (or other rvalues) to functions that used pass by reference!</p>",
      "preview": "Now we can understand the motivation for allowing const lvalue references to bin",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b8",
      "type": "section",
      "title": "Passing arguments of a different type to a const lvalue reference parameter",
      "html": "<h3 class=\"cpp-section-heading\">Passing arguments of a different type to a const lvalue reference parameter</h3>",
      "canCheck": false
    },
    {
      "id": "pass-by-const-lvalue-reference_b9",
      "type": "paragraph",
      "html": "<p>In lesson <a href=\"https://www.learncpp.com/cpp-tutorial/lvalue-references-to-const/\">12.4 -- Lvalue references to const</a>, we noted that a const lvalue reference can bind to a value of a different type, as long as that value is convertible to the type of the reference. The conversion creates a temporary object that the reference parameter can then bind to.</p>",
      "preview": "In lesson 12.4 -- Lvalue references to const, we noted that a const lvalue refer",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b10",
      "type": "paragraph",
      "html": "<p>The primary motivation for allowing this is so that we can pass a value as an argument to either a value parameter or a const reference parameter in exactly the same way:</p>",
      "preview": "The primary motivation for allowing this is so that we can pass a value as an ar",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b11",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nvoid printVal(double d)\n{\n    std::cout << d << '\\n';\n}\n\nvoid printRef(const double& d)\n{\n    std::cout << d << '\\n';\n}\n\nint main()\n{\n    printVal(5); // 5 converted to temporary double, copied to parameter d\n    printRef(5); // 5 converted to temporary double, bound to parameter d\n    \n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nvoid printVal(double d)\n{\n    std::cout &lt;&lt; d &lt;&lt; '\\n';\n}\n\nvoid printRef(const double&amp; d)\n{\n    std::cout &lt;&lt; d &lt;&lt; '\\n';\n}\n\nint main()\n{\n    printVal(5); // 5 converted to temporary double, copied to parameter d\n    printRef(5); // 5 converted to temporary double, bound to parameter d\n    \n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b12",
      "type": "paragraph",
      "html": "<p>With pass-by-value, we expect a copy to be made, so if a conversion occurs first (resulting in an additional copy) it’s rarely an issue (and the compiler will likely optimize one of the two copies away).</p>",
      "preview": "With pass-by-value, we expect a copy to be made, so if a conversion occurs first",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b13",
      "type": "paragraph",
      "html": "<p>However, we often use pass by reference when we <em>don’t</em> want a copy to be made. If a conversion occurs first, this will typically result in a (possibly expensive) copy being made, which can be suboptimal.</p>",
      "preview": "However, we often use pass by reference when we don’t want a copy to be made. If",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b14",
      "type": "callout",
      "calloutType": "warning",
      "html": "<div class=\"cpp-note cpp-lightredbackground\"><p class=\"cpp-note-title cpp-bottomline\">Warning</p><p>With pass by reference, ensure the type of the argument matches the type of the reference, or it will result in an unexpected (and possibly expensive) conversion.</p></div>",
      "preview": "WarningWith pass by reference, ensure the type of the argument matches the type of the reference, or",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b15",
      "type": "section",
      "title": "Mixing pass by value and pass by reference",
      "html": "<h3 class=\"cpp-section-heading\">Mixing pass by value and pass by reference</h3>",
      "canCheck": false
    },
    {
      "id": "pass-by-const-lvalue-reference_b16",
      "type": "paragraph",
      "html": "<p>A function with multiple parameters can determine whether each parameter is passed by value or passed by reference individually.</p>",
      "preview": "A function with multiple parameters can determine whether each parameter is pass",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b17",
      "type": "paragraph",
      "html": "<p>For example:</p>",
      "preview": "For example:",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b18",
      "type": "code",
      "language": "cpp",
      "code": "#include <string>\n\nvoid foo(int a, int& b, const std::string& c)\n{\n}\n\nint main()\n{\n    int x { 5 };\n    const std::string s { \"Hello, world!\" };\n\n    foo(5, x, s);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;string&gt;\n\nvoid foo(int a, int&amp; b, const std::string&amp; c)\n{\n}\n\nint main()\n{\n    int x { 5 };\n    const std::string s { \"Hello, world!\" };\n\n    foo(5, x, s);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b19",
      "type": "paragraph",
      "html": "<p>In the above example, the first argument is passed by value, the second by reference, and the third by const reference.</p>",
      "preview": "In the above example, the first argument is passed by value, the second by refer",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b20",
      "type": "section",
      "title": "When to use pass by value vs pass by reference",
      "html": "<h3 class=\"cpp-section-heading\">When to use pass by value vs pass by reference</h3>",
      "canCheck": false
    },
    {
      "id": "pass-by-const-lvalue-reference_b21",
      "type": "paragraph",
      "html": "<p>For most C++ beginners, the choice of whether to use pass by value or pass by reference isn’t very obvious. Fortunately, there’s a straightforward rule of thumb that will serve you well in the majority cases.</p>",
      "preview": "For most C++ beginners, the choice of whether to use pass by value or pass by re",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b22",
      "type": "list",
      "html": "<ul><li>Fundamental types and enumerated types are cheap to copy, so they are typically passed by value.</li><li>Class types can be expensive to copy (sometimes significantly so), so they are typically passed by const reference.</li></ul>",
      "preview": "Fundamental types and enumerated types are cheap to copy, so they are typically ",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b23",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>As a rule of thumb, pass fundamental types by value and class types by const reference.</p><p>If you aren’t sure what to do, pass by const reference, as you’re less likely to encounter unexpected behavior.</p></div>",
      "preview": "Best practiceAs a rule of thumb, pass fundamental types by value and class types by const reference.",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b24",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Tip</p><p>Here’s a partial list of other interesting cases:</p><p>The following are often passed by value (because it is more efficient):</p><ul><li>Enumerated types (unscoped and scoped enumerations).</li><li>Views and spans (e.g. <code>std::string_view</code>, <code>std::span</code>).</li><li>Types that mimic references or (non-owning) pointers (e.g. iterators, <code>std::reference_wrapper</code>).</li><li>Cheap-to-copy class types that have value semantics (e.g. <code>std::pair</code> with elements of fundamental types, <code>std::optional</code>, <code>std::expected</code>).</li></ul><p>Pass by reference should be used for the following:</p><ul><li>Arguments that need to be modified by the function.</li><li>Types that aren’t copyable (such as <code>std::ostream</code>).</li><li>Types where copying has ownership implications that we want to avoid (e.g. <code>std::unique_ptr</code>, <code>std::shared_ptr</code>).</li><li>Types that have virtual functions or are likely to be inherited from (due to object slicing concerns, covered in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/object-slicing/\">25.9 -- Object slicing</a>).</li></ul></div>",
      "preview": "TipHere’s a partial list of other interesting cases:The following are often passed by value (because",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b25",
      "type": "section",
      "title": "The cost of pass by value vs pass by reference Advanced",
      "html": "<h3 class=\"cpp-section-heading\">The cost of pass by value vs pass by reference <span class=\"cpp-section-pill cpp-section-advanced\">Advanced</span></h3>",
      "canCheck": false
    },
    {
      "id": "pass-by-const-lvalue-reference_b26",
      "type": "paragraph",
      "html": "<p>Not all class types need to be passed by reference (such as <code>std::string_view</code>, which is normally passed by value). And you may be wondering why we don’t just pass everything by reference. In this section (which is optional reading), we discuss the cost of pass by value vs pass by reference, and refine our best practice as to when we should use each.</p>",
      "preview": "Not all class types need to be passed by reference (such as std::string_view, wh",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b27",
      "type": "paragraph",
      "html": "<p>First, we need to consider the cost of initializing the function parameter. With pass by value, initialization means making a copy. The cost of copying an object is generally proportional to two things:</p>",
      "preview": "First, we need to consider the cost of initializing the function parameter. With",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b28",
      "type": "list",
      "html": "<ul><li>The size of the object. Objects that use more memory take more time to copy.</li><li>Any additional setup costs. Some class types do additional setup when they are instantiated (e.g. such as opening a file or database, or allocating a certain amount of dynamic memory to hold an object of a variable size). These setup costs must be paid each time an object is copied.</li></ul>",
      "preview": "The size of the object. Objects that use more memory take more time to copy.Any ",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b29",
      "type": "paragraph",
      "html": "<p>On the other hand, binding a reference to an object is always fast (about the same speed as copying a fundamental type).</p>",
      "preview": "On the other hand, binding a reference to an object is always fast (about the sa",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b30",
      "type": "paragraph",
      "html": "<p>Second, we need to consider the cost of using the function parameter. When setting up a function call, the compiler may be able to optimize by placing a reference or copy of a passed-by-value argument (if it is small in size) into a CPU register (which is fast to access) rather than into RAM (which is slower to access).</p>",
      "preview": "Second, we need to consider the cost of using the function parameter. When setti",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b31",
      "type": "paragraph",
      "html": "<p>Each time a value parameter is used, the running program can directly access the storage location (CPU register or RAM) of the copied argument. However, when a reference parameter is used, there is usually an extra step. The running program must first directly access the storage location (CPU register or RAM) allocated to the reference, in order to determine which object is being referenced. Only then can it access the storage location of the referenced object (in RAM).</p>",
      "preview": "Each time a value parameter is used, the running program can directly access the",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b32",
      "type": "paragraph",
      "html": "<p>Therefore, each use of a value parameter is a single CPU register or RAM access, whereas each use of a reference parameter is a single CPU register or RAM access plus a second RAM access.</p>",
      "preview": "Therefore, each use of a value parameter is a single CPU register or RAM access,",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b33",
      "type": "paragraph",
      "html": "<p>Third, the compiler can sometimes optimize code that uses pass by value more effectively than code that uses pass by reference. In particular, optimizers have to be conservative when there is any chance of <strong>aliasing</strong> (when two or more pointers or references can access the same object). Because pass by value results in the copy of argument values, there is no chance for aliasing to occur, allowing optimizers to be more aggressive.</p>",
      "preview": "Third, the compiler can sometimes optimize code that uses pass by value more eff",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b34",
      "type": "paragraph",
      "html": "<p>We can now answer these question of why we don’t pass everything by reference:</p>",
      "preview": "We can now answer these question of why we don’t pass everything by reference:",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b35",
      "type": "list",
      "html": "<ul><li>For objects that are cheap to copy, the cost of copying is similar to the cost of binding, but accessing the objects is faster and the compiler is likely to be able to optimize better.</li><li>For objects that are expensive to copy, the cost of the copy dominates other performance considerations.</li></ul>",
      "preview": "For objects that are cheap to copy, the cost of copying is similar to the cost o",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b36",
      "type": "paragraph",
      "html": "<p>The last question then is, how do we define “cheap to copy”? There is no absolute answer here, as this varies by compiler, use case, and architecture. However, we can formulate a good rule of thumb: An object is cheap to copy if it uses 2 or fewer “words” of memory (where a “word” is approximated by the size of a memory address) and it has no setup costs.</p>",
      "preview": "The last question then is, how do we define “cheap to copy”? There is no absolut",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b37",
      "type": "paragraph",
      "html": "<p>The following program defines a function-like macro that can be used to determine if a type (or object) is cheap to copy accordingly:</p>",
      "preview": "The following program defines a function-like macro that can be used to determin",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b38",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\n// Function-like macro that evaluates to true if the type (or object) is equal to or smaller than\n// the size of two memory addresses\n#define isSmall(T) (sizeof(T) <= 2 * sizeof(void*))\n\nstruct S\n{\n    double a;\n    double b;\n    double c;\n};\n\nint main()\n{\n    std::cout << std::boolalpha; // print true or false rather than 1 or 0\n    std::cout << isSmall(int) << '\\n'; // true\n\n    double d {};\n    std::cout << isSmall(d) << '\\n'; // true\n    std::cout << isSmall(S) << '\\n'; // false\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\n// Function-like macro that evaluates to true if the type (or object) is equal to or smaller than\n// the size of two memory addresses\n#define isSmall(T) (sizeof(T) &lt;= 2 * sizeof(void*))\n\nstruct S\n{\n    double a;\n    double b;\n    double c;\n};\n\nint main()\n{\n    std::cout &lt;&lt; std::boolalpha; // print true or false rather than 1 or 0\n    std::cout &lt;&lt; isSmall(int) &lt;&lt; '\\n'; // true\n\n    double d {};\n    std::cout &lt;&lt; isSmall(d) &lt;&lt; '\\n'; // true\n    std::cout &lt;&lt; isSmall(S) &lt;&lt; '\\n'; // false\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b39",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">As an aside…</p><p>We use a preprocessor function-like macro here so that we can provide either an object OR a type name as a parameter (as C++ functions disallow passing types as a parameter).</p></div>",
      "preview": "As an aside…We use a preprocessor function-like macro here so that we can provide either an object O",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b40",
      "type": "paragraph",
      "html": "<p>However, it can be hard to know whether a class type object has setup costs or not. It’s best to assume that most standard library classes have setup costs, unless you know otherwise that they don’t.</p>",
      "preview": "However, it can be hard to know whether a class type object has setup costs or n",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b41",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Tip</p><p>An object of type T is cheap to copy if <code>sizeof(T) &lt;= 2 * sizeof(void*)</code> and has no additional setup costs.</p></div>",
      "preview": "TipAn object of type T is cheap to copy if sizeof(T) <= 2 * sizeof(void*) and has no additional setu",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b42",
      "type": "section",
      "title": "For function parameters, prefer std::string_view over const std::string& in most cases",
      "html": "<h3 class=\"cpp-section-heading\"><a name=\"stringparameter\"></a>For function parameters, prefer <code>std::string_view</code> over <code>const std::string&amp;</code> in most cases <a href=\"#stringparameter\"><i class=\"fa fa-link\" style=\"font-size:.8em\"></i></a></h3>",
      "canCheck": false
    },
    {
      "id": "pass-by-const-lvalue-reference_b43",
      "type": "paragraph",
      "html": "<p>One question that comes up often in modern C++: when writing a function that has a string parameter, should the type of the parameter be <code>const std::string&amp;</code> or <code>std::string_view</code>?</p>",
      "preview": "One question that comes up often in modern C++: when writing a function that has",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b44",
      "type": "paragraph",
      "html": "<p>In most cases, <code>std::string_view</code> is the better choice, as it can handle a wider range of argument types efficiently. A <code>std::string_view</code> parameter also allows the caller to pass in a substring without having to copy that substring into its own string first.</p>",
      "preview": "In most cases, std::string_view is the better choice, as it can handle a wider r",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b45",
      "type": "code",
      "language": "cpp",
      "code": "void doSomething(const std::string&);\nvoid doSomething(std::string_view);   // prefer this in most cases",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">void doSomething(const std::string&amp;);\nvoid doSomething(std::string_view);   // prefer this in most cases</code></pre>",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b46",
      "type": "paragraph",
      "html": "<p>There are a few cases where using a <code>const std::string&amp;</code> parameter may be more appropriate:</p>",
      "preview": "There are a few cases where using a const std::string& parameter may be more app",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b47",
      "type": "list",
      "html": "<ul><li>If you’re using C++14 or older, <code>std::string_view</code> isn’t available.</li><li>If your function needs to call some other function that takes a C-style string or <code>std::string</code> parameter, then <code>const std::string&amp;</code> may be a better choice, as <code>std::string_view</code> is not guaranteed to be null-terminated (something that C-style string functions expect) and does not efficiently convert back to a <code>std::string</code>.</li></ul>",
      "preview": "If you’re using C++14 or older, std::string_view isn’t available.If your functio",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b48",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Prefer passing strings using <code>std::string_view</code> (by value) instead of <code>const std::string&amp;</code>, unless your function calls other functions that require C-style strings or <code>std::string</code> parameters.</p></div>",
      "preview": "Best practicePrefer passing strings using std::string_view (by value) instead of const std::string&,",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b49",
      "type": "section",
      "title": "Why std::string_view parameters are more efficient than const std::string& Advanced",
      "html": "<h3 class=\"cpp-section-heading\">Why <code>std::string_view</code> parameters are more efficient than <code>const std::string&amp;</code> <span class=\"cpp-section-pill cpp-section-advanced\">Advanced</span></h3>",
      "canCheck": false
    },
    {
      "id": "pass-by-const-lvalue-reference_b50",
      "type": "paragraph",
      "html": "<p>In C++, a string argument will typically be a <code>std::string</code>, a <code>std::string_view</code>, or a C-style string/string literal.</p>",
      "preview": "In C++, a string argument will typically be a std::string, a std::string_view, o",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b51",
      "type": "paragraph",
      "html": "<p>As reminders:</p>",
      "preview": "As reminders:",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b52",
      "type": "list",
      "html": "<ul><li>If the type of an argument does not match the type of the corresponding parameter, the compiler will try to implicitly convert the argument to match the type of the parameter.</li><li>Converting a value creates a temporary object of the converted type.</li><li>Creating (or copying) a <code>std::string_view</code> is inexpensive, as <code>std::string_view</code> does not make a copy of the string it is viewing.</li><li>Creating (or copying) a <code>std::string</code> can be expensive, as each <code>std::string</code> object makes a copy of the string.</li></ul>",
      "preview": "If the type of an argument does not match the type of the corresponding paramete",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b53",
      "type": "paragraph",
      "html": "<p>Here’s a table showing what happens when we try to pass each type:</p>",
      "preview": "Here’s a table showing what happens when we try to pass each type:",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b54",
      "type": "paragraph",
      "html": "<div class=\"cpp-table-wrapper\"><table class=\"cpp-table\"><tbody><tr><th>Argument Type</th><th>std::string_view parameter</th><th>const std::string&amp; parameter</th></tr><tr><td>std::string</td><td>Inexpensive conversion</td><td>Inexpensive reference binding</td></tr><tr><td>std::string_view</td><td>Inexpensive copy</td><td>Expensive explicit conversion to <code>std::string</code></td></tr><tr><td>C-style string / literal</td><td>Inexpensive conversion</td><td>Expensive conversion</td></tr></tbody></table></div>",
      "preview": "Argument Typestd::string_view parameterconst std::string& parameterstd::stringIn",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b55",
      "type": "paragraph",
      "html": "<p>With a <code>std::string_view</code> value parameter:</p>",
      "preview": "With a std::string_view value parameter:",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b56",
      "type": "list",
      "html": "<ul><li>If we pass in a <code>std::string</code> argument, the compiler will convert the <code>std::string</code> to a <code>std::string_view</code>, which is inexpensive, so this is fine.</li><li>If we pass in a <code>std::string_view</code> argument, the compiler will copy the argument into the parameter, which is inexpensive, so this is fine.</li><li>If we pass in a C-style string or string literal, the compiler will convert these to a <code>std::string_view</code>, which is inexpensive, so this is fine.</li></ul>",
      "preview": "If we pass in a std::string argument, the compiler will convert the std::string ",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b57",
      "type": "paragraph",
      "html": "<p>As you can see, <code>std::string_view</code> handles all three cases inexpensively.</p>",
      "preview": "As you can see, std::string_view handles all three cases inexpensively.",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b58",
      "type": "paragraph",
      "html": "<p>With a <code>const std::string&amp;</code> reference parameter:</p>",
      "preview": "With a const std::string& reference parameter:",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b59",
      "type": "list",
      "html": "<ul><li>If we pass in a <code>std::string</code> argument, the parameter will reference bind to the argument, which is inexpensive, so this is fine.</li><li>If we pass in a <code>std::string_view</code> argument, the compiler will refuse to do an implicit conversion, and produce a compilation error. We can use <code>static_cast</code> to do an explicit conversion (to <code>std::string</code>), but this conversion is expensive (since <code>std::string</code> will make a copy of the string being viewed). Once the conversion is done, the parameter will reference bind to the result, which is inexpensive. But we’ve made an expensive copy to do the conversion, so this isn’t great.</li><li>If we pass in a C-style string or string literal, the compiler will implicitly convert this to a <code>std::string</code>, which is expensive. So this isn’t great either.</li></ul>",
      "preview": "If we pass in a std::string argument, the parameter will reference bind to the a",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b60",
      "type": "paragraph",
      "html": "<p>Thus, a <code>const std::string&amp;</code> parameter only handles <code>std::string</code> arguments inexpensively.</p>",
      "preview": "Thus, a const std::string& parameter only handles std::string arguments inexpens",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b61",
      "type": "paragraph",
      "html": "<p>The same, in code form:</p>",
      "preview": "The same, in code form:",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b62",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <string>\n#include <string_view>\n\nvoid printSV(std::string_view sv)\n{\n    std::cout << sv << '\\n';\n}\n\nvoid printS(const std::string& s)\n{\n    std::cout << s << '\\n';\n}\n\nint main()\n{\n    std::string s{ \"Hello, world\" };\n    std::string_view sv { s };\n\n    // Pass to `std::string_view` parameter\n    printSV(s);              // ok: inexpensive conversion from std::string to std::string_view\n    printSV(sv);             // ok: inexpensive copy of std::string_view\n    printSV(\"Hello, world\"); // ok: inexpensive conversion of C-style string literal to std::string_view\n\n    // pass to `const std::string&` parameter\n    printS(s);              // ok: inexpensive bind to std::string argument\n    printS(sv);             // compile error: cannot implicit convert std::string_view to std::string\n    printS(static_cast<std::string>(sv)); // bad: expensive creation of std::string temporary\n    printS(\"Hello, world\"); // bad: expensive creation of std::string temporary\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n#include &lt;string_view&gt;\n\nvoid printSV(std::string_view sv)\n{\n    std::cout &lt;&lt; sv &lt;&lt; '\\n';\n}\n\nvoid printS(const std::string&amp; s)\n{\n    std::cout &lt;&lt; s &lt;&lt; '\\n';\n}\n\nint main()\n{\n    std::string s{ \"Hello, world\" };\n    std::string_view sv { s };\n\n    // Pass to `std::string_view` parameter\n    printSV(s);              // ok: inexpensive conversion from std::string to std::string_view\n    printSV(sv);             // ok: inexpensive copy of std::string_view\n    printSV(\"Hello, world\"); // ok: inexpensive conversion of C-style string literal to std::string_view\n\n    // pass to `const std::string&amp;` parameter\n    printS(s);              // ok: inexpensive bind to std::string argument\n    printS(sv);             // compile error: cannot implicit convert std::string_view to std::string\n    printS(static_cast&lt;std::string&gt;(sv)); // bad: expensive creation of std::string temporary\n    printS(\"Hello, world\"); // bad: expensive creation of std::string temporary\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b63",
      "type": "paragraph",
      "html": "<p>Additionally, we need to consider the cost of accessing the parameter inside the function. Because a <code>std::string_view</code> parameter is a normal object, the string being viewed can be accessed directly. Accessing a <code>std::string&amp;</code> parameter requires an additional step to get to the referenced object before the string can be accessed.</p>",
      "preview": "Additionally, we need to consider the cost of accessing the parameter inside the",
      "canCheck": true
    },
    {
      "id": "pass-by-const-lvalue-reference_b64",
      "type": "paragraph",
      "html": "<p>Finally, if we want to pass in a substring of an existing string (of any type), it is comparatively cheap to create a <code>std::string_view</code> substring, which can then be cheaply passed to a <code>std::string_view</code> parameter. In comparison, passing a substring to a <code>const std::string&amp;</code> is more expensive, as the substring must at some point be copied into the <code>std::string</code> that the reference parameter binds to.</p>",
      "preview": "Finally, if we want to pass in a substring of an existing string (of any type), ",
      "canCheck": true
    }
  ]
}