{
  "index": 162,
  "chapterId": "12",
  "number": "12.13",
  "title": "12.13 — In and out parameters",
  "url": "https://www.learncpp.com/cpp-tutorial/in-and-out-parameters/",
  "slug": "in-and-out-parameters",
  "scrapedAt": "2026-09-15T16:29:59.963Z",
  "totalBlocks": 55,
  "totalCheckpoints": 49,
  "blocks": [
    {
      "id": "in-and-out-parameters_b0",
      "type": "paragraph",
      "html": "<p>A function and its caller communicate with each other via two mechanisms: parameters and return values. When a function is called, the caller provides arguments, which the function receives via its parameters. These arguments can be passed by value, reference, or address.</p>",
      "preview": "A function and its caller communicate with each other via two mechanisms: parame",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b1",
      "type": "paragraph",
      "html": "<p>Typically, we’ll pass arguments by value or by const reference. But there are times when we may need to do otherwise.</p>",
      "preview": "Typically, we’ll pass arguments by value or by const reference. But there are ti",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b2",
      "type": "section",
      "title": "In parameters",
      "html": "<h3 class=\"cpp-section-heading\">In parameters</h3>",
      "canCheck": false
    },
    {
      "id": "in-and-out-parameters_b3",
      "type": "paragraph",
      "html": "<p>In most cases, a function parameter is used only to receive an input from the caller. Parameters that are used only for receiving input from the caller are sometimes called <strong>in parameters</strong>.</p>",
      "preview": "In most cases, a function parameter is used only to receive an input from the ca",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b4",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nvoid print(int x) // x is an in parameter\n{\n    std::cout << x << '\\n';\n}\n\nvoid print(const std::string& s) // s is an in parameter\n{\n    std::cout << s << '\\n';\n}\n\nint main()\n{\n    print(5);\n    std::string s { \"Hello, world!\" };\n    print(s);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nvoid print(int x) // x is an in parameter\n{\n    std::cout &lt;&lt; x &lt;&lt; '\\n';\n}\n\nvoid print(const std::string&amp; s) // s is an in parameter\n{\n    std::cout &lt;&lt; s &lt;&lt; '\\n';\n}\n\nint main()\n{\n    print(5);\n    std::string s { \"Hello, world!\" };\n    print(s);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b5",
      "type": "paragraph",
      "html": "<p>In-parameters are typically passed by value or by const reference.</p>",
      "preview": "In-parameters are typically passed by value or by const reference.",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b6",
      "type": "section",
      "title": "Out parameters",
      "html": "<h3 class=\"cpp-section-heading\">Out parameters</h3>",
      "canCheck": false
    },
    {
      "id": "in-and-out-parameters_b7",
      "type": "paragraph",
      "html": "<p>A function argument passed by non-const reference (or by pointer-to-non-const) allows the function to modify the value of an object passed as an argument. This provides a way for a function to return data back to the caller in cases where using a return value is not sufficient for some reason.</p>",
      "preview": "A function argument passed by non-const reference (or by pointer-to-non-const) a",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b8",
      "type": "paragraph",
      "html": "<p>A function parameter that is used only for the purpose of returning information back to the caller is called an <strong>out parameter</strong>.</p>",
      "preview": "A function parameter that is used only for the purpose of returning information ",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b9",
      "type": "paragraph",
      "html": "<p>For example:</p>",
      "preview": "For example:",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b10",
      "type": "code",
      "language": "cpp",
      "code": "#include <cmath>    // for std::sin() and std::cos()\n#include <iostream>\n\n// sinOut and cosOut are out parameters\nvoid getSinCos(double degrees, double& sinOut, double& cosOut)\n{\n    // sin() and cos() take radians, not degrees, so we need to convert\n    constexpr double pi { 3.14159265358979323846 }; // the value of pi\n    double radians = degrees * pi / 180.0;\n    sinOut = std::sin(radians);\n    cosOut = std::cos(radians);\n}\n \nint main()\n{\n    double sin { 0.0 };\n    double cos { 0.0 };\n \n    double degrees{};\n    std::cout << \"Enter the number of degrees: \";\n    std::cin >> degrees;\n\n    // getSinCos will return the sin and cos in variables sin and cos\n    getSinCos(degrees, sin, cos);\n \n    std::cout << \"The sin is \" << sin << '\\n';\n    std::cout << \"The cos is \" << cos << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cmath&gt;    // for std::sin() and std::cos()\n#include &lt;iostream&gt;\n\n// sinOut and cosOut are out parameters\nvoid getSinCos(double degrees, double&amp; sinOut, double&amp; cosOut)\n{\n    // sin() and cos() take radians, not degrees, so we need to convert\n    constexpr double pi { 3.14159265358979323846 }; // the value of pi\n    double radians = degrees * pi / 180.0;\n    sinOut = std::sin(radians);\n    cosOut = std::cos(radians);\n}\n \nint main()\n{\n    double sin { 0.0 };\n    double cos { 0.0 };\n \n    double degrees{};\n    std::cout &lt;&lt; \"Enter the number of degrees: \";\n    std::cin &gt;&gt; degrees;\n\n    // getSinCos will return the sin and cos in variables sin and cos\n    getSinCos(degrees, sin, cos);\n \n    std::cout &lt;&lt; \"The sin is \" &lt;&lt; sin &lt;&lt; '\\n';\n    std::cout &lt;&lt; \"The cos is \" &lt;&lt; cos &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b11",
      "type": "paragraph",
      "html": "<p>This function has one parameter <code>degrees</code> (whose argument is passed by value) as input, and “returns” two parameters (by reference) as output.</p>",
      "preview": "This function has one parameter degrees (whose argument is passed by value) as i",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b12",
      "type": "paragraph",
      "html": "<p>We’ve named these out parameters with the suffix “out” to denote that they’re out parameters. This helps remind the caller that the initial value passed to these parameters doesn’t matter, and that we should expect them to be overwritten. By convention, output parameters are typically the rightmost parameters.</p>",
      "preview": "We’ve named these out parameters with the suffix “out” to denote that they’re ou",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b13",
      "type": "paragraph",
      "html": "<p>Let’s explore how this works in more detail. First, the main function creates local variables <code>sin</code> and <code>cos</code>. Those are passed into function <code>getSinCos()</code> by reference (rather than by value). This means function <code>getSinCos()</code> has access to the actual <code>sin</code> and <code>cos</code> variables in <code>main()</code>, not just copies. <code>getSinCos()</code> accordingly assigns new values to <code>sin</code> and <code>cos</code> (through references <code>sinOut</code> and <code>cosOut</code> respectively), which overwrites the old values in <code>sin</code> and <code>cos</code>. Function <code>main()</code> then prints these updated values.</p>",
      "preview": "Let’s explore how this works in more detail. First, the main function creates lo",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b14",
      "type": "paragraph",
      "html": "<p>If <code>sin</code> and <code>cos</code> had been passed by value instead of reference, <code>getSinCos()</code> would have changed copies of <code>sin</code> and <code>cos</code>, leading to any changes being discarded at the end of the function. But because <code>sin</code> and <code>cos</code> were passed by reference, any changes made to <code>sin</code> or <code>cos</code> (through the references) are persisted beyond the function. We can therefore use this mechanism to return values back to the caller.</p>",
      "preview": "If sin and cos had been passed by value instead of reference, getSinCos() would ",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b15",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">As an aside…</p><p><a href=\"https://stackoverflow.com/a/9779765\">This answer on StackOverflow</a> is an interesting read that explains why non-const lvalue references are not allowed to bind to rvalues/temporary objects (due to implicit type conversion producing unexpected behavior when combined with out-parameters).</p></div>",
      "preview": "As an aside…This answer on StackOverflow is an interesting read that explains why non-const lvalue r",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b16",
      "type": "section",
      "title": "Out parameters have an unnatural usage syntax",
      "html": "<h3 class=\"cpp-section-heading\">Out parameters have an unnatural usage syntax</h3>",
      "canCheck": false
    },
    {
      "id": "in-and-out-parameters_b17",
      "type": "paragraph",
      "html": "<p>Out-parameters, while functional, have a few downsides.</p>",
      "preview": "Out-parameters, while functional, have a few downsides.",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b18",
      "type": "paragraph",
      "html": "<p>First, the caller must instantiate (and initialize) objects and pass them as arguments, even if it doesn’t intend to use them. These objects must be able to be assigned to, which means they can’t be made const.</p>",
      "preview": "First, the caller must instantiate (and initialize) objects and pass them as arg",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b19",
      "type": "paragraph",
      "html": "<p>Second, because the caller must pass in objects, these values can’t be used as temporaries, or easily used in a single expression.</p>",
      "preview": "Second, because the caller must pass in objects, these values can’t be used as t",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b20",
      "type": "paragraph",
      "html": "<p>The following example shows both of these downsides:</p>",
      "preview": "The following example shows both of these downsides:",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b21",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint getByValue()\n{\n    return 5;\n}\n\nvoid getByReference(int& x)\n{\n    x = 5;\n}\n\nint main()\n{\n    // return by value\n    [[maybe_unused]] int x{ getByValue() }; // can use to initialize object\n    std::cout << getByValue() << '\\n';      // can use temporary return value in expression\n\n    // return by out parameter\n    int y{};                // must first allocate an assignable object\n    getByReference(y);      // then pass to function to assign the desired value\n    std::cout << y << '\\n'; // and only then can we use that value\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint getByValue()\n{\n    return 5;\n}\n\nvoid getByReference(int&amp; x)\n{\n    x = 5;\n}\n\nint main()\n{\n    // return by value\n    [[maybe_unused]] int x{ getByValue() }; // can use to initialize object\n    std::cout &lt;&lt; getByValue() &lt;&lt; '\\n';      // can use temporary return value in expression\n\n    // return by out parameter\n    int y{};                // must first allocate an assignable object\n    getByReference(y);      // then pass to function to assign the desired value\n    std::cout &lt;&lt; y &lt;&lt; '\\n'; // and only then can we use that value\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b22",
      "type": "paragraph",
      "html": "<p>As you can see, the syntax for using out-parameters is a bit unnatural.</p>",
      "preview": "As you can see, the syntax for using out-parameters is a bit unnatural.",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b23",
      "type": "section",
      "title": "Out-parameters by reference don’t make it obvious the arguments will be modified",
      "html": "<h3 class=\"cpp-section-heading\">Out-parameters by reference don’t make it obvious the arguments will be modified</h3>",
      "canCheck": false
    },
    {
      "id": "in-and-out-parameters_b24",
      "type": "paragraph",
      "html": "<p>When we assign a function’s return value to an object, it is clear that the value of the object is being modified:</p>",
      "preview": "When we assign a function’s return value to an object, it is clear that the valu",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b25",
      "type": "code",
      "language": "cpp",
      "code": "x = getByValue(); // obvious that x is being modified",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">x = getByValue(); // obvious that x is being modified</code></pre>",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b26",
      "type": "paragraph",
      "html": "<p>This is good, as it makes it clear that we should expect the value of <code>x</code> to change.</p>",
      "preview": "This is good, as it makes it clear that we should expect the value of x to chang",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b27",
      "type": "paragraph",
      "html": "<p>However, let’s take a look at the function call to <code>getSinCos()</code> in the example above again:</p>",
      "preview": "However, let’s take a look at the function call to getSinCos() in the example ab",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b28",
      "type": "code",
      "language": "cpp",
      "code": "    getSinCos(degrees, sin, cos);",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">    getSinCos(degrees, sin, cos);</code></pre>",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b29",
      "type": "paragraph",
      "html": "<p>It is not clear from this function call that <code>degrees</code> is an in parameter, but <code>sin</code> and <code>cos</code> are out-parameters. If the caller does not realize that <code>sin</code> and <code>cos</code> will be modified, a semantic error will likely result.</p>",
      "preview": "It is not clear from this function call that degrees is an in parameter, but sin",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b30",
      "type": "paragraph",
      "html": "<p>Using pass by address instead of pass by reference can in some case help make out-parameters more obvious by requiring the caller to pass in the address of objects as arguments.</p>",
      "preview": "Using pass by address instead of pass by reference can in some case help make ou",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b31",
      "type": "paragraph",
      "html": "<p>Consider the following example:</p>",
      "preview": "Consider the following example:",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b32",
      "type": "code",
      "language": "cpp",
      "code": "void foo1(int x);  // pass by value\nvoid foo2(int& x); // pass by reference\nvoid foo3(int* x); // pass by address\n\nint main()\n{\n    int i{};\n \n    foo1(i);  // can't modify i\n    foo2(i);  // can modify i (not obvious)\n    foo3(&i); // can modify i\n\n    int *ptr { &i };\n    foo3(ptr); // can modify i (not obvious)\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">void foo1(int x);  // pass by value\nvoid foo2(int&amp; x); // pass by reference\nvoid foo3(int* x); // pass by address\n\nint main()\n{\n    int i{};\n \n    foo1(i);  // can't modify i\n    foo2(i);  // can modify i (not obvious)\n    foo3(&amp;i); // can modify i\n\n    int *ptr { &amp;i };\n    foo3(ptr); // can modify i (not obvious)\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b33",
      "type": "paragraph",
      "html": "<p>Notice that in the call to <code>foo3(&amp;i)</code>, we have to pass in <code>&amp;i</code> rather than <code>i</code>, which helps make it clearer that we should expect <code>i</code> to be modified.</p>",
      "preview": "Notice that in the call to foo3(&i), we have to pass in &i rather than i, which ",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b34",
      "type": "paragraph",
      "html": "<p>However, this is not fool-proof, as <code>foo3(ptr)</code> allows <code>foo3()</code> to modify <code>i</code> and does not require the caller to take the address-of <code>ptr</code>.</p>",
      "preview": "However, this is not fool-proof, as foo3(ptr) allows foo3() to modify i and does",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b35",
      "type": "paragraph",
      "html": "<p>The caller may also think they can pass in <code>nullptr</code> or a null pointer as a valid argument when this is disallowed. And the function is now required to do null pointer checking and handling, which adds more complexity. This need for added null pointer handling often causes more issues than just sticking with pass by reference.</p>",
      "preview": "The caller may also think they can pass in nullptr or a null pointer as a valid ",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b36",
      "type": "paragraph",
      "html": "<p>For all of these reasons, out-parameters should be avoided unless no other good options exist.</p>",
      "preview": "For all of these reasons, out-parameters should be avoided unless no other good ",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b37",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Avoid out-parameters (except in the rare case where no better options exist).</p><p>Prefer pass by reference for non-optional out-parameters.</p></div>",
      "preview": "Best practiceAvoid out-parameters (except in the rare case where no better options exist).Prefer pas",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b38",
      "type": "section",
      "title": "In/out parameters",
      "html": "<h3 class=\"cpp-section-heading\">In/out parameters</h3>",
      "canCheck": false
    },
    {
      "id": "in-and-out-parameters_b39",
      "type": "paragraph",
      "html": "<p>In rare cases, a function will actually use the value of an out-parameter before overwriting its value. Such a parameter is called an <strong>in-out parameter</strong>. In-out-parameters work identically to out-parameters and have all the same challenges.</p>",
      "preview": "In rare cases, a function will actually use the value of an out-parameter before",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b40",
      "type": "section",
      "title": "When to pass by non-const reference",
      "html": "<h3 class=\"cpp-section-heading\">When to pass by non-const reference</h3>",
      "canCheck": false
    },
    {
      "id": "in-and-out-parameters_b41",
      "type": "paragraph",
      "html": "<p>If you’re going to pass by reference in order to avoid making a copy of the argument, you should almost always pass by const reference.</p>",
      "preview": "If you’re going to pass by reference in order to avoid making a copy of the argu",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b42",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Author’s note</p><p>In the following examples, we will use <code>Foo</code> to represent some type that we care about. For now, you can imagine <code>Foo</code> as a type alias for a type of your choice (e.g. <code>std::string</code>).</p></div>",
      "preview": "Author’s noteIn the following examples, we will use Foo to represent some type that we care about. F",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b43",
      "type": "paragraph",
      "html": "<p>However, there are two primary cases where pass by non-const reference may be the better choice.</p>",
      "preview": "However, there are two primary cases where pass by non-const reference may be th",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b44",
      "type": "paragraph",
      "html": "<p>First, use pass by non-const reference when a parameter is an in-out-parameter. Since we’re already passing in the object we need back out, it’s often more straightforward and performant to just modify that object.</p>",
      "preview": "First, use pass by non-const reference when a parameter is an in-out-parameter. ",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b45",
      "type": "code",
      "language": "cpp",
      "code": "void someFcn(Foo& inout)\n{\n    // modify inout\n}\n\nint main()\n{\n    Foo foo{};\n    someFcn(foo); // foo modified after this call, may not be obvious\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">void someFcn(Foo&amp; inout)\n{\n    // modify inout\n}\n\nint main()\n{\n    Foo foo{};\n    someFcn(foo); // foo modified after this call, may not be obvious\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b46",
      "type": "paragraph",
      "html": "<p>Giving the function a good name can help:</p>",
      "preview": "Giving the function a good name can help:",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b47",
      "type": "code",
      "language": "cpp",
      "code": "void modifyFoo(Foo& inout)\n{\n    // modify inout\n}\n\nint main()\n{\n    Foo foo{};\n    modifyFoo(foo); // foo modified after this call, slightly more obvious\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">void modifyFoo(Foo&amp; inout)\n{\n    // modify inout\n}\n\nint main()\n{\n    Foo foo{};\n    modifyFoo(foo); // foo modified after this call, slightly more obvious\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b48",
      "type": "paragraph",
      "html": "<p>The alternative is to pass the object by value or const reference instead (as per usual) and return a new object by value, which the caller can then assign back to the original object:</p>",
      "preview": "The alternative is to pass the object by value or const reference instead (as pe",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b49",
      "type": "code",
      "language": "cpp",
      "code": "Foo someFcn(const Foo& in)\n{\n    Foo foo { in }; // copy here\n    // modify foo\n    return foo;\n}\n\nint main()\n{\n    Foo foo{};\n    foo = someFcn(foo); // makes it obvious foo is modified, but another copy made here\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">Foo someFcn(const Foo&amp; in)\n{\n    Foo foo { in }; // copy here\n    // modify foo\n    return foo;\n}\n\nint main()\n{\n    Foo foo{};\n    foo = someFcn(foo); // makes it obvious foo is modified, but another copy made here\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b50",
      "type": "paragraph",
      "html": "<p>This has the benefit of using a more conventional return syntax, but requires making 2 extra copies (sometimes the compiler can optimize one of these copies away).</p>",
      "preview": "This has the benefit of using a more conventional return syntax, but requires ma",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b51",
      "type": "paragraph",
      "html": "<p>Second, use pass by non-const reference when a function would otherwise return an object by value to the caller, but making a copy of that object is <em>extremely</em> expensive. Especially if the function is called many times in a performance-critical section of code.</p>",
      "preview": "Second, use pass by non-const reference when a function would otherwise return a",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b52",
      "type": "code",
      "language": "cpp",
      "code": "void generateExpensiveFoo(Foo& out)\n{\n    // modify out\n}\n\nint main()\n{\n    Foo foo{};\n    generateExpensiveFoo(foo); // foo modified after this call\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">void generateExpensiveFoo(Foo&amp; out)\n{\n    // modify out\n}\n\nint main()\n{\n    Foo foo{};\n    generateExpensiveFoo(foo); // foo modified after this call\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b53",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">For advanced readers</p><p>The most common example of the above is when a function needs to fill a large C-style array or <code>std::array</code> with data, and the array has an expensive-to-copy element type. We discuss arrays in a future chapter.</p></div>",
      "preview": "For advanced readersThe most common example of the above is when a function needs to fill a large C-",
      "canCheck": true
    },
    {
      "id": "in-and-out-parameters_b54",
      "type": "paragraph",
      "html": "<p>That said, objects are rarely so expensive to copy that resorting to non-conventional methods of returning those objects is worthwhile.</p>",
      "preview": "That said, objects are rarely so expensive to copy that resorting to non-convent",
      "canCheck": true
    }
  ]
}