{
  "index": 259,
  "chapterId": "21",
  "number": "21.4",
  "title": "21.4 — Overloading the I/O operators",
  "url": "https://www.learncpp.com/cpp-tutorial/overloading-the-io-operators/",
  "slug": "overloading-the-io-operators",
  "scrapedAt": "2026-09-15T16:30:50.431Z",
  "totalBlocks": 71,
  "totalCheckpoints": 61,
  "blocks": [
    {
      "id": "overloading-the-io-operators_b0",
      "type": "paragraph",
      "html": "<p>For classes that have multiple member variables, printing each of the individual variables on the screen can get tiresome fast. For example, consider the following class:</p>",
      "preview": "For classes that have multiple member variables, printing each of the individual",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b1",
      "type": "code",
      "language": "cpp",
      "code": "class Point\n{\nprivate:\n    double m_x{};\n    double m_y{};\n    double m_z{};\n\npublic:\n    Point(double x=0.0, double y=0.0, double z=0.0)\n      : m_x{x}, m_y{y}, m_z{z}\n    {\n    }\n\n    double getX() const { return m_x; }\n    double getY() const { return m_y; }\n    double getZ() const { return m_z; }\n};",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">class Point\n{\nprivate:\n    double m_x{};\n    double m_y{};\n    double m_z{};\n\npublic:\n    Point(double x=0.0, double y=0.0, double z=0.0)\n      : m_x{x}, m_y{y}, m_z{z}\n    {\n    }\n\n    double getX() const { return m_x; }\n    double getY() const { return m_y; }\n    double getZ() const { return m_z; }\n};</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b2",
      "type": "paragraph",
      "html": "<p>If you wanted to print an instance of this class to the screen, you’d have to do something like this:</p>",
      "preview": "If you wanted to print an instance of this class to the screen, you’d have to do",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b3",
      "type": "code",
      "language": "cpp",
      "code": "Point point { 5.0, 6.0, 7.0 };\n\nstd::cout << \"Point(\" << point.getX() << \", \" <<\n    point.getY() << \", \" <<\n    point.getZ() << ')';",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">Point point { 5.0, 6.0, 7.0 };\n\nstd::cout &lt;&lt; \"Point(\" &lt;&lt; point.getX() &lt;&lt; \", \" &lt;&lt;\n    point.getY() &lt;&lt; \", \" &lt;&lt;\n    point.getZ() &lt;&lt; ')';</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b4",
      "type": "paragraph",
      "html": "<p>Of course, it makes more sense to do this as a reusable function. And in previous examples, you’ve seen us create <code>print()</code> functions that work like this:</p>",
      "preview": "Of course, it makes more sense to do this as a reusable function. And in previou",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b5",
      "type": "code",
      "language": "cpp",
      "code": "class Point\n{\nprivate:\n    double m_x{};\n    double m_y{};\n    double m_z{};\n\npublic:\n    Point(double x=0.0, double y=0.0, double z=0.0)\n      : m_x{x}, m_y{y}, m_z{z}\n    {\n    }\n\n    double getX() const { return m_x; }\n    double getY() const { return m_y; }\n    double getZ() const { return m_z; }\n\n    void print() const\n    {\n        std::cout << \"Point(\" << m_x << \", \" << m_y << \", \" << m_z << ')';\n    }\n};",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">class Point\n{\nprivate:\n    double m_x{};\n    double m_y{};\n    double m_z{};\n\npublic:\n    Point(double x=0.0, double y=0.0, double z=0.0)\n      : m_x{x}, m_y{y}, m_z{z}\n    {\n    }\n\n    double getX() const { return m_x; }\n    double getY() const { return m_y; }\n    double getZ() const { return m_z; }\n\n    void print() const\n    {\n        std::cout &lt;&lt; \"Point(\" &lt;&lt; m_x &lt;&lt; \", \" &lt;&lt; m_y &lt;&lt; \", \" &lt;&lt; m_z &lt;&lt; ')';\n    }\n};</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b6",
      "type": "paragraph",
      "html": "<p>While this is much better, it still has some downsides. Because <code>print()</code> returns <code>void</code>, it can’t be called in the middle of an output statement. Instead, you have to do this:</p>",
      "preview": "While this is much better, it still has some downsides. Because print() returns ",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b7",
      "type": "code",
      "language": "cpp",
      "code": "int main()\n{\n    const Point point { 5.0, 6.0, 7.0 };\n\n    std::cout << \"My point is: \";\n    point.print();\n    std::cout << \" in Cartesian space.\\n\";\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int main()\n{\n    const Point point { 5.0, 6.0, 7.0 };\n\n    std::cout &lt;&lt; \"My point is: \";\n    point.print();\n    std::cout &lt;&lt; \" in Cartesian space.\\n\";\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b8",
      "type": "paragraph",
      "html": "<p>It would be much easier if you could simply type:</p>",
      "preview": "It would be much easier if you could simply type:",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b9",
      "type": "code",
      "language": "cpp",
      "code": "Point point{5.0, 6.0, 7.0};\ncout << \"My point is: \" << point << \" in Cartesian space.\\n\";",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">Point point{5.0, 6.0, 7.0};\ncout &lt;&lt; \"My point is: \" &lt;&lt; point &lt;&lt; \" in Cartesian space.\\n\";</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b10",
      "type": "paragraph",
      "html": "<p>and get the same result. There would be no breaking up output across multiple statements, and no having to remember what you named the print function.</p>",
      "preview": "and get the same result. There would be no breaking up output across multiple st",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b11",
      "type": "paragraph",
      "html": "<p>Fortunately, by overloading <code>operator&lt;&lt;</code>, you can!</p>",
      "preview": "Fortunately, by overloading operator<<, you can!",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b12",
      "type": "section",
      "title": "Overloading operator<<",
      "html": "<h3 class=\"cpp-section-heading\">Overloading <code>operator&lt;&lt;</code></h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-io-operators_b13",
      "type": "paragraph",
      "html": "<p>Overloading <code>operator&lt;&lt;</code> is similar to overloading operator+ (they are both binary operators), except that the parameter types are different.</p>",
      "preview": "Overloading operator<< is similar to overloading operator+ (they are both binary",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b14",
      "type": "paragraph",
      "html": "<p>Consider the expression <code>std::cout &lt;&lt; point</code>. If the operator is <code>&lt;&lt;</code>, what are the operands? The left operand is the <code>std::cout</code> object, and the right operand is your <code>Point</code> class object. <code>std::cout</code> is actually an object of type <code>std::ostream</code>. Therefore, our overloaded function will look like this:</p>",
      "preview": "Consider the expression std::cout << point. If the operator is <<, what are the ",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b15",
      "type": "code",
      "language": "cpp",
      "code": "// std::ostream is the type for object std::cout\nfriend std::ostream& operator<< (std::ostream& out, const Point& point);",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// std::ostream is the type for object std::cout\nfriend std::ostream&amp; operator&lt;&lt; (std::ostream&amp; out, const Point&amp; point);</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b16",
      "type": "paragraph",
      "html": "<p>Implementation of <code>operator&lt;&lt;</code> for our <code>Point</code> class is fairly straightforward -- because C++ already knows how to output doubles using <code>operator&lt;&lt;</code>, and our members are all doubles, we can simply use <code>operator&lt;&lt;</code> to output the data members of our <code>Point</code>. Here is the above <code>Point</code> class with the overloaded <code>operator&lt;&lt;</code>.</p>",
      "preview": "Implementation of operator<< for our Point class is fairly straightforward -- be",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b17",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nclass Point\n{\nprivate:\n    double m_x{};\n    double m_y{};\n    double m_z{};\n\npublic:\n    Point(double x=0.0, double y=0.0, double z=0.0)\n      : m_x{x}, m_y{y}, m_z{z}\n    {\n    }\n\n    friend std::ostream& operator<< (std::ostream& out, const Point& point);\n};\n\nstd::ostream& operator<< (std::ostream& out, const Point& point)\n{\n    // Since operator<< is a friend of the Point class, we can access Point's members directly.\n    out << \"Point(\" << point.m_x << \", \" << point.m_y << \", \" << point.m_z << ')'; // actual output done here\n\n    return out; // return std::ostream so we can chain calls to operator<<\n}\n\nint main()\n{\n    const Point point1 { 2.0, 3.0, 4.0 };\n\n    std::cout << point1 << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nclass Point\n{\nprivate:\n    double m_x{};\n    double m_y{};\n    double m_z{};\n\npublic:\n    Point(double x=0.0, double y=0.0, double z=0.0)\n      : m_x{x}, m_y{y}, m_z{z}\n    {\n    }\n\n    friend std::ostream&amp; operator&lt;&lt; (std::ostream&amp; out, const Point&amp; point);\n};\n\nstd::ostream&amp; operator&lt;&lt; (std::ostream&amp; out, const Point&amp; point)\n{\n    // Since operator&lt;&lt; is a friend of the Point class, we can access Point's members directly.\n    out &lt;&lt; \"Point(\" &lt;&lt; point.m_x &lt;&lt; \", \" &lt;&lt; point.m_y &lt;&lt; \", \" &lt;&lt; point.m_z &lt;&lt; ')'; // actual output done here\n\n    return out; // return std::ostream so we can chain calls to operator&lt;&lt;\n}\n\nint main()\n{\n    const Point point1 { 2.0, 3.0, 4.0 };\n\n    std::cout &lt;&lt; point1 &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b18",
      "type": "paragraph",
      "html": "<p>This is pretty straightforward -- note how similar our output line is to the line in the <code>print()</code> function we wrote previously. The most notable difference is that <code>std::cout</code> has become parameter <code>out</code> (which will be a reference to <code>std::cout</code> when the function is called).</p>",
      "preview": "This is pretty straightforward -- note how similar our output line is to the lin",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b19",
      "type": "paragraph",
      "html": "<p>The trickiest part here is the return type. With the arithmetic operators, we calculated and returned a single answer by value (because we were creating and returning a new result). However, if you try to return <code>std::ostream</code> by value, you’ll get a compiler error. This happens because <code>std::ostream</code> specifically disallows being copied.</p>",
      "preview": "The trickiest part here is the return type. With the arithmetic operators, we ca",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b20",
      "type": "paragraph",
      "html": "<p>In this case, we return the left hand parameter as a reference. This not only prevents a copy of <code>std::ostream</code> from being made, it also allows us to “chain” output commands together, such as <code>std::cout &lt;&lt; point &lt;&lt; '\\n'</code>.</p>",
      "preview": "In this case, we return the left hand parameter as a reference. This not only pr",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b21",
      "type": "paragraph",
      "html": "<p>Consider what would happen if our <code>operator&lt;&lt;</code> returned <code>void</code> instead. When the compiler evaluates <code>std::cout &lt;&lt; point &lt;&lt; '\\n'</code>, due to the precedence/associativity rules, it evaluates this expression as <code>(std::cout &lt;&lt; point) &lt;&lt; '\\n';</code>. <code>std::cout &lt;&lt; point</code> would call our void-returning overloaded <code>operator&lt;&lt;</code> function, which returns <code>void</code>. Then the partially evaluated expression becomes: <code>void &lt;&lt; '\\n';</code>, which makes no sense!</p>",
      "preview": "Consider what would happen if our operator<< returned void instead. When the com",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b22",
      "type": "paragraph",
      "html": "<p>By returning the <code>out</code> parameter as the return type instead, <code>(std::cout &lt;&lt; point)</code> returns <code>std::cout</code>. Then our partially evaluated expression becomes: <code>std::cout &lt;&lt; '\\n';</code>, which then gets evaluated itself!</p>",
      "preview": "By returning the out parameter as the return type instead, (std::cout << point) ",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b23",
      "type": "paragraph",
      "html": "<p>Any time we want our overloaded binary operators to be chainable in such a manner, the left operand should be returned (by reference). Returning the left-hand parameter by reference is okay in this case -- since the left-hand parameter was passed in by the calling function, it must still exist when the called function returns. Therefore, we don’t have to worry about referencing something that will go out of scope and get destroyed when the operator returns.</p>",
      "preview": "Any time we want our overloaded binary operators to be chainable in such a manne",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b24",
      "type": "paragraph",
      "html": "<p>Just to prove it works, consider the following example, which uses the Point class with the overloaded <code>operator&lt;&lt;</code> we wrote above:</p>",
      "preview": "Just to prove it works, consider the following example, which uses the Point cla",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b25",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nclass Point\n{\nprivate:\n    double m_x{};\n    double m_y{};\n    double m_z{};\n\npublic:\n    Point(double x=0.0, double y=0.0, double z=0.0)\n      : m_x{x}, m_y{y}, m_z{z}\n    {\n    }\n\n    friend std::ostream& operator<< (std::ostream& out, const Point& point);\n};\n\nstd::ostream& operator<< (std::ostream& out, const Point& point)\n{\n    // Since operator<< is a friend of the Point class, we can access Point's members directly.\n    out << \"Point(\" << point.m_x << \", \" << point.m_y << \", \" << point.m_z << ')';\n\n    return out;\n}\n\nint main()\n{\n    Point point1 { 2.0, 3.5, 4.0 };\n    Point point2 { 6.0, 7.5, 8.0 };\n\n    std::cout << point1 << ' ' << point2 << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nclass Point\n{\nprivate:\n    double m_x{};\n    double m_y{};\n    double m_z{};\n\npublic:\n    Point(double x=0.0, double y=0.0, double z=0.0)\n      : m_x{x}, m_y{y}, m_z{z}\n    {\n    }\n\n    friend std::ostream&amp; operator&lt;&lt; (std::ostream&amp; out, const Point&amp; point);\n};\n\nstd::ostream&amp; operator&lt;&lt; (std::ostream&amp; out, const Point&amp; point)\n{\n    // Since operator&lt;&lt; is a friend of the Point class, we can access Point's members directly.\n    out &lt;&lt; \"Point(\" &lt;&lt; point.m_x &lt;&lt; \", \" &lt;&lt; point.m_y &lt;&lt; \", \" &lt;&lt; point.m_z &lt;&lt; ')';\n\n    return out;\n}\n\nint main()\n{\n    Point point1 { 2.0, 3.5, 4.0 };\n    Point point2 { 6.0, 7.5, 8.0 };\n\n    std::cout &lt;&lt; point1 &lt;&lt; ' ' &lt;&lt; point2 &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b26",
      "type": "paragraph",
      "html": "<p>This produces the following result:</p>",
      "preview": "This produces the following result:",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b27",
      "type": "output",
      "text": "Point(2, 3.5, 4) Point(6, 7.5, 8)\n",
      "html": "<pre>Point(2, 3.5, 4) Point(6, 7.5, 8)\n</pre>",
      "canCheck": false
    },
    {
      "id": "overloading-the-io-operators_b28",
      "type": "paragraph",
      "html": "<p>In the above example, <code>operator&lt;&lt;</code> is a friend because it needs direct access to the member of <code>Point</code>. However, if the members could be accessed via getters, then <code>operator&lt;&lt;</code> could be implemented as a non-friend.</p>",
      "preview": "In the above example, operator<< is a friend because it needs direct access to t",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b29",
      "type": "section",
      "title": "Overloading operator>>",
      "html": "<h3 class=\"cpp-section-heading\">Overloading <code>operator&gt;&gt;</code></h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-io-operators_b30",
      "type": "paragraph",
      "html": "<p>It is also possible to overload the input operator. This is done in a manner analogous to overloading the output operator. The key thing you need to know is that <code>std::cin</code> is an object of type <code>std::istream</code>. Here’s our <code>Point</code> class with an overloaded <code>operator&gt;&gt;</code> added:</p>",
      "preview": "It is also possible to overload the input operator. This is done in a manner ana",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b31",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nclass Point\n{\nprivate:\n    double m_x{};\n    double m_y{};\n    double m_z{};\n\npublic:\n    Point(double x=0.0, double y=0.0, double z=0.0)\n      : m_x{x}, m_y{y}, m_z{z}\n    {\n    }\n\n    friend std::ostream& operator<< (std::ostream& out, const Point& point);\n    friend std::istream& operator>> (std::istream& out, Point& point);\n};\n\nstd::ostream& operator<< (std::ostream& out, const Point& point)\n{\n    // Since operator<< is a friend of the Point class, we can access Point's members directly.\n    out << \"Point(\" << point.m_x << \", \" << point.m_y << \", \" << point.m_z << ')';\n\n    return out;\n}\n\n// note that point must be non-const so we can modify the object\nstd::istream& operator>> (std::istream& in, Point& point)\n{\n    // This version subject to partial extraction issues (see below)\n    in >> point.m_x >> point.m_y >> point.m_z;\n\n    return in;\n}\n\nint main()\n{\n    std::cout << \"Enter a point: \";\n\n    Point point{ 1.0, 2.0, 3.0 }; // non-zero test data\n    std::cin >> point;\n\n    std::cout << \"You entered: \" << point << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nclass Point\n{\nprivate:\n    double m_x{};\n    double m_y{};\n    double m_z{};\n\npublic:\n    Point(double x=0.0, double y=0.0, double z=0.0)\n      : m_x{x}, m_y{y}, m_z{z}\n    {\n    }\n\n    friend std::ostream&amp; operator&lt;&lt; (std::ostream&amp; out, const Point&amp; point);\n    friend std::istream&amp; operator&gt;&gt; (std::istream&amp; out, Point&amp; point);\n};\n\nstd::ostream&amp; operator&lt;&lt; (std::ostream&amp; out, const Point&amp; point)\n{\n    // Since operator&lt;&lt; is a friend of the Point class, we can access Point's members directly.\n    out &lt;&lt; \"Point(\" &lt;&lt; point.m_x &lt;&lt; \", \" &lt;&lt; point.m_y &lt;&lt; \", \" &lt;&lt; point.m_z &lt;&lt; ')';\n\n    return out;\n}\n\n// note that point must be non-const so we can modify the object\nstd::istream&amp; operator&gt;&gt; (std::istream&amp; in, Point&amp; point)\n{\n    // This version subject to partial extraction issues (see below)\n    in &gt;&gt; point.m_x &gt;&gt; point.m_y &gt;&gt; point.m_z;\n\n    return in;\n}\n\nint main()\n{\n    std::cout &lt;&lt; \"Enter a point: \";\n\n    Point point{ 1.0, 2.0, 3.0 }; // non-zero test data\n    std::cin &gt;&gt; point;\n\n    std::cout &lt;&lt; \"You entered: \" &lt;&lt; point &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b32",
      "type": "paragraph",
      "html": "<p>Assuming the user enters <code>4.0 5.6 7.26</code> as input, the program produces the following result:</p>",
      "preview": "Assuming the user enters 4.0 5.6 7.26 as input, the program produces the followi",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b33",
      "type": "output",
      "text": "You entered: Point(4, 5.6, 7.26)\n",
      "html": "<pre>You entered: Point(4, 5.6, 7.26)\n</pre>",
      "canCheck": false
    },
    {
      "id": "overloading-the-io-operators_b34",
      "type": "paragraph",
      "html": "<p>Now let’s see what happens when the user enters <code>4.0b 5.6 7.26</code> as input (notice the <code>b</code> after the <code>4.0</code>):</p>",
      "preview": "Now let’s see what happens when the user enters 4.0b 5.6 7.26 as input (notice t",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b35",
      "type": "output",
      "text": "You entered: Point(4, 0, 3)\n",
      "html": "<pre>You entered: Point(4, 0, 3)\n</pre>",
      "canCheck": false
    },
    {
      "id": "overloading-the-io-operators_b36",
      "type": "paragraph",
      "html": "<p>Our point is now a weird hybrid consisting of one value from the user’s input (<code>4.0</code>), one value that has been zero-initialized (<code>0.0</code>), and one value that was untouched by the input function (<code>3.0</code>). That’s… not great!</p>",
      "preview": "Our point is now a weird hybrid consisting of one value from the user’s input (4",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b37",
      "type": "section",
      "title": "Guarding against partial extraction",
      "html": "<h3 class=\"cpp-section-heading\">Guarding against partial extraction</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-io-operators_b38",
      "type": "paragraph",
      "html": "<p>When we’re extracting a single value, there are only two possible outcomes: the extraction fails, or it is successful. However, when we’re extracting more than one value as part of an input operation, things get a bit more complicated.</p>",
      "preview": "When we’re extracting a single value, there are only two possible outcomes: the ",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b39",
      "type": "paragraph",
      "html": "<p>The above implementation of <code>operator&gt;&gt;</code> can result in a partial extraction. And this is exactly what we’re seeing with input <code>4.0b 5.6 7.26</code>. The extraction to <code>x_y</code> successfully extracts <code>4.0</code> from the user’s input, leaving <code>b 5.6 7.26</code> in the input stream. The extraction to <code>m_y</code> fails to extract <code>b</code>, so <code>m_y</code> is copy-assigned the value <code>0.0</code> and the input stream is set to failure mode. Since we haven’t cleared failure mode, the extraction to <code>m_z</code> aborts immediately, and the value that <code>m_z</code> had before the extraction attempt remains (<code>3.0</code>).</p>",
      "preview": "The above implementation of operator>> can result in a partial extraction. And t",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b40",
      "type": "paragraph",
      "html": "<p>There is no case where this is a desirable outcome. And in some cases, it might even be actively dangerous. Imagine we were writing an <code>operator&gt;&gt;</code> for a <code>Fraction</code> object instead. After successfully extracting the numerator, a failed extraction to the denominator would set the denominator to <code>0.0</code>, which might later cause a divide by zero and cause the application to crash.</p>",
      "preview": "There is no case where this is a desirable outcome. And in some cases, it might ",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b41",
      "type": "paragraph",
      "html": "<p>So how might we avoid this? One way is to make our operations transactional. A <strong>transactional operation</strong> must either completely succeed or completely fail -- no partial successes or failures are allowed. This is sometimes known as “all or nothing”. If a failure occurs at any point during the transaction, prior changes made by the operation must be undone.</p>",
      "preview": "So how might we avoid this? One way is to make our operations transactional. A t",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b42",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Key insight</p><p>Transactions occur all the time in real life. Consider the case where I want to transfer money from one bank account to another. This requires two steps: First the money must first be deducted from one account, and then it must be credited to the other account. In the execution of this operation, there are three possibilities:</p><ul><li>The deduction step fails (e.g. not enough funds). The transaction fails, and neither account balance reflects the transfer.</li><li>The crediting step fails (e.g. due to a technical problem). In this case, the deduction (which has already succeeded) must be undone. The transaction fails, and neither account balance reflects the transfer.</li><li>Both steps succeed. The transaction is successful, and both account balances reflect the transfer.</li></ul><p>The end result is that there are only two possible outcomes: the transfer fully fails and the account balances are unchanged, or the transfer succeeds and the account balances are both changed.</p></div>",
      "preview": "Key insightTransactions occur all the time in real life. Consider the case where I want to transfer ",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b43",
      "type": "paragraph",
      "html": "<p>Let’s reimplement our overloaded <code>Point</code> <code>operator&gt;&gt;</code> as a transactional operation:</p>",
      "preview": "Let’s reimplement our overloaded Point operator>> as a transactional operation:",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b44",
      "type": "code",
      "language": "cpp",
      "code": "// note that point must be non-const so we can modify the object\n// note that this implementation is a non-friend\nstd::istream& operator>> (std::istream& in, Point& point)\n{\n    double x{};\n    double y{};\n    double z{};\n    \n    if (in >> x >> y >> z)      // if all extractions succeeded\n        point = Point{x, y, z}; // overwrite our existing point\n        \n    return in;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// note that point must be non-const so we can modify the object\n// note that this implementation is a non-friend\nstd::istream&amp; operator&gt;&gt; (std::istream&amp; in, Point&amp; point)\n{\n    double x{};\n    double y{};\n    double z{};\n    \n    if (in &gt;&gt; x &gt;&gt; y &gt;&gt; z)      // if all extractions succeeded\n        point = Point{x, y, z}; // overwrite our existing point\n        \n    return in;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b45",
      "type": "paragraph",
      "html": "<p>In this implementation, we’re not overwriting the data members directly with the user’s input. Instead, we’re extracting the user’s input to temporary variables (<code>x</code>, <code>y</code>, and <code>z</code>). Once all extraction attempts have completed, we check whether all extractions were successful. If so, then we update all the members of <code>Point</code> together. Otherwise, we do not update any of them.</p>",
      "preview": "In this implementation, we’re not overwriting the data members directly with the",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b46",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Tip</p><p><code>if (in &gt;&gt; x &gt;&gt; y &gt;&gt; z)</code> is equivalent to <code>in &gt;&gt; x &gt;&gt; y &gt;&gt; z; if (in)</code>. Remember, each extraction returns <code>in</code> so that multiple extractions can be chained together. The single-statement version uses the <code>in</code> returned from the last extraction as the condition of the if-statement, whereas the multi-statement version uses <code>in</code> explicitly.</p></div>",
      "preview": "Tipif (in >> x >> y >> z) is equivalent to in >> x >> y >> z; if (in). Remember, each extraction ret",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b47",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Tip</p><p>Transactional operations can be implemented using a number of different strategies. For example:</p><ul><li>Alter on success: Store the result of each sub-operation. If all sub-operations succeed, replace the relevant data with the stored results. This is the strategy we use in the <code>Point</code> example above.</li><li>Restore on failure: Copy any data that can be altered. If any sub-operation fails, the changes made by prior sub-operations can be reverted using the data from copy.</li><li>Rollback on failure: If any sub-operation fails, each prior sub-operation is reversed (using an opposite sub-operation). This strategy is often used in databases, where the data is too large to back up, and the result of sub-operations can’t be stored.</li></ul></div>",
      "preview": "TipTransactional operations can be implemented using a number of different strategies. For example:A",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b48",
      "type": "paragraph",
      "html": "<p>While the above <code>operator&gt;&gt;</code> prevents partial extractions, it is inconsistent with how <code>operator&gt;&gt;</code> works for fundamental types. When extraction to an object with a fundamental type fails, the object isn’t left unaltered -- it is copy assigned the value <code>0</code> (this ensures the object has some consistent value in case it wasn’t initialized before the extraction attempt). Therefore, for consistency, you may wish to have a failed extraction reset the object to its default state (at least in cases where such a thing exists).</p>",
      "preview": "While the above operator>> prevents partial extractions, it is inconsistent with",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b49",
      "type": "paragraph",
      "html": "<p>Here’s an alternate version of <code>operator&gt;&gt;</code> that resets <code>Point</code> to its default state if any extraction fails:</p>",
      "preview": "Here’s an alternate version of operator>> that resets Point to its default state",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b50",
      "type": "code",
      "language": "cpp",
      "code": "// note that point must be non-const so we can modify the object\n// note that this implementation is a non-friend\nstd::istream& operator>> (std::istream& in, Point& point)\n{\n    double x{};\n    double y{};\n    double z{};\n    \n    in >> x >> y >> z;\n    point = in ? Point{x, y, z} : Point{};\n        \n    return in;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// note that point must be non-const so we can modify the object\n// note that this implementation is a non-friend\nstd::istream&amp; operator&gt;&gt; (std::istream&amp; in, Point&amp; point)\n{\n    double x{};\n    double y{};\n    double z{};\n    \n    in &gt;&gt; x &gt;&gt; y &gt;&gt; z;\n    point = in ? Point{x, y, z} : Point{};\n        \n    return in;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b51",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Author’s note</p><p>Such an operation is technically no longer transactional (because failure doesn’t “do nothing”). There doesn’t appear to be a general term for operations that guarantee no partial results. Perhaps “indivisible operation”.</p></div>",
      "preview": "Author’s noteSuch an operation is technically no longer transactional (because failure doesn’t “do n",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b52",
      "type": "section",
      "title": "Handling semantically invalid input",
      "html": "<h3 class=\"cpp-section-heading\">Handling semantically invalid input</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-io-operators_b53",
      "type": "paragraph",
      "html": "<p>Extraction can fail in different ways.</p>",
      "preview": "Extraction can fail in different ways.",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b54",
      "type": "paragraph",
      "html": "<p>In cases where <code>operator&gt;&gt;</code> simply fails to extract anything to a variable, <code>std::cin</code> will automatically be placed in failure mode (which we discuss in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/stdcin-and-handling-invalid-input/\">9.5 -- std::cin and handling invalid input</a>). The caller of this function can then check <code>std::cin</code> to see if it failed and handle that case as appropriate.</p>",
      "preview": "In cases where operator>> simply fails to extract anything to a variable, std::c",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b55",
      "type": "paragraph",
      "html": "<p>But what about cases where the user inputs a value that is extractable but semantically invalid (e.g. a <code>Fraction</code> with a denominator of <code>0</code>)? Because <code>std::cin</code> did extract something, it won’t go into failure mode automatically. And then the caller probably won’t realize something went wrong.</p>",
      "preview": "But what about cases where the user inputs a value that is extractable but seman",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b56",
      "type": "paragraph",
      "html": "<p>To address this, we can have our overloaded <code>operator&gt;&gt;</code> determine whether any of the values that were extracted are semantically invalid, and if so, manually put the input stream in failure mode. This can be done by calling <code>std::cin.setstate(std::ios_base::failbit);</code>.</p>",
      "preview": "To address this, we can have our overloaded operator>> determine whether any of ",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b57",
      "type": "paragraph",
      "html": "<p>Here’s an example of a transactional overloaded <code>operator&gt;&gt;</code> for <code>Point</code> that will cause the input stream to enter failure mode if the user inputs an extractable negative value:</p>",
      "preview": "Here’s an example of a transactional overloaded operator>> for Point that will c",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b58",
      "type": "code",
      "language": "cpp",
      "code": "std::istream& operator>> (std::istream& in, Point& point)\n{\n    double x{};\n    double y{};\n    double z{};\n    \n    in >> x >> y >> z;\n    if (x < 0.0 || y < 0.0 || z < 0.0)       // if any extractable input is negative\n        in.setstate(std::ios_base::failbit); // set failure mode manually\n    point = in ? Point{x, y, z} : Point{};\n       \n    return in;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">std::istream&amp; operator&gt;&gt; (std::istream&amp; in, Point&amp; point)\n{\n    double x{};\n    double y{};\n    double z{};\n    \n    in &gt;&gt; x &gt;&gt; y &gt;&gt; z;\n    if (x &lt; 0.0 || y &lt; 0.0 || z &lt; 0.0)       // if any extractable input is negative\n        in.setstate(std::ios_base::failbit); // set failure mode manually\n    point = in ? Point{x, y, z} : Point{};\n       \n    return in;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b59",
      "type": "section",
      "title": "Conclusion",
      "html": "<h3 class=\"cpp-section-heading\">Conclusion</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-io-operators_b60",
      "type": "paragraph",
      "html": "<p>Overloading <code>operator&lt;&lt;</code> and <code>operator&gt;&gt;</code> make it easy to output your class to screen and accept user input from the console.</p>",
      "preview": "Overloading operator<< and operator>> make it easy to output your class to scree",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b61",
      "type": "section",
      "title": "Quiz time",
      "html": "<h3 class=\"cpp-section-heading\">Quiz time</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-io-operators_b62",
      "type": "quiz",
      "questionTitle": "Question #1",
      "questionHtml": "Take the Fraction class below and add an overloaded <code>operator&lt;&lt;</code> and <code>operator&gt;&gt;</code> to it. Your <code>operator&gt;&gt;</code> should avoid partial extractions and fail if user inputs a denominator of <code>0</code>. It should not reset the Fraction to default on failure.",
      "hintHtml": "",
      "solutionHtml": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;limits&gt;\n#include &lt;numeric&gt; // for std::gcd\n\nclass Fraction\n{\nprivate:\n    int m_numerator{ 0 };\n    int m_denominator{ 1 };\n\npublic:\n    Fraction(int numerator=0, int denominator = 1) :\n        m_numerator{ numerator }, m_denominator{ denominator }\n    {\n        // We put reduce() in the constructor to ensure any new fractions we make get reduced!\n        // Any fractions that are overwritten will need to be re-reduced\n        reduce();\n    }\n\n    void reduce()\n    {\n        int gcd{ std::gcd(m_numerator, m_denominator) };\n        if (gcd)\n        {\n            m_numerator /= gcd;\n            m_denominator /= gcd;\n        }\n    }\n\n    friend Fraction operator*(const Fraction&amp; f1, const Fraction&amp; f2);\n    friend Fraction operator*(const Fraction&amp; f1, int value);\n    friend Fraction operator*(int value, const Fraction&amp; f1);\n\n    friend std::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const Fraction&amp; f1);\n\n    void print() const\n    {\n        std::cout &lt;&lt; m_numerator &lt;&lt; '/' &lt;&lt; m_denominator &lt;&lt; '\\n';\n    }\n};\n\nFraction operator*(const Fraction&amp; f1, const Fraction&amp; f2)\n{\n    return Fraction { f1.m_numerator * f2.m_numerator, f1.m_denominator * f2.m_denominator };\n}\n\nFraction operator*(const Fraction&amp; f1, int value)\n{\n    return Fraction { f1.m_numerator * value, f1.m_denominator };\n}\n\nFraction operator*(int value, const Fraction&amp; f1)\n{\n    return Fraction { f1.m_numerator * value, f1.m_denominator };\n}\n\nstd::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const Fraction&amp; f1)\n{\n    out &lt;&lt; f1.m_numerator &lt;&lt; '/' &lt;&lt; f1.m_denominator;\n    return out;\n}\n\nstd::istream&amp; operator&gt;&gt;(std::istream&amp; in, Fraction&amp; f1)\n{\n    int numerator {};\n    char ignore {};\n    int denominator {};\n    \n    in &gt;&gt; numerator &gt;&gt; ignore &gt;&gt; denominator;\n    if (denominator == 0)                       // if our denominator is semantically invalid\n        in.setstate(std::ios_base::failbit);    // set failure mode manually\n    if (in)                                     // if we're not in failure mode\n        f1 = Fraction {numerator, denominator}; // update our object to the extracted values\n\n    return in;\n}\n\nint main()\n{\n    Fraction f1{};\n    std::cout &lt;&lt; \"Enter fraction 1: \";\n    std::cin &gt;&gt; f1;\n\n    Fraction f2{};\n    std::cout &lt;&lt; \"Enter fraction 2: \";\n    std::cin &gt;&gt; f2;\n\n    std::cout &lt;&lt; f1 &lt;&lt; \" * \" &lt;&lt; f2 &lt;&lt; \" is \" &lt;&lt; f1 * f2 &lt;&lt; '\\n'; // note: The result of f1 * f2 is an r-value\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b63",
      "type": "paragraph",
      "html": "<p>The following program should compile:</p>",
      "preview": "The following program should compile:",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b64",
      "type": "code",
      "language": "cpp",
      "code": "int main()\n{\n\tFraction f1{};\n\tstd::cout << \"Enter fraction 1: \";\n\tstd::cin >> f1;\n\n\tFraction f2{};\n\tstd::cout << \"Enter fraction 2: \";\n\tstd::cin >> f2;\n\n\tstd::cout << f1 << \" * \" << f2 << \" is \" << f1 * f2 << '\\n'; // note: The result of f1 * f2 is an r-value\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int main()\n{\n\tFraction f1{};\n\tstd::cout &lt;&lt; \"Enter fraction 1: \";\n\tstd::cin &gt;&gt; f1;\n\n\tFraction f2{};\n\tstd::cout &lt;&lt; \"Enter fraction 2: \";\n\tstd::cin &gt;&gt; f2;\n\n\tstd::cout &lt;&lt; f1 &lt;&lt; \" * \" &lt;&lt; f2 &lt;&lt; \" is \" &lt;&lt; f1 * f2 &lt;&lt; '\\n'; // note: The result of f1 * f2 is an r-value\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b65",
      "type": "paragraph",
      "html": "<p>And produce the result:</p>",
      "preview": "And produce the result:",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b66",
      "type": "output",
      "text": "Enter fraction 1: 2/3\nEnter fraction 2: 3/8\n2/3 * 3/8 is 1/4\n",
      "html": "<pre>Enter fraction 1: 2/3\nEnter fraction 2: 3/8\n2/3 * 3/8 is 1/4\n</pre>",
      "canCheck": false
    },
    {
      "id": "overloading-the-io-operators_b67",
      "type": "paragraph",
      "html": "<p>Here’s the Fraction class:</p>",
      "preview": "Here’s the Fraction class:",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b68",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <numeric> // for std::gcd\n \nclass Fraction\n{\nprivate:\n\tint m_numerator{};\n\tint m_denominator{};\n \npublic:\n\tFraction(int numerator=0, int denominator=1):\n\t\tm_numerator{numerator}, m_denominator{denominator}\n\t{\n\t\t// We put reduce() in the constructor to ensure any new fractions we make get reduced!\n\t\t// Any fractions that are overwritten will need to be re-reduced\n\t\treduce();\n\t}\n\n\tvoid reduce()\n\t{\n\t\tint gcd{ std::gcd(m_numerator, m_denominator) };\n\t\tif (gcd)\n\t\t{\n\t\t\tm_numerator /= gcd;\n\t\t\tm_denominator /= gcd;\n\t\t}\n\t}\n \n\tfriend Fraction operator*(const Fraction& f1, const Fraction& f2);\n\tfriend Fraction operator*(const Fraction& f1, int value);\n\tfriend Fraction operator*(int value, const Fraction& f1);\n \n\tvoid print() const\n\t{\n\t\tstd::cout << m_numerator << '/' << m_denominator << '\\n';\n\t}\n};\n \nFraction operator*(const Fraction& f1, const Fraction& f2)\n{\n\treturn Fraction { f1.m_numerator * f2.m_numerator, f1.m_denominator * f2.m_denominator };\n}\n \nFraction operator*(const Fraction& f1, int value)\n{\n\treturn Fraction { f1.m_numerator * value, f1.m_denominator };\n}\n \nFraction operator*(int value, const Fraction& f1)\n{\n\treturn Fraction { f1.m_numerator * value, f1.m_denominator };\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;numeric&gt; // for std::gcd\n \nclass Fraction\n{\nprivate:\n\tint m_numerator{};\n\tint m_denominator{};\n \npublic:\n\tFraction(int numerator=0, int denominator=1):\n\t\tm_numerator{numerator}, m_denominator{denominator}\n\t{\n\t\t// We put reduce() in the constructor to ensure any new fractions we make get reduced!\n\t\t// Any fractions that are overwritten will need to be re-reduced\n\t\treduce();\n\t}\n\n\tvoid reduce()\n\t{\n\t\tint gcd{ std::gcd(m_numerator, m_denominator) };\n\t\tif (gcd)\n\t\t{\n\t\t\tm_numerator /= gcd;\n\t\t\tm_denominator /= gcd;\n\t\t}\n\t}\n \n\tfriend Fraction operator*(const Fraction&amp; f1, const Fraction&amp; f2);\n\tfriend Fraction operator*(const Fraction&amp; f1, int value);\n\tfriend Fraction operator*(int value, const Fraction&amp; f1);\n \n\tvoid print() const\n\t{\n\t\tstd::cout &lt;&lt; m_numerator &lt;&lt; '/' &lt;&lt; m_denominator &lt;&lt; '\\n';\n\t}\n};\n \nFraction operator*(const Fraction&amp; f1, const Fraction&amp; f2)\n{\n\treturn Fraction { f1.m_numerator * f2.m_numerator, f1.m_denominator * f2.m_denominator };\n}\n \nFraction operator*(const Fraction&amp; f1, int value)\n{\n\treturn Fraction { f1.m_numerator * value, f1.m_denominator };\n}\n \nFraction operator*(int value, const Fraction&amp; f1)\n{\n\treturn Fraction { f1.m_numerator * value, f1.m_denominator };\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b69",
      "type": "paragraph",
      "html": "<p>If you’re on a pre-C++17 compiler, you can replace std::gcd with this function:</p>",
      "preview": "If you’re on a pre-C++17 compiler, you can replace std::gcd with this function:",
      "canCheck": true
    },
    {
      "id": "overloading-the-io-operators_b70",
      "type": "code",
      "language": "cpp",
      "code": "#include <cmath>\n \nint gcd(int a, int b) {\n    return (b == 0) ? std::abs(a) : gcd(b, a % b);\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cmath&gt;\n \nint gcd(int a, int b) {\n    return (b == 0) ? std::abs(a) : gcd(b, a % b);\n}</code></pre>",
      "canCheck": true
    }
  ]
}