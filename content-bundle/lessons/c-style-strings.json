{
  "index": 234,
  "chapterId": "17",
  "number": "17.10",
  "title": "17.10 — C-style strings",
  "url": "https://www.learncpp.com/cpp-tutorial/c-style-strings/",
  "slug": "c-style-strings",
  "scrapedAt": "2026-09-15T16:30:37.485Z",
  "totalBlocks": 56,
  "totalCheckpoints": 46,
  "blocks": [
    {
      "id": "c-style-strings_b0",
      "type": "paragraph",
      "html": "<p>In lesson <a href=\"https://www.learncpp.com/cpp-tutorial/introduction-to-c-style-arrays/\">17.7 -- Introduction to C-style arrays</a>, we introduced C-style arrays, which allow us to define a sequential collection of elements:</p>",
      "preview": "In lesson 17.7 -- Introduction to C-style arrays, we introduced C-style arrays, ",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b1",
      "type": "code",
      "language": "cpp",
      "code": "    int testScore[30] {}; // an array of 30 ints, indices 0 through 29",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">    int testScore[30] {}; // an array of 30 ints, indices 0 through 29</code></pre>",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b2",
      "type": "paragraph",
      "html": "<p>In lesson <a href=\"https://www.learncpp.com/cpp-tutorial/literals/\">5.2 -- Literals</a>, we defined a string as a collection of sequential characters (such as “Hello, world!”), and introduced C-style string literals. We also noted that the C-style string literal “Hello, world!” has type <code>const char[14]</code> (13 explicit characters plus 1 hidden null-terminator character).</p>",
      "preview": "In lesson 5.2 -- Literals, we defined a string as a collection of sequential cha",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b3",
      "type": "paragraph",
      "html": "<p>If you hadn’t connected the dots before, it should be obvious now that C-style strings are just C-style arrays whose element type is <code>char</code> or <code>const char</code>!</p>",
      "preview": "If you hadn’t connected the dots before, it should be obvious now that C-style s",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b4",
      "type": "paragraph",
      "html": "<p>Although C-style string literals are fine to use in our code, C-style string objects have fallen out of favor in modern C++ because they are hard to use and dangerous (with <code>std::string</code> and <code>std::string_view</code> being the modern replacements). Regardless, you may still run across uses of C-style string objects in older code, and we would be remiss not to cover them at all.</p>",
      "preview": "Although C-style string literals are fine to use in our code, C-style string obj",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b5",
      "type": "paragraph",
      "html": "<p>Therefore, in this lesson, we’ll take a look at the most important points regarding C-style string objects in modern C++.</p>",
      "preview": "Therefore, in this lesson, we’ll take a look at the most important points regard",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b6",
      "type": "section",
      "title": "Defining C-style strings",
      "html": "<h3 class=\"cpp-section-heading\">Defining C-style strings</h3>",
      "canCheck": false
    },
    {
      "id": "c-style-strings_b7",
      "type": "paragraph",
      "html": "<p>To define a C-style string variable, simply declare a C-style array variable of <code>char</code> (or <code>const char</code> / <code>constexpr char</code>):</p>",
      "preview": "To define a C-style string variable, simply declare a C-style array variable of ",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b8",
      "type": "code",
      "language": "cpp",
      "code": "char str1[8]{};                    // an array of 8 char, indices 0 through 7\n\nconst char str2[]{ \"string\" };     // an array of 7 char, indices 0 through 6\nconstexpr char str3[] { \"hello\" }; // an array of 6 const char, indices 0 through 5",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">char str1[8]{};                    // an array of 8 char, indices 0 through 7\n\nconst char str2[]{ \"string\" };     // an array of 7 char, indices 0 through 6\nconstexpr char str3[] { \"hello\" }; // an array of 6 const char, indices 0 through 5</code></pre>",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b9",
      "type": "paragraph",
      "html": "<p>Remember that we need an extra character for the implicit null terminator.</p>",
      "preview": "Remember that we need an extra character for the implicit null terminator.",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b10",
      "type": "paragraph",
      "html": "<p>When defining C-style strings with an initializer, we highly recommend omitting the array length and letting the compiler calculate the length. That way if the initializer changes in the future, you won’t have to remember to update the length, and there is no risk in forgetting to include an extra element to hold the null terminator.</p>",
      "preview": "When defining C-style strings with an initializer, we highly recommend omitting ",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b11",
      "type": "section",
      "title": "C-style strings will decay",
      "html": "<h3 class=\"cpp-section-heading\">C-style strings will decay</h3>",
      "canCheck": false
    },
    {
      "id": "c-style-strings_b12",
      "type": "paragraph",
      "html": "<p>In lesson <a href=\"https://www.learncpp.com/cpp-tutorial/c-style-array-decay/\">17.8 -- C-style array decay</a>, we discussed how C-style arrays will decay into a pointer in most circumstances. Because C-style strings are C-style arrays, they will decay -- C-style string literals decay into a <code>const char*</code>, and C-style string arrays decay into either a <code>const char*</code> or <code>char*</code> depending on whether the array is const. And when a C-style string decays into a pointer, the length of the string (encoded in the type information) is lost.</p>",
      "preview": "In lesson 17.8 -- C-style array decay, we discussed how C-style arrays will deca",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b13",
      "type": "paragraph",
      "html": "<p>This loss of length information is the reason C-style strings have a null-terminator. The length of the string can be (inefficiently) regenerated by counting the number of elements between the start of the string and the null terminator.</p>",
      "preview": "This loss of length information is the reason C-style strings have a null-termin",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b14",
      "type": "section",
      "title": "Outputting a C-style string",
      "html": "<h3 class=\"cpp-section-heading\">Outputting a C-style string</h3>",
      "canCheck": false
    },
    {
      "id": "c-style-strings_b15",
      "type": "paragraph",
      "html": "<p>When outputting a C-style string, <code>std::cout</code> outputs characters until it encounters the null terminator. This null terminator marks the end of the string, so that decayed strings (which have lost their length information) can still be printed.</p>",
      "preview": "When outputting a C-style string, std::cout outputs characters until it encounte",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b16",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nvoid print(char ptr[])\n{\n    std::cout << ptr << '\\n'; // output string\n}\n\nint main()\n{\n    char str[]{ \"string\" };\n    std::cout << str << '\\n'; // outputs string\n\n    print(str);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nvoid print(char ptr[])\n{\n    std::cout &lt;&lt; ptr &lt;&lt; '\\n'; // output string\n}\n\nint main()\n{\n    char str[]{ \"string\" };\n    std::cout &lt;&lt; str &lt;&lt; '\\n'; // outputs string\n\n    print(str);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b17",
      "type": "paragraph",
      "html": "<p>If you try to print a string that does not have a null terminator (e.g. because the null-terminator was overwritten somehow), the result will be undefined behavior. The most likely outcome in this case will be that all the characters in the string are printed, and then it will just keep printing everything in adjacent memory slots (interpreted as a character) until it happens to hit a byte of memory containing a 0 (which will be interpreted as a null terminator)!</p>",
      "preview": "If you try to print a string that does not have a null terminator (e.g. because ",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b18",
      "type": "section",
      "title": "Inputting C-style strings",
      "html": "<h3 class=\"cpp-section-heading\">Inputting C-style strings</h3>",
      "canCheck": false
    },
    {
      "id": "c-style-strings_b19",
      "type": "paragraph",
      "html": "<p>Consider the case where we are asking the user to roll a die as many times as they wish and enter the numbers rolled without spaces (e.g. <code>524412616</code>). How many characters will the user enter? We have no idea.</p>",
      "preview": "Consider the case where we are asking the user to roll a die as many times as th",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b20",
      "type": "paragraph",
      "html": "<p>Because C-style strings are fixed-size arrays, the solution is to declare an array larger than we are ever likely to need:</p>",
      "preview": "Because C-style strings are fixed-size arrays, the solution is to declare an arr",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b21",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n    char rolls[255] {}; // declare array large enough to hold 254 characters + null terminator\n    std::cout << \"Enter your rolls: \";\n    std::cin >> rolls;\n    std::cout << \"You entered: \" << rolls << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n    char rolls[255] {}; // declare array large enough to hold 254 characters + null terminator\n    std::cout &lt;&lt; \"Enter your rolls: \";\n    std::cin &gt;&gt; rolls;\n    std::cout &lt;&lt; \"You entered: \" &lt;&lt; rolls &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b22",
      "type": "paragraph",
      "html": "<p>Prior to C++20, <code>std::cin &gt;&gt; rolls</code> would extract as many characters as possible to <code>rolls</code> (stopping at the first non-leading whitespace). Nothing is stopping the user from entering more than 254 characters (either unintentionally, or maliciously). And if that happens, the user’s input will overflow the <code>rolls</code> array and undefined behavior will result.</p>",
      "preview": "Prior to C++20, std::cin >> rolls would extract as many characters as possible t",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b23",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Key insight</p><p><strong>Array overflow</strong> or <strong>buffer overflow</strong> is a computer security issue that occurs when more data is copied into storage than the storage can hold. In such cases, the memory just beyond the storage will be overwritten, leading to undefined behavior. Malicious actors can potentially exploit such flaws to overwrite the contents of memory, hoping to change the program’s behavior in some advantageous way.</p></div>",
      "preview": "Key insightArray overflow or buffer overflow is a computer security issue that occurs when more data",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b24",
      "type": "paragraph",
      "html": "<p>In C++20, <code>operator&gt;&gt;</code> was changed so that it only works for inputting non-decayed C-style strings. This allows <code>operator&gt;&gt;</code> to extract only as many characters as the C-style string’s length will allow, preventing overflow. But this also means you can no longer use <code>operator&gt;&gt;</code> to input to decayed C-style strings.</p>",
      "preview": "In C++20, operator>> was changed so that it only works for inputting non-decayed",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b25",
      "type": "paragraph",
      "html": "<p>The recommended way of reading C-style strings using <code>std::cin</code> is as follows:</p>",
      "preview": "The recommended way of reading C-style strings using std::cin is as follows:",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b26",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <iterator> // for std::size\n\nint main()\n{\n    char rolls[255] {}; // declare array large enough to hold 254 characters + null terminator\n    std::cout << \"Enter your rolls: \";\n    std::cin.getline(rolls, std::size(rolls));\n    std::cout << \"You entered: \" << rolls << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;iterator&gt; // for std::size\n\nint main()\n{\n    char rolls[255] {}; // declare array large enough to hold 254 characters + null terminator\n    std::cout &lt;&lt; \"Enter your rolls: \";\n    std::cin.getline(rolls, std::size(rolls));\n    std::cout &lt;&lt; \"You entered: \" &lt;&lt; rolls &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b27",
      "type": "paragraph",
      "html": "<p>This call to <code>cin.getline()</code> will read up to 254 characters (including whitespace) into <code>rolls</code>. Any excess characters will be discarded. Because <code>getline()</code> takes a length, we can provide the maximum number of characters to accept. With a non-decayed array, this is easy -- we can use <code>std::size()</code> to get the array length. With a decayed array, we have to determine the length in some other way. And if we provide the wrong length, our program may malfunction or have security issues.</p>",
      "preview": "This call to cin.getline() will read up to 254 characters (including whitespace)",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b28",
      "type": "paragraph",
      "html": "<p>In modern C++, when storing inputted text from the user, it’s safer to use <code>std::string</code>, as <code>std::string</code> will adjust automatically to hold as many characters as needed.</p>",
      "preview": "In modern C++, when storing inputted text from the user, it’s safer to use std::",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b29",
      "type": "section",
      "title": "Modifying C-style strings",
      "html": "<h3 class=\"cpp-section-heading\">Modifying C-style strings</h3>",
      "canCheck": false
    },
    {
      "id": "c-style-strings_b30",
      "type": "paragraph",
      "html": "<p>One important point to note is that C-style strings follow the same rules as C-style arrays. This means you can initialize the string upon creation, but you can not assign values to it using the assignment operator after that!</p>",
      "preview": "One important point to note is that C-style strings follow the same rules as C-s",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b31",
      "type": "code",
      "language": "cpp",
      "code": "char str[]{ \"string\" }; // ok\nstr = \"rope\";           // not ok!",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">char str[]{ \"string\" }; // ok\nstr = \"rope\";           // not ok!</code></pre>",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b32",
      "type": "paragraph",
      "html": "<p>This makes using C-style strings a bit awkward.</p>",
      "preview": "This makes using C-style strings a bit awkward.",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b33",
      "type": "paragraph",
      "html": "<p>Since C-style strings are arrays, you can use the [] operator to change individual characters in the string:</p>",
      "preview": "Since C-style strings are arrays, you can use the [] operator to change individu",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b34",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n    char str[]{ \"string\" };\n    std::cout << str << '\\n';\n    str[1] = 'p';\n    std::cout << str << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n    char str[]{ \"string\" };\n    std::cout &lt;&lt; str &lt;&lt; '\\n';\n    str[1] = 'p';\n    std::cout &lt;&lt; str &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b35",
      "type": "paragraph",
      "html": "<p>This program prints:</p>",
      "preview": "This program prints:",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b36",
      "type": "output",
      "text": "string\nspring\n",
      "html": "<pre>string\nspring\n</pre>",
      "canCheck": false
    },
    {
      "id": "c-style-strings_b37",
      "type": "section",
      "title": "Getting the length of an C-style string",
      "html": "<h3 class=\"cpp-section-heading\">Getting the length of an C-style string</h3>",
      "canCheck": false
    },
    {
      "id": "c-style-strings_b38",
      "type": "paragraph",
      "html": "<p>Because C-style strings are C-style arrays, you can use <code>std::size()</code> (or in C++20, <code>std::ssize()</code>) to get the length of the string as an array. There are two caveats here:</p>",
      "preview": "Because C-style strings are C-style arrays, you can use std::size() (or in C++20",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b39",
      "type": "list",
      "html": "<ol start=\"1\"><li>This doesn’t work on decayed strings.</li><li>Returns the actual length of the C-style array, not the length of the string.</li></ol>",
      "preview": "This doesn’t work on decayed strings.Returns the actual length of the C-style ar",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b40",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n    char str[255]{ \"string\" }; // 6 characters + null terminator\n    std::cout << \"length = \" << std::size(str) << '\\n'; // prints length = 255\n\n    char *ptr { str };\n    std::cout << \"length = \" << std::size(ptr) << '\\n'; // compile error\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n    char str[255]{ \"string\" }; // 6 characters + null terminator\n    std::cout &lt;&lt; \"length = \" &lt;&lt; std::size(str) &lt;&lt; '\\n'; // prints length = 255\n\n    char *ptr { str };\n    std::cout &lt;&lt; \"length = \" &lt;&lt; std::size(ptr) &lt;&lt; '\\n'; // compile error\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b41",
      "type": "paragraph",
      "html": "<p>An alternate solution is to use the <code>strlen()</code> function, which lives in the <code>&lt;cstring&gt;</code> header. <code>strlen()</code> will work on decayed arrays, and returns the length of the string being held, excluding the null terminator:</p>",
      "preview": "An alternate solution is to use the strlen() function, which lives in the <cstri",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b42",
      "type": "code",
      "language": "cpp",
      "code": "#include <cstring> // for std::strlen\n#include <iostream>\n\nint main()\n{\n    char str[255]{ \"string\" }; // 6 characters + null terminator\n    std::cout << \"length = \" << std::strlen(str) << '\\n'; // prints length = 6\n\n    char *ptr { str };\n    std::cout << \"length = \" << std::strlen(ptr) << '\\n';   // prints length = 6\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cstring&gt; // for std::strlen\n#include &lt;iostream&gt;\n\nint main()\n{\n    char str[255]{ \"string\" }; // 6 characters + null terminator\n    std::cout &lt;&lt; \"length = \" &lt;&lt; std::strlen(str) &lt;&lt; '\\n'; // prints length = 6\n\n    char *ptr { str };\n    std::cout &lt;&lt; \"length = \" &lt;&lt; std::strlen(ptr) &lt;&lt; '\\n';   // prints length = 6\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b43",
      "type": "paragraph",
      "html": "<p>However, <code>std::strlen()</code> is slow, as it has to traverse through the whole array, counting characters until it hits the null terminator.</p>",
      "preview": "However, std::strlen() is slow, as it has to traverse through the whole array, c",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b44",
      "type": "section",
      "title": "Other C-style string manipulating functions",
      "html": "<h3 class=\"cpp-section-heading\">Other C-style string manipulating functions</h3>",
      "canCheck": false
    },
    {
      "id": "c-style-strings_b45",
      "type": "paragraph",
      "html": "<p>Because C-style strings are the primary string type in C, the C language provides many functions for manipulating C-style strings. These functions have been inherited by C++ as part of the <code>&lt;cstring&gt;</code> header.</p>",
      "preview": "Because C-style strings are the primary string type in C, the C language provide",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b46",
      "type": "paragraph",
      "html": "<p>Here are a few of the most useful that you may see in older code:</p>",
      "preview": "Here are a few of the most useful that you may see in older code:",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b47",
      "type": "list",
      "html": "<ul><li>strlen() -- returns the length of a C-style string</li><li>strcpy(), strncpy(), strcpy_s() -- overwrites one C-style string with another</li><li>strcat(), strncat() -- Appends one C-style string to the end of another</li><li>strcmp(), strncmp() -- Compares two C-style strings (returns <code>0</code> if equal)</li></ul>",
      "preview": "strlen() -- returns the length of a C-style stringstrcpy(), strncpy(), strcpy_s(",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b48",
      "type": "paragraph",
      "html": "<p>Except for <code>strlen()</code>, we generally recommend avoiding these.</p>",
      "preview": "Except for strlen(), we generally recommend avoiding these.",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b49",
      "type": "section",
      "title": "Avoid non-const C-style string objects",
      "html": "<h3 class=\"cpp-section-heading\">Avoid non-const C-style string objects</h3>",
      "canCheck": false
    },
    {
      "id": "c-style-strings_b50",
      "type": "paragraph",
      "html": "<p>Unless you have a specific, compelling reason to use non-const C-style strings, they are best avoided, as they are awkward to work with and are prone to overruns, which will cause undefined behavior (and are potential security issues).</p>",
      "preview": "Unless you have a specific, compelling reason to use non-const C-style strings, ",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b51",
      "type": "paragraph",
      "html": "<p>In the rare case that you do need to work with C-style strings or fixed buffer sizes (e.g. for memory-limited devices), we’d recommend using a well-tested 3rd party fixed-length string library designed for the purpose.</p>",
      "preview": "In the rare case that you do need to work with C-style strings or fixed buffer s",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b52",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Avoid non-const C-style string objects in favor of <code>std::string</code>.</p></div>",
      "preview": "Best practiceAvoid non-const C-style string objects in favor of std::string.",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b53",
      "type": "section",
      "title": "Quiz time",
      "html": "<h3 class=\"cpp-section-heading\">Quiz time</h3>",
      "canCheck": false
    },
    {
      "id": "c-style-strings_b54",
      "type": "quiz",
      "questionTitle": "Question #1",
      "questionHtml": "Write a function to print a C-style string character by character. Use a pointer and pointer arithmetic to step through each character of the string and print that character. Write a <code>main</code> function that tests the function with the string literal “Hello, world!”.",
      "hintHtml": "",
      "solutionHtml": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\n// str will point to the first letter of the C-style string.\n// Note that str points to a const char, so we can not change the values it points to.\n// However, we can point str at something else.  This does not change the value of the argument.\nvoid printCString(const char str[])\n{\n    // While we haven't encountered a null terminator\n    while (*str != '\\0')\n    {\n        // print the current character\n        std::cout &lt;&lt; *str;\n\n        // and use pointer arithmetic to move str to the next character\n        ++str;\n    }\n}\n\nint main()\n{\n    printCString(\"Hello world!\");\n\n    std::cout &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "c-style-strings_b55",
      "type": "quiz",
      "questionTitle": "Question #2",
      "questionHtml": "Repeat quiz #1, but this time the function should print the string backwards.",
      "hintHtml": "",
      "solutionHtml": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nvoid printCStringBackwards(const char str[])\n{\n    // We can't modify str this time (we need it later)\n    // So we'll define a new pointer with the same address as str\n    const char *ptr{ str };\n\n    // Find the null terminator\n    while (*ptr != '\\0')\n        ++ptr;\n\n    // Now walk backwards and print characters until ptr reaches str again\n    while (ptr-- != str)\n    {\n        std::cout &lt;&lt; *ptr;\n    } \n}\n\nint main()\n{\n    printCStringBackwards(\"Hello world!\");\n\n    std::cout &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    }
  ]
}