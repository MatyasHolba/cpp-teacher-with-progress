{
  "index": 161,
  "chapterId": "12",
  "number": "12.12",
  "title": "12.12 — Return by reference and return by address",
  "url": "https://www.learncpp.com/cpp-tutorial/return-by-reference-and-return-by-address/",
  "slug": "return-by-reference-and-return-by-address",
  "scrapedAt": "2026-09-15T16:29:59.451Z",
  "totalBlocks": 81,
  "totalCheckpoints": 67,
  "blocks": [
    {
      "id": "return-by-reference-and-return-by-address_b0",
      "type": "paragraph",
      "html": "<p>In previous lessons, we discussed that when passing an argument by value, a copy of the argument is made into the function parameter. For fundamental types (which are cheap to copy), this is fine. But copying is typically expensive for class types (such as <code>std::string</code>). We can avoid making an expensive copy by utilizing passing by (const) reference (or pass by address) instead.</p>",
      "preview": "In previous lessons, we discussed that when passing an argument by value, a copy",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b1",
      "type": "paragraph",
      "html": "<p>We encounter a similar situation when returning by value: a copy of the return value is passed back to the caller. If the return type of the function is a class type, this can be expensive.</p>",
      "preview": "We encounter a similar situation when returning by value: a copy of the return v",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b2",
      "type": "code",
      "language": "cpp",
      "code": "std::string returnByValue(); // returns a copy of a std::string (expensive)",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">std::string returnByValue(); // returns a copy of a std::string (expensive)</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b3",
      "type": "section",
      "title": "Return by reference",
      "html": "<h3 class=\"cpp-section-heading\">Return by reference</h3>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b4",
      "type": "paragraph",
      "html": "<p>In cases where we’re passing a class type back to the caller, we may (or may not) want to return by reference instead. <strong>Return by reference</strong> returns a reference that is bound to the object being returned, which avoids making a copy of the return value. To return by reference, we simply define the return value of the function to be a reference type:</p>",
      "preview": "In cases where we’re passing a class type back to the caller, we may (or may not",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b5",
      "type": "code",
      "language": "cpp",
      "code": "std::string&       returnByReference(); // returns a reference to an existing std::string (cheap)\nconst std::string& returnByReferenceToConst(); // returns a const reference to an existing std::string (cheap)",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">std::string&amp;       returnByReference(); // returns a reference to an existing std::string (cheap)\nconst std::string&amp; returnByReferenceToConst(); // returns a const reference to an existing std::string (cheap)</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b6",
      "type": "paragraph",
      "html": "<p>Here is an academic program to demonstrate the mechanics of return by reference:</p>",
      "preview": "Here is an academic program to demonstrate the mechanics of return by reference:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b7",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <string>\n\nconst std::string& getProgramName() // returns a const reference\n{\n    static const std::string s_programName { \"Calculator\" }; // has static duration, destroyed at end of program\n\n    return s_programName;\n}\n\nint main()\n{\n    std::cout << \"This program is named \" << getProgramName();\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n\nconst std::string&amp; getProgramName() // returns a const reference\n{\n    static const std::string s_programName { \"Calculator\" }; // has static duration, destroyed at end of program\n\n    return s_programName;\n}\n\nint main()\n{\n    std::cout &lt;&lt; \"This program is named \" &lt;&lt; getProgramName();\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b8",
      "type": "paragraph",
      "html": "<p>This program prints:</p>",
      "preview": "This program prints:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b9",
      "type": "output",
      "text": "This program is named Calculator\n",
      "html": "<pre>This program is named Calculator\n</pre>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b10",
      "type": "paragraph",
      "html": "<p>Because <code>getProgramName()</code> returns a const reference, when the line <code>return s_programName</code> is executed, <code>getProgramName()</code> will return a const reference to <code>s_programName</code> (thus avoiding making a copy). That const reference can then be used by the caller to access the value of <code>s_programName</code>, which is printed.</p>",
      "preview": "Because getProgramName() returns a const reference, when the line return s_progr",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b11",
      "type": "section",
      "title": "The object being returned by reference must exist after the function returns",
      "html": "<h3 class=\"cpp-section-heading\">The object being returned by reference must exist after the function returns</h3>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b12",
      "type": "paragraph",
      "html": "<p>Using return by reference has one major caveat: the programmer <em>must</em> be sure that the object being referenced outlives the function returning the reference. Otherwise, the reference being returned will be left dangling (referencing an object that has been destroyed), and use of that reference will result in undefined behavior.</p>",
      "preview": "Using return by reference has one major caveat: the programmer must be sure that",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b13",
      "type": "paragraph",
      "html": "<p>In the program above, because <code>s_programName</code> has static duration, <code>s_programName</code> will exist until the end of the program. When <code>main()</code> accesses the returned reference, it is actually accessing <code>s_programName</code>, which is fine, because <code>s_programName</code> won’t be destroyed until later.</p>",
      "preview": "In the program above, because s_programName has static duration, s_programName w",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b14",
      "type": "paragraph",
      "html": "<p>Now let’s modify the above program to show what happens in the case where our function returns a dangling reference:</p>",
      "preview": "Now let’s modify the above program to show what happens in the case where our fu",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b15",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <string>\n\nconst std::string& getProgramName()\n{\n    const std::string programName { \"Calculator\" }; // now a non-static local variable, destroyed when function ends\n\n    return programName;\n}\n\nint main()\n{\n    std::cout << \"This program is named \" << getProgramName(); // undefined behavior\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n\nconst std::string&amp; getProgramName()\n{\n    const std::string programName { \"Calculator\" }; // now a non-static local variable, destroyed when function ends\n\n    return programName;\n}\n\nint main()\n{\n    std::cout &lt;&lt; \"This program is named \" &lt;&lt; getProgramName(); // undefined behavior\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b16",
      "type": "paragraph",
      "html": "<p>The result of this program is undefined. When <code>getProgramName()</code> returns, a reference bound to local variable <code>programName</code> is returned. Then, because <code>programName</code> is a local variable with automatic duration, <code>programName</code> is destroyed at the end of the function. That means the returned reference is now dangling, and use of <code>programName</code> in the <code>main()</code> function results in undefined behavior.</p>",
      "preview": "The result of this program is undefined. When getProgramName() returns, a refere",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b17",
      "type": "paragraph",
      "html": "<p>Modern compilers will produce a warning or error if you try to return a local variable by reference (so the above program may not even compile), but compilers sometimes have trouble detecting more complicated cases.</p>",
      "preview": "Modern compilers will produce a warning or error if you try to return a local va",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b18",
      "type": "callout",
      "calloutType": "warning",
      "html": "<div class=\"cpp-note cpp-lightredbackground\"><p class=\"cpp-note-title cpp-bottomline\">Warning</p><p>Objects returned by reference must live beyond the scope of the function returning the reference, or a dangling reference will result. Never return a (non-static) local variable or temporary by reference.</p></div>",
      "preview": "WarningObjects returned by reference must live beyond the scope of the function returning the refere",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b19",
      "type": "section",
      "title": "Lifetime extension doesn’t work across function boundaries",
      "html": "<h3 class=\"cpp-section-heading\">Lifetime extension doesn’t work across function boundaries</h3>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b20",
      "type": "paragraph",
      "html": "<p>Let’s take a look at an example where we return a temporary by reference:</p>",
      "preview": "Let’s take a look at an example where we return a temporary by reference:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b21",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nconst int& returnByConstReference()\n{\n    return 5; // returns const reference to temporary object\n}\n\nint main()\n{\n    const int& ref { returnByConstReference() };\n\n    std::cout << ref; // undefined behavior\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nconst int&amp; returnByConstReference()\n{\n    return 5; // returns const reference to temporary object\n}\n\nint main()\n{\n    const int&amp; ref { returnByConstReference() };\n\n    std::cout &lt;&lt; ref; // undefined behavior\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b22",
      "type": "paragraph",
      "html": "<p>In the above program, <code>returnByConstReference()</code> is returning an integer literal, but the return type of the function is <code>const int&amp;</code>. This results in the creation and return of a temporary reference bound to a temporary object holding value 5. This returned reference is copied into a temporary reference in the scope of the caller. The temporary object then goes out of scope, leaving the temporary reference in the scope of the caller dangling.</p>",
      "preview": "In the above program, returnByConstReference() is returning an integer literal, ",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b23",
      "type": "paragraph",
      "html": "<p>By the time the temporary reference in the scope of the caller is bound to const reference variable <code>ref</code> (in <code>main()</code>), it is too late to extend the lifetime of the temporary object -- as it has already been destroyed. Thus <code>ref</code> is a dangling reference, and use of the value of <code>ref</code> will result in undefined behavior.</p>",
      "preview": "By the time the temporary reference in the scope of the caller is bound to const",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b24",
      "type": "paragraph",
      "html": "<p>Here’s a less obvious example that similarly doesn’t work:</p>",
      "preview": "Here’s a less obvious example that similarly doesn’t work:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b25",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nconst int& returnByConstReference(const int& ref)\n{\n    return ref;\n}\n\nint main()\n{\n    // case 1: direct binding\n    const int& ref1 { 5 }; // extends lifetime\n    std::cout << ref1 << '\\n'; // okay\n\n    // case 2: indirect binding\n    const int& ref2 { returnByConstReference(5) }; // binds to dangling reference\n    std::cout << ref2 << '\\n'; // undefined behavior\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nconst int&amp; returnByConstReference(const int&amp; ref)\n{\n    return ref;\n}\n\nint main()\n{\n    // case 1: direct binding\n    const int&amp; ref1 { 5 }; // extends lifetime\n    std::cout &lt;&lt; ref1 &lt;&lt; '\\n'; // okay\n\n    // case 2: indirect binding\n    const int&amp; ref2 { returnByConstReference(5) }; // binds to dangling reference\n    std::cout &lt;&lt; ref2 &lt;&lt; '\\n'; // undefined behavior\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b26",
      "type": "paragraph",
      "html": "<p>In case 2, a temporary object is created to hold value <code>5</code>, which function parameter <code>ref</code> binds to. The function just returns this reference back to the caller, which then uses the reference to initialize <code>ref2</code>. Because this is not a direct binding to the temporary object (as the refrence was bounced through a function), lifetime extension doesn’t apply. This leaves <code>ref2</code> dangling, and its subsequent use is undefined behavior.</p>",
      "preview": "In case 2, a temporary object is created to hold value 5, which function paramet",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b27",
      "type": "callout",
      "calloutType": "warning",
      "html": "<div class=\"cpp-note cpp-lightredbackground\"><p class=\"cpp-note-title cpp-bottomline\">Warning</p><p>Reference lifetime extension does not work across function boundaries.</p></div>",
      "preview": "WarningReference lifetime extension does not work across function boundaries.",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b28",
      "type": "section",
      "title": "Don’t return non-const static local variables by reference",
      "html": "<h3 class=\"cpp-section-heading\">Don’t return non-const static local variables by reference</h3>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b29",
      "type": "paragraph",
      "html": "<p>In the original example above, we returned a const static local variable by reference to illustrate the mechanics of return by reference in a simple way. However, returning non-const static local variables by reference is fairly non-idiomatic, and should generally be avoided. Here’s a simplified example that illustrates one such problem that can occur:</p>",
      "preview": "In the original example above, we returned a const static local variable by refe",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b30",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <string>\n\nconst int& getNextId()\n{\n    static int s_x{ 0 }; // note: variable is non-const\n    ++s_x; // generate the next id\n    return s_x; // and return a reference to it\n}\n\nint main()\n{\n    const int& id1 { getNextId() }; // id1 is a reference\n    const int& id2 { getNextId() }; // id2 is a reference\n\n    std::cout << id1 << id2 << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n\nconst int&amp; getNextId()\n{\n    static int s_x{ 0 }; // note: variable is non-const\n    ++s_x; // generate the next id\n    return s_x; // and return a reference to it\n}\n\nint main()\n{\n    const int&amp; id1 { getNextId() }; // id1 is a reference\n    const int&amp; id2 { getNextId() }; // id2 is a reference\n\n    std::cout &lt;&lt; id1 &lt;&lt; id2 &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b31",
      "type": "paragraph",
      "html": "<p>This program prints:</p>",
      "preview": "This program prints:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b32",
      "type": "output",
      "text": "22\n",
      "html": "<pre>22\n</pre>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b33",
      "type": "paragraph",
      "html": "<p>This happens because <code>id1</code> and <code>id2</code> are referencing the same object (the static variable <code>s_x</code>), so when anything (e.g. <code>getNextId()</code>) modifies that value, all references are now accessing the modified value.</p>",
      "preview": "This happens because id1 and id2 are referencing the same object (the static var",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b34",
      "type": "paragraph",
      "html": "<p>This above example can be fixed by making <code>id1</code> and <code>id2</code> normal variables (rather than references) so they save a copy of the return value rather than a reference to <code>s_x</code>.</p>",
      "preview": "This above example can be fixed by making id1 and id2 normal variables (rather t",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b35",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">For advanced readers</p><p>Here’s another example with a less obvious version of the same problem:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n#include &lt;string_view&gt;\n\nstd::string&amp; getName()\n{\n    static std::string s_name{};\n    std::cout &lt;&lt; \"Enter a name: \";\n    std::cin &gt;&gt; s_name;\n    return s_name;\n}\n\nvoid printFirstAlphabetical(const std::string&amp; s1, const std::string&amp; s2)\n{\n    if (s1 &lt; s2)\n        std::cout &lt;&lt; s1 &lt;&lt; \" comes before \" &lt;&lt; s2 &lt;&lt; '\\n';\n    else\n        std::cout &lt;&lt; s2 &lt;&lt; \" comes before \" &lt;&lt; s1 &lt;&lt; '\\n';\n}\n\nint main()\n{\n    printFirstAlphabetical(getName(), getName());\n    \n    return 0;\n}</code></pre><p>Here’s the result from one run of this program:</p><pre>Enter a name: Dave\nEnter a name: Stan\nStan comes before Stan\n</pre><p>In this example, <code>getName()</code> returns a reference to static local <code>s_name</code>. Initializing a <code>const std::string&amp;</code> with a reference to <code>s_name</code> causes that <code>std::string&amp;</code> to bind to <code>s_name</code> (not make a copy of it).</p><p>Thus, both <code>s1</code> and <code>s2</code> end up viewing <code>s_name</code> (which was assigned the last name we entered).</p><p>Note that if we use <code>std::string_view</code> parameters instead, the first <code>std::string_view</code> parameter will be invalidated when the underlying <code>std::string</code> is changed.</p></div>",
      "preview": "For advanced readersHere’s another example with a less obvious version of the same problem:#include ",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b36",
      "type": "paragraph",
      "html": "<p>Another issue that commonly occurs with programs that return a non-const static local by reference is that there is no standardized way to reset <code>s_x</code> back to the default state. Such programs must either use a non-conventional solution (e.g. a reset function parameter), or can only be reset by quitting and restarting the program.</p>",
      "preview": "Another issue that commonly occurs with programs that return a non-const static ",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b37",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Avoid returning references to non-const local static variables.</p></div>",
      "preview": "Best practiceAvoid returning references to non-const local static variables.",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b38",
      "type": "paragraph",
      "html": "<p>Returning a const reference to a <em>const</em> local static variable is sometimes done if the local variable being returned by reference is expensive to create and/or initialize (so we don’t have to recreate the variable every function call). But this is rare.</p>",
      "preview": "Returning a const reference to a const local static variable is sometimes done i",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b39",
      "type": "paragraph",
      "html": "<p>Returning a const reference to a <em>const</em> global variable is also sometimes done as a way to encapsulate access to a global variable. We discuss this in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/why-non-const-global-variables-are-evil/\">7.8 -- Why (non-const) global variables are evil</a>. When used intentionally and carefully, this is also okay.</p>",
      "preview": "Returning a const reference to a const global variable is also sometimes done as",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b40",
      "type": "section",
      "title": "Assigning/initializing a normal variable with a returned reference makes a copy",
      "html": "<h3 class=\"cpp-section-heading\">Assigning/initializing a normal variable with a returned reference makes a copy</h3>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b41",
      "type": "paragraph",
      "html": "<p>If a function returns a reference, and that reference is used to initialize or assign to a non-reference variable, the return value will be copied (as if it had been returned by value).</p>",
      "preview": "If a function returns a reference, and that reference is used to initialize or a",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b42",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <string>\n\nconst int& getNextId()\n{\n    static int s_x{ 0 };\n    ++s_x;\n    return s_x;\n}\n\nint main()\n{\n    const int id1 { getNextId() }; // id1 is a normal variable now and receives a copy of the value returned by reference from getNextId()\n    const int id2 { getNextId() }; // id2 is a normal variable now and receives a copy of the value returned by reference from getNextId()\n\n    std::cout << id1 << id2 << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n\nconst int&amp; getNextId()\n{\n    static int s_x{ 0 };\n    ++s_x;\n    return s_x;\n}\n\nint main()\n{\n    const int id1 { getNextId() }; // id1 is a normal variable now and receives a copy of the value returned by reference from getNextId()\n    const int id2 { getNextId() }; // id2 is a normal variable now and receives a copy of the value returned by reference from getNextId()\n\n    std::cout &lt;&lt; id1 &lt;&lt; id2 &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b43",
      "type": "paragraph",
      "html": "<p>In the above example, <code>getNextId()</code> is returning a reference, but <code>id1</code> and <code>id2</code> are non-reference variables. In such a case, the value of the returned reference is copied into the normal variable. Thus, this program prints:</p>",
      "preview": "In the above example, getNextId() is returning a reference, but id1 and id2 are ",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b44",
      "type": "output",
      "text": "12\n",
      "html": "<pre>12\n</pre>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b45",
      "type": "paragraph",
      "html": "<p>Also note that if a program returns a dangling reference, the reference is left dangling before the copy is made, which will lead to undefined behavior:</p>",
      "preview": "Also note that if a program returns a dangling reference, the reference is left ",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b46",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <string>\n\nconst std::string& getProgramName() // will return a const reference\n{\n    const std::string programName{ \"Calculator\" };\n\n    return programName;\n}\n\nint main()\n{\n    std::string name { getProgramName() }; // makes a copy of a dangling reference\n    std::cout << \"This program is named \" << name << '\\n'; // undefined behavior\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n\nconst std::string&amp; getProgramName() // will return a const reference\n{\n    const std::string programName{ \"Calculator\" };\n\n    return programName;\n}\n\nint main()\n{\n    std::string name { getProgramName() }; // makes a copy of a dangling reference\n    std::cout &lt;&lt; \"This program is named \" &lt;&lt; name &lt;&lt; '\\n'; // undefined behavior\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b47",
      "type": "section",
      "title": "It’s okay to return reference parameters by reference",
      "html": "<h3 class=\"cpp-section-heading\">It’s okay to return reference parameters by reference</h3>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b48",
      "type": "paragraph",
      "html": "<p>There are quite a few cases where returning objects by reference makes sense, and we’ll encounter many of those in future lessons. However, there is one useful example that we can show now.</p>",
      "preview": "There are quite a few cases where returning objects by reference makes sense, an",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b49",
      "type": "paragraph",
      "html": "<p>If a parameter is passed into a function by reference, it’s safe to return that parameter by reference. This makes sense: in order to pass an argument to a function, the argument must exist in the scope of the caller. When the called function returns, that object must still exist in the scope of the caller.</p>",
      "preview": "If a parameter is passed into a function by reference, it’s safe to return that ",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b50",
      "type": "paragraph",
      "html": "<p>Here is a simple example of such a function:</p>",
      "preview": "Here is a simple example of such a function:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b51",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <string>\n\n// Takes two std::string objects, returns the one that comes first alphabetically\nconst std::string& firstAlphabetical(const std::string& a, const std::string& b)\n{\n\treturn (a < b) ? a : b; // We can use operator< on std::string to determine which comes first alphabetically\n}\n\nint main()\n{\n\tstd::string hello { \"Hello\" };\n\tstd::string world { \"World\" };\n\n\tstd::cout << firstAlphabetical(hello, world) << '\\n';\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n\n// Takes two std::string objects, returns the one that comes first alphabetically\nconst std::string&amp; firstAlphabetical(const std::string&amp; a, const std::string&amp; b)\n{\n\treturn (a &lt; b) ? a : b; // We can use operator&lt; on std::string to determine which comes first alphabetically\n}\n\nint main()\n{\n\tstd::string hello { \"Hello\" };\n\tstd::string world { \"World\" };\n\n\tstd::cout &lt;&lt; firstAlphabetical(hello, world) &lt;&lt; '\\n';\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b52",
      "type": "paragraph",
      "html": "<p>This prints:</p>",
      "preview": "This prints:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b53",
      "type": "output",
      "text": "Hello\n",
      "html": "<pre>Hello\n</pre>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b54",
      "type": "paragraph",
      "html": "<p>In the above function, the caller passes in two std::string objects by const reference, and whichever of these strings comes first alphabetically is passed back by const reference. If we had used pass by value and return by value, we would have made up to 3 copies of std::string (one for each parameter, one for the return value). By using pass by reference/return by reference, we can avoid those copies.</p>",
      "preview": "In the above function, the caller passes in two std::string objects by const ref",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b55",
      "type": "section",
      "title": "It’s okay for an rvalue passed by const reference to be returned by const reference",
      "html": "<h3 class=\"cpp-section-heading\">It’s okay for an rvalue passed by const reference to be returned by const reference</h3>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b56",
      "type": "paragraph",
      "html": "<p>When an argument for a const reference parameter is an rvalue, it’s still okay to return that parameter by const reference.</p>",
      "preview": "When an argument for a const reference parameter is an rvalue, it’s still okay t",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b57",
      "type": "paragraph",
      "html": "<p>This is because rvalues are not destroyed until the end of the full expression in which they are created.</p>",
      "preview": "This is because rvalues are not destroyed until the end of the full expression i",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b58",
      "type": "paragraph",
      "html": "<p>First, let’s look at this example:</p>",
      "preview": "First, let’s look at this example:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b59",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <string>\n\nstd::string getHello()\n{\n    return \"Hello\"; // implicit conversion to std::string\n}\n\nint main()\n{\n    const std::string s{ getHello() };\n\n    std::cout << s;\n    \n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n\nstd::string getHello()\n{\n    return \"Hello\"; // implicit conversion to std::string\n}\n\nint main()\n{\n    const std::string s{ getHello() };\n\n    std::cout &lt;&lt; s;\n    \n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b60",
      "type": "paragraph",
      "html": "<p>In this case, <code>getHello()</code> returns a <code>std::string</code> by value, which is an rvalue. This rvalue is then used to initialize <code>s</code>. After the initialization of <code>s</code>, the expression in which the rvalue was created has finished evaluating, and the rvalue is destroyed.</p>",
      "preview": "In this case, getHello() returns a std::string by value, which is an rvalue. Thi",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b61",
      "type": "paragraph",
      "html": "<p>Now let’s take a look at this similar example:</p>",
      "preview": "Now let’s take a look at this similar example:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b62",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <string>\n\nconst std::string& foo(const std::string& s)\n{\n    return s;\n}\n\nstd::string getHello()\n{\n    return \"Hello\"; // implicit conversion to std::string\n}\n\nint main()\n{\n    const std::string s{ foo(getHello()) };\n\n    std::cout << s;\n    \n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n\nconst std::string&amp; foo(const std::string&amp; s)\n{\n    return s;\n}\n\nstd::string getHello()\n{\n    return \"Hello\"; // implicit conversion to std::string\n}\n\nint main()\n{\n    const std::string s{ foo(getHello()) };\n\n    std::cout &lt;&lt; s;\n    \n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b63",
      "type": "paragraph",
      "html": "<p>The only difference in this case is that the rvalue is passed by const reference to <code>foo()</code> and then returned by const reference back to the caller before it is used to initialize <code>s</code>. Everything else works identically.</p>",
      "preview": "The only difference in this case is that the rvalue is passed by const reference",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b64",
      "type": "paragraph",
      "html": "<p>We discuss a similar case in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/access-functions/\">14.6 -- Access functions</a>.</p>",
      "preview": "We discuss a similar case in lesson 14.6 -- Access functions.",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b65",
      "type": "section",
      "title": "The caller can modify values through the reference",
      "html": "<h3 class=\"cpp-section-heading\">The caller can modify values through the reference</h3>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b66",
      "type": "paragraph",
      "html": "<p>When an argument is passed to a function by non-const reference, the function can use the reference to modify the value of the argument.</p>",
      "preview": "When an argument is passed to a function by non-const reference, the function ca",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b67",
      "type": "paragraph",
      "html": "<p>Similarly, when a non-const reference is returned from a function, the caller can use the reference to modify the value being returned.</p>",
      "preview": "Similarly, when a non-const reference is returned from a function, the caller ca",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b68",
      "type": "paragraph",
      "html": "<p>Here’s an illustrative example:</p>",
      "preview": "Here’s an illustrative example:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b69",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\n// takes two integers by non-const reference, and returns the greater by reference\nint& max(int& x, int& y)\n{\n    return (x > y) ? x : y;\n}\n\nint main()\n{\n    int a{ 5 };\n    int b{ 6 };\n\n    max(a, b) = 7; // sets the greater of a or b to 7\n\n    std::cout << a << b << '\\n';\n        \n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\n// takes two integers by non-const reference, and returns the greater by reference\nint&amp; max(int&amp; x, int&amp; y)\n{\n    return (x &gt; y) ? x : y;\n}\n\nint main()\n{\n    int a{ 5 };\n    int b{ 6 };\n\n    max(a, b) = 7; // sets the greater of a or b to 7\n\n    std::cout &lt;&lt; a &lt;&lt; b &lt;&lt; '\\n';\n        \n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b70",
      "type": "paragraph",
      "html": "<p>In the above program, <code>max(a, b)</code> calls the <code>max()</code> function with <code>a</code> and <code>b</code> as arguments. Reference parameter <code>x</code> binds to argument <code>a</code>, and reference parameter <code>y</code> binds to argument <code>b</code>. The function then determines which of <code>x</code> (<code>5</code>) and <code>y</code> (<code>6</code>) is greater. In this case, that’s <code>y</code>, so the function returns <code>y</code> (which is still bound to <code>b</code>) back to the caller. The caller then assigns the value <code>7</code> to this returned reference.</p>",
      "preview": "In the above program, max(a, b) calls the max() function with a and b as argumen",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b71",
      "type": "paragraph",
      "html": "<p>Therefore, the expression <code>max(a, b) = 7</code> effectively resolves to <code>b = 7</code>.</p>",
      "preview": "Therefore, the expression max(a, b) = 7 effectively resolves to b = 7.",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b72",
      "type": "paragraph",
      "html": "<p>This prints:</p>",
      "preview": "This prints:",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b73",
      "type": "output",
      "text": "57\n",
      "html": "<pre>57\n</pre>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b74",
      "type": "section",
      "title": "Return by address",
      "html": "<h3 class=\"cpp-section-heading\">Return by address</h3>",
      "canCheck": false
    },
    {
      "id": "return-by-reference-and-return-by-address_b75",
      "type": "paragraph",
      "html": "<p><strong>Return by address</strong> works almost identically to return by reference, except a pointer to an object is returned instead of a reference to an object. Return by address has the same primary caveat as return by reference -- the object being returned by address must outlive the scope of the function returning the address, otherwise the caller will receive a dangling pointer.</p>",
      "preview": "Return by address works almost identically to return by reference, except a poin",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b76",
      "type": "paragraph",
      "html": "<p>The major advantage of return by address over return by reference is that we can have the function return <code>nullptr</code> if there is no valid object to return. For example, let’s say we have a list of students that we want to search. If we find the student we are looking for in the list, we can return a pointer to the object representing the matching student. If we don’t find any students matching, we can return <code>nullptr</code> to indicate a matching student object was not found.</p>",
      "preview": "The major advantage of return by address over return by reference is that we can",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b77",
      "type": "paragraph",
      "html": "<p>The major disadvantage of return by address is that the caller has to remember to do a <code>nullptr</code> check before dereferencing the return value, otherwise a null pointer dereference may occur and undefined behavior will result. Because of this danger, return by reference should be preferred over return by address unless the ability to return “no object” is needed.</p>",
      "preview": "The major disadvantage of return by address is that the caller has to remember t",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b78",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Prefer return by reference over return by address unless the ability to return “no object” (using <code>nullptr</code>) is important.</p></div>",
      "preview": "Best practicePrefer return by reference over return by address unless the ability to return “no obje",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b79",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>If you need the ability to return “no object” or a value (rather than an object) <a href=\"https://www.learncpp.com/cpp-tutorial/stdoptional/\">12.15 -- std::optional</a> describes a good alternative.</p></div>",
      "preview": "Related contentIf you need the ability to return “no object” or a value (rather than an object) 12.1",
      "canCheck": true
    },
    {
      "id": "return-by-reference-and-return-by-address_b80",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>See <a href=\"https://www.learncpp.com/cpp-tutorial/stdstring_view-part-2/#stringvsstringview\">5.9 -- std::string_view (part 2)</a> for a quick guide on when to return <code>std::string_view</code> vs <code>const std::string&amp;</code> .</p></div>",
      "preview": "Related contentSee 5.9 -- std::string_view (part 2) for a quick guide on when to return std::string_",
      "canCheck": true
    }
  ]
}