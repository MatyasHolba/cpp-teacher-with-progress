{
  "index": 285,
  "chapterId": "23",
  "number": "23.6",
  "title": "23.6 — Container classes",
  "url": "https://www.learncpp.com/cpp-tutorial/container-classes/",
  "slug": "container-classes",
  "scrapedAt": "2026-09-15T16:31:04.484Z",
  "totalBlocks": 42,
  "totalCheckpoints": 41,
  "blocks": [
    {
      "id": "container-classes_b0",
      "type": "paragraph",
      "html": "<p>In real life, we use containers all the time. Your breakfast cereal comes in a box, the pages in your book come inside a cover and binding, and you might store any number of items in containers in your garage. Without containers, it would be extremely inconvenient to work with many of these objects. Imagine trying to read a book that didn’t have any sort of binding, or eat cereal that didn’t come in a box without using a bowl. It would be a mess. The value the container provides is largely in its ability to help organize and store items that are put inside it.</p>",
      "preview": "In real life, we use containers all the time. Your breakfast cereal comes in a b",
      "canCheck": true,
      "html_cs": "<p>V reálném životě používáme kontejnery (obaly, krabice, nádoby) neustále. Snídaňové cereálie se prodávají v krabici, stránky knihy drží pohromadě obálka a vazba a v garáži možná skladujete spoustu věcí v úložných boxech. Bez kontejnerů by bylo velmi nepraktické s mnoha těmito předměty pracovat. Představte si, že byste se pokoušeli číst knihu bez jakékoli vazby nebo jíst cereálie, které nepřišly v krabici, bez použití misky. Byl by v tom zmatek. Přínos kontejneru spočívá do značné míry v jeho schopnosti pomáhat organizovat a uchovávat položky, které jsou do něj vloženy.</p>"
    },
    {
      "id": "container-classes_b1",
      "type": "paragraph",
      "html": "<p>Similarly, a <strong>container class</strong> is a class designed to hold and organize multiple instances of another type (either another class, or a fundamental type). There are many different kinds of container classes, each of which has various advantages, disadvantages, and restrictions in their use. By far the most commonly used container in programming is the array, which you have already seen many examples of. Although C++ has built-in array functionality, programmers will often use an array container class (std::array or std::vector) instead because of the additional benefits they provide. Unlike built-in arrays, array container classes generally provide dynamic resizing (when elements are added or removed), remember their size when they are passed to functions, and do bounds-checking. This not only makes array container classes more convenient than normal arrays, but safer too.</p>",
      "preview": "Similarly, a container class is a class designed to hold and organize multiple i",
      "canCheck": true,
      "html_cs": "<p>Podobně <strong>kontejnerová třída</strong> (container class) je třída navržená tak, aby uchovávala a organizovala více instancí jiného typu (buď jiné třídy, nebo základního typu). Existuje mnoho různých druhů kontejnerových tříd, z nichž každá má při použití různé výhody, nevýhody a omezení. Zdaleka nejčastěji používaným kontejnerem v programování je pole, jehož mnoho příkladů jste již viděli. Ačkoli má C++ vestavěnou funkcionalitu polí, programátoři často místo nich používají kontejnerovou třídu pole (std::array nebo std::vector) kvůli dalším výhodám, které nabízejí. Na rozdíl od vestavěných polí poskytují kontejnerové třídy polí obecně dynamickou změnu velikosti (při přidávání nebo odebírání prvků), pamatují si svou velikost při předání do funkcí a provádějí kontrolu mezí. Díky tomu jsou kontejnerové třídy polí nejen pohodlnější než běžná pole, ale také bezpečnější.</p>"
    },
    {
      "id": "container-classes_b2",
      "type": "paragraph",
      "html": "<p>Container classes typically implement a fairly standardized minimal set of functionality. Most well-defined containers will include functions that:</p>",
      "preview": "Container classes typically implement a fairly standardized minimal set of funct",
      "canCheck": true,
      "html_cs": "<p>Kontejnerové třídy obvykle implementují poměrně standardizovanou minimální sadu funkcionality. Většina dobře definovaných kontejnerů obsahuje funkce, které:</p>"
    },
    {
      "id": "container-classes_b3",
      "type": "list",
      "html": "<ul><li>Create an empty container (via a constructor)</li><li>Insert a new object into the container</li><li>Remove an object from the container</li><li>Report the number of objects currently in the container</li><li>Empty the container of all objects</li><li>Provide access to the stored objects</li><li>Sort the elements (optional)</li></ul>",
      "preview": "Create an empty container (via a constructor)Insert a new object into the contai",
      "canCheck": true,
      "html_cs": "<ul><li>Vytvořit prázdný kontejner (pomocí konstruktoru)</li><li>Vložit nový objekt do kontejneru</li><li>Odebrat objekt z kontejneru</li><li>Vrátit počet objektů aktuálně se nacházejících v kontejneru</li><li>Vyprázdnit kontejner (odstranit všechny objekty)</li><li>Poskytnout přístup k uloženým objektům</li><li>Seřadit prvky (volitelné)</li></ul>"
    },
    {
      "id": "container-classes_b4",
      "type": "paragraph",
      "html": "<p>Sometimes certain container classes will omit some of this functionality. For example, arrays container classes often omit the insert and remove functions because they are slow and the class designer does not want to encourage their use.</p>",
      "preview": "Sometimes certain container classes will omit some of this functionality. For ex",
      "canCheck": true,
      "html_cs": "<p>Někdy určité kontejnerové třídy některé z těchto funkcionalit vynechávají. Například kontejnerové třídy polí často vynechávají funkce pro vkládání a odebírání, protože jsou pomalé a návrhář třídy nechce podporovat jejich používání.</p>"
    },
    {
      "id": "container-classes_b5",
      "type": "paragraph",
      "html": "<p>Container classes implement a member-of relationship. For example, elements of an array are members-of (belong to) the array. Note that we’re using “member-of” in the conventional sense, not the C++ class member sense.</p>",
      "preview": "Container classes implement a member-of relationship. For example, elements of a",
      "canCheck": true,
      "html_cs": "<p>Kontejnerové třídy implementují vztah „member-of“ (být prvkem). Například prvky pole jsou členy (patří do) daného pole. Všimněte si, že „member-of“ zde používáme v běžném významu, nikoli ve smyslu člena třídy v C++.</p>"
    },
    {
      "id": "container-classes_b6",
      "type": "paragraph",
      "html": "<p><strong>Types of containers</strong></p>",
      "preview": "Types of containers",
      "canCheck": true,
      "html_cs": "<p><strong>Typy kontejnerů</strong></p>"
    },
    {
      "id": "container-classes_b7",
      "type": "paragraph",
      "html": "<p>Container classes generally come in two different varieties. <strong>Value containers</strong> are <a href=\"https://www.learncpp.com/cpp-tutorial/102-composition/\">compositions</a> that store copies of the objects that they are holding (and thus are responsible for creating and destroying those copies). <strong>Reference containers</strong> are <a href=\"https://www.learncpp.com/cpp-tutorial/103-aggregation/\">aggregations</a> that store pointers or references to other objects (and thus are not responsible for creation or destruction of those objects).</p>",
      "preview": "Container classes generally come in two different varieties. Value containers ar",
      "canCheck": true,
      "html_cs": "<p>Kontejnerové třídy obecně existují ve dvou různých variantách. <strong>Hodnotové kontejnery</strong> jsou <a href=\"https://www.learncpp.com/cpp-tutorial/102-composition/\">kompozice</a>, které ukládají kopie objektů, jež obsahují (a jsou tedy zodpovědné za vytváření a destrukci těchto kopií). <strong>Referenční kontejnery</strong> jsou <a href=\"https://www.learncpp.com/cpp-tutorial/103-aggregation/\">agregace</a>, které ukládají ukazatele nebo reference na jiné objekty (a nenesou tedy zodpovědnost za vytváření ani destrukci těchto objektů).</p>"
    },
    {
      "id": "container-classes_b8",
      "type": "paragraph",
      "html": "<p>Unlike in real life, where containers can hold whatever types of objects you put in them, in C++, containers typically only hold one type of data. For example, if you have an array of integers, it will only hold integers. Unlike some other languages, many C++ containers do not allow you to arbitrarily mix types. If you need containers to hold integers and doubles, you will generally have to write two separate containers to do this (or use templates, which is an advanced C++ feature). Despite the restrictions on their use, containers are immensely useful, and they make programming easier, safer, and faster.</p>",
      "preview": "Unlike in real life, where containers can hold whatever types of objects you put",
      "canCheck": true,
      "html_cs": "<p>Na rozdíl od skutečného života, kde kontejnery mohou obsahovat jakékoli typy objektů, které do nich vložíte, v C++ kontejnery obvykle obsahují pouze jeden typ dat. Pokud máte například pole celých čísel, bude obsahovat pouze celá čísla. Na rozdíl od některých jiných jazyků mnoho kontejnerů C++ neumožňuje libovolně míchat typy. Pokud potřebujete kontejnery pro uchování celých čísel a dvojic, budete k tomu obecně muset napsat dva samostatné kontejnery (nebo použít šablony, což je pokročilá funkce C++). Navzdory omezením jejich použití jsou kontejnery nesmírně užitečné a programování je snazší, bezpečnější a rychlejší.</p>"
    },
    {
      "id": "container-classes_b9",
      "type": "paragraph",
      "html": "<p><strong>An array container class</strong></p>",
      "preview": "An array container class",
      "canCheck": true,
      "html_cs": "<p><strong>Třída kontejneru pole</strong></p>"
    },
    {
      "id": "container-classes_b10",
      "type": "paragraph",
      "html": "<p>In this example, we are going to write an integer array class from scratch that implements most of the common functionality that containers should have. This array class is going to be a value container, which will hold copies of the elements it’s organizing. As the name suggests, the container will hold an array of integers, similar to <code>std::vector&lt;int&gt;</code>.</p>",
      "preview": "In this example, we are going to write an integer array class from scratch that ",
      "canCheck": true,
      "html_cs": "<p>V tomto příkladu napíšeme celou třídu pole od začátku, která implementuje většinu běžných funkcí, které by kontejnery měly mít. Tato třída pole bude kontejnerem hodnot, který bude obsahovat kopie prvků, které organizuje. Jak název napovídá, kontejner bude obsahovat pole celých čísel, podobně jako <code>std::vector<int></code>.</p>"
    },
    {
      "id": "container-classes_b11",
      "type": "paragraph",
      "html": "<p>First, let’s create the IntArray.h file:</p>",
      "preview": "First, let’s create the IntArray.h file:",
      "canCheck": true,
      "html_cs": "<p>Nejprve vytvořte soubor IntArray.h:</p>"
    },
    {
      "id": "container-classes_b12",
      "type": "code",
      "language": "cpp",
      "code": "#ifndef INTARRAY_H\n#define INTARRAY_H\n\nclass IntArray\n{\n};\n\n#endif",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef INTARRAY_H\n#define INTARRAY_H\n\nclass IntArray\n{\n};\n\n#endif</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef INTARRAY_H\n#define INTARRAY_H\n\ntřídy IntArray\n{\n};\n\n#endif</code></pre>"
    },
    {
      "id": "container-classes_b13",
      "type": "paragraph",
      "html": "<p>Our IntArray is going to need to keep track of two values: the data itself, and the size of the array. Because we want our array to be able to change in size, we’ll have to do some dynamic allocation, which means we’ll have to use a pointer to store the data.</p>",
      "preview": "Our IntArray is going to need to keep track of two values: the data itself, and ",
      "canCheck": true,
      "html_cs": "<p>Naše IntArray bude muset sledovat dvě hodnoty: samotná data a velikost pole. Protože chceme, aby se velikost našeho pole mohla měnit, budeme muset provést nějakou dynamickou alokaci, což znamená, že k uložení dat budeme muset použít ukazatel.</p>"
    },
    {
      "id": "container-classes_b14",
      "type": "code",
      "language": "cpp",
      "code": "#ifndef INTARRAY_H\n#define INTARRAY_H\n\nclass IntArray\n{\nprivate:\n    int m_length{};\n    int* m_data{};\n};\n\n#endif",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef INTARRAY_H\n#define INTARRAY_H\n\nclass IntArray\n{\nprivate:\n    int m_length{};\n    int* m_data{};\n};\n\n#endif</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef INTARRAY_H\n#define INTARRAY_H\n\ntřídy IntArray\n{\nsoukromé:\n    int m_délka{};\n    int* m_data{};\n};\n\n#endif</code></pre>"
    },
    {
      "id": "container-classes_b15",
      "type": "paragraph",
      "html": "<p>Now we need to add some constructors that will allow us to create IntArrays. We are going to add two constructors: one that constructs an empty array, and one that will allow us to construct an array of a predetermined size.</p>",
      "preview": "Now we need to add some constructors that will allow us to create IntArrays. We ",
      "canCheck": true,
      "html_cs": "<p>Nyní musíme přidat nějaké konstruktory, které nám umožní vytvářet IntArrays. Přidáme dva konstruktory: jeden, který vytvoří prázdné pole, a jeden, který nám umožní sestavit pole předem určené velikosti.</p>"
    },
    {
      "id": "container-classes_b16",
      "type": "code",
      "language": "cpp",
      "code": "#ifndef INTARRAY_H\n#define INTARRAY_H\n\n#include <cassert> // for assert()\n#include <cstddef> // for std::size_t\n\nclass IntArray\n{\nprivate:\n    int m_length{};\n    int* m_data{};\n\npublic:\n    IntArray() = default;\n\n    IntArray(int length):\n        m_length{ length }\n    {\n        assert(length >= 0);\n\n        if (length > 0)\n            m_data = new int[static_cast<std::size_t>(length)]{};\n    }\n};\n\n#endif",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef INTARRAY_H\n#define INTARRAY_H\n\n#include &lt;cassert&gt; // for assert()\n#include &lt;cstddef&gt; // for std::size_t\n\nclass IntArray\n{\nprivate:\n    int m_length{};\n    int* m_data{};\n\npublic:\n    IntArray() = default;\n\n    IntArray(int length):\n        m_length{ length }\n    {\n        assert(length &gt;= 0);\n\n        if (length &gt; 0)\n            m_data = new int[static_cast&lt;std::size_t&gt;(length)]{};\n    }\n};\n\n#endif</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef INTARRAY_H\n#define INTARRAY_H\n\n#include <cassert> // pro sustain()\n#include <cstddef> // pro std::size_t\n\ntřídy IntArray\n{\nsoukromé:\n    int m_délka{};\n    int* m_data{};\n\nveřejnost:\n    IntArray() = výchozí;\n\n    IntArray (délka int):\n        m_délka{ délka }\n    {\n        tvrdit(délka >= 0);\n\n        if (délka > 0)\n            m_data = new int[static_cast<std::size_t>(délka)]{};\n    }\n};\n\n#endif</code></pre>"
    },
    {
      "id": "container-classes_b17",
      "type": "paragraph",
      "html": "<p>We’ll also need some functions to help us clean up IntArrays. First, we’ll write a destructor, which simply deallocates any dynamically allocated data. Second, we’ll write a function called erase(), which will erase the array and set the length to 0.</p>",
      "preview": "We’ll also need some functions to help us clean up IntArrays. First, we’ll write",
      "canCheck": true,
      "html_cs": "<p>Budeme také potřebovat některé funkce, které nám pomohou vyčistit IntArrays. Nejprve napíšeme destruktor, který jednoduše uvolní všechna dynamicky alokovaná data. Za druhé napíšeme funkci nazvanou erase(), která pole vymaže a nastaví délku na 0.</p>"
    },
    {
      "id": "container-classes_b18",
      "type": "code",
      "language": "cpp",
      "code": "    ~IntArray()\n    {\n        delete[] m_data;\n        // we don't need to set m_data to null or m_length to 0 here, since the object will be destroyed immediately after this function anyway\n    }\n\n    void erase()\n    {\n        delete[] m_data;\n\n        // We need to make sure we set m_data to nullptr here, otherwise it will\n        // be left pointing at deallocated memory!\n        m_data = nullptr;\n        m_length = 0;\n    }",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">    ~IntArray()\n    {\n        delete[] m_data;\n        // we don't need to set m_data to null or m_length to 0 here, since the object will be destroyed immediately after this function anyway\n    }\n\n    void erase()\n    {\n        delete[] m_data;\n\n        // We need to make sure we set m_data to nullptr here, otherwise it will\n        // be left pointing at deallocated memory!\n        m_data = nullptr;\n        m_length = 0;\n    }</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\"> ~IntArray()\n    {\n        smazat[] m_data;\n        // zde nemusíme nastavovat m_data na null nebo m_length na 0, protože objekt bude stejně zničen ihned po této funkci\n    }\n\n    void vymazat()\n    {\n        smazat[] m_data;\n\n        // Musíme se ujistit, že jsme zde nastavili m_data na nullptr, jinak tomu tak bude\n        // zůstane ukazovat na uvolněnou paměť!\n        m_data = nullptr;\n        m_délka = 0;\n    }</code></pre>"
    },
    {
      "id": "container-classes_b19",
      "type": "paragraph",
      "html": "<p>Now let’s overload the [] operator so we can access the elements of the array. We should ensure the index parameter has a valid value, which we can do by using the assert() function. We’ll also add an access function to return the length of the array. Here’s everything so far:</p>",
      "preview": "Now let’s overload the [] operator so we can access the elements of the array. W",
      "canCheck": true,
      "html_cs": "<p>Nyní přetížíme operátor [], abychom měli přístup k prvkům pole. Měli bychom zajistit, aby parametr index měl platnou hodnotu, což můžeme provést pomocí funkce sustain(). Přidáme také přístupovou funkci, která vrátí délku pole. Zde je zatím vše:</p>"
    },
    {
      "id": "container-classes_b20",
      "type": "code",
      "language": "cpp",
      "code": "#ifndef INTARRAY_H\n#define INTARRAY_H\n\n#include <cassert> // for assert()\n#include <cstddef> // for std::size_t\n\nclass IntArray\n{\nprivate:\n    int m_length{};\n    int* m_data{};\n\npublic:\n    IntArray() = default;\n\n    IntArray(int length):\n        m_length{ length }\n    {\n        assert(length >= 0);\n\n        if (length > 0)\n            m_data = new int[static_cast<std::size_t>(length)]{};\n    }\n\n    ~IntArray()\n    {\n        delete[] m_data;\n        // we don't need to set m_data to null or m_length to 0 here, since the object will be destroyed immediately after this function anyway\n    }\n\n    void erase()\n    {\n        delete[] m_data;\n        // We need to make sure we set m_data to nullptr here, otherwise it will\n        // be left pointing at deallocated memory!\n        m_data = nullptr;\n        m_length = 0;\n    }\n\n    int& operator[](int index)\n    {\n        assert(index >= 0 && index < m_length);\n        return m_data[index];\n    }\n\n    int getLength() const { return m_length; }\n};\n\n#endif",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef INTARRAY_H\n#define INTARRAY_H\n\n#include &lt;cassert&gt; // for assert()\n#include &lt;cstddef&gt; // for std::size_t\n\nclass IntArray\n{\nprivate:\n    int m_length{};\n    int* m_data{};\n\npublic:\n    IntArray() = default;\n\n    IntArray(int length):\n        m_length{ length }\n    {\n        assert(length &gt;= 0);\n\n        if (length &gt; 0)\n            m_data = new int[static_cast&lt;std::size_t&gt;(length)]{};\n    }\n\n    ~IntArray()\n    {\n        delete[] m_data;\n        // we don't need to set m_data to null or m_length to 0 here, since the object will be destroyed immediately after this function anyway\n    }\n\n    void erase()\n    {\n        delete[] m_data;\n        // We need to make sure we set m_data to nullptr here, otherwise it will\n        // be left pointing at deallocated memory!\n        m_data = nullptr;\n        m_length = 0;\n    }\n\n    int&amp; operator[](int index)\n    {\n        assert(index &gt;= 0 &amp;&amp; index &lt; m_length);\n        return m_data[index];\n    }\n\n    int getLength() const { return m_length; }\n};\n\n#endif</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef INTARRAY_H\n#define INTARRAY_H\n\n#include <cassert> // pro sustain()\n#include <cstddef> // pro std::size_t\n\ntřídy IntArray\n{\nsoukromé:\n    int m_délka{};\n    int* m_data{};\n\nveřejnost:\n    IntArray() = výchozí;\n\n    IntArray (délka int):\n        m_délka{ délka }\n    {\n        tvrdit(délka >= 0);\n\n        if (délka > 0)\n            m_data = new int[static_cast<std::size_t>(délka)]{};\n    }\n\n    ~IntArray()\n    {\n        smazat[] m_data;\n        // zde nemusíme nastavovat m_data na null nebo m_length na 0, protože objekt bude stejně zničen ihned po této funkci\n    }\n\n    void vymazat()\n    {\n        smazat[] m_data;\n        // Musíme se ujistit, že jsme zde nastavili m_data na nullptr, jinak tomu tak bude\n        // zůstane ukazovat na uvolněnou paměť!\n        m_data = nullptr;\n        m_délka = 0;\n    }\n\n    operátor int&[](index int)\n    {\n        tvrdit(index >= 0 && index < m_délka);\n        return m_data[index];\n    }\n\n    int getLength() const { return m_délka; }\n};\n\n#endif</code></pre>"
    },
    {
      "id": "container-classes_b21",
      "type": "paragraph",
      "html": "<p>At this point, we already have an IntArray class that we can use. We can allocate IntArrays of a given size, and we can use the [] operator to retrieve or change the value of the elements.</p>",
      "preview": "At this point, we already have an IntArray class that we can use. We can allocat",
      "canCheck": true,
      "html_cs": "<p>V tuto chvíli již máme třídu IntArray, kterou můžeme použít. Můžeme alokovat IntArrays dané velikosti a můžeme použít operátor [] k načtení nebo změně hodnoty prvků.</p>"
    },
    {
      "id": "container-classes_b22",
      "type": "paragraph",
      "html": "<p>However, there are still a few thing we can’t do with our IntArray. We still can’t change its size, still can’t insert or delete elements, and we still can’t sort it. Copying the array will also cause problems, since that will shallow copy the data pointer.</p>",
      "preview": "However, there are still a few thing we can’t do with our IntArray. We still can",
      "canCheck": true,
      "html_cs": "<p>Stále však existuje několik věcí, které s naším IntArray nemůžeme udělat. Stále nemůžeme změnit jeho velikost, stále nemůžeme vkládat nebo mazat prvky a stále jej nemůžeme třídit. Kopírování pole také způsobí problémy, protože to způsobí mělké zkopírování datového ukazatele.</p>"
    },
    {
      "id": "container-classes_b23",
      "type": "paragraph",
      "html": "<p>First, let’s write some code that will allow us to resize an array. We are going to write two different functions to do this. The first function, reallocate(), will destroy any existing elements in the array when it is resized, but it will be fast. The second function, resize(), will keep any existing elements in the array when it is resized, but it will be slow.</p>",
      "preview": "First, let’s write some code that will allow us to resize an array. We are going",
      "canCheck": true,
      "html_cs": "<p>Nejprve napíšeme nějaký kód, který nám umožní změnit velikost pole. K tomu napíšeme dvě různé funkce. První funkce, reallocate(), zničí všechny existující prvky v poli při změně velikosti, ale bude rychlá. Druhá funkce, resize(), zachová všechny existující prvky v poli při změně velikosti, ale bude pomalá.</p>"
    },
    {
      "id": "container-classes_b24",
      "type": "code",
      "language": "cpp",
      "code": "#include <algorithm> // for std::copy_n\n\n    // reallocate resizes the array.  Any existing elements will be destroyed.  This function operates quickly.\n    void reallocate(int newLength)\n    {\n        // First we delete any existing elements\n        erase();\n\n        // If our array is going to be empty now, return here\n        if (newLength <= 0)\n            return;\n\n        // Then we have to allocate new elements\n        m_data = new int[static_cast<std::size_t>(newLength)];\n        m_length = newLength;\n    }\n\n    // resize resizes the array.  Any existing elements will be kept.  This function operates slowly.\n    void resize(int newLength)\n    {\n        // if the array is already the right length, we're done\n        if (newLength == m_length)\n            return;\n\n        // If we are resizing to an empty array, do that and return\n        if (newLength <= 0)\n        {\n            erase();\n            return;\n        }\n\n        // Now we can assume newLength is at least 1 element.  This algorithm\n        // works as follows: First we are going to allocate a new array.  Then we\n        // are going to copy elements from the existing array to the new array.\n        // Once that is done, we can destroy the old array, and make m_data\n        // point to the new array.\n\n        // First we have to allocate a new array\n        int* data{ new int[static_cast<std::size_t>(newLength)] };\n\n        // Then we have to figure out how many elements to copy from the existing\n        // array to the new array.  We want to copy as many elements as there are\n        // in the smaller of the two arrays.\n        if (m_length > 0)\n        {\n            int elementsToCopy{ (newLength > m_length) ? m_length : newLength };\n            std::copy_n(m_data, elementsToCopy, data); // copy the elements\n        }\n \n        // Now we can delete the old array because we don't need it any more\n        delete[] m_data;\n\n        // And use the new array instead!  Note that this simply makes m_data point\n        // to the same address as the new array we dynamically allocated.  Because\n        // data was dynamically allocated, it won't be destroyed when it goes out of scope.\n        m_data = data;\n        m_length = newLength;\n    }",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;algorithm&gt; // for std::copy_n\n\n    // reallocate resizes the array.  Any existing elements will be destroyed.  This function operates quickly.\n    void reallocate(int newLength)\n    {\n        // First we delete any existing elements\n        erase();\n\n        // If our array is going to be empty now, return here\n        if (newLength &lt;= 0)\n            return;\n\n        // Then we have to allocate new elements\n        m_data = new int[static_cast&lt;std::size_t&gt;(newLength)];\n        m_length = newLength;\n    }\n\n    // resize resizes the array.  Any existing elements will be kept.  This function operates slowly.\n    void resize(int newLength)\n    {\n        // if the array is already the right length, we're done\n        if (newLength == m_length)\n            return;\n\n        // If we are resizing to an empty array, do that and return\n        if (newLength &lt;= 0)\n        {\n            erase();\n            return;\n        }\n\n        // Now we can assume newLength is at least 1 element.  This algorithm\n        // works as follows: First we are going to allocate a new array.  Then we\n        // are going to copy elements from the existing array to the new array.\n        // Once that is done, we can destroy the old array, and make m_data\n        // point to the new array.\n\n        // First we have to allocate a new array\n        int* data{ new int[static_cast&lt;std::size_t&gt;(newLength)] };\n\n        // Then we have to figure out how many elements to copy from the existing\n        // array to the new array.  We want to copy as many elements as there are\n        // in the smaller of the two arrays.\n        if (m_length &gt; 0)\n        {\n            int elementsToCopy{ (newLength &gt; m_length) ? m_length : newLength };\n            std::copy_n(m_data, elementsToCopy, data); // copy the elements\n        }\n \n        // Now we can delete the old array because we don't need it any more\n        delete[] m_data;\n\n        // And use the new array instead!  Note that this simply makes m_data point\n        // to the same address as the new array we dynamically allocated.  Because\n        // data was dynamically allocated, it won't be destroyed when it goes out of scope.\n        m_data = data;\n        m_length = newLength;\n    }</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include <algorithm> // pro std::copy_n\n\n    // přerozdělení změní velikost pole.  Všechny existující prvky budou zničeny.  Tato funkce funguje rychle.\n    void přerozdělení (int newLength)\n    {\n        // Nejprve odstraníme všechny existující prvky\n        vymazat();\n\n        // Pokud bude naše pole nyní prázdné, vraťte se sem\n        if (newLength <= 0)\n            návrat;\n\n        // Potom musíme alokovat nové prvky\n        m_data = new int[static_cast<std::size_t>(newLength)];\n        m_délka = newLength;\n    }\n\n    // resize změní velikost pole.  Všechny stávající prvky budou zachovány.  Tato funkce pracuje pomalu.\n    void resize (int newLength)\n    {\n        // pokud má pole již správnou délku, máme hotovo\n        if (newLength == m_length)\n            návrat;\n\n        // Pokud měníme velikost na prázdné pole, udělejte to a vraťte se\n        if (newLength <= 0)\n        {\n            vymazat();\n            návrat;\n        }\n\n        // Nyní můžeme předpokládat, že newLength je alespoň 1 prvek.  Tento algoritmus\n        // funguje následovně: Nejprve alokujeme nové pole.  Pak my\n        // se chystáte zkopírovat prvky ze stávajícího pole do nového pole.\n        // Jakmile je to hotovo, můžeme zničit staré pole a vytvořit m_data\n        // ukažte na nové pole.\n\n        // Nejprve musíme alokovat nové pole\n        int* data{ new int[static_cast<std::velikost_t>(newLength)] };\n\n        // Potom musíme zjistit, kolik prvků zkopírovat z existujících\n        // pole do nového pole.  Chceme zkopírovat tolik prvků, kolik jich je\n        // v menším ze dvou polí.\n        if (m_length > 0)\n        {\n            int elementsToCopy{ (newLength > m_length) ? m_length : newLength };\n            std::copy_n(m_data, elementsToCopy, data); // zkopírujte prvky\n        }\n \n        // Nyní můžeme smazat staré pole, protože už ho nepotřebujeme\n        smazat[] m_data;\n\n        // A místo toho použijte nové pole!  Všimněte si, že to jednoduše dělá m_data bod\n        // na stejnou adresu jako nové pole, které jsme dynamicky alokovali.  Protože\n        // data byla dynamicky alokována, nebudou zničena, když přejdou mimo rozsah.\n        m_data = data;\n        m_délka = newLength;\n    }</code></pre>"
    },
    {
      "id": "container-classes_b25",
      "type": "paragraph",
      "html": "<p>Whew! That was a little tricky!</p>",
      "preview": "Whew! That was a little tricky!",
      "canCheck": true,
      "html_cs": "<p>Uf! To bylo trochu zapeklité!</p>"
    },
    {
      "id": "container-classes_b26",
      "type": "paragraph",
      "html": "<p>Let’s also add a copy constructor and assignment operator so we can copy the array.</p>",
      "preview": "Let’s also add a copy constructor and assignment operator so we can copy the arr",
      "canCheck": true,
      "html_cs": "<p>Přidejme také konstruktor kopírování a operátor přiřazení, abychom mohli pole zkopírovat.</p>"
    },
    {
      "id": "container-classes_b27",
      "type": "code",
      "language": "cpp",
      "code": "    IntArray(const IntArray& a): IntArray(a.getLength()) // use normal constructor to set size of array appropriately\n    {\n        std::copy_n(a.m_data, m_length, m_data); // copy the elements\n    }\n\n    IntArray& operator=(const IntArray& a)\n    {\n        // Self-assignment check\n        if (&a == this)\n            return *this;\n\n        // Set the size of the new array appropriately\n        reallocate(a.getLength());\n        std::copy_n(a.m_data, m_length, m_data); // copy the elements\n\n        return *this;\n    }",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">    IntArray(const IntArray&amp; a): IntArray(a.getLength()) // use normal constructor to set size of array appropriately\n    {\n        std::copy_n(a.m_data, m_length, m_data); // copy the elements\n    }\n\n    IntArray&amp; operator=(const IntArray&amp; a)\n    {\n        // Self-assignment check\n        if (&amp;a == this)\n            return *this;\n\n        // Set the size of the new array appropriately\n        reallocate(a.getLength());\n        std::copy_n(a.m_data, m_length, m_data); // copy the elements\n\n        return *this;\n    }</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\"> IntArray(const IntArray& a): IntArray(a.getLength()) // pro správné nastavení velikosti pole použijte normální konstruktor\n    {\n        std::copy_n(a.m_data, m_length, m_data); // zkopírujte prvky\n    }\n\n    IntArray& operator=(const IntArray& a)\n    {\n        // Kontrola vlastního přiřazení\n        if (&a == toto)\n            vrátit *toto;\n\n        // Správně nastavte velikost nového pole\n        reallocate(a.getLength());\n        std::copy_n(a.m_data, m_length, m_data); // zkopírujte prvky\n\n        vrátit *toto;\n    }</code></pre>"
    },
    {
      "id": "container-classes_b28",
      "type": "paragraph",
      "html": "<p>Many array container classes would stop here. However, just in case you want to see how insert and delete functionality would be implemented we’ll go ahead and write those too. Both of these algorithms are very similar to resize().</p>",
      "preview": "Many array container classes would stop here. However, just in case you want to ",
      "canCheck": true,
      "html_cs": "<p>Mnoho tříd kontejnerů pole by zde skončilo. Nicméně v případě, že chcete vidět, jak by byla implementována funkce vkládání a mazání, budeme pokračovat a napíšeme je také. Oba tyto algoritmy jsou velmi podobné resize().</p>"
    },
    {
      "id": "container-classes_b29",
      "type": "code",
      "language": "cpp",
      "code": "   void insertBefore(int value, int index)\n    {\n        // Sanity check our index value\n        assert(index >= 0 && index <= m_length);\n\n        // First create a new array one element larger than the old array\n        int* data{ new int[static_cast<std::size_t>(m_length+1)] };\n\n        // Copy all of the elements up to the index\n        std::copy_n(m_data, index, data); \n\n        // Insert our new element into the new array\n        data[index] = value;\n\n        // Copy all of the values after the inserted element\n        std::copy_n(m_data + index, m_length - index, data + index + 1);\n\n        // Finally, delete the old array, and use the new array instead\n        delete[] m_data;\n        m_data = data;\n        ++m_length;\n    }\n\n    void remove(int index)\n    {\n        // Sanity check our index value\n        assert(index >= 0 && index < m_length);\n\n        // If this is the last remaining element in the array, set the array to empty and bail out\n        if (m_length == 1)\n        {\n            erase();\n            return;\n        }\n\n        // First create a new array one element smaller than the old array\n        int* data{ new int[static_cast<std::size_t>(m_length-1)] };\n\n        // Copy all of the elements up to the index\n        std::copy_n(m_data, index, data); \n\n        // Copy all of the values after the removed element\n        std::copy_n(m_data + index + 1, m_length - index - 1, data + index); \n\n        // Finally, delete the old array, and use the new array instead\n        delete[] m_data;\n        m_data = data;\n        --m_length;\n    }",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">   void insertBefore(int value, int index)\n    {\n        // Sanity check our index value\n        assert(index &gt;= 0 &amp;&amp; index &lt;= m_length);\n\n        // First create a new array one element larger than the old array\n        int* data{ new int[static_cast&lt;std::size_t&gt;(m_length+1)] };\n\n        // Copy all of the elements up to the index\n        std::copy_n(m_data, index, data); \n\n        // Insert our new element into the new array\n        data[index] = value;\n\n        // Copy all of the values after the inserted element\n        std::copy_n(m_data + index, m_length - index, data + index + 1);\n\n        // Finally, delete the old array, and use the new array instead\n        delete[] m_data;\n        m_data = data;\n        ++m_length;\n    }\n\n    void remove(int index)\n    {\n        // Sanity check our index value\n        assert(index &gt;= 0 &amp;&amp; index &lt; m_length);\n\n        // If this is the last remaining element in the array, set the array to empty and bail out\n        if (m_length == 1)\n        {\n            erase();\n            return;\n        }\n\n        // First create a new array one element smaller than the old array\n        int* data{ new int[static_cast&lt;std::size_t&gt;(m_length-1)] };\n\n        // Copy all of the elements up to the index\n        std::copy_n(m_data, index, data); \n\n        // Copy all of the values after the removed element\n        std::copy_n(m_data + index + 1, m_length - index - 1, data + index); \n\n        // Finally, delete the old array, and use the new array instead\n        delete[] m_data;\n        m_data = data;\n        --m_length;\n    }</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\"> void insertBefore(int value, int index)\n    {\n        // Příčetnost zkontrolujte hodnotu našeho indexu\n        tvrdit(index >= 0 && index <= m_délka);\n\n        // Nejprve vytvořte nové pole o jeden prvek větší než staré pole\n        int* data{ new int[static_cast<std::size_t>(m_length+1)] };\n\n        // Zkopírujte všechny prvky až do indexu\n        std::copy_n(m_data, index, data); \n\n        // Vložte náš nový prvek do nového pole\n        data[index] = hodnota;\n\n        // Zkopírujte všechny hodnoty za vložený prvek\n        std::copy_n(m_data + index, m_length - index, data + index + 1);\n\n        // Nakonec smažte staré pole a použijte místo něj nové pole\n        smazat[] m_data;\n        m_data = data;\n        ++m_délka;\n    }\n\n    void remove (int index)\n    {\n        // Příčetnost zkontrolujte hodnotu našeho indexu\n        tvrdit(index >= 0 && index < m_délka);\n\n        // Pokud je toto poslední zbývající prvek v poli, nastavte pole na prázdné a vylévejte\n        if (m_length == 1)\n        {\n            vymazat();\n            návrat;\n        }\n\n        // Nejprve vytvořte nové pole o jeden prvek menší než staré pole\n        int* data{ new int[static_cast<std::size_t>(m_length-1)] };\n\n        // Zkopírujte všechny prvky až do indexu\n        std::copy_n(m_data, index, data); \n\n        // Zkopírujte všechny hodnoty za odstraněným prvkem\n        std::copy_n(m_data + index + 1, m_length - index - 1, data + index); \n\n        // Nakonec smažte staré pole a použijte místo něj nové pole\n        smazat[] m_data;\n        m_data = data;\n        --m_délka;\n    }</code></pre>"
    },
    {
      "id": "container-classes_b30",
      "type": "paragraph",
      "html": "<p>Here is our IntArray container class in its entirety.</p>",
      "preview": "Here is our IntArray container class in its entirety.",
      "canCheck": true,
      "html_cs": "<p>Zde je celá naše třída kontejnerů IntArray.</p>"
    },
    {
      "id": "container-classes_b31",
      "type": "paragraph",
      "html": "<p>IntArray.h:</p>",
      "preview": "IntArray.h:",
      "canCheck": true,
      "html_cs": "<p>IntArray.h:</p>"
    },
    {
      "id": "container-classes_b32",
      "type": "code",
      "language": "cpp",
      "code": "#ifndef INTARRAY_H\n#define INTARRAY_H\n\n#include <algorithm> // for std::copy_n\n#include <cassert> // for assert()\n#include <cstddef> // for std::size_t\n\nclass IntArray\n{\nprivate:\n    int m_length{};\n    int* m_data{};\n\npublic:\n    IntArray() = default;\n\n    IntArray(int length):\n        m_length{ length }\n    {\n        assert(length >= 0);\n\n        if (length > 0)\n            m_data = new int[static_cast<std::size_t>(length)]{};\n    }\n\n    ~IntArray()\n    {\n        delete[] m_data;\n        // we don't need to set m_data to null or m_length to 0 here, since the object will be destroyed immediately after this function anyway\n    }\n\n    IntArray(const IntArray& a): IntArray(a.getLength()) // use normal constructor to set size of array appropriately\n    {\n        std::copy_n(a.m_data, m_length, m_data); // copy the elements\n    }\n\n    IntArray& operator=(const IntArray& a)\n    {\n        // Self-assignment check\n        if (&a == this)\n            return *this;\n\n        // Set the size of the new array appropriately\n        reallocate(a.getLength());\n        std::copy_n(a.m_data, m_length, m_data); // copy the elements\n\n        return *this;\n    }\n\n    void erase()\n    {\n        delete[] m_data;\n        // We need to make sure we set m_data to nullptr here, otherwise it will\n        // be left pointing at deallocated memory!\n        m_data = nullptr;\n        m_length = 0;\n    }\n\n    int& operator[](int index)\n    {\n        assert(index >= 0 && index < m_length);\n        return m_data[index];\n    }\n\n\n    // reallocate resizes the array.  Any existing elements will be destroyed.  This function operates quickly.\n    void reallocate(int newLength)\n    {\n        // First we delete any existing elements\n        erase();\n\n        // If our array is going to be empty now, return here\n        if (newLength <= 0)\n            return;\n\n        // Then we have to allocate new elements\n        m_data = new int[static_cast<std::size_t>(newLength)];\n        m_length = newLength;\n    }\n\n    // resize resizes the array.  Any existing elements will be kept.  This function operates slowly.\n    void resize(int newLength)\n    {\n        // if the array is already the right length, we're done\n        if (newLength == m_length)\n            return;\n\n        // If we are resizing to an empty array, do that and return\n        if (newLength <= 0)\n        {\n            erase();\n            return;\n        }\n\n        // Now we can assume newLength is at least 1 element.  This algorithm\n        // works as follows: First we are going to allocate a new array.  Then we\n        // are going to copy elements from the existing array to the new array.\n        // Once that is done, we can destroy the old array, and make m_data\n        // point to the new array.\n\n        // First we have to allocate a new array\n        int* data{ new int[static_cast<std::size_t>(newLength)] };\n\n        // Then we have to figure out how many elements to copy from the existing\n        // array to the new array.  We want to copy as many elements as there are\n        // in the smaller of the two arrays.\n        if (m_length > 0)\n        {\n            int elementsToCopy{ (newLength > m_length) ? m_length : newLength };\n            std::copy_n(m_data, elementsToCopy, data); // copy the elements\n        }\n \n        // Now we can delete the old array because we don't need it any more\n        delete[] m_data;\n\n        // And use the new array instead!  Note that this simply makes m_data point\n        // to the same address as the new array we dynamically allocated.  Because\n        // data was dynamically allocated, it won't be destroyed when it goes out of scope.\n        m_data = data;\n        m_length = newLength;\n    }\n\n    void insertBefore(int value, int index)\n    {\n        // Sanity check our index value\n        assert(index >= 0 && index <= m_length);\n\n        // First create a new array one element larger than the old array\n        int* data{ new int[static_cast<std::size_t>(m_length+1)] };\n\n        // Copy all of the elements up to the index\n        std::copy_n(m_data, index, data); \n\n        // Insert our new element into the new array\n        data[index] = value;\n\n        // Copy all of the values after the inserted element\n        std::copy_n(m_data + index, m_length - index, data + index + 1);\n\n        // Finally, delete the old array, and use the new array instead\n        delete[] m_data;\n        m_data = data;\n        ++m_length;\n    }\n\n    void remove(int index)\n    {\n        // Sanity check our index value\n        assert(index >= 0 && index < m_length);\n\n        // If this is the last remaining element in the array, set the array to empty and bail out\n        if (m_length == 1)\n        {\n            erase();\n            return;\n        }\n\n        // First create a new array one element smaller than the old array\n        int* data{ new int[static_cast<std::size_t>(m_length-1)] };\n\n        // Copy all of the elements up to the index\n        std::copy_n(m_data, index, data); \n\n        // Copy all of the values after the removed element\n        std::copy_n(m_data + index + 1, m_length - index - 1, data + index); \n\n        // Finally, delete the old array, and use the new array instead\n        delete[] m_data;\n        m_data = data;\n        --m_length;\n    }\n\n    // A couple of additional functions just for convenience\n    void insertAtBeginning(int value) { insertBefore(value, 0); }\n    void insertAtEnd(int value) { insertBefore(value, m_length); }\n\n    int getLength() const { return m_length; }\n};\n\n#endif",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef INTARRAY_H\n#define INTARRAY_H\n\n#include &lt;algorithm&gt; // for std::copy_n\n#include &lt;cassert&gt; // for assert()\n#include &lt;cstddef&gt; // for std::size_t\n\nclass IntArray\n{\nprivate:\n    int m_length{};\n    int* m_data{};\n\npublic:\n    IntArray() = default;\n\n    IntArray(int length):\n        m_length{ length }\n    {\n        assert(length &gt;= 0);\n\n        if (length &gt; 0)\n            m_data = new int[static_cast&lt;std::size_t&gt;(length)]{};\n    }\n\n    ~IntArray()\n    {\n        delete[] m_data;\n        // we don't need to set m_data to null or m_length to 0 here, since the object will be destroyed immediately after this function anyway\n    }\n\n    IntArray(const IntArray&amp; a): IntArray(a.getLength()) // use normal constructor to set size of array appropriately\n    {\n        std::copy_n(a.m_data, m_length, m_data); // copy the elements\n    }\n\n    IntArray&amp; operator=(const IntArray&amp; a)\n    {\n        // Self-assignment check\n        if (&amp;a == this)\n            return *this;\n\n        // Set the size of the new array appropriately\n        reallocate(a.getLength());\n        std::copy_n(a.m_data, m_length, m_data); // copy the elements\n\n        return *this;\n    }\n\n    void erase()\n    {\n        delete[] m_data;\n        // We need to make sure we set m_data to nullptr here, otherwise it will\n        // be left pointing at deallocated memory!\n        m_data = nullptr;\n        m_length = 0;\n    }\n\n    int&amp; operator[](int index)\n    {\n        assert(index &gt;= 0 &amp;&amp; index &lt; m_length);\n        return m_data[index];\n    }\n\n\n    // reallocate resizes the array.  Any existing elements will be destroyed.  This function operates quickly.\n    void reallocate(int newLength)\n    {\n        // First we delete any existing elements\n        erase();\n\n        // If our array is going to be empty now, return here\n        if (newLength &lt;= 0)\n            return;\n\n        // Then we have to allocate new elements\n        m_data = new int[static_cast&lt;std::size_t&gt;(newLength)];\n        m_length = newLength;\n    }\n\n    // resize resizes the array.  Any existing elements will be kept.  This function operates slowly.\n    void resize(int newLength)\n    {\n        // if the array is already the right length, we're done\n        if (newLength == m_length)\n            return;\n\n        // If we are resizing to an empty array, do that and return\n        if (newLength &lt;= 0)\n        {\n            erase();\n            return;\n        }\n\n        // Now we can assume newLength is at least 1 element.  This algorithm\n        // works as follows: First we are going to allocate a new array.  Then we\n        // are going to copy elements from the existing array to the new array.\n        // Once that is done, we can destroy the old array, and make m_data\n        // point to the new array.\n\n        // First we have to allocate a new array\n        int* data{ new int[static_cast&lt;std::size_t&gt;(newLength)] };\n\n        // Then we have to figure out how many elements to copy from the existing\n        // array to the new array.  We want to copy as many elements as there are\n        // in the smaller of the two arrays.\n        if (m_length &gt; 0)\n        {\n            int elementsToCopy{ (newLength &gt; m_length) ? m_length : newLength };\n            std::copy_n(m_data, elementsToCopy, data); // copy the elements\n        }\n \n        // Now we can delete the old array because we don't need it any more\n        delete[] m_data;\n\n        // And use the new array instead!  Note that this simply makes m_data point\n        // to the same address as the new array we dynamically allocated.  Because\n        // data was dynamically allocated, it won't be destroyed when it goes out of scope.\n        m_data = data;\n        m_length = newLength;\n    }\n\n    void insertBefore(int value, int index)\n    {\n        // Sanity check our index value\n        assert(index &gt;= 0 &amp;&amp; index &lt;= m_length);\n\n        // First create a new array one element larger than the old array\n        int* data{ new int[static_cast&lt;std::size_t&gt;(m_length+1)] };\n\n        // Copy all of the elements up to the index\n        std::copy_n(m_data, index, data); \n\n        // Insert our new element into the new array\n        data[index] = value;\n\n        // Copy all of the values after the inserted element\n        std::copy_n(m_data + index, m_length - index, data + index + 1);\n\n        // Finally, delete the old array, and use the new array instead\n        delete[] m_data;\n        m_data = data;\n        ++m_length;\n    }\n\n    void remove(int index)\n    {\n        // Sanity check our index value\n        assert(index &gt;= 0 &amp;&amp; index &lt; m_length);\n\n        // If this is the last remaining element in the array, set the array to empty and bail out\n        if (m_length == 1)\n        {\n            erase();\n            return;\n        }\n\n        // First create a new array one element smaller than the old array\n        int* data{ new int[static_cast&lt;std::size_t&gt;(m_length-1)] };\n\n        // Copy all of the elements up to the index\n        std::copy_n(m_data, index, data); \n\n        // Copy all of the values after the removed element\n        std::copy_n(m_data + index + 1, m_length - index - 1, data + index); \n\n        // Finally, delete the old array, and use the new array instead\n        delete[] m_data;\n        m_data = data;\n        --m_length;\n    }\n\n    // A couple of additional functions just for convenience\n    void insertAtBeginning(int value) { insertBefore(value, 0); }\n    void insertAtEnd(int value) { insertBefore(value, m_length); }\n\n    int getLength() const { return m_length; }\n};\n\n#endif</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef INTARRAY_H\n#define INTARRAY_H\n\n#include <algoritmus> // pro std::copy_n\n#include <cassert> // pro sustain()\n#include <cstddef> // pro std::size_t\n\ntřídy IntArray\n{\nsoukromé:\n    int m_délka{};\n    int* m_data{};\n\nveřejnost:\n    IntArray() = výchozí;\n\n    IntArray (délka int):\n        m_délka{ délka }\n    {\n        tvrdit(délka >= 0);\n\n        if (délka > 0)\n            m_data = new int[static_cast<std::size_t>(délka)]{};\n    }\n\n    ~IntArray()\n    {\n        smazat[] m_data;\n        // zde nemusíme nastavovat m_data na null nebo m_length na 0, protože objekt bude stejně zničen ihned po této funkci\n    }\n\n    IntArray(const IntArray& a): IntArray(a.getLength()) // použijte normální konstruktor pro správné nastavení velikosti pole\n    {\n        std::copy_n(a.m_data, m_length, m_data); // zkopírujte prvky\n    }\n\n    IntArray& operator=(const IntArray& a)\n    {\n        // Kontrola vlastního přiřazení\n        if (&a == toto)\n            vrátit *toto;\n\n        // Správně nastavte velikost nového pole\n        reallocate(a.getLength());\n        std::copy_n(a.m_data, m_length, m_data); // zkopírujte prvky\n\n        vrátit *toto;\n    }\n\n    void vymazat()\n    {\n        smazat[] m_data;\n        // Musíme se ujistit, že jsme zde nastavili m_data na nullptr, jinak tomu tak bude\n        // zůstane ukazovat na uvolněnou paměť!\n        m_data = nullptr;\n        m_délka = 0;\n    }\n\n    operátor int&[](index int)\n    {\n        tvrdit(index >= 0 && index < m_délka);\n        return m_data[index];\n    }\n\n\n    // přerozdělení změní velikost pole.  Všechny existující prvky budou zničeny.  Tato funkce funguje rychle.\n    void přerozdělení (int newLength)\n    {\n        // Nejprve odstraníme všechny existující prvky\n        vymazat();\n\n        // Pokud bude naše pole nyní prázdné, vraťte se sem\n        if (newLength <= 0)\n            návrat;\n\n        // Potom musíme alokovat nové prvky\n        m_data = new int[static_cast<std::size_t>(newLength)];\n        m_délka = newLength;\n    }\n\n    // resize změní velikost pole.  Všechny stávající prvky budou zachovány.  Tato funkce pracuje pomalu.\n    void resize (int newLength)\n    {\n        // pokud má pole již správnou délku, máme hotovo\n        if (newLength == m_length)\n            návrat;\n\n        // Pokud měníme velikost na prázdné pole, udělejte to a vraťte se\n        if (newLength <= 0)\n        {\n            vymazat();\n            návrat;\n        }\n\n        // Nyní můžeme předpokládat, že newLength je alespoň 1 prvek.  Tento algoritmus\n        // funguje následovně: Nejprve alokujeme nové pole.  Pak my\n        // se chystáte zkopírovat prvky ze stávajícího pole do nového pole.\n        // Jakmile je to hotovo, můžeme zničit staré pole a vytvořit m_data\n        // ukažte na nové pole.\n\n        // Nejprve musíme alokovat nové pole\n        int* data{ new int[static_cast<std::velikost_t>(newLength)] };\n\n        // Potom musíme zjistit, kolik prvků zkopírovat z existujících\n        // pole do nového pole.  Chceme zkopírovat tolik prvků, kolik jich je\n        // v menším ze dvou polí.\n        if (m_length > 0)\n        {\n            int elementsToCopy{ (newLength > m_length) ? m_length : newLength };\n            std::copy_n(m_data, elementsToCopy, data); // zkopírujte prvky\n        }\n \n        // Nyní můžeme smazat staré pole, protože už ho nepotřebujeme\n        smazat[] m_data;\n\n        // A místo toho použijte nové pole!  Všimněte si, že to jednoduše dělá m_data bod\n        // na stejnou adresu jako nové pole, které jsme dynamicky alokovali.  Protože\n        // data byla dynamicky alokována, nebudou zničena, když přejdou mimo rozsah.\n        m_data = data;\n        m_délka = newLength;\n    }\n\n    void insertBefore (hodnota int, index int)\n    {\n        // Příčetnost zkontrolujte hodnotu našeho indexu\n        tvrdit(index >= 0 && index <= m_délka);\n\n        // Nejprve vytvořte nové pole o jeden prvek větší než staré pole\n        int* data{ new int[static_cast<std::size_t>(m_length+1)] };\n\n        // Zkopírujte všechny prvky až do indexu\n        std::copy_n(m_data, index, data); \n\n        // Vložte náš nový prvek do nového pole\n        data[index] = hodnota;\n\n        // Zkopírujte všechny hodnoty za vložený prvek\n        std::copy_n(m_data + index, m_length - index, data + index + 1);\n\n        // Nakonec smažte staré pole a použijte místo něj nové pole\n        smazat[] m_data;\n        m_data = data;\n        ++m_délka;\n    }\n\n    void remove (int index)\n    {\n        // Příčetnost zkontrolujte hodnotu našeho indexu\n        tvrdit(index >= 0 && index < m_délka);\n\n        // Pokud je toto poslední zbývající prvek v poli, nastavte pole na prázdné a vylévejte\n        if (m_length == 1)\n        {\n            vymazat();\n            návrat;\n        }\n\n        // Nejprve vytvořte nové pole o jeden prvek menší než staré pole\n        int* data{ new int[static_cast<std::size_t>(m_length-1)] };\n\n        // Zkopírujte všechny prvky až do indexu\n        std::copy_n(m_data, index, data); \n\n        // Zkopírujte všechny hodnoty za odstraněným prvkem\n        std::copy_n(m_data + index + 1, m_length - index - 1, data + index); \n\n        // Nakonec smažte staré pole a použijte místo něj nové pole\n        smazat[] m_data;\n        m_data = data;\n        --m_délka;\n    }\n\n    // Pár dalších funkcí jen pro pohodlí\n    void insertAtBeginning(int value) { insertBefore(value, 0); }\n    void insertAtEnd(int value) { insertBefore(hodnota, m_délka); }\n\n    int getLength() const { return m_délka; }\n};\n\n#endif</code></pre>"
    },
    {
      "id": "container-classes_b33",
      "type": "paragraph",
      "html": "<p>Now, let’s test it just to prove it works:</p>",
      "preview": "Now, let’s test it just to prove it works:",
      "canCheck": true,
      "html_cs": "<p>Nyní to otestujme, jen abychom dokázali, že to funguje:</p>"
    },
    {
      "id": "container-classes_b34",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include \"IntArray.h\"\n\nint main()\n{\n    // Declare an array with 10 elements\n    IntArray array(10);\n\n    // Fill the array with numbers 1 through 10\n    for (int i{ 0 }; i<10; ++i)\n        array[i] = i+1;\n\n    // Resize the array to 8 elements\n    array.resize(8);\n\n    // Insert the number 20 before element with index 5\n    array.insertBefore(20, 5);\n\n    // Remove the element with index 3\n    array.remove(3);\n\n    // Add 30 and 40 to the end and beginning\n    array.insertAtEnd(30);\n    array.insertAtBeginning(40);\n\n    // A few more tests to ensure copy constructing / assigning arrays\n    // doesn't break things\n    {\n        IntArray b{ array };\n        b = array;\n        b = b;\n        array = array;\n    }\n\n    // Print out all the numbers\n    for (int i{ 0 }; i<array.getLength(); ++i)\n        std::cout << array[i] << ' ';\n\n    std::cout << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include \"IntArray.h\"\n\nint main()\n{\n    // Declare an array with 10 elements\n    IntArray array(10);\n\n    // Fill the array with numbers 1 through 10\n    for (int i{ 0 }; i&lt;10; ++i)\n        array[i] = i+1;\n\n    // Resize the array to 8 elements\n    array.resize(8);\n\n    // Insert the number 20 before element with index 5\n    array.insertBefore(20, 5);\n\n    // Remove the element with index 3\n    array.remove(3);\n\n    // Add 30 and 40 to the end and beginning\n    array.insertAtEnd(30);\n    array.insertAtBeginning(40);\n\n    // A few more tests to ensure copy constructing / assigning arrays\n    // doesn't break things\n    {\n        IntArray b{ array };\n        b = array;\n        b = b;\n        array = array;\n    }\n\n    // Print out all the numbers\n    for (int i{ 0 }; i&lt;array.getLength(); ++i)\n        std::cout &lt;&lt; array[i] &lt;&lt; ' ';\n\n    std::cout &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include <iostream>\n#include \"IntArray.h\"\n\nint main()\n{\n    // Deklaruje pole s 10 prvky\n    IntArray pole(10);\n\n    // Vyplňte pole čísly 1 až 10\n    for (int i{ 0 }; i<10; ++i)\n        pole[i] = i+1;\n\n    // Změní velikost pole na 8 prvků\n    array.resize(8);\n\n    // Před prvek s indexem 5 vložíme číslo 20\n    array.insertBefore(20, 5);\n\n    // Odebere prvek s indexem 3\n    array.remove(3);\n\n    // Přidejte 30 a 40 na konec a začátek\n    array.insertAtEnd(30);\n    array.insertAtBeginning(40);\n\n    // Několik dalších testů pro zajištění kopírování / přiřazování polí\n    // nerozbije věci\n    {\n        IntArray b{ pole };\n        b = pole;\n        b = b;\n        pole = pole;\n    }\n\n    // Vytiskněte všechna čísla\n    for (int i{ 0 }; i<array.getLength(); ++i)\n        std::cout << pole[i] << ' ';\n\n    std::cout << '\\n';\n\n    návrat 0;\n}</code></pre>"
    },
    {
      "id": "container-classes_b35",
      "type": "paragraph",
      "html": "<p>This produces the result:</p>",
      "preview": "This produces the result:",
      "canCheck": true,
      "html_cs": "<p>Toto produkuje výsledek:</p>"
    },
    {
      "id": "container-classes_b36",
      "type": "output",
      "text": "40 1 2 3 5 20 6 7 8 30\n",
      "html": "<pre>40 1 2 3 5 20 6 7 8 30\n</pre>",
      "canCheck": false,
      "html_cs": "<před>40 1 2 3 5 20 6 7 8 30\n</pre>"
    },
    {
      "id": "container-classes_b37",
      "type": "paragraph",
      "html": "<p>Although writing container classes can be pretty complex, the good news is that you only have to write them once. Once the container class is working, you can use and reuse it as often as you like without any additional programming effort required.</p>",
      "preview": "Although writing container classes can be pretty complex, the good news is that ",
      "canCheck": true,
      "html_cs": "<p>Přestože psaní tříd kontejnerů může být docela složité, dobrou zprávou je, že je stačí napsat pouze jednou. Jakmile je třída kontejneru funkční, můžete ji používat a opakovaně používat, jak často chcete, aniž byste vyžadovali další programování.</p>"
    },
    {
      "id": "container-classes_b38",
      "type": "paragraph",
      "html": "<p>A few additional improvements that could/should be made:</p>",
      "preview": "A few additional improvements that could/should be made:",
      "canCheck": true,
      "html_cs": "<p>Několik dalších vylepšení, která by mohla/měla být provedena:</p>"
    },
    {
      "id": "container-classes_b39",
      "type": "list",
      "html": "<ul><li>We could have made this a template class, so that it would work with any copyable type rather than just int.</li><li>We should add const overloads of various member functions to properly support const IntArrays.</li><li>We should add support for move semantics (via adding a move constructor and move assignment).</li><li>When performing resize or insertion operations, we can move elements instead of copying them.</li></ul>",
      "preview": "We could have made this a template class, so that it would work with any copyabl",
      "canCheck": true,
      "html_cs": "<ul><li>Mohli jsme z toho udělat třídu šablony, aby fungovala s jakýmkoli kopírovatelným typem spíše než jen s int.</li><li>Měli bychom přidat const přetížení různých členských funkcí, abychom správně podporovali const IntArrays.</li><li>Měli bychom přidat podporu pro sémantiku přesunu (prostřednictvím přidání konstruktoru přesunu a přiřazení přesunu).</li><li>Při provádění změn velikosti/přesouvání prvků můžeme místo toho kopírovat a přesunovat operace vkládání.</li>"
    },
    {
      "id": "container-classes_b40",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">For advanced readers</p><p>Some advanced improvements related to exception handling:</p><ul><li>When performing resize or insertion operations, move elements only if their move constructor is noexcept, otherwise copy them (<a href=\"https://www.learncpp.com/cpp-tutorial/stdmove_if_noexcept/\">27.10 -- std::move_if_noexcept</a>).</li><li>Provide a strong exception safety guarantee for resize or insertion operations (<a href=\"https://www.learncpp.com/cpp-tutorial/exception-specifications-and-noexcept/\">27.9 -- Exception specifications and noexcept</a>).</li></ul></div>",
      "preview": "For advanced readersSome advanced improvements related to exception handling:When performing resize ",
      "canCheck": true,
      "html_cs": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Pro pokročilé čtenáře</p><p>Některá pokročilá vylepšení související se zpracováním výjimek:</p><ul><li>Při provádění operací změny velikosti nebo vkládání přesouvejte prvky pouze v případě, že jejich konstruktor přesunu je noexcept, jinak je zkopírujte (<a href=\"https://www.learncpp.com/cpp-tutorial/stdmove_if_noexcept/\">27.10 -- std::move_if_noexcept</a>).</li><li>Poskytněte silnou záruku bezpečnosti výjimek pro operace změny velikosti nebo vkládání (<a href=\"https://www.learncpp.com//exceptions-7-specific. Specifikace výjimek a noexcept</a>).</li></ul></div>"
    },
    {
      "id": "container-classes_b41",
      "type": "paragraph",
      "html": "<p>One more thing: If a class in the standard library meets your needs, use that instead of creating your own. For example, instead of using IntArray, you’re better off using <code>std::vector&lt;int&gt;</code>. It’s battle tested, efficient, and plays nicely with the other classes in the standard library. But sometimes you need a specialized container class that doesn’t exist in the standard library, so it’s good to know how to create your own when you need to.</p>",
      "preview": "One more thing: If a class in the standard library meets your needs, use that in",
      "canCheck": true,
      "html_cs": "<p>Ještě jedna věc: Pokud třída ve standardní knihovně vyhovuje vašim potřebám, použijte ji namísto vytváření vlastní. Například místo použití IntArray je lepší použít <code>std::vector<int></code>. Je otestován v bitvě, je efektivní a dobře se hraje s ostatními třídami ve standardní knihovně. Někdy ale potřebujete specializovanou třídu kontejneru, která ve standardní knihovně neexistuje, takže je dobré vědět, jak si vytvořit vlastní, když to potřebujete.</p>"
    }
  ],
  "title_cs": "23.6 — Kontejnerové třídy"
}