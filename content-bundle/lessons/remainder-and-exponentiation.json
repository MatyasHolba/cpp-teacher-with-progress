{
  "index": 75,
  "chapterId": "6",
  "number": "6.3",
  "title": "6.3 — Remainder and Exponentiation",
  "url": "https://www.learncpp.com/cpp-tutorial/remainder-and-exponentiation/",
  "slug": "remainder-and-exponentiation",
  "scrapedAt": "2026-09-15T16:29:15.414Z",
  "totalBlocks": 41,
  "totalCheckpoints": 31,
  "blocks": [
    {
      "id": "remainder-and-exponentiation_b0",
      "type": "section",
      "title": "The remainder operator (operator%)",
      "html": "<h3 class=\"cpp-section-heading\">The remainder operator (<code>operator%</code>)</h3>",
      "canCheck": false
    },
    {
      "id": "remainder-and-exponentiation_b1",
      "type": "paragraph",
      "html": "<p>The <strong>remainder operator</strong> (also commonly called the <strong>modulo operator</strong> or <strong>modulus operator</strong>) is an operator that returns the remainder after doing an integer division. For example, 7 / 4 = 1 remainder 3. Therefore, 7 % 4 = 3. As another example, 25 / 7 = 3 remainder 4, thus 25 % 7 = 4. The remainder operator only works with integer operands.</p>",
      "preview": "The remainder operator (also commonly called the modulo operator or modulus oper",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b2",
      "type": "paragraph",
      "html": "<p>This is most useful for testing whether a number is evenly divisible by another number (meaning that after division, there is no remainder): if <em>x % y</em> evaluates to 0, then we know that <em>x</em> is evenly divisible by <em>y</em>.</p>",
      "preview": "This is most useful for testing whether a number is evenly divisible by another ",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b3",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n\tstd::cout << \"Enter an integer: \";\n\tint x{};\n\tstd::cin >> x;\n\n\tstd::cout << \"Enter another integer: \";\n\tint y{};\n\tstd::cin >> y;\n\n\tstd::cout << \"The remainder is: \" << x % y << '\\n';\n\n\tif ((x % y) == 0)\n\t\tstd::cout << x << \" is evenly divisible by \" << y << '\\n';\n\telse\n\t\tstd::cout << x << \" is not evenly divisible by \" << y << '\\n';\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n\tstd::cout &lt;&lt; \"Enter an integer: \";\n\tint x{};\n\tstd::cin &gt;&gt; x;\n\n\tstd::cout &lt;&lt; \"Enter another integer: \";\n\tint y{};\n\tstd::cin &gt;&gt; y;\n\n\tstd::cout &lt;&lt; \"The remainder is: \" &lt;&lt; x % y &lt;&lt; '\\n';\n\n\tif ((x % y) == 0)\n\t\tstd::cout &lt;&lt; x &lt;&lt; \" is evenly divisible by \" &lt;&lt; y &lt;&lt; '\\n';\n\telse\n\t\tstd::cout &lt;&lt; x &lt;&lt; \" is not evenly divisible by \" &lt;&lt; y &lt;&lt; '\\n';\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b4",
      "type": "paragraph",
      "html": "<p>Here are a couple runs of this program:</p>",
      "preview": "Here are a couple runs of this program:",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b5",
      "type": "output",
      "text": "Enter an integer: 6\nEnter another integer: 3\nThe remainder is: 0\n6 is evenly divisible by 3\n",
      "html": "<pre>Enter an integer: 6\nEnter another integer: 3\nThe remainder is: 0\n6 is evenly divisible by 3\n</pre>",
      "canCheck": false
    },
    {
      "id": "remainder-and-exponentiation_b6",
      "type": "output",
      "text": "Enter an integer: 6\nEnter another integer: 4\nThe remainder is: 2\n6 is not evenly divisible by 4\n",
      "html": "<pre>Enter an integer: 6\nEnter another integer: 4\nThe remainder is: 2\n6 is not evenly divisible by 4\n</pre>",
      "canCheck": false
    },
    {
      "id": "remainder-and-exponentiation_b7",
      "type": "paragraph",
      "html": "<p>Now let’s try an example where the second number is bigger than the first:</p>",
      "preview": "Now let’s try an example where the second number is bigger than the first:",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b8",
      "type": "output",
      "text": "Enter an integer: 2\nEnter another integer: 4\nThe remainder is: 2\n2 is not evenly divisible by 4\n",
      "html": "<pre>Enter an integer: 2\nEnter another integer: 4\nThe remainder is: 2\n2 is not evenly divisible by 4\n</pre>",
      "canCheck": false
    },
    {
      "id": "remainder-and-exponentiation_b9",
      "type": "paragraph",
      "html": "<p>A remainder of 2 might be a little non-obvious at first, but it’s simple: 2 / 4 is 0 (using integer division) remainder 2. Whenever the second number is larger than the first, the second number will divide the first 0 times, so the first number will be the remainder.</p>",
      "preview": "A remainder of 2 might be a little non-obvious at first, but it’s simple: 2 / 4 ",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b10",
      "type": "section",
      "title": "Remainder with negative numbers",
      "html": "<h3 class=\"cpp-section-heading\">Remainder with negative numbers</h3>",
      "canCheck": false
    },
    {
      "id": "remainder-and-exponentiation_b11",
      "type": "paragraph",
      "html": "<p>The remainder operator can also work with negative operands. <code>x % y</code> always returns results with the sign of <em>x</em>.</p>",
      "preview": "The remainder operator can also work with negative operands. x % y always return",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b12",
      "type": "paragraph",
      "html": "<p>Running the above program:</p>",
      "preview": "Running the above program:",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b13",
      "type": "output",
      "text": "Enter an integer: -6\nEnter another integer: 4\nThe remainder is: -2\n-6 is not evenly divisible by 4\n",
      "html": "<pre>Enter an integer: -6\nEnter another integer: 4\nThe remainder is: -2\n-6 is not evenly divisible by 4\n</pre>",
      "canCheck": false
    },
    {
      "id": "remainder-and-exponentiation_b14",
      "type": "output",
      "text": "Enter an integer: 6\nEnter another integer: -4\nThe remainder is: 2\n6 is not evenly divisible by -4\n",
      "html": "<pre>Enter an integer: 6\nEnter another integer: -4\nThe remainder is: 2\n6 is not evenly divisible by -4\n</pre>",
      "canCheck": false
    },
    {
      "id": "remainder-and-exponentiation_b15",
      "type": "paragraph",
      "html": "<p>In both cases, you can see the remainder takes the sign of the first operand.</p>",
      "preview": "In both cases, you can see the remainder takes the sign of the first operand.",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b16",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Nomenclature</p><p>The C++ standard does not actually give a name to <code>operator%</code>. However, the C++20 standard does say, “the binary % operator yields the remainder from the division of the first expression by the second”.</p><p>Although <code>operator%</code> is often called the “modulo” or “modulus” operator, this can be confusing, because modulo in mathematics is often defined in a way that yields a different result to what <code>operator%</code> in C++ produces when one (and only one) of the operands is negative.</p><p>For example, in mathematics:<br>-21 modulo 4 = 3<br>-21 remainder 4 = -1</p><p>For this reason, we believe the name “remainder” is a more accurate name for <code>operator%</code> than “modulo”.</p></div>",
      "preview": "NomenclatureThe C++ standard does not actually give a name to operator%. However, the C++20 standard",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b17",
      "type": "paragraph",
      "html": "<p>In cases where the first operand can be negative, one must take care to note that the remainder can also be negative. For example, you might think to write a function that returns whether a number is odd like this:</p>",
      "preview": "In cases where the first operand can be negative, one must take care to note tha",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b18",
      "type": "code",
      "language": "cpp",
      "code": "bool isOdd(int x)\n{\n    return (x % 2) == 1; // fails when x is -5\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">bool isOdd(int x)\n{\n    return (x % 2) == 1; // fails when x is -5\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b19",
      "type": "paragraph",
      "html": "<p>However, this will fail when x is a negative odd number, such as <code>-5</code>, because <code>-5 % 2</code> is -1, and -1 != 1.</p>",
      "preview": "However, this will fail when x is a negative odd number, such as -5, because -5 ",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b20",
      "type": "paragraph",
      "html": "<p>For this reason, if you’re going to compare the result of a remainder operation, it’s better to compare against 0, which does not have positive/negative number issues:</p>",
      "preview": "For this reason, if you’re going to compare the result of a remainder operation,",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b21",
      "type": "code",
      "language": "cpp",
      "code": "bool isOdd(int x)\n{\n    return (x % 2) != 0; // could also write return (x % 2)\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">bool isOdd(int x)\n{\n    return (x % 2) != 0; // could also write return (x % 2)\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b22",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Prefer to compare the result of the remainder operator (<code>operator%</code>) against 0 if possible.</p></div>",
      "preview": "Best practicePrefer to compare the result of the remainder operator (operator%) against 0 if possibl",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b23",
      "type": "section",
      "title": "Where’s the exponent operator?",
      "html": "<h3 class=\"cpp-section-heading\">Where’s the exponent operator?</h3>",
      "canCheck": false
    },
    {
      "id": "remainder-and-exponentiation_b24",
      "type": "paragraph",
      "html": "<p>You’ll note that the <em>^</em> operator (commonly used to denote exponentiation in mathematics) is a <em>Bitwise XOR</em> operation in C++ (covered in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/bit-manipulation-with-bitwise-operators-and-bit-masks/\">O.3 -- Bit manipulation with bitwise operators and bit masks</a>). C++ does not include an exponent operator.</p>",
      "preview": "You’ll note that the ^ operator (commonly used to denote exponentiation in mathe",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b25",
      "type": "paragraph",
      "html": "<p>To do exponents in C++, #include the &lt;cmath&gt; header, and use the pow() function:</p>",
      "preview": "To do exponents in C++, #include the <cmath> header, and use the pow() function:",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b26",
      "type": "code",
      "language": "cpp",
      "code": "#include <cmath>\n\ndouble x{ std::pow(3.0, 4.0) }; // 3 to the 4th power",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cmath&gt;\n\ndouble x{ std::pow(3.0, 4.0) }; // 3 to the 4th power</code></pre>",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b27",
      "type": "paragraph",
      "html": "<p>Note that the parameters (and return value) of function pow() are of type double. Due to rounding errors in floating point numbers, the results of pow() may not be precise (even if you pass it integers or whole numbers).</p>",
      "preview": "Note that the parameters (and return value) of function pow() are of type double",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b28",
      "type": "paragraph",
      "html": "<p>If you want to do integer exponentiation, you’re best off using your own function to do so. The following function implements integer exponentiation (using the non-intuitive “exponentiation by squaring” algorithm for efficiency):</p>",
      "preview": "If you want to do integer exponentiation, you’re best off using your own functio",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b29",
      "type": "code",
      "language": "cpp",
      "code": "#include <cassert> // for assert\n#include <cstdint> // for std::int64_t\n#include <iostream>\n\n// note: exp must be non-negative\n// note: does not perform range/overflow checking, use with caution\nconstexpr std::int64_t powint(std::int64_t base, int exp)\n{\n\tassert(exp >= 0 && \"powint: exp parameter has negative value\");\n\n\t// Handle 0 case\n\tif (base == 0)\n\t\treturn (exp == 0) ? 1 : 0;\n\n\tstd::int64_t result{ 1 };\n\twhile (exp > 0)\n\t{\n\t\tif (exp & 1)  // if exp is odd\n\t\t\tresult *= base;\n\t\texp /= 2;\n\t\tbase *= base;\n\t}\n\n\treturn result;\n}\n\nint main()\n{\n\tstd::cout << powint(7, 12) << '\\n'; // 7 to the 12th power\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt; // for assert\n#include &lt;cstdint&gt; // for std::int64_t\n#include &lt;iostream&gt;\n\n// note: exp must be non-negative\n// note: does not perform range/overflow checking, use with caution\nconstexpr std::int64_t powint(std::int64_t base, int exp)\n{\n\tassert(exp &gt;= 0 &amp;&amp; \"powint: exp parameter has negative value\");\n\n\t// Handle 0 case\n\tif (base == 0)\n\t\treturn (exp == 0) ? 1 : 0;\n\n\tstd::int64_t result{ 1 };\n\twhile (exp &gt; 0)\n\t{\n\t\tif (exp &amp; 1)  // if exp is odd\n\t\t\tresult *= base;\n\t\texp /= 2;\n\t\tbase *= base;\n\t}\n\n\treturn result;\n}\n\nint main()\n{\n\tstd::cout &lt;&lt; powint(7, 12) &lt;&lt; '\\n'; // 7 to the 12th power\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b30",
      "type": "paragraph",
      "html": "<p>Produces:</p>",
      "preview": "Produces:",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b31",
      "type": "output",
      "text": "13841287201\n",
      "html": "<pre>13841287201\n</pre>",
      "canCheck": false
    },
    {
      "id": "remainder-and-exponentiation_b32",
      "type": "paragraph",
      "html": "<p>Don’t worry if you don’t understand how this function works -- you don’t need to understand it in order to call it.</p>",
      "preview": "Don’t worry if you don’t understand how this function works -- you don’t need to",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b33",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>We cover asserts in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/assert-and-static_assert/\">9.6 -- Assert and static_assert</a>, and constexpr functions in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/constexpr-functions/\">F.1 -- Constexpr functions</a>.</p></div>",
      "preview": "Related contentWe cover asserts in lesson 9.6 -- Assert and static_assert, and constexpr functions i",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b34",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">For advanced readers</p><p>The <code>constexpr</code> specifier allows a function to be evaluated at compile-time if used in a constant expression; otherwise, it behaves like a regular function and is evaluated at runtime.</p></div>",
      "preview": "For advanced readersThe constexpr specifier allows a function to be evaluated at compile-time if use",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b35",
      "type": "callout",
      "calloutType": "warning",
      "html": "<div class=\"cpp-note cpp-lightredbackground\"><p class=\"cpp-note-title cpp-bottomline\">Warning</p><p>In the vast majority of cases, integer exponentiation will overflow the integral type. This is likely why such a function wasn’t included in the standard library in the first place.</p></div>",
      "preview": "WarningIn the vast majority of cases, integer exponentiation will overflow the integral type. This i",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b36",
      "type": "paragraph",
      "html": "<p>Here is a safer version of the exponentiation function above that checks for overflow:</p>",
      "preview": "Here is a safer version of the exponentiation function above that checks for ove",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b37",
      "type": "code",
      "language": "cpp",
      "code": "#include <cassert> // for assert\n#include <cstdint> // for std::int64_t\n#include <iostream>\n#include <limits> // for std::numeric_limits\n\n// A safer (but slower) version of powint() that checks for overflow\n// note: exp must be non-negative\n// Returns std::numeric_limits<std::int64_t>::max() if overflow occurs\nconstexpr std::int64_t powint_safe(std::int64_t base, int exp)\n{\n    assert(exp >= 0 && \"powint_safe: exp parameter has negative value\");\n\n    // Handle 0 case\n    if (base == 0)\n        return (exp == 0) ? 1 : 0;\n\n    std::int64_t result { 1 };\n\n    // To make the range checks easier, we'll ensure base is positive\n    // We'll flip the result at the end if needed\n    bool negativeResult{ false };\n\n    if (base < 0)\n    {\n        base = -base;\n        negativeResult = (exp & 1);\n    }\n\n    while (exp > 0)\n    {\n        if (exp & 1) // if exp is odd\n        {\n            // Check if result will overflow when multiplied by base\n            if (result > std::numeric_limits<std::int64_t>::max() / base)\n            {\n                std::cerr << \"powint_safe(): result overflowed\\n\";\n                return std::numeric_limits<std::int64_t>::max();\n            }\n\n            result *= base;\n        }\n\n        exp /= 2;\n\n        // If we're done, get out here\n        if (exp <= 0)\n            break;\n\n        // The following only needs to execute if we're going to iterate again\n\n        // Check if base will overflow when multiplied by base\n        if (base > std::numeric_limits<std::int64_t>::max() / base)\n        {\n            std::cerr << \"powint_safe(): base overflowed\\n\";\n            return std::numeric_limits<std::int64_t>::max();\n        }\n\n        base *= base;\n    }\n\n    if (negativeResult)\n        return -result;\n\n    return result;\n}\n\nint main()\n{\n\tstd::cout << powint_safe(7, 12) << '\\n'; // 7 to the 12th power\n\tstd::cout << powint_safe(70, 12) << '\\n'; // 70 to the 12th power (will return the max 64-bit int value)\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt; // for assert\n#include &lt;cstdint&gt; // for std::int64_t\n#include &lt;iostream&gt;\n#include &lt;limits&gt; // for std::numeric_limits\n\n// A safer (but slower) version of powint() that checks for overflow\n// note: exp must be non-negative\n// Returns std::numeric_limits&lt;std::int64_t&gt;::max() if overflow occurs\nconstexpr std::int64_t powint_safe(std::int64_t base, int exp)\n{\n    assert(exp &gt;= 0 &amp;&amp; \"powint_safe: exp parameter has negative value\");\n\n    // Handle 0 case\n    if (base == 0)\n        return (exp == 0) ? 1 : 0;\n\n    std::int64_t result { 1 };\n\n    // To make the range checks easier, we'll ensure base is positive\n    // We'll flip the result at the end if needed\n    bool negativeResult{ false };\n\n    if (base &lt; 0)\n    {\n        base = -base;\n        negativeResult = (exp &amp; 1);\n    }\n\n    while (exp &gt; 0)\n    {\n        if (exp &amp; 1) // if exp is odd\n        {\n            // Check if result will overflow when multiplied by base\n            if (result &gt; std::numeric_limits&lt;std::int64_t&gt;::max() / base)\n            {\n                std::cerr &lt;&lt; \"powint_safe(): result overflowed\\n\";\n                return std::numeric_limits&lt;std::int64_t&gt;::max();\n            }\n\n            result *= base;\n        }\n\n        exp /= 2;\n\n        // If we're done, get out here\n        if (exp &lt;= 0)\n            break;\n\n        // The following only needs to execute if we're going to iterate again\n\n        // Check if base will overflow when multiplied by base\n        if (base &gt; std::numeric_limits&lt;std::int64_t&gt;::max() / base)\n        {\n            std::cerr &lt;&lt; \"powint_safe(): base overflowed\\n\";\n            return std::numeric_limits&lt;std::int64_t&gt;::max();\n        }\n\n        base *= base;\n    }\n\n    if (negativeResult)\n        return -result;\n\n    return result;\n}\n\nint main()\n{\n\tstd::cout &lt;&lt; powint_safe(7, 12) &lt;&lt; '\\n'; // 7 to the 12th power\n\tstd::cout &lt;&lt; powint_safe(70, 12) &lt;&lt; '\\n'; // 70 to the 12th power (will return the max 64-bit int value)\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b38",
      "type": "section",
      "title": "Quiz time",
      "html": "<h3 class=\"cpp-section-heading\">Quiz time</h3>",
      "canCheck": false
    },
    {
      "id": "remainder-and-exponentiation_b39",
      "type": "paragraph",
      "html": "<div class=\"quiz\" style=\"clear:both\"><p class=\"quiz-header\">Question #1</p><p>What does the following expression evaluate to? <code>6 + 5 * 4 % 3</code></p><p><a class=\"solution_link_show\" href=\"javascript:void(0)\" onclick=\"cppSolutionToggle(document.getElementById('cpp_solution_id_0'),this,'Show Solution','Hide Solution')\">Show Solution</a></p><div class=\"wpsolution\" id=\"cpp_solution_id_0\" style=\"display:none\">Because * and % have higher precedence than +, the + will evaluate last. We can rewrite our expression as 6 + (5 * 4 % 3). Operators * and % have the same precedence, so we have to look at the associativity to resolve them. The associativity for operators * and % is left to right, so we resolve the left operator first. We can rewrite our expression like this: 6 + ((5 * 4) % 3).<p></p><p>6 + ((5 * 4) % 3) = 6 + (20 % 3) = 6 + 2 = 8</p></div><p></p></div>",
      "preview": "Question #1What does the following expression evaluate to? 6 + 5 * 4 % 3Show Sol",
      "canCheck": true
    },
    {
      "id": "remainder-and-exponentiation_b40",
      "type": "paragraph",
      "html": "<div class=\"quiz\" style=\"clear:both\"><p class=\"quiz-header\">Question #2</p><p>Write a program that asks the user to input an integer, and tells the user whether the number is even or odd. Write a constexpr function called <code>isEven()</code> that returns true if an integer passed to it is even, and false otherwise. Use the remainder operator to test whether the integer parameter is even. Make sure <code>isEven()</code> works with both positive and negative numbers.</p><p><a class=\"hint_link_show\" href=\"javascript:void(0)\" onclick=\"cppHintToggle(document.getElementById('cpp_hint_id_0'),this,'Show Hint','')\">Show Hint</a></p><div class=\"wphint\" id=\"cpp_hint_id_0\" style=\"display:none;margin-bottom:1em\">Hint: You may need to use if-statements and the comparison operator (==) for this program. See lesson <a href=\"https://www.learncpp.com/cpp-tutorial/boolean-values/\">4.9 -- Boolean values</a> if you need a refresher on how to do this.</div><p>Your program should match the following output:</p><pre>Enter an integer: 5\n5 is odd\n</pre><p><a class=\"solution_link_show\" href=\"javascript:void(0)\" onclick=\"cppSolutionToggle(document.getElementById('cpp_solution_id_1'),this,'Show Solution','Hide Solution')\">Show Solution</a></p><div class=\"wpsolution\" id=\"cpp_solution_id_1\" style=\"display:none\"><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nconstexpr bool isEven(int x)\n{\n    // if x % 2 == 0, 2 divides evenly into our number, which means it must be an even number\n    return (x % 2) == 0;\n}\n\nint main()\n{\n    std::cout &lt;&lt; \"Enter an integer: \";\n    int x{};\n    std::cin &gt;&gt; x;\n\n    if (isEven(x))\n        std::cout &lt;&lt; x &lt;&lt; \" is even\\n\";\n    else\n        std::cout &lt;&lt; x &lt;&lt; \" is odd\\n\";\n\n    return 0;\n}</code></pre><p>Note: You may have been tempted to write function isEven() like this:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">constexpr bool isEven(int x)\n{\n    if ((x % 2) == 0)\n        return true;\n    else\n        return false;\n}</code></pre><p>While this works, it’s more complicated than it needs to be. Let’s take a look at how we can simplify it. First, let’s pull out the if statement conditional and assign it to a separate boolean:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">constexpr bool isEven(int x)\n{\n    bool isEven = (x % 2) == 0;\n    if (isEven) // isEven is true\n        return true;\n    else // isEven is false\n        return false;\n}</code></pre><p>Now, note that the if statement above essentially says “if isEven is true, return true, otherwise if isEven is false, return false”. If that’s the case, we can just return isEven:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">constexpr bool isEven(int x)\n{\n    bool isEven = (x % 2) == 0;\n    return isEven;\n}</code></pre><p>And in this case, since we only use variable isEven once, we might as well eliminate the variable:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">constexpr bool isEven(int x)\n{\n    return (x % 2) == 0;\n}</code></pre></div></div>",
      "preview": "Question #2Write a program that asks the user to input an integer, and tells the",
      "canCheck": true
    }
  ]
}