{
  "index": 314,
  "chapterId": "26",
  "number": "26.5",
  "title": "26.5 — Partial template specialization",
  "url": "https://www.learncpp.com/cpp-tutorial/partial-template-specialization/",
  "slug": "partial-template-specialization",
  "scrapedAt": "2026-09-15T16:31:21.494Z",
  "totalBlocks": 61,
  "totalCheckpoints": 53,
  "blocks": [
    {
      "id": "partial-template-specialization_b0",
      "type": "paragraph",
      "html": "<p>This lesson and the next are optional reading for those desiring a deeper knowledge of C++ templates. Partial template specialization is not used all that often (but can be useful in specific cases).</p>",
      "preview": "This lesson and the next are optional reading for those desiring a deeper knowle",
      "canCheck": true,
      "html_cs": "<p>Tato a následující lekce jsou nepovinnou četbou pro ty, kteří chtějí hlubší znalosti šablon C++. Částečná specializace šablon se nepoužívá tak často (ale může být užitečná ve specifických případech).</p>"
    },
    {
      "id": "partial-template-specialization_b1",
      "type": "paragraph",
      "html": "<p>In lesson <a href=\"https://www.learncpp.com/cpp-tutorial/template-non-type-parameters/\">26.2 -- Template non-type parameters</a>, you learned how expression parameters could be used to parameterize template classes.</p>",
      "preview": "In lesson 26.2 -- Template non-type parameters, you learned how expression param",
      "canCheck": true,
      "html_cs": "<p>V lekci <a href=\"https://www.learncpp.com/cpp-tutorial/template-non-type-parameters/\">26.2 -- Netypové parametry šablon</a> jste se naučili, jak lze parametry výrazu použít k parametrizaci tříd šablon.</p>"
    },
    {
      "id": "partial-template-specialization_b2",
      "type": "paragraph",
      "html": "<p>Let’s take another look at the Static Array class we used in one of our previous examples:</p>",
      "preview": "Let’s take another look at the Static Array class we used in one of our previous",
      "canCheck": true,
      "html_cs": "<p>Podívejme se znovu na třídu Static Array, kterou jsme použili v jednom z našich předchozích příkladů:</p>"
    },
    {
      "id": "partial-template-specialization_b3",
      "type": "code",
      "language": "cpp",
      "code": "template <typename T, int size> // size is the expression parameter\nclass StaticArray\n{\nprivate:\n    // The expression parameter controls the size of the array\n    T m_array[size]{};\n \npublic:\n    T* getArray() { return m_array; }\n\t\n    const T& operator[](int index) const { return m_array[index]; }\n    T& operator[](int index) { return m_array[index]; }\n};",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">template &lt;typename T, int size&gt; // size is the expression parameter\nclass StaticArray\n{\nprivate:\n    // The expression parameter controls the size of the array\n    T m_array[size]{};\n \npublic:\n    T* getArray() { return m_array; }\n\t\n    const T&amp; operator[](int index) const { return m_array[index]; }\n    T&amp; operator[](int index) { return m_array[index]; }\n};</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">šablona <typename T, int size> // velikost je parametr výrazu\ntřídy StaticArray\n{\nsoukromé:\n    // Parametr expression řídí velikost pole\n    T m_array[velikost]{};\n \nveřejnost:\n    T* getArray() { return m_array; }\n\t\n    const T& operator[](int index) const { return m_array[index]; }\n    T& operator[](int index) { return m_array[index]; }\n};</code></pre>"
    },
    {
      "id": "partial-template-specialization_b4",
      "type": "paragraph",
      "html": "<p>This class takes two template parameters: one type parameter and one expression parameter.</p>",
      "preview": "This class takes two template parameters: one type parameter and one expression ",
      "canCheck": true,
      "html_cs": "<p>Tato třída má dva parametry šablony: jeden parametr typu a jeden parametr výrazu.</p>"
    },
    {
      "id": "partial-template-specialization_b5",
      "type": "paragraph",
      "html": "<p>Now, let’s say we wanted to write a function to print out the whole array. Although we could implement this as a member function, we’re going to do it as a non-member function instead because it will make the successive examples easier to follow.</p>",
      "preview": "Now, let’s say we wanted to write a function to print out the whole array. Altho",
      "canCheck": true,
      "html_cs": "<p>Nyní řekněme, že jsme chtěli napsat funkci, která vytiskne celé pole. I když bychom to mohli implementovat jako členskou funkci, uděláme to jako nečlenskou funkci, protože to usnadní následování následujících příkladů.</p>"
    },
    {
      "id": "partial-template-specialization_b6",
      "type": "paragraph",
      "html": "<p>Using templates, we might write something like this:</p>",
      "preview": "Using templates, we might write something like this:",
      "canCheck": true,
      "html_cs": "<p>Pomocí šablon bychom mohli napsat něco takového:</p>"
    },
    {
      "id": "partial-template-specialization_b7",
      "type": "code",
      "language": "cpp",
      "code": "template <typename T, int size>\nvoid print(const StaticArray<T, size>& array)\n{\n    for (int count{ 0 }; count < size; ++count)\n        std::cout << array[count] << ' ';\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">template &lt;typename T, int size&gt;\nvoid print(const StaticArray&lt;T, size&gt;&amp; array)\n{\n    for (int count{ 0 }; count &lt; size; ++count)\n        std::cout &lt;&lt; array[count] &lt;&lt; ' ';\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">šablona <typename T, int size>\nvoid print(const StaticArray<T, size>& array)\n{\n    for (int count{ 0 }; count < size; ++count)\n        std::cout << pole[počet] << ' ';\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b8",
      "type": "paragraph",
      "html": "<p>This would allow us to do the following:</p>",
      "preview": "This would allow us to do the following:",
      "canCheck": true,
      "html_cs": "<p>To nám umožní provést následující:</p>"
    },
    {
      "id": "partial-template-specialization_b9",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\ntemplate <typename T, int size> // size is a template non-type parameter\nclass StaticArray\n{\nprivate:\n\tT m_array[size]{};\n\npublic:\n\tT* getArray() { return m_array; }\n\n\tconst T& operator[](int index) const { return m_array[index]; }\n\tT& operator[](int index) { return m_array[index]; }\n};\n\ntemplate <typename T, int size>\nvoid print(const StaticArray<T, size>& array)\n{\n\tfor (int count{ 0 }; count < size; ++count)\n\t\tstd::cout << array[count] << ' ';\n}\n\nint main()\n{\n\t// declare an int array\n\tStaticArray<int, 4> int4{};\n\tint4[0] = 0;\n\tint4[1] = 1;\n\tint4[2] = 2;\n\tint4[3] = 3;\n\n\t// Print the array\n\tprint(int4);\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\ntemplate &lt;typename T, int size&gt; // size is a template non-type parameter\nclass StaticArray\n{\nprivate:\n\tT m_array[size]{};\n\npublic:\n\tT* getArray() { return m_array; }\n\n\tconst T&amp; operator[](int index) const { return m_array[index]; }\n\tT&amp; operator[](int index) { return m_array[index]; }\n};\n\ntemplate &lt;typename T, int size&gt;\nvoid print(const StaticArray&lt;T, size&gt;&amp; array)\n{\n\tfor (int count{ 0 }; count &lt; size; ++count)\n\t\tstd::cout &lt;&lt; array[count] &lt;&lt; ' ';\n}\n\nint main()\n{\n\t// declare an int array\n\tStaticArray&lt;int, 4&gt; int4{};\n\tint4[0] = 0;\n\tint4[1] = 1;\n\tint4[2] = 2;\n\tint4[3] = 3;\n\n\t// Print the array\n\tprint(int4);\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include <iostream>\n\ntemplate <typename T, int size> // velikost je netypový parametr šablony\ntřídy StaticArray\n{\nsoukromé:\n\tT m_array[velikost]{};\n\nveřejnost:\n\tT* getArray() { return m_array; }\n\n\tconst T& operator[](int index) const { return m_array[index]; }\n\tT& operator[](int index) { return m_array[index]; }\n};\n\nšablona <název typu T, velikost int>\nvoid print(const StaticArray<T, size>& array)\n{\n\tfor (int count{ 0 }; count < size; ++count)\n\t\tstd::cout << pole[počet] << ' ';\n}\n\nint main()\n{\n\t// deklaruje pole int\n\tStaticArray<int, 4> int4{};\n\tint4[0] = 0;\n\tint4[1] = 1;\n\tint4[2] = 2;\n\tint4[3] = 3;\n\n\t// Vytiskne pole\n\tprint(int4);\n\n\tnávrat 0;\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b10",
      "type": "paragraph",
      "html": "<p>and get the following result:</p>",
      "preview": "and get the following result:",
      "canCheck": true,
      "html_cs": "<p>a získáte následující výsledek:</p>"
    },
    {
      "id": "partial-template-specialization_b11",
      "type": "output",
      "text": "0 1 2 3\n",
      "html": "<pre>0 1 2 3\n</pre>",
      "canCheck": false,
      "html_cs": "<před>0 1 2 3\n</pre>"
    },
    {
      "id": "partial-template-specialization_b12",
      "type": "paragraph",
      "html": "<p>Although this works, it has a design flaw. Consider the following:</p>",
      "preview": "Although this works, it has a design flaw. Consider the following:",
      "canCheck": true,
      "html_cs": "<p>Přestože to funguje, má designovou chybu. Zvažte následující:</p>"
    },
    {
      "id": "partial-template-specialization_b13",
      "type": "code",
      "language": "cpp",
      "code": "#include <algorithm>\n#include <iostream>\n#include <string_view>\n\nint main()\n{\n    // Declare a char array\n    StaticArray<char, 14> char14{};\n\n    // Copy some data into it\n    constexpr std::string_view hello{ \"Hello, world!\" };\n    std::copy_n(hello.begin(), hello.size(), char14.getArray());\n\n    // Print the array\n    print(char14);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;algorithm&gt;\n#include &lt;iostream&gt;\n#include &lt;string_view&gt;\n\nint main()\n{\n    // Declare a char array\n    StaticArray&lt;char, 14&gt; char14{};\n\n    // Copy some data into it\n    constexpr std::string_view hello{ \"Hello, world!\" };\n    std::copy_n(hello.begin(), hello.size(), char14.getArray());\n\n    // Print the array\n    print(char14);\n\n    return 0;\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include <algorithm>\n#include <iostream>\n#include <string_view>\n\nint main()\n{\n    // Deklaruje pole char\n    StaticArray<znak, 14> znak14{};\n\n    // Zkopírujte do něj nějaká data\n    constexpr std::string_view ahoj{ \"Ahoj světe!\" };\n    std::copy_n(hello.begin(), hello.size(), char14.getArray());\n\n    // Vytiskne pole\n    print(char14);\n\n    návrat 0;\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b14",
      "type": "paragraph",
      "html": "<p>(We covered std::strcpy in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/c-style-strings/\">17.10 -- C-style strings</a> if you need a refresher)</p>",
      "preview": "(We covered std::strcpy in lesson 17.10 -- C-style strings if you need a refresh",
      "canCheck": true,
      "html_cs": "<p>(Std::strcpy jsme probrali v lekci <a href=\"https://www.learncpp.com/cpp-tutorial/c-style-strings/\">17.10 -- Řetězce ve stylu C</a>, pokud potřebujete opakování)</p>"
    },
    {
      "id": "partial-template-specialization_b15",
      "type": "paragraph",
      "html": "<p>This program will compile, execute, and produce the following value (or one similar):</p>",
      "preview": "This program will compile, execute, and produce the following value (or one simi",
      "canCheck": true,
      "html_cs": "<p>Tento program zkompiluje, spustí a vytvoří následující hodnotu (nebo podobnou):</p>"
    },
    {
      "id": "partial-template-specialization_b16",
      "type": "output",
      "text": "H e l l o ,   w o r l d !\n",
      "html": "<pre>H e l l o ,   w o r l d !\n</pre>",
      "canCheck": false,
      "html_cs": "<pre>H e l l o , w o r l d !\n</pre>"
    },
    {
      "id": "partial-template-specialization_b17",
      "type": "paragraph",
      "html": "<p>For non-char types, it makes sense to put a space between each array element, so they don’t run together. However, with a char type, it makes more sense to print everything run together as a C-style string, which our print() function doesn’t do.</p>",
      "preview": "For non-char types, it makes sense to put a space between each array element, so",
      "canCheck": true,
      "html_cs": "<p>U typů bez znaků má smysl vložit mezi každý prvek pole mezeru, aby se nespouštěly společně. S typem char však dává větší smysl tisknout vše spuštěné společně jako řetězec ve stylu C, což naše funkce print() nedělá.</p>"
    },
    {
      "id": "partial-template-specialization_b18",
      "type": "paragraph",
      "html": "<p>So how can we fix this?</p>",
      "preview": "So how can we fix this?",
      "canCheck": true,
      "html_cs": "<p>Jak to tedy můžeme opravit?</p>"
    },
    {
      "id": "partial-template-specialization_b19",
      "type": "section",
      "title": "Template specialization to the rescue?",
      "html": "<h3 class=\"cpp-section-heading\">Template specialization to the rescue?</h3>",
      "canCheck": false,
      "html_cs": "<h3 class=\"cpp-section-heading\">Šablona specializace na záchranu?</h3>",
      "title_cs": "Specializace šablony k záchraně?"
    },
    {
      "id": "partial-template-specialization_b20",
      "type": "paragraph",
      "html": "<p>One might first think of using template specialization. The problem with full template specialization is that all template parameters must be explicitly defined.</p>",
      "preview": "One might first think of using template specialization. The problem with full te",
      "canCheck": true,
      "html_cs": "<p>Nejprve by se dalo uvažovat o použití specializace šablony. Problém s plnou specializací šablony je v tom, že všechny parametry šablony musí být explicitně definovány.</p>"
    },
    {
      "id": "partial-template-specialization_b21",
      "type": "paragraph",
      "html": "<p>Consider:</p>",
      "preview": "Consider:",
      "canCheck": true,
      "html_cs": "<p>Zvažte:</p>"
    },
    {
      "id": "partial-template-specialization_b22",
      "type": "code",
      "language": "cpp",
      "code": "#include <algorithm>\n#include <iostream>\n#include <string_view>\n\ntemplate <typename T, int size> // size is the expression parameter\nclass StaticArray\n{\nprivate:\n\t// The expression parameter controls the size of the array\n\tT m_array[size]{};\n\npublic:\n\tT* getArray() { return m_array; }\n\n\tconst T& operator[](int index) const { return m_array[index]; }\n\tT& operator[](int index) { return m_array[index]; }\n};\n\ntemplate <typename T, int size>\nvoid print(const StaticArray<T, size>& array)\n{\n\tfor (int count{ 0 }; count < size; ++count)\n\t\tstd::cout << array[count] << ' ';\n}\n\n// Override print() for fully specialized StaticArray<char, 14>\ntemplate <>\nvoid print(const StaticArray<char, 14>& array)\n{\n\tfor (int count{ 0 }; count < 14; ++count)\n\t\tstd::cout << array[count];\n}\n\nint main()\n{\n    // Declare a char array\n    StaticArray<char, 14> char14{};\n\n    // Copy some data into it\n    constexpr std::string_view hello{ \"Hello, world!\" };\n    std::copy_n(hello.begin(), hello.size(), char14.getArray());\n\n    // Print the array\n    print(char14);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;algorithm&gt;\n#include &lt;iostream&gt;\n#include &lt;string_view&gt;\n\ntemplate &lt;typename T, int size&gt; // size is the expression parameter\nclass StaticArray\n{\nprivate:\n\t// The expression parameter controls the size of the array\n\tT m_array[size]{};\n\npublic:\n\tT* getArray() { return m_array; }\n\n\tconst T&amp; operator[](int index) const { return m_array[index]; }\n\tT&amp; operator[](int index) { return m_array[index]; }\n};\n\ntemplate &lt;typename T, int size&gt;\nvoid print(const StaticArray&lt;T, size&gt;&amp; array)\n{\n\tfor (int count{ 0 }; count &lt; size; ++count)\n\t\tstd::cout &lt;&lt; array[count] &lt;&lt; ' ';\n}\n\n// Override print() for fully specialized StaticArray&lt;char, 14&gt;\ntemplate &lt;&gt;\nvoid print(const StaticArray&lt;char, 14&gt;&amp; array)\n{\n\tfor (int count{ 0 }; count &lt; 14; ++count)\n\t\tstd::cout &lt;&lt; array[count];\n}\n\nint main()\n{\n    // Declare a char array\n    StaticArray&lt;char, 14&gt; char14{};\n\n    // Copy some data into it\n    constexpr std::string_view hello{ \"Hello, world!\" };\n    std::copy_n(hello.begin(), hello.size(), char14.getArray());\n\n    // Print the array\n    print(char14);\n\n    return 0;\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include <algorithm>\n#include <iostream>\n#include <string_view>\n\ntemplate <typename T, int size> // velikost je parametr výrazu\ntřídy StaticArray\n{\nsoukromé:\n\t// Parametr expression řídí velikost pole\n\tT m_array[velikost]{};\n\nveřejnost:\n\tT* getArray() { return m_array; }\n\n\tconst T& operator[](int index) const { return m_array[index]; }\n\tT& operator[](int index) { return m_array[index]; }\n};\n\nšablona <název typu T, velikost int>\nvoid print(const StaticArray<T, size>& array)\n{\n\tfor (int count{ 0 }; count < size; ++count)\n\t\tstd::cout << pole[počet] << ' ';\n}\n\n// Přepíše print() pro plně specializované StaticArray<char, 14>\nšablona <>\nvoid print(const StaticArray<char, 14>& pole)\n{\n\tfor (int počet{ 0 }; počet < 14; ++počet)\n\t\tstd::cout << pole[pocet];\n}\n\nint main()\n{\n    // Deklaruje pole char\n    StaticArray<znak, 14> znak14{};\n\n    // Zkopírujte do něj nějaká data\n    constexpr std::string_view ahoj{ \"Ahoj světe!\" };\n    std::copy_n(hello.begin(), hello.size(), char14.getArray());\n\n    // Vytiskne pole\n    print(char14);\n\n    návrat 0;\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b23",
      "type": "paragraph",
      "html": "<p>As you can see, we’ve now provided an overloaded print function for fully specialized <code>StaticArray&lt;char, 14&gt;</code>. Indeed, this prints:</p>",
      "preview": "As you can see, we’ve now provided an overloaded print function for fully specia",
      "canCheck": true,
      "html_cs": "<p>Jak můžete vidět, nyní jsme poskytli přetíženou tiskovou funkci pro plně specializované <code>StaticArray<char, 14></code>. Opravdu, toto vytiskne:</p>"
    },
    {
      "id": "partial-template-specialization_b24",
      "type": "output",
      "text": "Hello, world!\n",
      "html": "<pre>Hello, world!\n</pre>",
      "canCheck": false,
      "html_cs": "<pre>Ahoj, světe!\n</pre>"
    },
    {
      "id": "partial-template-specialization_b25",
      "type": "paragraph",
      "html": "<p>Although this solves the issue of making sure print() can be called with a <code>StaticArray&lt;char, 14&gt;</code>, it brings up another problem: using full template specialization means we have to explicitly define the length of the array this function will accept! Consider the following example:</p>",
      "preview": "Although this solves the issue of making sure print() can be called with a Stati",
      "canCheck": true,
      "html_cs": "<p>Přestože to řeší problém zajištění funkce print() pomocí <code>StaticArray<char, 14></code>, přináší to další problém: použití plné specializace šablony znamená, že musíme explicitně definovat délku pole, kterou tato funkce přijme! Zvažte následující příklad:</p>"
    },
    {
      "id": "partial-template-specialization_b26",
      "type": "code",
      "language": "cpp",
      "code": "int main()\n{\n    // Declare a char array\n    StaticArray<char, 12> char12{};\n\n    // Copy some data into it\n    constexpr std::string_view hello{ \"Hello, mom!\" };\n    std::copy_n(hello.begin(), hello.size(), char12.getArray());\n\n    // Print the array\n    print(char12);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int main()\n{\n    // Declare a char array\n    StaticArray&lt;char, 12&gt; char12{};\n\n    // Copy some data into it\n    constexpr std::string_view hello{ \"Hello, mom!\" };\n    std::copy_n(hello.begin(), hello.size(), char12.getArray());\n\n    // Print the array\n    print(char12);\n\n    return 0;\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int main()\n{\n    // Deklaruje pole char\n    StaticArray<znak, 12> znak12{};\n\n    // Zkopírujte do něj nějaká data\n    constexpr std::string_view ahoj{ \"Ahoj, mami!\" };\n    std::copy_n(hello.begin(), hello.size(), char12.getArray());\n\n    // Vytiskne pole\n    print(char12);\n\n    návrat 0;\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b27",
      "type": "paragraph",
      "html": "<p>Calling <code>print()</code> with <code>char12</code> will call the version of <code>print()</code> that takes a <code>StaticArray&lt;T, size&gt;</code>, because <code>char12</code> is of type <code>StaticArray&lt;char, 12&gt;</code>, and our overloaded print() will only be called when passed a <code>StaticArray&lt;char, 14&gt;</code>.</p>",
      "preview": "Calling print() with char12 will call the version of print() that takes a Static",
      "canCheck": true,
      "html_cs": "<p>Volání <code>print()</code> pomocí <code>char12</code> zavolá verzi <code>print()</code>, která přebírá <code>StaticArray<T, size></code>, protože <code>char12</code> je typu <code>StaticArray<char, 12></code>, a náš pouze přetížený kód se bude nazývat StaticArray, když bude předán aArray 14></code>.</p>"
    },
    {
      "id": "partial-template-specialization_b28",
      "type": "paragraph",
      "html": "<p>Although we could make a copy of print() that handles <code>StaticArray&lt;char, 12&gt;</code>, what happens when we want to call print() with an array size of 5, or 22? We’d have to copy the function for each different array size. That’s redundant.</p>",
      "preview": "Although we could make a copy of print() that handles StaticArray<char, 12>, wha",
      "canCheck": true,
      "html_cs": "<p>Přestože bychom mohli vytvořit kopii print(), která zpracovává <code>StaticArray<char, 12></code>, co se stane, když chceme volat print() s velikostí pole 5 nebo 22? Museli bychom zkopírovat funkci pro každou jinou velikost pole. To je nadbytečné.</p>"
    },
    {
      "id": "partial-template-specialization_b29",
      "type": "paragraph",
      "html": "<p>Obviously full template specialization is too restrictive a solution here. The solution we are looking for is partial template specialization.</p>",
      "preview": "Obviously full template specialization is too restrictive a solution here. The s",
      "canCheck": true,
      "html_cs": "<p>Je zřejmé, že plná specializace na šablony je zde příliš restriktivní řešení. Řešením, které hledáme, je částečná specializace na šablony.</p>"
    },
    {
      "id": "partial-template-specialization_b30",
      "type": "section",
      "title": "Partial template specialization",
      "html": "<h3 class=\"cpp-section-heading\">Partial template specialization</h3>",
      "canCheck": false,
      "html_cs": "<h3 class=\"cpp-section-heading\">Částečná specializace šablony</h3>",
      "title_cs": "Částečná specializace šablony"
    },
    {
      "id": "partial-template-specialization_b31",
      "type": "paragraph",
      "html": "<p>Partial template specialization allows us to specialize classes (but not individual functions!) where some, but not all, of the template parameters have been explicitly defined. For our challenge above, the ideal solution would be to have our overloaded print function work with StaticArray of type char, but leave the length expression parameter templated so it can vary as needed. Partial template specialization allows us to do just that!</p>",
      "preview": "Partial template specialization allows us to specialize classes (but not individ",
      "canCheck": true,
      "html_cs": "<p>Částečná specializace šablony nám umožňuje specializovat třídy (ale ne jednotlivé funkce!), kde byly explicitně definovány některé, ale ne všechny, parametry šablony. Pro naši výše uvedenou výzvu by ideálním řešením bylo, aby naše přetížená tisková funkce pracovala se StaticArray typu char, ale parametr length expression ponechali šablonu, aby se mohl měnit podle potřeby. Částečná specializace šablony nám to umožňuje!</p>"
    },
    {
      "id": "partial-template-specialization_b32",
      "type": "paragraph",
      "html": "<p>Here’s our example with an overloaded print function that takes a partially specialized StaticArray:</p>",
      "preview": "Here’s our example with an overloaded print function that takes a partially spec",
      "canCheck": true,
      "html_cs": "<p>Zde je náš příklad s přetíženou tiskovou funkcí, která využívá částečně specializované StaticArray:</p>"
    },
    {
      "id": "partial-template-specialization_b33",
      "type": "code",
      "language": "cpp",
      "code": "// overload of print() function for partially specialized StaticArray<char, size>\ntemplate <int size> // size is still a template non-type parameter\nvoid print(const StaticArray<char, size>& array) // we're explicitly defining type char here\n{\n\tfor (int count{ 0 }; count < size; ++count)\n\t\tstd::cout << array[count];\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// overload of print() function for partially specialized StaticArray&lt;char, size&gt;\ntemplate &lt;int size&gt; // size is still a template non-type parameter\nvoid print(const StaticArray&lt;char, size&gt;&amp; array) // we're explicitly defining type char here\n{\n\tfor (int count{ 0 }; count &lt; size; ++count)\n\t\tstd::cout &lt;&lt; array[count];\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// přetížení funkce print() pro částečně specializované StaticArray<char, size>\ntemplate <int size> // velikost je stále netypový parametr šablony\nvoid print(const StaticArray<znak, velikost>& pole) // zde explicitně definujeme typ char\n{\n\tfor (int count{ 0 }; count < size; ++count)\n\t\tstd::cout << pole[pocet];\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b34",
      "type": "paragraph",
      "html": "<p>As you can see here, we’ve explicitly declared that this function will only work for StaticArray of type char, but size is still a templated expression parameter, so it will work for char arrays of any size. That’s all there is to it!</p>",
      "preview": "As you can see here, we’ve explicitly declared that this function will only work",
      "canCheck": true,
      "html_cs": "<p>Jak můžete vidět zde, výslovně jsme deklarovali, že tato funkce bude fungovat pouze pro StaticArray typu char, ale velikost je stále parametr šablony výrazu, takže bude fungovat pro pole char libovolné velikosti. To je vše!</p>"
    },
    {
      "id": "partial-template-specialization_b35",
      "type": "paragraph",
      "html": "<p>Here’s a full program using this:</p>",
      "preview": "Here’s a full program using this:",
      "canCheck": true,
      "html_cs": "<p>Zde je kompletní program využívající toto:</p>"
    },
    {
      "id": "partial-template-specialization_b36",
      "type": "code",
      "language": "cpp",
      "code": "#include <algorithm>\n#include <iostream>\n#include <string_view>\n\ntemplate <typename T, int size> // size is the expression parameter\nclass StaticArray\n{\nprivate:\n\t// The expression parameter controls the size of the array\n\tT m_array[size]{};\n\npublic:\n\tT* getArray() { return m_array; }\n\n\tconst T& operator[](int index) const { return m_array[index]; }\n\tT& operator[](int index) { return m_array[index]; }\n};\n\ntemplate <typename T, int size>\nvoid print(const StaticArray<T, size>& array)\n{\n\tfor (int count{ 0 }; count < size; ++count)\n\t\tstd::cout << array[count] << ' ';\n}\n\n// overload of print() function for partially specialized StaticArray<char, size>\ntemplate <int size>\nvoid print(const StaticArray<char, size>& array)\n{\n\tfor (int count{ 0 }; count < size; ++count)\n\t\tstd::cout << array[count];\n}\n\nint main()\n{\n\t// Declare an char array of size 14\n\tStaticArray<char, 14> char14{};\n\n\t// Copy some data into it\n\tconstexpr std::string_view hello14{ \"Hello, world!\" };\n\tstd::copy_n(hello14.begin(), hello14.size(), char14.getArray());\n\n\t// Print the array\n\tprint(char14);\n\n\tstd::cout << ' ';\n\n\t// Now declare an char array of size 12\n\tStaticArray<char, 12> char12{};\n\n\t// Copy some data into it\n\tconstexpr std::string_view hello12{ \"Hello, mom!\" };\n\tstd::copy_n(hello12.begin(), hello12.size(), char12.getArray());\n\n\t// Print the array\n\tprint(char12);\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;algorithm&gt;\n#include &lt;iostream&gt;\n#include &lt;string_view&gt;\n\ntemplate &lt;typename T, int size&gt; // size is the expression parameter\nclass StaticArray\n{\nprivate:\n\t// The expression parameter controls the size of the array\n\tT m_array[size]{};\n\npublic:\n\tT* getArray() { return m_array; }\n\n\tconst T&amp; operator[](int index) const { return m_array[index]; }\n\tT&amp; operator[](int index) { return m_array[index]; }\n};\n\ntemplate &lt;typename T, int size&gt;\nvoid print(const StaticArray&lt;T, size&gt;&amp; array)\n{\n\tfor (int count{ 0 }; count &lt; size; ++count)\n\t\tstd::cout &lt;&lt; array[count] &lt;&lt; ' ';\n}\n\n// overload of print() function for partially specialized StaticArray&lt;char, size&gt;\ntemplate &lt;int size&gt;\nvoid print(const StaticArray&lt;char, size&gt;&amp; array)\n{\n\tfor (int count{ 0 }; count &lt; size; ++count)\n\t\tstd::cout &lt;&lt; array[count];\n}\n\nint main()\n{\n\t// Declare an char array of size 14\n\tStaticArray&lt;char, 14&gt; char14{};\n\n\t// Copy some data into it\n\tconstexpr std::string_view hello14{ \"Hello, world!\" };\n\tstd::copy_n(hello14.begin(), hello14.size(), char14.getArray());\n\n\t// Print the array\n\tprint(char14);\n\n\tstd::cout &lt;&lt; ' ';\n\n\t// Now declare an char array of size 12\n\tStaticArray&lt;char, 12&gt; char12{};\n\n\t// Copy some data into it\n\tconstexpr std::string_view hello12{ \"Hello, mom!\" };\n\tstd::copy_n(hello12.begin(), hello12.size(), char12.getArray());\n\n\t// Print the array\n\tprint(char12);\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include <algorithm>\n#include <iostream>\n#include <string_view>\n\ntemplate <typename T, int size> // velikost je parametr výrazu\ntřídy StaticArray\n{\nsoukromé:\n\t// Parametr expression řídí velikost pole\n\tT m_array[velikost]{};\n\nveřejnost:\n\tT* getArray() { return m_array; }\n\n\tconst T& operator[](int index) const { return m_array[index]; }\n\tT& operator[](int index) { return m_array[index]; }\n};\n\nšablona <název typu T, velikost int>\nvoid print(const StaticArray<T, size>& array)\n{\n\tfor (int count{ 0 }; count < size; ++count)\n\t\tstd::cout << pole[počet] << ' ';\n}\n\n// přetížení funkce print() pro částečně specializované StaticArray<char, size>\nšablona <int size>\nvoid print(const StaticArray<znak, velikost>& pole)\n{\n\tfor (int count{ 0 }; count < size; ++count)\n\t\tstd::cout << pole[pocet];\n}\n\nint main()\n{\n\t// Deklarujte pole znaků o velikosti 14\n\tStaticArray<znak, 14> znak14{};\n\n\t// Zkopírujte do něj nějaká data\n\tconstexpr std::string_view hello14{ \"Ahoj světe!\" };\n\tstd::copy_n(hello14.begin(), hello14.size(), char14.getArray());\n\n\t// Vytiskne pole\n\tprint(char14);\n\n\tstd::cout << ' ';\n\n\t// Nyní deklarujte pole znaků o velikosti 12\n\tStaticArray<znak, 12> znak12{};\n\n\t// Zkopírujte do něj nějaká data\n\tconstexpr std::string_view hello12{ \"Ahoj, mami!\" };\n\tstd::copy_n(hello12.begin(), hello12.size(), char12.getArray());\n\n\t// Vytiskne pole\n\tprint(char12);\n\n\tnávrat 0;\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b37",
      "type": "paragraph",
      "html": "<p>This prints:</p>",
      "preview": "This prints:",
      "canCheck": true,
      "html_cs": "<p>Toto vytiskne:</p>"
    },
    {
      "id": "partial-template-specialization_b38",
      "type": "output",
      "text": "Hello, world! Hello, mom!\n",
      "html": "<pre>Hello, world! Hello, mom!\n</pre>",
      "canCheck": false,
      "html_cs": "<pre>Ahoj, světe! Ahoj, mami!\n</pre>"
    },
    {
      "id": "partial-template-specialization_b39",
      "type": "paragraph",
      "html": "<p>Just as we expect.</p>",
      "preview": "Just as we expect.",
      "canCheck": true,
      "html_cs": "<p>Jak očekáváme.</p>"
    },
    {
      "id": "partial-template-specialization_b40",
      "type": "paragraph",
      "html": "<p>Partial template specialization can only be used with classes, not template functions (functions must be fully specialized). Our <code>void print(StaticArray&lt;char, size&gt; &amp;array)</code> example works because the print function is not partially specialized (it’s just an overloaded template function that happens to have a partially-specialized class parameter).</p>",
      "preview": "Partial template specialization can only be used with classes, not template func",
      "canCheck": true,
      "html_cs": "<p>Částečnou specializaci šablony lze použít pouze s třídami, nikoli s funkcemi šablon (funkce musí být plně specializované). Náš příklad <code>void print(StaticArray<char, size> &array)</code> funguje, protože funkce tisku není částečně specializovaná (je to pouze přetížená funkce šablony, která má náhodou částečně specializovaný parametr třídy).</p>"
    },
    {
      "id": "partial-template-specialization_b41",
      "type": "section",
      "title": "Partial template specialization for member functions",
      "html": "<h3 class=\"cpp-section-heading\">Partial template specialization for member functions</h3>",
      "canCheck": false,
      "html_cs": "<h3 class=\"cpp-section-heading\">Částečná specializace šablony pro členské funkce</h3>",
      "title_cs": "Částečná specializace šablony pro členské funkce"
    },
    {
      "id": "partial-template-specialization_b42",
      "type": "paragraph",
      "html": "<p>The limitation on the partial specialization of functions can lead to some challenges when dealing with member functions. For example, what if we had defined StaticArray like this?</p>",
      "preview": "The limitation on the partial specialization of functions can lead to some chall",
      "canCheck": true,
      "html_cs": "<p>Omezení částečné specializace funkcí může vést k určitým problémům při práci s členskými funkcemi. Co kdybychom například takto definovali StaticArray?</p>"
    },
    {
      "id": "partial-template-specialization_b43",
      "type": "code",
      "language": "cpp",
      "code": "template <typename T, int size>\nclass StaticArray\n{\nprivate:\n    T m_array[size]{};\n \npublic:\n    T* getArray() { return m_array; }\n\t\n    const T& operator[](int index) const { return m_array[index]; }\n    T& operator[](int index) { return m_array[index]; }\n\n    void print() const;\n};\n\ntemplate <typename T, int size> \nvoid StaticArray<T, size>::print() const\n{\n    for (int i{ 0 }; i < size; ++i)\n        std::cout << m_array[i] << ' ';\n    std::cout << '\\n';\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">template &lt;typename T, int size&gt;\nclass StaticArray\n{\nprivate:\n    T m_array[size]{};\n \npublic:\n    T* getArray() { return m_array; }\n\t\n    const T&amp; operator[](int index) const { return m_array[index]; }\n    T&amp; operator[](int index) { return m_array[index]; }\n\n    void print() const;\n};\n\ntemplate &lt;typename T, int size&gt; \nvoid StaticArray&lt;T, size&gt;::print() const\n{\n    for (int i{ 0 }; i &lt; size; ++i)\n        std::cout &lt;&lt; m_array[i] &lt;&lt; ' ';\n    std::cout &lt;&lt; '\\n';\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">šablona <typename T, int size>\ntřídy StaticArray\n{\nsoukromé:\n    T m_array[velikost]{};\n \nveřejnost:\n    T* getArray() { return m_array; }\n\t\n    const T& operator[](int index) const { return m_array[index]; }\n    T& operator[](int index) { return m_array[index]; }\n\n    void print() const;\n};\n\nšablona <název typu T, velikost int> \nvoid StaticArray<T, size>::print() const\n{\n    for (int i{ 0 }; i < size; ++i)\n        std::cout << m_array[i] << ' ';\n    std::cout << '\\n';\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b44",
      "type": "paragraph",
      "html": "<p>print() is now a member function of class <code>StaticArray&lt;T, int&gt;</code>. So what happens when we want to partially specialize print(), so that it works differently? You might try this:</p>",
      "preview": "print() is now a member function of class StaticArray<T, int>. So what happens w",
      "canCheck": true,
      "html_cs": "<p>print() je nyní členská funkce třídy <code>StaticArray<T, int></code>. Co se tedy stane, když chceme print() částečně specializovat, aby fungoval jinak? You might try this:</p>"
    },
    {
      "id": "partial-template-specialization_b45",
      "type": "code",
      "language": "cpp",
      "code": "// Doesn't work, can't partially specialize functions\ntemplate <int size>\nvoid StaticArray<double, size>::print() const\n{\n\tfor (int i{ 0 }; i < size; ++i)\n\t\tstd::cout << std::scientific << m_array[i] << ' ';\n\tstd::cout << '\\n';\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// Doesn't work, can't partially specialize functions\ntemplate &lt;int size&gt;\nvoid StaticArray&lt;double, size&gt;::print() const\n{\n\tfor (int i{ 0 }; i &lt; size; ++i)\n\t\tstd::cout &lt;&lt; std::scientific &lt;&lt; m_array[i] &lt;&lt; ' ';\n\tstd::cout &lt;&lt; '\\n';\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// Nefunguje, nelze částečně specializovat funkce\nšablona <int size>\nvoid StaticArray<double, size>::print() const\n{\n\tfor (int i{ 0 }; i < size; ++i)\n\t\tstd::cout << std::scientific << m_array[i] << ' ';\n\tstd::cout << '\\n';\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b46",
      "type": "paragraph",
      "html": "<p>Unfortunately, this doesn’t work, because we’re trying to partially specialize a function, which is disallowed.</p>",
      "preview": "Unfortunately, this doesn’t work, because we’re trying to partially specialize a",
      "canCheck": true,
      "html_cs": "<p>To bohužel nefunguje, protože se snažíme funkci částečně specializovat, což je zakázáno.</p>"
    },
    {
      "id": "partial-template-specialization_b47",
      "type": "paragraph",
      "html": "<p>So how do we get around this? One obvious way is to partially specialize the entire class:</p>",
      "preview": "So how do we get around this? One obvious way is to partially specialize the ent",
      "canCheck": true,
      "html_cs": "<p>So how do we get around this? Jedním zřejmým způsobem je částečná specializace celé třídy:</p>"
    },
    {
      "id": "partial-template-specialization_b48",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\ntemplate <typename T, int size>\nclass StaticArray\n{\nprivate:\n\tT m_array[size]{};\n\npublic:\n\tT* getArray() { return m_array; }\n\n\tconst T& operator[](int index) const { return m_array[index]; }\n\tT& operator[](int index) { return m_array[index]; }\n\n\tvoid print() const;\n};\n\ntemplate <typename T, int size> \nvoid StaticArray<T, size>::print() const\n{\n\tfor (int i{ 0 }; i < size; ++i)\n\t\tstd::cout << m_array[i] << ' ';\n\tstd::cout << '\\n';\n}\n\n// Partially specialized class\ntemplate <int size>\nclass StaticArray<double, size>\n{\nprivate:\n\tdouble m_array[size]{};\n\npublic:\n\tdouble* getArray() { return m_array; }\n\n\tconst double& operator[](int index) const { return m_array[index]; }\n\tdouble& operator[](int index) { return m_array[index]; }\n\n\tvoid print() const;\n};\n\n// Member function of partially specialized class\ntemplate <int size>\nvoid StaticArray<double, size>::print() const\n{\n\tfor (int i{ 0 }; i < size; ++i)\n\t\tstd::cout << std::scientific << m_array[i] << ' ';\n\tstd::cout << '\\n';\n}\n\nint main()\n{\n\t// declare an integer array with room for 6 integers\n\tStaticArray<int, 6> intArray{};\n\n\t// Fill it up in order, then print it\n\tfor (int count{ 0 }; count < 6; ++count)\n\t\tintArray[count] = count;\n\n\tintArray.print();\n\n\t// declare a double buffer with room for 4 doubles\n\tStaticArray<double, 4> doubleArray{};\n\n\tfor (int count{ 0 }; count < 4; ++count)\n\t\tdoubleArray[count] = (4.0 + 0.1 * count);\n\n\tdoubleArray.print();\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\ntemplate &lt;typename T, int size&gt;\nclass StaticArray\n{\nprivate:\n\tT m_array[size]{};\n\npublic:\n\tT* getArray() { return m_array; }\n\n\tconst T&amp; operator[](int index) const { return m_array[index]; }\n\tT&amp; operator[](int index) { return m_array[index]; }\n\n\tvoid print() const;\n};\n\ntemplate &lt;typename T, int size&gt; \nvoid StaticArray&lt;T, size&gt;::print() const\n{\n\tfor (int i{ 0 }; i &lt; size; ++i)\n\t\tstd::cout &lt;&lt; m_array[i] &lt;&lt; ' ';\n\tstd::cout &lt;&lt; '\\n';\n}\n\n// Partially specialized class\ntemplate &lt;int size&gt;\nclass StaticArray&lt;double, size&gt;\n{\nprivate:\n\tdouble m_array[size]{};\n\npublic:\n\tdouble* getArray() { return m_array; }\n\n\tconst double&amp; operator[](int index) const { return m_array[index]; }\n\tdouble&amp; operator[](int index) { return m_array[index]; }\n\n\tvoid print() const;\n};\n\n// Member function of partially specialized class\ntemplate &lt;int size&gt;\nvoid StaticArray&lt;double, size&gt;::print() const\n{\n\tfor (int i{ 0 }; i &lt; size; ++i)\n\t\tstd::cout &lt;&lt; std::scientific &lt;&lt; m_array[i] &lt;&lt; ' ';\n\tstd::cout &lt;&lt; '\\n';\n}\n\nint main()\n{\n\t// declare an integer array with room for 6 integers\n\tStaticArray&lt;int, 6&gt; intArray{};\n\n\t// Fill it up in order, then print it\n\tfor (int count{ 0 }; count &lt; 6; ++count)\n\t\tintArray[count] = count;\n\n\tintArray.print();\n\n\t// declare a double buffer with room for 4 doubles\n\tStaticArray&lt;double, 4&gt; doubleArray{};\n\n\tfor (int count{ 0 }; count &lt; 4; ++count)\n\t\tdoubleArray[count] = (4.0 + 0.1 * count);\n\n\tdoubleArray.print();\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include <iostream>\n\nšablona <název typu T, velikost int>\ntřídy StaticArray\n{\nsoukromé:\n\tT m_array[velikost]{};\n\nveřejnost:\n\tT* getArray() { return m_array; }\n\n\tconst T& operator[](int index) const { return m_array[index]; }\n\tT& operator[](int index) { return m_array[index]; }\n\n\tvoid print() const;\n};\n\nšablona <název typu T, velikost int> \nvoid StaticArray<T, size>::print() const\n{\n\tfor (int i{ 0 }; i < velikost; ++i)\n\t\tstd::cout << m_array[i] << ' ';\n\tstd::cout << '\\n';\n}\n\n// Částečně specializovaná třída\nšablona <int size>\ntřída StaticArray<double, size>\n{\nsoukromé:\n\tdouble m_array[velikost]{};\n\nveřejnost:\n\tdouble* getArray() { return m_array; }\n\n\tconst double& operator[](int index) const { return m_array[index]; }\n\tdouble& operátor[](int index) { return m_array[index]; }\n\n\tvoid print() const;\n};\n\n// Členská funkce částečně specializované třídy\nšablona <int size>\nvoid StaticArray<double, size>::print() const\n{\n\tfor (int i{ 0 }; i < velikost; ++i)\n\t\tstd::cout << std::scientific << m_array[i] << ' ';\n\tstd::cout << '\\n';\n}\n\nint main()\n{\n\t// deklaruje celočíselné pole s místem pro 6 celých čísel\n\tStaticArray<int, 6> intArray{};\n\n\t// Vyplňte jej v pořadí a poté vytiskněte\n\tfor (int počet{ 0 }; počet < 6; ++počet)\n\t\tintArray[počet] = počet;\n\n\tintArray.print();\n\n\t// vyhlásí dvojitý buffer s místem pro 4 double\n\tStaticArray<double, 4> doubleArray{};\n\n\tfor (int počet{ 0 }; počet < 4; ++počet)\n\t\tdoubleArray[počet] = (4,0 + 0,1 * počet);\n\n\tdoubleArray.print();\n\n\tnávrat 0;\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b49",
      "type": "paragraph",
      "html": "<p>This prints:</p>",
      "preview": "This prints:",
      "canCheck": true,
      "html_cs": "<p>Toto vytiskne:</p>"
    },
    {
      "id": "partial-template-specialization_b50",
      "type": "output",
      "text": "0 1 2 3 4 5\n4.000000e+00 4.100000e+00 4.200000e+00 4.300000e+00\n",
      "html": "<pre>0 1 2 3 4 5\n4.000000e+00 4.100000e+00 4.200000e+00 4.300000e+00\n</pre>",
      "canCheck": false,
      "html_cs": "<před>0 1 2 3 4 5\n4,000000e+00 4,100000e+00 4,200000e+00 4,300000e+00\n</pre>"
    },
    {
      "id": "partial-template-specialization_b51",
      "type": "paragraph",
      "html": "<p>This works because <code>StaticArray&lt;double, size&gt;::print()</code> is no longer a partially specialized function -- it is a non-specialized member of partially specialized class <code>StaticArray&lt;double, size&gt;</code>.</p>",
      "preview": "This works because StaticArray<double, size>::print() is no longer a partially s",
      "canCheck": true,
      "html_cs": "<p>To funguje, protože <code>StaticArray<double, size>::print()</code> již není částečně specializovaná funkce -- je to nespecializovaný člen částečně specializované třídy <code>StaticArray<double, size></code>.</p>"
    },
    {
      "id": "partial-template-specialization_b52",
      "type": "paragraph",
      "html": "<p>However, this isn’t a great solution, because we have to duplicate a lot of code from <code>StaticArray&lt;T, size&gt;</code> to <code>StaticArray&lt;double, size&gt;</code>.</p>",
      "preview": "However, this isn’t a great solution, because we have to duplicate a lot of code",
      "canCheck": true,
      "html_cs": "<p>To však není skvělé řešení, protože musíme duplikovat spoustu kódu z <code>StaticArray<T, size></code> do <code>StaticArray<double, size></code>.</p>"
    },
    {
      "id": "partial-template-specialization_b53",
      "type": "paragraph",
      "html": "<p>If only there were some way to reuse the code in <code>StaticArray&lt;T, size&gt;</code> in <code>StaticArray&lt;double, size&gt;</code>. Sounds like a job for inheritance!</p>",
      "preview": "If only there were some way to reuse the code in StaticArray<T, size> in StaticA",
      "canCheck": true,
      "html_cs": "<p>Kdyby existoval nějaký způsob, jak znovu použít kód v <code>StaticArray<T, size></code> v <code>StaticArray<double, size></code>. Zní to jako práce pro dědictví!</p>"
    },
    {
      "id": "partial-template-specialization_b54",
      "type": "paragraph",
      "html": "<p>You might start off trying to write that code like this:</p>",
      "preview": "You might start off trying to write that code like this:",
      "canCheck": true,
      "html_cs": "<p>Mohli byste začít pokusit se napsat tento kód takto:</p>"
    },
    {
      "id": "partial-template-specialization_b55",
      "type": "code",
      "language": "cpp",
      "code": "template <int size> // size is the expression parameter\nclass StaticArray<double, size>: public StaticArray<T, size>",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">template &lt;int size&gt; // size is the expression parameter\nclass StaticArray&lt;double, size&gt;: public StaticArray&lt;T, size&gt;</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">šablona <int size> // velikost je parametr výrazu\nclass StaticArray<double, size>: public StaticArray<T, size></code></pre>"
    },
    {
      "id": "partial-template-specialization_b56",
      "type": "paragraph",
      "html": "<p>But this doesn’t work, because we’ve used <code>T</code> without defining it. There is no syntax that allows us to inherit in such a manner.</p>",
      "preview": "But this doesn’t work, because we’ve used T without defining it. There is no syn",
      "canCheck": true,
      "html_cs": "<p>To ale nefunguje, protože jsme použili <code>T</code>, aniž bychom jej definovali. Neexistuje žádná syntaxe, která by nám umožňovala dědit tímto způsobem.</p>"
    },
    {
      "id": "partial-template-specialization_b57",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">As an aside…</p><p>Even if we were able to define <code>T</code> as a type template parameter, when <code>StaticArray&lt;double, size&gt;</code> was instantiated, the compiler would need to replace the <code>T</code> in <code>StaticArray&lt;T, size&gt;</code> with an actual type. What actual type would it use? The only type that makes sense is <code>T=double</code>, but that would leave <code>StaticArray&lt;double, size&gt;</code> inheriting from itself!</p></div>",
      "preview": "As an aside…Even if we were able to define T as a type template parameter, when StaticArray<double, ",
      "canCheck": true,
      "html_cs": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Kromě toho…</p><p>I kdybychom byli schopni definovat <code>T</code> jako parametr šablony typu, když byla vytvořena instance <code>StaticArray<double, size></code>, kompilátor by musel nahradit <T,Array> <T,Static> incode> size></code> se skutečným typem. Jaký skutečný typ by použil? Jediný typ, který dává smysl, je <code>T=double</code>, ale to by nechalo <code>StaticArray<double, size></code> zdědit od sebe!</p></div>"
    },
    {
      "id": "partial-template-specialization_b58",
      "type": "paragraph",
      "html": "<p>Fortunately, there’s a workaround, by using a common base class:</p>",
      "preview": "Fortunately, there’s a workaround, by using a common base class:",
      "canCheck": true,
      "html_cs": "<p>Naštěstí existuje řešení pomocí společné základní třídy:</p>"
    },
    {
      "id": "partial-template-specialization_b59",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\ntemplate <typename T, int size>\nclass StaticArray_Base\n{\nprotected:\n\tT m_array[size]{};\n\npublic:\n\tT* getArray() { return m_array; }\n\n\tconst T& operator[](int index) const { return m_array[index]; }\n\tT& operator[](int index) { return m_array[index]; }\n\n\tvoid print() const\n\t{\n\t\tfor (int i{ 0 }; i < size; ++i)\n\t\t\tstd::cout << m_array[i] << ' ';\n\t\tstd::cout << '\\n';\n\t}\n\n\t// Don't forget a virtual destructor if you're going to use virtual function resolution\n};\n\ntemplate <typename T, int size>\nclass StaticArray: public StaticArray_Base<T, size>\n{\n};\n\ntemplate <int size>\nclass StaticArray<double, size>: public StaticArray_Base<double, size>\n{\npublic:\n\n\tvoid print() const\n\t{\n\t\tfor (int i{ 0 }; i < size; ++i)\n\t\t\tstd::cout << std::scientific << this->m_array[i] << ' ';\n// note: The this-> prefix in the above line is needed.\n// See https://stackoverflow.com/a/6592617 or https://isocpp.org/wiki/faq/templates#nondependent-name-lookup-members for more info on why.\n\t\tstd::cout << '\\n';\n\t}\n};\n\nint main()\n{\n\t// declare an integer array with room for 6 integers\n\tStaticArray<int, 6> intArray{};\n\n\t// Fill it up in order, then print it\n\tfor (int count{ 0 }; count < 6; ++count)\n\t\tintArray[count] = count;\n\n\tintArray.print();\n\n\t// declare a double buffer with room for 4 doubles\n\tStaticArray<double, 4> doubleArray{};\n\n\tfor (int count{ 0 }; count < 4; ++count)\n\t\tdoubleArray[count] = (4.0 + 0.1 * count);\n\n\tdoubleArray.print();\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\ntemplate &lt;typename T, int size&gt;\nclass StaticArray_Base\n{\nprotected:\n\tT m_array[size]{};\n\npublic:\n\tT* getArray() { return m_array; }\n\n\tconst T&amp; operator[](int index) const { return m_array[index]; }\n\tT&amp; operator[](int index) { return m_array[index]; }\n\n\tvoid print() const\n\t{\n\t\tfor (int i{ 0 }; i &lt; size; ++i)\n\t\t\tstd::cout &lt;&lt; m_array[i] &lt;&lt; ' ';\n\t\tstd::cout &lt;&lt; '\\n';\n\t}\n\n\t// Don't forget a virtual destructor if you're going to use virtual function resolution\n};\n\ntemplate &lt;typename T, int size&gt;\nclass StaticArray: public StaticArray_Base&lt;T, size&gt;\n{\n};\n\ntemplate &lt;int size&gt;\nclass StaticArray&lt;double, size&gt;: public StaticArray_Base&lt;double, size&gt;\n{\npublic:\n\n\tvoid print() const\n\t{\n\t\tfor (int i{ 0 }; i &lt; size; ++i)\n\t\t\tstd::cout &lt;&lt; std::scientific &lt;&lt; this-&gt;m_array[i] &lt;&lt; ' ';\n// note: The this-&gt; prefix in the above line is needed.\n// See https://stackoverflow.com/a/6592617 or https://isocpp.org/wiki/faq/templates#nondependent-name-lookup-members for more info on why.\n\t\tstd::cout &lt;&lt; '\\n';\n\t}\n};\n\nint main()\n{\n\t// declare an integer array with room for 6 integers\n\tStaticArray&lt;int, 6&gt; intArray{};\n\n\t// Fill it up in order, then print it\n\tfor (int count{ 0 }; count &lt; 6; ++count)\n\t\tintArray[count] = count;\n\n\tintArray.print();\n\n\t// declare a double buffer with room for 4 doubles\n\tStaticArray&lt;double, 4&gt; doubleArray{};\n\n\tfor (int count{ 0 }; count &lt; 4; ++count)\n\t\tdoubleArray[count] = (4.0 + 0.1 * count);\n\n\tdoubleArray.print();\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true,
      "html_cs": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include <iostream>\n\nšablona <název typu T, velikost int>\ntřídy StaticArray_Base\n{\nchráněno:\n\tT m_array[velikost]{};\n\nveřejnost:\n\tT* getArray() { return m_array; }\n\n\tconst T& operator[](int index) const { return m_array[index]; }\n\tT& operator[](int index) { return m_array[index]; }\n\n\tvoid print() konst\n\t{\n\t\tfor (int i{ 0 }; i < velikost; ++i)\n\t\t\tstd::cout << m_array[i] << ' ';\n\t\tstd::cout << '\\n';\n\t}\n\n\t// Pokud budete používat rozlišení virtuálních funkcí, nezapomeňte na virtuální destruktor\n};\n\nšablona <název typu T, velikost int>\nclass StaticArray: public StaticArray_Base<T, velikost>\n{\n};\n\nšablona <int size>\nclass StaticArray<double, size>: public StaticArray_Base<double, size>\n{\nveřejnost:\n\n\tvoid print() konst\n\t{\n\t\tfor (int i{ 0 }; i < velikost; ++i)\n\t\t\tstd::cout << std::scientific << this->m_array[i] << ' ';\n// poznámka: Předpona this-> ve výše uvedeném řádku je nutná.\n// Další informace o důvodech naleznete na https://stackoverflow.com/a/6592617 nebo https://isocpp.org/wiki/faq/templates#nondependent-name-lookup-members.\n\t\tstd::cout << '\\n';\n\t}\n};\n\nint main()\n{\n\t// deklaruje celočíselné pole s místem pro 6 celých čísel\n\tStaticArray<int, 6> intArray{};\n\n\t// Vyplňte jej v pořadí a poté vytiskněte\n\tfor (int počet{ 0 }; počet < 6; ++počet)\n\t\tintArray[počet] = počet;\n\n\tintArray.print();\n\n\t// vyhlásí dvojitý buffer s místem pro 4 double\n\tStaticArray<double, 4> doubleArray{};\n\n\tfor (int počet{ 0 }; počet < 4; ++počet)\n\t\tdoubleArray[počet] = (4,0 + 0,1 * počet);\n\n\tdoubleArray.print();\n\n\tnávrat 0;\n}</code></pre>"
    },
    {
      "id": "partial-template-specialization_b60",
      "type": "paragraph",
      "html": "<p>This prints the same as above, but has significantly less duplicated code.</p>",
      "preview": "This prints the same as above, but has significantly less duplicated code.",
      "canCheck": true,
      "html_cs": "<p>Vytiskne se stejně jako výše, ale má výrazně méně duplicitního kódu.</p>"
    }
  ],
  "title_cs": "26.5 — Partial template specialization"
}