{
  "index": 226,
  "chapterId": "17",
  "number": "17.2",
  "title": "17.2 — std::array length and indexing",
  "url": "https://www.learncpp.com/cpp-tutorial/stdarray-length-and-indexing/",
  "slug": "stdarray-length-and-indexing",
  "scrapedAt": "2026-09-15T16:30:33.258Z",
  "totalBlocks": 47,
  "totalCheckpoints": 38,
  "blocks": [
    {
      "id": "stdarray-length-and-indexing_b0",
      "type": "paragraph",
      "html": "<p>In lesson <a href=\"https://www.learncpp.com/cpp-tutorial/stdvector-and-the-unsigned-length-and-subscript-problem/\">16.3 -- std::vector and the unsigned length and subscript problem</a>, we discussed the unfortunate decision to make the standard library container classes use unsigned values for lengths and indices. Because <code>std::array</code> is a standard library container class, it is subject to the same issues.</p>",
      "preview": "In lesson 16.3 -- std::vector and the unsigned length and subscript problem, we ",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b1",
      "type": "paragraph",
      "html": "<p>In this lesson, we’ll recap ways to index and get the length of a <code>std::array</code>. Because <code>std::vector</code> and <code>std::array</code> have similar interfaces, this will parallel the what we covered for <code>std::vector</code>. But since only <code>std::array</code> has full support for constexpr, we’ll focus a little more on that.</p>",
      "preview": "In this lesson, we’ll recap ways to index and get the length of a std::array. Be",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b2",
      "type": "paragraph",
      "html": "<p>Before proceeding, now would be a good time to refresh your memory on “sign conversions are narrowing conversions, except when constexpr” (see <a href=\"https://www.learncpp.com/cpp-tutorial/stdvector-and-the-unsigned-length-and-subscript-problem/#constexprconversions\">16.3 -- std::vector and the unsigned length and subscript problem</a>).</p>",
      "preview": "Before proceeding, now would be a good time to refresh your memory on “sign conv",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b3",
      "type": "section",
      "title": "The length of a std::array has type std::size_t",
      "html": "<h3 class=\"cpp-section-heading\">The length of a <code>std::array</code> has type <code>std::size_t</code></h3>",
      "canCheck": false
    },
    {
      "id": "stdarray-length-and-indexing_b4",
      "type": "paragraph",
      "html": "<p><code>std::array</code> is implemented as a template struct whose declaration looks like this:</p>",
      "preview": "std::array is implemented as a template struct whose declaration looks like this",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b5",
      "type": "code",
      "language": "cpp",
      "code": "template<typename T, std::size_t N> // N is a non-type template parameter\nstruct array;",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">template&lt;typename T, std::size_t N&gt; // N is a non-type template parameter\nstruct array;</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b6",
      "type": "paragraph",
      "html": "<p>As you can see, the non-type template parameter representing the array length (<code>N</code>) has type <code>std::size_t</code>. And as you’re probably aware by now, <code>std::size_t</code> is a large unsigned integral type.</p>",
      "preview": "As you can see, the non-type template parameter representing the array length (N",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b7",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>We cover class templates (which includes struct templates) in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/class-templates/\">13.13 -- Class templates</a> and non-type template parameters in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/non-type-template-parameters/\">11.9 -- Non-type template parameters</a>.</p></div>",
      "preview": "Related contentWe cover class templates (which includes struct templates) in lesson 13.13 -- Class t",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b8",
      "type": "paragraph",
      "html": "<p>Thus, when we define a <code>std::array</code>, the length non-type template argument must either have type <code>std::size_t</code>, or be convertible to a value of type <code>std::size_t</code>. Because this value must be constexpr, we don’t run into sign conversion issues when we use a signed integral value, as the compiler will happily convert a signed integral value to a <code>std::size_t</code> at compile-time without it being considered a narrowing conversion.</p>",
      "preview": "Thus, when we define a std::array, the length non-type template argument must ei",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b9",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">As an aside…</p><p>Prior to C++23, C++ didn’t even have a literal suffix for <code>std::size_t</code>, as the implicit compile-time conversion from <code>int</code> to <code>std::size_t</code> typically suffices for cases where we need a constexpr <code>std::size_t</code>.</p><p>The suffix was added primarily for type deduction purposes, as <code>constexpr auto x { 0 }</code> will give you an <code>int</code> rather than a <code>std::size_t</code>. In such cases, being able to differentiate <code>0</code> (<code>int</code>) from <code>0UZ</code> (<code>std::size_t</code>) without having to use an explicit <code>static_cast</code> is useful.</p></div>",
      "preview": "As an aside…Prior to C++23, C++ didn’t even have a literal suffix for std::size_t, as the implicit c",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b10",
      "type": "section",
      "title": "The length and indices of std::array have type size_type, which is always std::size_t",
      "html": "<h3 class=\"cpp-section-heading\"><a name=\"size_type\"></a>The length and indices of <code>std::array</code> have type <code>size_type</code>, which is always <code>std::size_t</code> <a href=\"#size_type\"><i class=\"fa fa-link\" style=\"font-size:.8em\"></i></a></h3>",
      "canCheck": false
    },
    {
      "id": "stdarray-length-and-indexing_b11",
      "type": "paragraph",
      "html": "<p>Just like a <code>std::vector</code>, <code>std::array</code> defines a nested typedef member named <code>size_type</code>, which is an alias for the type used for the length (and indices, if supported) of the container. In the case of <code>std::array</code>, <code>size_type</code> is <em>always</em> an alias for <code>std::size_t</code>.</p>",
      "preview": "Just like a std::vector, std::array defines a nested typedef member named size_t",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b12",
      "type": "paragraph",
      "html": "<p>Note that the non-type template parameter defining the length of the <code>std::array</code> is explicitly defined as <code>std::size_t</code> rather than <code>size_type</code>. This is because <code>size_type</code> is a member of <code>std::array</code>, and isn’t defined at that point. This is the only place that uses <code>std::size_t</code> explicitly -- everywhere else uses <code>size_type</code>.</p>",
      "preview": "Note that the non-type template parameter defining the length of the std::array ",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b13",
      "type": "section",
      "title": "Getting the length of a std::array",
      "html": "<h3 class=\"cpp-section-heading\">Getting the length of a <code>std::array</code></h3>",
      "canCheck": false
    },
    {
      "id": "stdarray-length-and-indexing_b14",
      "type": "paragraph",
      "html": "<p>There are three common ways to get the length of a <code>std::array</code> object.</p>",
      "preview": "There are three common ways to get the length of a std::array object.",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b15",
      "type": "paragraph",
      "html": "<p>First, we can ask a <code>std::array</code> object for its length using the <code>size()</code> member function (which returns the length as unsigned <code>size_type</code>):</p>",
      "preview": "First, we can ask a std::array object for its length using the size() member fun",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b16",
      "type": "code",
      "language": "cpp",
      "code": "#include <array>\n#include <iostream>\n\nint main()\n{\n    constexpr std::array arr { 9.0, 7.2, 5.4, 3.6, 1.8 };\n    std::cout << \"length: \" << arr.size() << '\\n'; // returns length as type `size_type` (alias for `std::size_t`)\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;array&gt;\n#include &lt;iostream&gt;\n\nint main()\n{\n    constexpr std::array arr { 9.0, 7.2, 5.4, 3.6, 1.8 };\n    std::cout &lt;&lt; \"length: \" &lt;&lt; arr.size() &lt;&lt; '\\n'; // returns length as type `size_type` (alias for `std::size_t`)\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b17",
      "type": "paragraph",
      "html": "<p>This prints:</p>",
      "preview": "This prints:",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b18",
      "type": "output",
      "text": "length: 5\n",
      "html": "<pre>length: 5\n</pre>",
      "canCheck": false
    },
    {
      "id": "stdarray-length-and-indexing_b19",
      "type": "paragraph",
      "html": "<p>Unlike <code>std::string</code> and <code>std::string_view</code>, which have both a <code>length()</code> and a <code>size()</code> member function (that do the same thing), <code>std::array</code> (and most other container types in C++) only have <code>size()</code>.</p>",
      "preview": "Unlike std::string and std::string_view, which have both a length() and a size()",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b20",
      "type": "paragraph",
      "html": "<p>Second, in C++17, we can use the <code>std::size()</code> non-member function (which for <code>std::array</code> just calls the <code>size()</code> member function, thus returning the length as unsigned <code>size_type</code>).</p>",
      "preview": "Second, in C++17, we can use the std::size() non-member function (which for std:",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b21",
      "type": "code",
      "language": "cpp",
      "code": "#include <array>\n#include <iostream>\n\nint main()\n{\n    constexpr std::array arr{ 9, 7, 5, 3, 1 };\n    std::cout << \"length: \" << std::size(arr); // C++17, returns length as type `size_type` (alias for `std::size_t`)\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;array&gt;\n#include &lt;iostream&gt;\n\nint main()\n{\n    constexpr std::array arr{ 9, 7, 5, 3, 1 };\n    std::cout &lt;&lt; \"length: \" &lt;&lt; std::size(arr); // C++17, returns length as type `size_type` (alias for `std::size_t`)\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b22",
      "type": "paragraph",
      "html": "<p>Finally, in C++20, we can use the <code>std::ssize()</code> non-member function, which returns the length as a large <em>signed</em> integral type (usually<code>std::ptrdiff_t</code>):</p>",
      "preview": "Finally, in C++20, we can use the std::ssize() non-member function, which return",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b23",
      "type": "code",
      "language": "cpp",
      "code": "#include <array>\n#include <iostream>\n\nint main()\n{\n    constexpr std::array arr { 9, 7, 5, 3, 1 };\n    std::cout << \"length: \" << std::ssize(arr); // C++20, returns length as a large signed integral type\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;array&gt;\n#include &lt;iostream&gt;\n\nint main()\n{\n    constexpr std::array arr { 9, 7, 5, 3, 1 };\n    std::cout &lt;&lt; \"length: \" &lt;&lt; std::ssize(arr); // C++20, returns length as a large signed integral type\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b24",
      "type": "paragraph",
      "html": "<p>This is the only function of the three which returns the length as a signed type.</p>",
      "preview": "This is the only function of the three which returns the length as a signed type",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b25",
      "type": "section",
      "title": "Getting the length of a std::array as a constexpr value",
      "html": "<h3 class=\"cpp-section-heading\">Getting the length of a <code>std::array</code> as a constexpr value</h3>",
      "canCheck": false
    },
    {
      "id": "stdarray-length-and-indexing_b26",
      "type": "paragraph",
      "html": "<p>Because the length of a <code>std::array</code> is constexpr, each of the above functions will return the length of a <code>std::array</code> as a constexpr value (even when called on a non-constexpr <code>std::array</code> object)! This means we can use any of these functions in constant expressions, and the length returned can be implicitly converted to an <code>int</code> without it being a narrowing conversion:</p>",
      "preview": "Because the length of a std::array is constexpr, each of the above functions wil",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b27",
      "type": "code",
      "language": "cpp",
      "code": "#include <array>\n#include <iostream>\n\nint main()\n{\n    std::array arr { 9, 7, 5, 3, 1 }; // note: not constexpr for this example\n    constexpr int length{ std::size(arr) }; // ok: return value is constexpr std::size_t and can be converted to int, not a narrowing conversion\n\n    std::cout << \"length: \" << length << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;array&gt;\n#include &lt;iostream&gt;\n\nint main()\n{\n    std::array arr { 9, 7, 5, 3, 1 }; // note: not constexpr for this example\n    constexpr int length{ std::size(arr) }; // ok: return value is constexpr std::size_t and can be converted to int, not a narrowing conversion\n\n    std::cout &lt;&lt; \"length: \" &lt;&lt; length &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b28",
      "type": "callout",
      "calloutType": "tip",
      "html": "<div class=\"cpp-note cpp-lightyellowbackground\"><p class=\"cpp-note-title cpp-bottomline\">For Visual Studio users</p><p>Visual Studio incorrectly triggers warning C4365 for the above example. The issue has been <a href=\"https://developercommunity.visualstudio.com/t/Bug:-C4365-triggers-incorrectly-for-cons/10372737\">reported to Microsoft</a>.</p></div>",
      "preview": "For Visual Studio usersVisual Studio incorrectly triggers warning C4365 for the above example. The i",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b29",
      "type": "callout",
      "calloutType": "warning",
      "html": "<div class=\"cpp-note cpp-lightredbackground\"><p class=\"cpp-note-title cpp-bottomline\">Warning</p><p>Due to a language defect, the above functions will return a non-constexpr value when called on a <code>std::array</code> function parameter passed by (const) reference:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;array&gt;\n#include &lt;iostream&gt;\n\nvoid printLength(const std::array&lt;int, 5&gt; &amp;arr)\n{\n    constexpr int length{ std::size(arr) }; // compile error!\n    std::cout &lt;&lt; \"length: \" &lt;&lt; length &lt;&lt; '\\n';\n}\n\nint main()\n{\n    std::array arr { 9, 7, 5, 3, 1 };\n    constexpr int length{ std::size(arr) }; // works just fine\n    std::cout &lt;&lt; \"length: \" &lt;&lt; length &lt;&lt; '\\n';\n\n    printLength(arr);\n\n    return 0;\n}</code></pre><p>This defect has been addressed in C++23 by <a href=\"https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2022/p2280r4.html\">P2280</a>. At the time of writing, few compilers currently <a href=\"https://en.cppreference.com/w/cpp/compiler_support/23\">support</a> this feature.</p><p>A workaround is to make <code>foo()</code> a function template where the array length is a non-type template parameter. This non-type template parameter can then be used inside the function. We discuss this further in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/passing-and-returning-stdarray/\">17.3 -- Passing and returning std::array</a>.</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">template &lt;auto Length&gt;\nvoid printLength(const std::array&lt;int, Length&gt; &amp;arr)\n{\n    std::cout &lt;&lt; \"length: \" &lt;&lt; Length &lt;&lt; '\\n';\n}</code></pre></div>",
      "preview": "WarningDue to a language defect, the above functions will return a non-constexpr value when called o",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b30",
      "type": "section",
      "title": "Subscripting std::array using operator[] or the at() member function",
      "html": "<h3 class=\"cpp-section-heading\">Subscripting <code>std::array</code> using <code>operator[]</code> or the <code>at()</code> member function</h3>",
      "canCheck": false
    },
    {
      "id": "stdarray-length-and-indexing_b31",
      "type": "paragraph",
      "html": "<p>In the prior lesson <a href=\"https://www.learncpp.com/cpp-tutorial/introduction-to-stdarray/\">17.1 -- Introduction to std::array</a>, we covered that the most common way to index a <code>std::array</code> is to use the subscript operator (<code>operator[]</code>). No bounds checking is done in this case, and passing in an invalid index will result in undefined behavior.</p>",
      "preview": "In the prior lesson 17.1 -- Introduction to std::array, we covered that the most",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b32",
      "type": "paragraph",
      "html": "<p>Just like <code>std::vector</code>, <code>std::array</code> also has an <code>at()</code> member function that does subscripting with runtime bounds checking. We recommend avoiding this function since we typically want to do bounds checking before indexing, or we want compile-time bounds checking.</p>",
      "preview": "Just like std::vector, std::array also has an at() member function that does sub",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b33",
      "type": "paragraph",
      "html": "<p>Both of these functions expect the index to be of type <code>size_type</code> (<code>std::size_t</code>).</p>",
      "preview": "Both of these functions expect the index to be of type size_type (std::size_t).",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b34",
      "type": "paragraph",
      "html": "<p>If either of these functions are called with a constexpr value, the compiler will do a constexpr conversion to <code>std::size_t</code>. This isn’t considered to be a narrowing conversion, so you won’t run into sign problems here.</p>",
      "preview": "If either of these functions are called with a constexpr value, the compiler wil",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b35",
      "type": "paragraph",
      "html": "<p>However, if either of these functions are called with a non-constexpr signed integral value, the conversion to <code>std::size_t</code> is considered narrowing and your compiler may emit a warning. We discuss this case further (using <code>std::vector</code>) in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/stdvector-and-the-unsigned-length-and-subscript-problem/\">16.3 -- std::vector and the unsigned length and subscript problem</a>.</p>",
      "preview": "However, if either of these functions are called with a non-constexpr signed int",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b36",
      "type": "section",
      "title": "std::get() does compile-time bounds checking for constexpr indices",
      "html": "<h3 class=\"cpp-section-heading\"><code>std::get()</code> does compile-time bounds checking for constexpr indices</h3>",
      "canCheck": false
    },
    {
      "id": "stdarray-length-and-indexing_b37",
      "type": "paragraph",
      "html": "<p>Since the length of a <code>std::array</code> is constexpr, if our index is also a constexpr value, then the compiler should be able to validate at compile-time that our constexpr index is within the bounds of the array (and stop compilation if the constexpr index is out of bounds).</p>",
      "preview": "Since the length of a std::array is constexpr, if our index is also a constexpr ",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b38",
      "type": "paragraph",
      "html": "<p>However, <code>operator[]</code> does no bounds checking by definition, and the <code>at()</code> member function only does runtime bounds checking. And function parameters can’t be constexpr (even for constexpr or consteval functions), so how do we even pass a constexpr index?</p>",
      "preview": "However, operator[] does no bounds checking by definition, and the at() member f",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b39",
      "type": "paragraph",
      "html": "<p>To get compile-time bounds checking when we have a constexpr index, we can use the <code>std::get()</code> function template, which takes the index as a non-type template argument:</p>",
      "preview": "To get compile-time bounds checking when we have a constexpr index, we can use t",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b40",
      "type": "code",
      "language": "cpp",
      "code": "#include <array>\n#include <iostream>\n\nint main()\n{\n    constexpr std::array prime{ 2, 3, 5, 7, 11 };\n\n    std::cout << std::get<3>(prime); // print the value of element with index 3\n    std::cout << std::get<9>(prime); // invalid index (compile error)\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;array&gt;\n#include &lt;iostream&gt;\n\nint main()\n{\n    constexpr std::array prime{ 2, 3, 5, 7, 11 };\n\n    std::cout &lt;&lt; std::get&lt;3&gt;(prime); // print the value of element with index 3\n    std::cout &lt;&lt; std::get&lt;9&gt;(prime); // invalid index (compile error)\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b41",
      "type": "paragraph",
      "html": "<p>Inside the implementation of <code>std::get()</code>, there is a static_assert that checks to ensure that the non-type template argument is smaller than the array length. If it isn’t, then the static_assert will halt the compilation process with compilation error.</p>",
      "preview": "Inside the implementation of std::get(), there is a static_assert that checks to",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b42",
      "type": "paragraph",
      "html": "<p>Since template arguments must be constexpr, <code>std::get()</code> can only be called with constexpr indices.</p>",
      "preview": "Since template arguments must be constexpr, std::get() can only be called with c",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b43",
      "type": "section",
      "title": "Quiz time",
      "html": "<h3 class=\"cpp-section-heading\">Quiz time</h3>",
      "canCheck": false
    },
    {
      "id": "stdarray-length-and-indexing_b44",
      "type": "quiz",
      "questionTitle": "Question #1",
      "questionHtml": "Initialize a <code>std::array</code> with the following values: ‘h’, ‘e’, ‘l’, ‘l’, ‘o’. Print the length of the array, and then use <code>operator[]</code>, <code>at()</code> and <code>std::get()</code> to print the value of the element with index 1.",
      "hintHtml": "",
      "solutionHtml": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;array&gt;\n#include &lt;iostream&gt;\n\nint main()\n{\n    constexpr std::array arr { 'h', 'e', 'l', 'l', 'o' };\n    std::cout &lt;&lt; \"The length is \" &lt;&lt; std::size(arr) &lt;&lt; '\\n';\n    std::cout &lt;&lt; arr[1] &lt;&lt; arr.at(1) &lt;&lt; std::get&lt;1&gt;(arr) &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b45",
      "type": "paragraph",
      "html": "<p>The program should print:</p>",
      "preview": "The program should print:",
      "canCheck": true
    },
    {
      "id": "stdarray-length-and-indexing_b46",
      "type": "output",
      "text": "The length is 5\neee\n",
      "html": "<pre>The length is 5\neee\n</pre>",
      "canCheck": false
    }
  ]
}