{
  "index": 121,
  "chapterId": "9",
  "number": "9.5",
  "title": "9.5 — std::cin and handling invalid input",
  "url": "https://www.learncpp.com/cpp-tutorial/stdcin-and-handling-invalid-input/",
  "slug": "stdcin-and-handling-invalid-input",
  "scrapedAt": "2026-09-15T16:29:38.866Z",
  "totalBlocks": 116,
  "totalCheckpoints": 98,
  "blocks": [
    {
      "id": "stdcin-and-handling-invalid-input_b0",
      "type": "paragraph",
      "html": "<p>Most programs that have a user interface of some kind need to handle user input. In the programs that you have been writing, you have been using std::cin to ask the user to enter text input. Because text input is so free-form (the user can enter anything), it’s very easy for the user to enter input that is not expected.</p>",
      "preview": "Most programs that have a user interface of some kind need to handle user input.",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b1",
      "type": "paragraph",
      "html": "<p>As you write programs, you should always consider how users will (unintentionally or otherwise) misuse your programs. A well-written program will anticipate how users will misuse it, and either handle those cases gracefully or prevent them from happening in the first place (if possible). A program that handles error cases well is said to be <strong>robust</strong>.</p>",
      "preview": "As you write programs, you should always consider how users will (unintentionall",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b2",
      "type": "paragraph",
      "html": "<p>In this lesson, we’ll take a look specifically at ways the user can enter invalid text input via std::cin, and show you some different ways to handle those cases.</p>",
      "preview": "In this lesson, we’ll take a look specifically at ways the user can enter invali",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b3",
      "type": "paragraph",
      "html": "<p>Before we discuss how <code>std::cin</code> and <code>operator&gt;&gt;</code> can fail, let’s recap how they work. We discussed this material in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/introduction-to-iostream-cout-cin-and-endl/#extraction\">1.5 -- Introduction to iostream: cout, cin, and endl</a>.</p>",
      "preview": "Before we discuss how std::cin and operator>> can fail, let’s recap how they wor",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b4",
      "type": "paragraph",
      "html": "<p>Here’s a simplified view of how <code>operator&gt;&gt;</code> works for input:</p>",
      "preview": "Here’s a simplified view of how operator>> works for input:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b5",
      "type": "list",
      "html": "<ol start=\"1\"><li>First, leading whitespace (spaces, tabs, and newlines at the front of the buffer) is discarded from the input buffer. This will discard any unextracted newline character remaining from a prior line of input.</li><li>If the input buffer is now empty, <code>operator&gt;&gt;</code> will wait for the user to enter more data. Leading whitespace is again discarded.</li><li><code>operator&gt;&gt;</code> then extracts as many consecutive characters as it can, until it encounters either a newline character (representing the end of the line of input) or a character that is not valid for the variable being extracted to.</li></ol>",
      "preview": "First, leading whitespace (spaces, tabs, and newlines at the front of the buffer",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b6",
      "type": "paragraph",
      "html": "<p>The result of the extraction is as follows:</p>",
      "preview": "The result of the extraction is as follows:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b7",
      "type": "list",
      "html": "<ul><li>If any characters were extracted in step 3 above, extraction is a success. The extracted characters are converted into a value that is then assigned to the variable.</li><li>If no characters could be extracted in step 3 above, extraction has failed. The object being extracted to is assigned the value <code>0</code> (as of C++11), and any future extractions will immediately fail (until <code>std::cin</code> is cleared).</li></ul>",
      "preview": "If any characters were extracted in step 3 above, extraction is a success. The e",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b8",
      "type": "section",
      "title": "Validating input",
      "html": "<h3 class=\"cpp-section-heading\">Validating input</h3>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b9",
      "type": "paragraph",
      "html": "<p>The process of checking whether user input conforms to what the program is expecting is called <strong>input validation</strong>.</p>",
      "preview": "The process of checking whether user input conforms to what the program is expec",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b10",
      "type": "paragraph",
      "html": "<p>There are three basic ways to do input validation:</p>",
      "preview": "There are three basic ways to do input validation:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b11",
      "type": "paragraph",
      "html": "<p>Inline (as the user types):</p>",
      "preview": "Inline (as the user types):",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b12",
      "type": "list",
      "html": "<ol start=\"1\"><li>Prevent the user from typing invalid input in the first place.</li></ol>",
      "preview": "Prevent the user from typing invalid input in the first place.",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b13",
      "type": "paragraph",
      "html": "<p>Post-entry (after the user types):</p>",
      "preview": "Post-entry (after the user types):",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b14",
      "type": "list",
      "html": "<ol start=\"2\"><li>Let the user enter whatever they want into a string, then validate whether the string is correct, and if so, convert the string to the final variable format.</li><li>Let the user enter whatever they want, let std::cin and operator&gt;&gt; try to extract it, and handle the error cases.</li></ol>",
      "preview": "Let the user enter whatever they want into a string, then validate whether the s",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b15",
      "type": "paragraph",
      "html": "<p>Some graphical user interfaces and advanced text interfaces will let you validate input as the user enters it (character by character). Generally speaking, the programmer provides a validation function that accepts the input the user has entered so far, and returns true if the input is valid, and false otherwise. This function is called every time the user presses a key. If the validation function returns true, the key the user just pressed is accepted. If the validation function returns false, the character the user just input is discarded (and not shown on the screen). Using this method, you can ensure that any input the user enters is guaranteed to be valid, because any invalid keystrokes are discovered and discarded immediately. Unfortunately, std::cin does not support this style of validation.</p>",
      "preview": "Some graphical user interfaces and advanced text interfaces will let you validat",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b16",
      "type": "paragraph",
      "html": "<p>Since strings do not have any restrictions on what characters can be entered, extraction is guaranteed to succeed (though remember that std::cin stops extracting at the first non-leading whitespace character). Once a string is entered, the program can then parse the string to see if it is valid or not. However, parsing strings and converting string input to other types (e.g. numbers) can be challenging, so this is only done in rare cases.</p>",
      "preview": "Since strings do not have any restrictions on what characters can be entered, ex",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b17",
      "type": "paragraph",
      "html": "<p>Most often, we let std::cin and the extraction operator do the hard work. Under this method, we let the user enter whatever they want, have std::cin and operator&gt;&gt; try to extract it, and deal with the fallout if it fails. This is the easiest method, and the one we’ll talk more about below.</p>",
      "preview": "Most often, we let std::cin and the extraction operator do the hard work. Under ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b18",
      "type": "section",
      "title": "A sample program",
      "html": "<h3 class=\"cpp-section-heading\">A sample program</h3>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b19",
      "type": "paragraph",
      "html": "<p>Consider the following calculator program that has no error handling:</p>",
      "preview": "Consider the following calculator program that has no error handling:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b20",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n \ndouble getDouble()\n{\n    std::cout << \"Enter a decimal number: \";\n    double x{};\n    std::cin >> x;\n    return x;\n}\n \nchar getOperator()\n{\n    std::cout << \"Enter one of the following: +, -, *, or /: \";\n    char op{};\n    std::cin >> op;\n    return op;\n}\n \nvoid printResult(double x, char operation, double y)\n{\n    std::cout << x << ' ' << operation << ' ' << y << \" is \";\n\n    switch (operation)\n    {\n    case '+':\n        std::cout << x + y << '\\n';\n        return;\n    case '-':\n        std::cout << x - y << '\\n';\n        return;\n    case '*':\n        std::cout << x * y << '\\n';\n        return;\n    case '/':\n        std::cout << x / y << '\\n';\n        return;\n    }\n}\n \nint main()\n{\n    double x{ getDouble() };\n    char operation{ getOperator() };\n    double y{ getDouble() };\n\n    printResult(x, operation, y);\n \n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n \ndouble getDouble()\n{\n    std::cout &lt;&lt; \"Enter a decimal number: \";\n    double x{};\n    std::cin &gt;&gt; x;\n    return x;\n}\n \nchar getOperator()\n{\n    std::cout &lt;&lt; \"Enter one of the following: +, -, *, or /: \";\n    char op{};\n    std::cin &gt;&gt; op;\n    return op;\n}\n \nvoid printResult(double x, char operation, double y)\n{\n    std::cout &lt;&lt; x &lt;&lt; ' ' &lt;&lt; operation &lt;&lt; ' ' &lt;&lt; y &lt;&lt; \" is \";\n\n    switch (operation)\n    {\n    case '+':\n        std::cout &lt;&lt; x + y &lt;&lt; '\\n';\n        return;\n    case '-':\n        std::cout &lt;&lt; x - y &lt;&lt; '\\n';\n        return;\n    case '*':\n        std::cout &lt;&lt; x * y &lt;&lt; '\\n';\n        return;\n    case '/':\n        std::cout &lt;&lt; x / y &lt;&lt; '\\n';\n        return;\n    }\n}\n \nint main()\n{\n    double x{ getDouble() };\n    char operation{ getOperator() };\n    double y{ getDouble() };\n\n    printResult(x, operation, y);\n \n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b21",
      "type": "paragraph",
      "html": "<p>This simple program asks the user to enter two numbers and a mathematical operator.</p>",
      "preview": "This simple program asks the user to enter two numbers and a mathematical operat",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b22",
      "type": "output",
      "text": "Enter a decimal number: 5\nEnter one of the following: +, -, *, or /: *\nEnter a decimal number: 7\n5 * 7 is 35\n",
      "html": "<pre>Enter a decimal number: 5\nEnter one of the following: +, -, *, or /: *\nEnter a decimal number: 7\n5 * 7 is 35\n</pre>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b23",
      "type": "paragraph",
      "html": "<p>Now, consider where invalid user input might break this program.</p>",
      "preview": "Now, consider where invalid user input might break this program.",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b24",
      "type": "paragraph",
      "html": "<p>First, we ask the user to enter some numbers. What if they enter something other than a number (e.g. ‘q’)? In this case, extraction will fail.</p>",
      "preview": "First, we ask the user to enter some numbers. What if they enter something other",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b25",
      "type": "paragraph",
      "html": "<p>Second, we ask the user to enter one of four possible symbols. What if they enter a character other than one of the symbols we’re expecting? We’ll be able to extract the input, but we don’t currently handle what happens afterward.</p>",
      "preview": "Second, we ask the user to enter one of four possible symbols. What if they ente",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b26",
      "type": "paragraph",
      "html": "<p>Third, what if we ask the user to enter a symbol and they enter a string like <code>\"*q hello\"</code>. Although we can extract the <code>'*'</code> character we need, there’s additional input left in the buffer that could cause problems down the road.</p>",
      "preview": "Third, what if we ask the user to enter a symbol and they enter a string like \"*",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b27",
      "type": "section",
      "title": "Types of invalid text input",
      "html": "<h3 class=\"cpp-section-heading\">Types of invalid text input</h3>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b28",
      "type": "paragraph",
      "html": "<p>We can generally separate input text errors into four types:</p>",
      "preview": "We can generally separate input text errors into four types:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b29",
      "type": "list",
      "html": "<ul><li>Input extraction succeeds but the input is meaningless to the program (e.g. entering ‘k’ as your mathematical operator).</li><li>Input extraction succeeds but the user enters additional input (e.g. entering ‘*q hello’ as your mathematical operator).</li><li>Input extraction fails (e.g. trying to enter ‘q’ into a numeric input).</li><li>Input extraction succeeds but the user overflows a numeric value.</li></ul>",
      "preview": "Input extraction succeeds but the input is meaningless to the program (e.g. ente",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b30",
      "type": "paragraph",
      "html": "<p>Thus, to make our programs robust, whenever we ask the user for input, we ideally should determine whether each of the above can possibly occur, and if so, write code to handle those cases.</p>",
      "preview": "Thus, to make our programs robust, whenever we ask the user for input, we ideall",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b31",
      "type": "paragraph",
      "html": "<p>Let’s dig into each of these cases, and how to handle them using std::cin.</p>",
      "preview": "Let’s dig into each of these cases, and how to handle them using std::cin.",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b32",
      "type": "section",
      "title": "Error case 1: Extraction succeeds but input is meaningless",
      "html": "<h3 class=\"cpp-section-heading\">Error case 1: Extraction succeeds but input is meaningless</h3>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b33",
      "type": "paragraph",
      "html": "<p>This is the simplest case. Consider the following execution of the above program:</p>",
      "preview": "This is the simplest case. Consider the following execution of the above program",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b34",
      "type": "output",
      "text": "Enter a decimal number: 5\nEnter one of the following: +, -, *, or /: k\nEnter a decimal number: 7\n",
      "html": "<pre>Enter a decimal number: 5\nEnter one of the following: +, -, *, or /: k\nEnter a decimal number: 7\n</pre>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b35",
      "type": "paragraph",
      "html": "<p>In this case, we asked the user to enter one of four symbols, but they entered ‘k’ instead. ‘k’ is a valid character, so <code>std::cin</code> happily extracts it to variable <code>op</code>, and this gets returned to <code>main</code>. But our program wasn’t expecting this to happen, so it doesn’t properly deal with this case. As a result, it outputs:</p>",
      "preview": "In this case, we asked the user to enter one of four symbols, but they entered ‘",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b36",
      "type": "output",
      "text": "5 k 7 is\n",
      "html": "<pre>5 k 7 is\n</pre>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b37",
      "type": "paragraph",
      "html": "<p>The solution here is simple: do input validation. This usually consists of 3 steps:</p>",
      "preview": "The solution here is simple: do input validation. This usually consists of 3 ste",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b38",
      "type": "list",
      "html": "<ol start=\"1\"><li>Check whether the user’s input was what you were expecting.</li><li>If so, return the value to the caller.</li><li>If not, tell the user something went wrong and have them try again.</li></ol>",
      "preview": "Check whether the user’s input was what you were expecting.If so, return the val",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b39",
      "type": "paragraph",
      "html": "<p>Here’s an updated getOperator() function that does input validation.</p>",
      "preview": "Here’s an updated getOperator() function that does input validation.",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b40",
      "type": "code",
      "language": "cpp",
      "code": "char getOperator()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout << \"Enter one of the following: +, -, *, or /: \";\n        char operation{};\n        std::cin >> operation;\n\n        // Check whether the user entered meaningful input\n        switch (operation)\n        {\n        case '+':\n        case '-':\n        case '*':\n        case '/':\n            return operation; // return it to the caller\n        default: // otherwise tell the user what went wrong\n            std::cout << \"Oops, that input is invalid.  Please try again.\\n\";\n        }\n    } // and try again\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">char getOperator()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout &lt;&lt; \"Enter one of the following: +, -, *, or /: \";\n        char operation{};\n        std::cin &gt;&gt; operation;\n\n        // Check whether the user entered meaningful input\n        switch (operation)\n        {\n        case '+':\n        case '-':\n        case '*':\n        case '/':\n            return operation; // return it to the caller\n        default: // otherwise tell the user what went wrong\n            std::cout &lt;&lt; \"Oops, that input is invalid.  Please try again.\\n\";\n        }\n    } // and try again\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b41",
      "type": "paragraph",
      "html": "<p>As you can see, we’re using a while loop to continuously loop until the user provides valid input. If they don’t, we ask them to try again until they either give us valid input, shutdown the program, or destroy their computer.</p>",
      "preview": "As you can see, we’re using a while loop to continuously loop until the user pro",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b42",
      "type": "section",
      "title": "Error case 2: Extraction succeeds but with extraneous input",
      "html": "<h3 class=\"cpp-section-heading\">Error case 2: Extraction succeeds but with extraneous input</h3>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b43",
      "type": "paragraph",
      "html": "<p>Consider the following execution of the above program:</p>",
      "preview": "Consider the following execution of the above program:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b44",
      "type": "output",
      "text": "Enter a decimal number: 5*7\n",
      "html": "<pre>Enter a decimal number: 5*7\n</pre>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b45",
      "type": "paragraph",
      "html": "<p>What do you think happens next?</p>",
      "preview": "What do you think happens next?",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b46",
      "type": "output",
      "text": "Enter a decimal number: 5*7\nEnter one of the following: +, -, *, or /: Enter a decimal number: 5 * 7 is 35\n",
      "html": "<pre>Enter a decimal number: 5*7\nEnter one of the following: +, -, *, or /: Enter a decimal number: 5 * 7 is 35\n</pre>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b47",
      "type": "paragraph",
      "html": "<p>The program prints the right answer, but the formatting is all messed up. Let’s take a closer look at why.</p>",
      "preview": "The program prints the right answer, but the formatting is all messed up. Let’s ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b48",
      "type": "paragraph",
      "html": "<p>When the user enters <code>5*7</code> as input, that input goes into the buffer. Then <code>operator&gt;&gt;</code> extracts the 5 to variable x, leaving <code>*7\\n</code> in the buffer. Next, the program prints “Enter one of the following: +, -, *, or /:”. However, when the extraction operator was called, it sees <code>*7\\n</code> waiting in the buffer to be extracted, so it uses that instead of asking the user for more input. Consequently, it extracts the ‘*’ character, leaving <code>7\\n</code> in the buffer.</p>",
      "preview": "When the user enters 5*7 as input, that input goes into the buffer. Then operato",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b49",
      "type": "paragraph",
      "html": "<p>After asking the user to enter another decimal number, the <code>7</code> in the buffer gets extracted without asking the user. Since the user never had an opportunity to enter additional data and hit enter (causing a newline), the output prompts all run together on the same line.</p>",
      "preview": "After asking the user to enter another decimal number, the 7 in the buffer gets ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b50",
      "type": "paragraph",
      "html": "<p>Although the above program works, the execution is messy. It would be better if any extraneous characters entered were simply ignored. Fortunately, it’s easy to ignore characters:</p>",
      "preview": "Although the above program works, the execution is messy. It would be better if ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b51",
      "type": "code",
      "language": "cpp",
      "code": "std::cin.ignore(100, '\\n');  // clear up to 100 characters out of the buffer, or until a '\\n' character is removed",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">std::cin.ignore(100, '\\n');  // clear up to 100 characters out of the buffer, or until a '\\n' character is removed</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b52",
      "type": "paragraph",
      "html": "<p>This call would remove up to 100 characters, but if the user entered more than 100 characters we’ll get messy output again. To ignore all characters up to the next ‘\\n’, we can pass <code>std::numeric_limits&lt;std::streamsize&gt;::max()</code> to <code>&gt;std::cin.ignore()</code>. <code>std::numeric_limits&lt;std::streamsize&gt;::max()</code> returns the largest value that can be stored in a variable of type <code>std::streamsize</code>. Passing this value to <code>std::cin.ignore()</code> causes it to disable the count check.</p>",
      "preview": "This call would remove up to 100 characters, but if the user entered more than 1",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b53",
      "type": "paragraph",
      "html": "<p>To ignore everything up to and including the next ‘\\n’ character, we call</p>",
      "preview": "To ignore everything up to and including the next ‘\\n’ character, we call",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b54",
      "type": "code",
      "language": "cpp",
      "code": "std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\\n');",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">std::cin.ignore(std::numeric_limits&lt;std::streamsize&gt;::max(), '\\n');</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b55",
      "type": "paragraph",
      "html": "<p>Because this line is quite long for what it does, it’s handy to wrap it in a function which can be called in place of <code>std::cin.ignore()</code>.</p>",
      "preview": "Because this line is quite long for what it does, it’s handy to wrap it in a fun",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b56",
      "type": "code",
      "language": "cpp",
      "code": "#include <limits> // for std::numeric_limits\n\nvoid ignoreLine()\n{\n    std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\\n');\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;limits&gt; // for std::numeric_limits\n\nvoid ignoreLine()\n{\n    std::cin.ignore(std::numeric_limits&lt;std::streamsize&gt;::max(), '\\n');\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b57",
      "type": "paragraph",
      "html": "<p>Since the last character the user entered is typically a ‘\\n’, we can tell <code>std::cin</code> to ignore buffered characters until it finds a newline character (which is removed as well).</p>",
      "preview": "Since the last character the user entered is typically a ‘\\n’, we can tell std::",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b58",
      "type": "paragraph",
      "html": "<p>Let’s update our getDouble() function to ignore any extraneous input:</p>",
      "preview": "Let’s update our getDouble() function to ignore any extraneous input:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b59",
      "type": "code",
      "language": "cpp",
      "code": "double getDouble()\n{\n    std::cout << \"Enter a decimal number: \";\n    double x{};\n    std::cin >> x;\n\n    ignoreLine();\n    return x;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">double getDouble()\n{\n    std::cout &lt;&lt; \"Enter a decimal number: \";\n    double x{};\n    std::cin &gt;&gt; x;\n\n    ignoreLine();\n    return x;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b60",
      "type": "paragraph",
      "html": "<p>Now our program will work as expected, even if we enter <code>5*7</code> for the first input -- the 5 will be extracted, and the rest of the characters will be removed from the input buffer. Since the input buffer is now empty, the user will be properly asked for input the next time an extraction operation is performed!</p>",
      "preview": "Now our program will work as expected, even if we enter 5*7 for the first input ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b61",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Tip</p><p>In certain cases, it may be better to treat extraneous input as a failure case (rather than just ignoring it). We can then ask the user to re-enter their input.</p><p>Here’s a variation of <code>getDouble()</code> that asks the user to re-enter their input if there is any extraneous input entered:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// returns true if std::cin has unextracted input on the current line, false otherwise\nbool hasUnextractedInput()\n{\n    return !std::cin.eof() &amp;&amp; std::cin.peek() != '\\n';\n}\n\ndouble getDouble()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout &lt;&lt; \"Enter a decimal number: \";\n        double x{};\n        std::cin &gt;&gt; x;\n\n        // NOTE: YOU SHOULD CHECK FOR A FAILED EXTRACTION HERE (see section below)\n\n        // If there is extraneous input, treat as failure case\n        if (hasUnextractedInput())\n        {\n            ignoreLine(); // remove extraneous input\n            continue;\n        }\n    \n        return x;\n    }\n}</code></pre><p>The above snippet makes use of two functions we haven’t seen before:</p><ul><li>The <code>std::cin.eof()</code> function returns <code>true</code> if the last input operation (in this case the extraction to <code>x</code>) reached the end of the input stream.</li><li>The <code>std::cin.peek()</code> function allows us to peek at the next character in the input stream without extracting it.</li></ul><p>Here’s how this function works. After the user’s input has been extracted to <code>x</code>, there may or may not be additional (unextracted) characters left in <code>std::cin</code>.</p><p>First, we call <code>std::cin.eof()</code> to see if the extraction to <code>x</code> reached the end of the input stream. If so, then we know all characters were extracted, which is a success case.</p><p>Otherwise, there must be additional characters still inside <code>std::cin</code> waiting to be extracted. In that case, we call <code>std::cin.peek()</code> to peek at the next character waiting to be extracted without actually extracting it. If that next character is a <code>'\\n'</code>, that means we’ve already extracted all of the characters on this line of input to <code>x</code>. This is also a success case.</p><p>However, if the next character is something other than <code>'\\n'</code>, then the user must have entered extraneous input that wasn’t extracted to <code>x</code>. That’s our failure case. We clear out all of that extraneous input, and <code>continue</code> back to the top of the loop to try again.</p><p>If you’re having trouble deciphering how the boolean expression in <code>hasUnextractedInput()</code> evaluates, that’s not surprising -- boolean expressions with negations can be difficult to understand. In such cases, using de Morgan’s law can help. An equivalent statement is <code>return !(std::cin.eof() || std::cin.peek() == '\\n');</code>. This makes it clearer that we are testing for EOF or a newline. If either are true, then we’ve extracted all the input. We then apply <code>operator!</code> to tell us whether we haven’t extracted all the input, meaning there is still unextracted input.</p></div>",
      "preview": "TipIn certain cases, it may be better to treat extraneous input as a failure case (rather than just ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b62",
      "type": "section",
      "title": "Error case 3: Extraction fails",
      "html": "<h3 class=\"cpp-section-heading\">Error case 3: Extraction fails</h3>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b63",
      "type": "paragraph",
      "html": "<p>Extraction fails when no input can be extracted to the specified variable.</p>",
      "preview": "Extraction fails when no input can be extracted to the specified variable.",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b64",
      "type": "paragraph",
      "html": "<p>Now consider the following execution of our updated calculator program:</p>",
      "preview": "Now consider the following execution of our updated calculator program:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b65",
      "type": "output",
      "text": "Enter a decimal number: a\n",
      "html": "<pre>Enter a decimal number: a\n</pre>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b66",
      "type": "paragraph",
      "html": "<p>You shouldn’t be surprised that the program doesn’t perform as expected, but how it fails is interesting:</p>",
      "preview": "You shouldn’t be surprised that the program doesn’t perform as expected, but how",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b67",
      "type": "output",
      "text": "Enter a decimal number: a\nEnter one of the following: +, -, *, or /: Oops, that input is invalid.  Please try again.\nEnter one of the following: +, -, *, or /: Oops, that input is invalid.  Please try again.\nEnter one of the following: +, -, *, or /: Oops, that input is invalid.  Please try again.\n",
      "html": "<pre>Enter a decimal number: a\nEnter one of the following: +, -, *, or /: Oops, that input is invalid.  Please try again.\nEnter one of the following: +, -, *, or /: Oops, that input is invalid.  Please try again.\nEnter one of the following: +, -, *, or /: Oops, that input is invalid.  Please try again.\n</pre>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b68",
      "type": "paragraph",
      "html": "<p>and that last line keeps printing until the program is closed.</p>",
      "preview": "and that last line keeps printing until the program is closed.",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b69",
      "type": "paragraph",
      "html": "<p>This looks pretty similar to the extraneous input case, but it’s a little different. Let’s take a closer look.</p>",
      "preview": "This looks pretty similar to the extraneous input case, but it’s a little differ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b70",
      "type": "paragraph",
      "html": "<p>When the user enters ‘a’, that character is placed in the buffer. Then operator&gt;&gt; tries to extract ‘a’ to variable x, which is of type double. Since ‘a’ can’t be converted to a double, <code>operator&gt;&gt;</code> can’t do the extraction. Two things happen at this point: ‘a’ is left in the buffer, and std::cin goes into “failure mode”.</p>",
      "preview": "When the user enters ‘a’, that character is placed in the buffer. Then operator>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b71",
      "type": "paragraph",
      "html": "<p>Once in “failure mode”, future requests for input extraction will silently fail. Thus in our calculator program, the output prompts still print, but any requests for further extraction are ignored. This means that instead waiting for us to enter an operation, the input prompt is skipped, and we get stuck in an infinite loop because there is no way to reach one of the valid cases.</p>",
      "preview": "Once in “failure mode”, future requests for input extraction will silently fail.",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b72",
      "type": "paragraph",
      "html": "<p>In order to get <code>std::cin</code> working properly again, we typically need to do three things:</p>",
      "preview": "In order to get std::cin working properly again, we typically need to do three t",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b73",
      "type": "list",
      "html": "<ul><li>Detect that a prior extraction has failed.</li><li>Put <code>std::cin</code> back in normal operation mode.</li><li>Remove the input that caused the failure (so the next extraction request doesn’t fail in an identical manner).</li></ul>",
      "preview": "Detect that a prior extraction has failed.Put std::cin back in normal operation ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b74",
      "type": "paragraph",
      "html": "<p>Here’s what that looks like:</p>",
      "preview": "Here’s what that looks like:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b75",
      "type": "code",
      "language": "cpp",
      "code": "if (std::cin.fail()) // If the previous extraction failed\n{\n    // Let's handle the failure\n    std::cin.clear(); // Put us back in 'normal' operation mode\n    ignoreLine();     // And remove the bad input\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">if (std::cin.fail()) // If the previous extraction failed\n{\n    // Let's handle the failure\n    std::cin.clear(); // Put us back in 'normal' operation mode\n    ignoreLine();     // And remove the bad input\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b76",
      "type": "paragraph",
      "html": "<p>Because <code>std::cin</code> has a Boolean conversion indicating whether the last input succeeded, it’s more idiomatic to write the above as following:</p>",
      "preview": "Because std::cin has a Boolean conversion indicating whether the last input succ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b77",
      "type": "code",
      "language": "cpp",
      "code": "if (!std::cin) // If the previous extraction failed\n{\n    // Let's handle the failure\n    std::cin.clear(); // Put us back in 'normal' operation mode\n    ignoreLine();     // And remove the bad input\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">if (!std::cin) // If the previous extraction failed\n{\n    // Let's handle the failure\n    std::cin.clear(); // Put us back in 'normal' operation mode\n    ignoreLine();     // And remove the bad input\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b78",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Key insight</p><p>Once an extraction has failed, future requests for input extraction (including calls to <code>ignore()</code>) will silently fail until the <code>clear()</code> function is called. Thus, after detecting a failed extraction, calling <code>clear()</code> is usually the first thing you should do.</p></div>",
      "preview": "Key insightOnce an extraction has failed, future requests for input extraction (including calls to i",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b79",
      "type": "paragraph",
      "html": "<p>Let’s integrate that into our getDouble() function:</p>",
      "preview": "Let’s integrate that into our getDouble() function:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b80",
      "type": "code",
      "language": "cpp",
      "code": "double getDouble()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout << \"Enter a decimal number: \";\n        double x{};\n        std::cin >> x;\n\n        if (!std::cin) // If the previous extraction failed\n        {\n            // Let's handle the failure\n            std::cin.clear(); // Put us back in 'normal' operation mode\n            ignoreLine();     // And remove the bad input\n            continue;\n        }\n\n        // Our extraction succeeded\n        ignoreLine(); // Ignore any additional input on this line\n        return x;     // Return the value we extracted\n    }\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">double getDouble()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout &lt;&lt; \"Enter a decimal number: \";\n        double x{};\n        std::cin &gt;&gt; x;\n\n        if (!std::cin) // If the previous extraction failed\n        {\n            // Let's handle the failure\n            std::cin.clear(); // Put us back in 'normal' operation mode\n            ignoreLine();     // And remove the bad input\n            continue;\n        }\n\n        // Our extraction succeeded\n        ignoreLine(); // Ignore any additional input on this line\n        return x;     // Return the value we extracted\n    }\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b81",
      "type": "paragraph",
      "html": "<p>For fundamental types, a failed extraction due to invalid input will cause the variable to be assigned the value <code>0</code> (or whatever value <code>0</code> converts to in the variable’s type).</p>",
      "preview": "For fundamental types, a failed extraction due to invalid input will cause the v",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b82",
      "type": "paragraph",
      "html": "<p>It is fine to call <code>clear()</code> even when extraction hasn’t failed -- it won’t do anything. In cases where we are going to call <code>ignoreLine()</code> regardless of whether we succeeded or failed, we can essentially combine the two cases:</p>",
      "preview": "It is fine to call clear() even when extraction hasn’t failed -- it won’t do any",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b83",
      "type": "code",
      "language": "cpp",
      "code": "double getDouble()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout << \"Enter a decimal number: \";\n        double x{};\n        std::cin >> x;\n\n        bool success { std::cin }; // Remember whether we had a successful extraction\n        std::cin.clear();          // Put us back in 'normal' operation mode (in case we failed)\n        ignoreLine();              // Ignore any additional input on this line (regardless)\n\n        if (success)               // If we actually extracted a value\n            return x;              // Return it (otherwise, we go back to top of loop)\n    }\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">double getDouble()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout &lt;&lt; \"Enter a decimal number: \";\n        double x{};\n        std::cin &gt;&gt; x;\n\n        bool success { std::cin }; // Remember whether we had a successful extraction\n        std::cin.clear();          // Put us back in 'normal' operation mode (in case we failed)\n        ignoreLine();              // Ignore any additional input on this line (regardless)\n\n        if (success)               // If we actually extracted a value\n            return x;              // Return it (otherwise, we go back to top of loop)\n    }\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b84",
      "type": "section",
      "title": "Checking for EOF",
      "html": "<h3 class=\"cpp-section-heading\">Checking for EOF</h3>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b85",
      "type": "paragraph",
      "html": "<p>There’s another case that we need to address.</p>",
      "preview": "There’s another case that we need to address.",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b86",
      "type": "paragraph",
      "html": "<p>An end-of-file (EOF) is a special error state that means “No more data available”. This is normally generated <strong>after</strong> an input operation fails due to no data being available. For example, if you were reading the contents of a file on disk, and then try to read in more data after you’ve already reached the end of the file, an EOF will be generated to tell you that no more data is available. In the case of file input, this is no problem -- we can just close the file and move on.</p>",
      "preview": "An end-of-file (EOF) is a special error state that means “No more data available",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b87",
      "type": "paragraph",
      "html": "<p>Now consider <code>std::cin</code>. If we try to extract input from <code>std::cin</code> and there is none, by design it won’t generate an EOF -- it will just wait for the user to enter more input. However, <code>std::cin</code> can generate an EOF in certain cases -- most commonly when the user enters a special key combination for their OS. Both Unix (via ctrl-D) and Windows (via ctrl-Z + ENTER) support entering an “EOF character” from the keyboard.</p>",
      "preview": "Now consider std::cin. If we try to extract input from std::cin and there is non",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b88",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Key insight</p><p>In C++, EOF is an error state, not a character. Different OSes have special character combinations that are treated as a “user-entered EOF request”. These are sometimes called “EOF characters”.</p></div>",
      "preview": "Key insightIn C++, EOF is an error state, not a character. Different OSes have special character com",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b89",
      "type": "paragraph",
      "html": "<p>When extracting data to <code>std::cin</code> and the user enters an EOF character, the behavior is OS-specific. Here’s what typically happens:</p>",
      "preview": "When extracting data to std::cin and the user enters an EOF character, the behav",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b90",
      "type": "list",
      "html": "<ul><li>If the EOF is not the first character of input: All input prior to the EOF will be flushed, and the EOF character is ignored. On Windows, any characters entered after the EOF are ignored, except for the newline.</li><li>If the EOF is the first character entered: The EOF error will be set. The input stream may (or may not) be disconnected.</li></ul>",
      "preview": "If the EOF is not the first character of input: All input prior to the EOF will ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b91",
      "type": "paragraph",
      "html": "<p>Although <code>std::cin.clear()</code> will clear an EOF error, if the input stream is disconnected, the next input request will generate another EOF error. This is problematic when our input is inside a <code>while(true)</code> loop, as we will get stuck in an infinite loop of EOF errors.</p>",
      "preview": "Although std::cin.clear() will clear an EOF error, if the input stream is discon",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b92",
      "type": "paragraph",
      "html": "<p>Since the intent of an keyboard-entered <code>EOF</code> character is to terminate the input stream, the best thing to do is detect the EOF (via <code>std::cin.eof()</code>) and then terminate the program.</p>",
      "preview": "Since the intent of an keyboard-entered EOF character is to terminate the input ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b93",
      "type": "paragraph",
      "html": "<p>Because clearing a failed input stream is something we’re likely to be checking for a lot, this is a good candidate for a reusable function:</p>",
      "preview": "Because clearing a failed input stream is something we’re likely to be checking ",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b94",
      "type": "code",
      "language": "cpp",
      "code": "#include <limits> // for std::numeric_limits\n\nvoid ignoreLine()\n{\n    std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\\n');\n}\n\n// returns true if extraction failed, false otherwise\nbool clearFailedExtraction()\n{\n    // Check for failed extraction\n    if (!std::cin) // If the previous extraction failed\n    {\n        if (std::cin.eof()) // If the user entered an EOF\n        {\n            std::exit(0); // Shut down the program now\n        }\n\n        // Let's handle the failure\n        std::cin.clear(); // Put us back in 'normal' operation mode\n        ignoreLine();     // And remove the bad input\n\n        return true;\n    }\n    \n    return false;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;limits&gt; // for std::numeric_limits\n\nvoid ignoreLine()\n{\n    std::cin.ignore(std::numeric_limits&lt;std::streamsize&gt;::max(), '\\n');\n}\n\n// returns true if extraction failed, false otherwise\nbool clearFailedExtraction()\n{\n    // Check for failed extraction\n    if (!std::cin) // If the previous extraction failed\n    {\n        if (std::cin.eof()) // If the user entered an EOF\n        {\n            std::exit(0); // Shut down the program now\n        }\n\n        // Let's handle the failure\n        std::cin.clear(); // Put us back in 'normal' operation mode\n        ignoreLine();     // And remove the bad input\n\n        return true;\n    }\n    \n    return false;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b95",
      "type": "section",
      "title": "Error case 4: Extraction succeeds but the user overflows a numeric value",
      "html": "<h3 class=\"cpp-section-heading\">Error case 4: Extraction succeeds but the user overflows a numeric value</h3>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b96",
      "type": "paragraph",
      "html": "<p>Consider the following simple example:</p>",
      "preview": "Consider the following simple example:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b97",
      "type": "code",
      "language": "cpp",
      "code": "#include <cstdint>\n#include <iostream>\n\nint main()\n{\n    std::int16_t x{}; // x is 16 bits, holds from -32768 to 32767\n    std::cout << \"Enter a number between -32768 and 32767: \";\n    std::cin >> x;\n\n    std::int16_t y{}; // y is 16 bits, holds from -32768 to 32767\n    std::cout << \"Enter another number between -32768 and 32767: \";\n    std::cin >> y;\n\n    std::cout << \"The sum is: \" << x + y << '\\n';\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cstdint&gt;\n#include &lt;iostream&gt;\n\nint main()\n{\n    std::int16_t x{}; // x is 16 bits, holds from -32768 to 32767\n    std::cout &lt;&lt; \"Enter a number between -32768 and 32767: \";\n    std::cin &gt;&gt; x;\n\n    std::int16_t y{}; // y is 16 bits, holds from -32768 to 32767\n    std::cout &lt;&lt; \"Enter another number between -32768 and 32767: \";\n    std::cin &gt;&gt; y;\n\n    std::cout &lt;&lt; \"The sum is: \" &lt;&lt; x + y &lt;&lt; '\\n';\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b98",
      "type": "paragraph",
      "html": "<p>What happens if the user enters a number that is too large (e.g. 40000)?</p>",
      "preview": "What happens if the user enters a number that is too large (e.g. 40000)?",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b99",
      "type": "output",
      "text": "Enter a number between -32768 and 32767: 40000\nEnter another number between -32768 and 32767: The sum is: 32767\n",
      "html": "<pre>Enter a number between -32768 and 32767: 40000\nEnter another number between -32768 and 32767: The sum is: 32767\n</pre>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b100",
      "type": "paragraph",
      "html": "<p>In the above case, <code>std::cin</code> goes immediately into “failure mode”, but also assigns the closest in-range value to the variable. When the entered value is larger than the largest possible value for the type, the closest in-range value is the largest possible value for the type. Consequently, <code>x</code> is left with the assigned value of <code>32767</code>. Additional inputs are skipped, leaving <code>y</code> with the initialized value of <code>0</code>. We can handle this kind of error in the same way as a failed extraction.</p>",
      "preview": "In the above case, std::cin goes immediately into “failure mode”, but also assig",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b101",
      "type": "section",
      "title": "Putting it all together",
      "html": "<h3 class=\"cpp-section-heading\">Putting it all together</h3>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b102",
      "type": "paragraph",
      "html": "<p>Here’s our example calculator, updated with a few additional bits of error checking:</p>",
      "preview": "Here’s our example calculator, updated with a few additional bits of error check",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b103",
      "type": "code",
      "language": "cpp",
      "code": "#include <cstdlib> // for std::exit\n#include <iostream>\n#include <limits> // for std::numeric_limits\n\nvoid ignoreLine()\n{\n    std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\\n');\n}\n\n// returns true if extraction failed, false otherwise\nbool clearFailedExtraction()\n{\n    // Check for failed extraction\n    if (!std::cin) // If the previous extraction failed\n    {\n        if (std::cin.eof()) // If the stream was closed\n        {\n            std::exit(0); // Shut down the program now\n        }\n\n        // Let's handle the failure\n        std::cin.clear(); // Put us back in 'normal' operation mode\n        ignoreLine();     // And remove the bad input\n\n        return true;\n    }\n    \n    return false;\n}\n\ndouble getDouble()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout << \"Enter a decimal number: \";\n        double x{};\n        std::cin >> x;\n\n        if (clearFailedExtraction())\n        {\n            std::cout << \"Oops, that input is invalid.  Please try again.\\n\";\n            continue;\n        }\n\n        ignoreLine(); // Remove any extraneous input\n        return x;     // Return the value we extracted\n    }\n}\n\nchar getOperator()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout << \"Enter one of the following: +, -, *, or /: \";\n        char operation{};\n        std::cin >> operation;\n\n        if (!clearFailedExtraction()) // we'll handle error messaging if extraction failed below\n             ignoreLine(); // remove any extraneous input (only if extraction succeded)\n\n        // Check whether the user entered meaningful input\n        switch (operation)\n        {\n        case '+':\n        case '-':\n        case '*':\n        case '/':\n            return operation; // Return the entered char to the caller\n        default: // Otherwise tell the user what went wrong\n            std::cout << \"Oops, that input is invalid.  Please try again.\\n\";\n        }\n    }\n}\n \nvoid printResult(double x, char operation, double y)\n{\n    std::cout << x << ' ' << operation << ' ' << y << \" is \";\n\n    switch (operation)\n    {\n    case '+':\n        std::cout << x + y << '\\n';\n        return;\n    case '-':\n        std::cout << x - y << '\\n';\n        return;\n    case '*':\n        std::cout << x * y << '\\n';\n        return;\n    case '/':\n        if (y == 0.0)\n            break;\n\n        std::cout << x / y << '\\n';\n        return;\n    }\n\n    std::cout << \"???\";  // Being robust means handling unexpected parameters as well, even though getOperator() guarantees operation is valid in this particular program\n}\n \nint main()\n{\n    double x{ getDouble() };\n    char operation{ getOperator() };\n    double y{ getDouble() };\n\n    // Handle division by 0\n    while (operation == '/' && y == 0.0) \n    {\n        std::cout << \"The denominator cannot be zero.  Try again.\\n\";\n        y = getDouble();\n    }\n \n    printResult(x, operation, y);\n \n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cstdlib&gt; // for std::exit\n#include &lt;iostream&gt;\n#include &lt;limits&gt; // for std::numeric_limits\n\nvoid ignoreLine()\n{\n    std::cin.ignore(std::numeric_limits&lt;std::streamsize&gt;::max(), '\\n');\n}\n\n// returns true if extraction failed, false otherwise\nbool clearFailedExtraction()\n{\n    // Check for failed extraction\n    if (!std::cin) // If the previous extraction failed\n    {\n        if (std::cin.eof()) // If the stream was closed\n        {\n            std::exit(0); // Shut down the program now\n        }\n\n        // Let's handle the failure\n        std::cin.clear(); // Put us back in 'normal' operation mode\n        ignoreLine();     // And remove the bad input\n\n        return true;\n    }\n    \n    return false;\n}\n\ndouble getDouble()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout &lt;&lt; \"Enter a decimal number: \";\n        double x{};\n        std::cin &gt;&gt; x;\n\n        if (clearFailedExtraction())\n        {\n            std::cout &lt;&lt; \"Oops, that input is invalid.  Please try again.\\n\";\n            continue;\n        }\n\n        ignoreLine(); // Remove any extraneous input\n        return x;     // Return the value we extracted\n    }\n}\n\nchar getOperator()\n{\n    while (true) // Loop until user enters a valid input\n    {\n        std::cout &lt;&lt; \"Enter one of the following: +, -, *, or /: \";\n        char operation{};\n        std::cin &gt;&gt; operation;\n\n        if (!clearFailedExtraction()) // we'll handle error messaging if extraction failed below\n             ignoreLine(); // remove any extraneous input (only if extraction succeded)\n\n        // Check whether the user entered meaningful input\n        switch (operation)\n        {\n        case '+':\n        case '-':\n        case '*':\n        case '/':\n            return operation; // Return the entered char to the caller\n        default: // Otherwise tell the user what went wrong\n            std::cout &lt;&lt; \"Oops, that input is invalid.  Please try again.\\n\";\n        }\n    }\n}\n \nvoid printResult(double x, char operation, double y)\n{\n    std::cout &lt;&lt; x &lt;&lt; ' ' &lt;&lt; operation &lt;&lt; ' ' &lt;&lt; y &lt;&lt; \" is \";\n\n    switch (operation)\n    {\n    case '+':\n        std::cout &lt;&lt; x + y &lt;&lt; '\\n';\n        return;\n    case '-':\n        std::cout &lt;&lt; x - y &lt;&lt; '\\n';\n        return;\n    case '*':\n        std::cout &lt;&lt; x * y &lt;&lt; '\\n';\n        return;\n    case '/':\n        if (y == 0.0)\n            break;\n\n        std::cout &lt;&lt; x / y &lt;&lt; '\\n';\n        return;\n    }\n\n    std::cout &lt;&lt; \"???\";  // Being robust means handling unexpected parameters as well, even though getOperator() guarantees operation is valid in this particular program\n}\n \nint main()\n{\n    double x{ getDouble() };\n    char operation{ getOperator() };\n    double y{ getDouble() };\n\n    // Handle division by 0\n    while (operation == '/' &amp;&amp; y == 0.0) \n    {\n        std::cout &lt;&lt; \"The denominator cannot be zero.  Try again.\\n\";\n        y = getDouble();\n    }\n \n    printResult(x, operation, y);\n \n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b104",
      "type": "section",
      "title": "Conclusion",
      "html": "<h3 class=\"cpp-section-heading\">Conclusion</h3>",
      "canCheck": false
    },
    {
      "id": "stdcin-and-handling-invalid-input_b105",
      "type": "paragraph",
      "html": "<p>As you write your programs, consider how users will misuse your program, especially around text input. For each point of text input, consider:</p>",
      "preview": "As you write your programs, consider how users will misuse your program, especia",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b106",
      "type": "list",
      "html": "<ul><li>Could extraction fail?</li><li>Could the user enter more input than expected?</li><li>Could the user enter meaningless input?</li><li>Could the user overflow an input?</li></ul>",
      "preview": "Could extraction fail?Could the user enter more input than expected?Could the us",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b107",
      "type": "paragraph",
      "html": "<p>You can use if statements and boolean logic to test whether input is expected and meaningful.</p>",
      "preview": "You can use if statements and boolean logic to test whether input is expected an",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b108",
      "type": "paragraph",
      "html": "<p>The following code will clear any extraneous input:</p>",
      "preview": "The following code will clear any extraneous input:",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b109",
      "type": "code",
      "language": "cpp",
      "code": "#include <limits> // for std::numeric_limits\n\nvoid ignoreLine()\n{\n    std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\\n');\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;limits&gt; // for std::numeric_limits\n\nvoid ignoreLine()\n{\n    std::cin.ignore(std::numeric_limits&lt;std::streamsize&gt;::max(), '\\n');\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b110",
      "type": "paragraph",
      "html": "<p>The following code will test for and fix failed extractions or overflow (and remove extraneous input):</p>",
      "preview": "The following code will test for and fix failed extractions or overflow (and rem",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b111",
      "type": "code",
      "language": "cpp",
      "code": "// returns true if extraction failed, false otherwise\nbool clearFailedExtraction()\n{\n    // Check for failed extraction\n    if (!std::cin) // If the previous extraction failed\n    {\n        if (std::cin.eof()) // If the stream was closed\n        {\n            std::exit(0); // Shut down the program now\n        }\n\n        // Let's handle the failure\n        std::cin.clear(); // Put us back in 'normal' operation mode\n        ignoreLine();     // And remove the bad input\n\n        return true;\n    }\n    \n    return false;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// returns true if extraction failed, false otherwise\nbool clearFailedExtraction()\n{\n    // Check for failed extraction\n    if (!std::cin) // If the previous extraction failed\n    {\n        if (std::cin.eof()) // If the stream was closed\n        {\n            std::exit(0); // Shut down the program now\n        }\n\n        // Let's handle the failure\n        std::cin.clear(); // Put us back in 'normal' operation mode\n        ignoreLine();     // And remove the bad input\n\n        return true;\n    }\n    \n    return false;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b112",
      "type": "paragraph",
      "html": "<p>We can test to see if there is an unextracted input (other than a newline) as follows:</p>",
      "preview": "We can test to see if there is an unextracted input (other than a newline) as fo",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b113",
      "type": "code",
      "language": "cpp",
      "code": "// returns true if std::cin has unextracted input on the current line, false otherwise\nbool hasUnextractedInput()\n{\n    return !std::cin.eof() && std::cin.peek() != '\\n';\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// returns true if std::cin has unextracted input on the current line, false otherwise\nbool hasUnextractedInput()\n{\n    return !std::cin.eof() &amp;&amp; std::cin.peek() != '\\n';\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b114",
      "type": "paragraph",
      "html": "<p>Finally, use loops to ask the user to re-enter input if the original input was invalid.</p>",
      "preview": "Finally, use loops to ask the user to re-enter input if the original input was i",
      "canCheck": true
    },
    {
      "id": "stdcin-and-handling-invalid-input_b115",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Author’s note</p><p>Input validation is important and useful, but it also tends to make examples more complicated and harder to follow. Accordingly, in future lessons, we will generally not do any kind of input validation unless it’s relevant to something we’re trying to teach.</p></div>",
      "preview": "Author’s noteInput validation is important and useful, but it also tends to make examples more compl",
      "canCheck": true
    }
  ]
}