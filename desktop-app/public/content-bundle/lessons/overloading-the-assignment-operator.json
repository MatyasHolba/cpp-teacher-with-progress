{
  "index": 267,
  "chapterId": "21",
  "number": "21.12",
  "title": "21.12 — Overloading the assignment operator",
  "url": "https://www.learncpp.com/cpp-tutorial/overloading-the-assignment-operator/",
  "slug": "overloading-the-assignment-operator",
  "scrapedAt": "2026-09-15T16:30:55.202Z",
  "totalBlocks": 46,
  "totalCheckpoints": 38,
  "blocks": [
    {
      "id": "overloading-the-assignment-operator_b0",
      "type": "paragraph",
      "html": "<p>The <strong>copy assignment operator</strong> (operator=) is used to copy values from one object to another <em>already existing object</em>.</p>",
      "preview": "The copy assignment operator (operator=) is used to copy values from one object ",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b1",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>As of C++11, C++ also supports “Move assignment”. We discuss move assignment in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/move-constructors-and-move-assignment/\">22.3 -- Move constructors and move assignment</a>.</p></div>",
      "preview": "Related contentAs of C++11, C++ also supports “Move assignment”. We discuss move assignment in lesso",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b2",
      "type": "section",
      "title": "Copy assignment vs Copy constructor",
      "html": "<h3 class=\"cpp-section-heading\">Copy assignment vs Copy constructor</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-assignment-operator_b3",
      "type": "paragraph",
      "html": "<p>The purpose of the copy constructor and the copy assignment operator are almost equivalent -- both copy one object to another. However, the copy constructor initializes new objects, whereas the assignment operator replaces the contents of existing objects.</p>",
      "preview": "The purpose of the copy constructor and the copy assignment operator are almost ",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b4",
      "type": "paragraph",
      "html": "<p>The difference between the copy constructor and the copy assignment operator causes a lot of confusion for new programmers, but it’s really not all that difficult. Summarizing:</p>",
      "preview": "The difference between the copy constructor and the copy assignment operator cau",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b5",
      "type": "list",
      "html": "<ul><li>If a new object has to be created before the copying can occur, the copy constructor is used (note: this includes passing or returning objects by value).</li><li>If a new object does not have to be created before the copying can occur, the assignment operator is used.</li></ul>",
      "preview": "If a new object has to be created before the copying can occur, the copy constru",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b6",
      "type": "section",
      "title": "Overloading the assignment operator",
      "html": "<h3 class=\"cpp-section-heading\">Overloading the assignment operator</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-assignment-operator_b7",
      "type": "paragraph",
      "html": "<p>Overloading the copy assignment operator (operator=) is fairly straightforward, with one specific caveat that we’ll get to. The copy assignment operator must be overloaded as a member function.</p>",
      "preview": "Overloading the copy assignment operator (operator=) is fairly straightforward, ",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b8",
      "type": "code",
      "language": "cpp",
      "code": "#include <cassert>\n#include <iostream>\n\nclass Fraction\n{\nprivate:\n\tint m_numerator { 0 };\n\tint m_denominator { 1 };\n\npublic:\n\t// Default constructor\n\tFraction(int numerator = 0, int denominator = 1 )\n\t\t: m_numerator { numerator }, m_denominator { denominator }\n\t{\n\t\tassert(denominator != 0);\n\t}\n\n\t// Copy constructor\n\tFraction(const Fraction& copy)\n\t\t: m_numerator { copy.m_numerator }, m_denominator { copy.m_denominator }\n\t{\n\t\t// no need to check for a denominator of 0 here since copy must already be a valid Fraction\n\t\tstd::cout << \"Copy constructor called\\n\"; // just to prove it works\n\t}\n\n\t// Overloaded assignment\n\tFraction& operator= (const Fraction& fraction);\n\n\tfriend std::ostream& operator<<(std::ostream& out, const Fraction& f1);\n        \n};\n\nstd::ostream& operator<<(std::ostream& out, const Fraction& f1)\n{\n\tout << f1.m_numerator << '/' << f1.m_denominator;\n\treturn out;\n}\n\n// A simplistic implementation of operator= (see better implementation below)\nFraction& Fraction::operator= (const Fraction& fraction)\n{\n    // do the copy\n    m_numerator = fraction.m_numerator;\n    m_denominator = fraction.m_denominator;\n\n    // return the existing object so we can chain this operator\n    return *this;\n}\n\nint main()\n{\n    Fraction fiveThirds { 5, 3 };\n    Fraction f;\n    f = fiveThirds; // calls overloaded assignment\n    std::cout << f;\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt;\n#include &lt;iostream&gt;\n\nclass Fraction\n{\nprivate:\n\tint m_numerator { 0 };\n\tint m_denominator { 1 };\n\npublic:\n\t// Default constructor\n\tFraction(int numerator = 0, int denominator = 1 )\n\t\t: m_numerator { numerator }, m_denominator { denominator }\n\t{\n\t\tassert(denominator != 0);\n\t}\n\n\t// Copy constructor\n\tFraction(const Fraction&amp; copy)\n\t\t: m_numerator { copy.m_numerator }, m_denominator { copy.m_denominator }\n\t{\n\t\t// no need to check for a denominator of 0 here since copy must already be a valid Fraction\n\t\tstd::cout &lt;&lt; \"Copy constructor called\\n\"; // just to prove it works\n\t}\n\n\t// Overloaded assignment\n\tFraction&amp; operator= (const Fraction&amp; fraction);\n\n\tfriend std::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const Fraction&amp; f1);\n        \n};\n\nstd::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const Fraction&amp; f1)\n{\n\tout &lt;&lt; f1.m_numerator &lt;&lt; '/' &lt;&lt; f1.m_denominator;\n\treturn out;\n}\n\n// A simplistic implementation of operator= (see better implementation below)\nFraction&amp; Fraction::operator= (const Fraction&amp; fraction)\n{\n    // do the copy\n    m_numerator = fraction.m_numerator;\n    m_denominator = fraction.m_denominator;\n\n    // return the existing object so we can chain this operator\n    return *this;\n}\n\nint main()\n{\n    Fraction fiveThirds { 5, 3 };\n    Fraction f;\n    f = fiveThirds; // calls overloaded assignment\n    std::cout &lt;&lt; f;\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b9",
      "type": "paragraph",
      "html": "<p>This prints:</p>",
      "preview": "This prints:",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b10",
      "type": "output",
      "text": "5/3\n",
      "html": "<pre>5/3\n</pre>",
      "canCheck": false
    },
    {
      "id": "overloading-the-assignment-operator_b11",
      "type": "paragraph",
      "html": "<p>This should all be pretty straightforward by now. Our overloaded operator= returns *this, so that we can chain multiple assignments together:</p>",
      "preview": "This should all be pretty straightforward by now. Our overloaded operator= retur",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b12",
      "type": "code",
      "language": "cpp",
      "code": "int main()\n{\n    Fraction f1 { 5, 3 };\n    Fraction f2 { 7, 2 };\n    Fraction f3 { 9, 5 };\n\n    f1 = f2 = f3; // chained assignment\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int main()\n{\n    Fraction f1 { 5, 3 };\n    Fraction f2 { 7, 2 };\n    Fraction f3 { 9, 5 };\n\n    f1 = f2 = f3; // chained assignment\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b13",
      "type": "section",
      "title": "Issues due to self-assignment",
      "html": "<h3 class=\"cpp-section-heading\">Issues due to self-assignment</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-assignment-operator_b14",
      "type": "paragraph",
      "html": "<p>Here’s where things start to get a little more interesting. C++ allows self-assignment:</p>",
      "preview": "Here’s where things start to get a little more interesting. C++ allows self-assi",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b15",
      "type": "code",
      "language": "cpp",
      "code": "int main()\n{\n    Fraction f1 { 5, 3 };\n    f1 = f1; // self assignment\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int main()\n{\n    Fraction f1 { 5, 3 };\n    f1 = f1; // self assignment\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b16",
      "type": "paragraph",
      "html": "<p>This will call f1.operator=(f1), and under the simplistic implementation above, all of the members will be assigned to themselves. In this particular example, the self-assignment causes each member to be assigned to itself, which has no overall impact, other than wasting time. In most cases, a self-assignment doesn’t need to do anything at all!</p>",
      "preview": "This will call f1.operator=(f1), and under the simplistic implementation above, ",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b17",
      "type": "paragraph",
      "html": "<p>However, in cases where an assignment operator needs to dynamically assign memory, self-assignment can actually be dangerous:</p>",
      "preview": "However, in cases where an assignment operator needs to dynamically assign memor",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b18",
      "type": "code",
      "language": "cpp",
      "code": "#include <algorithm> // for std::max and std::copy_n\n#include <iostream>\n\nclass MyString\n{\nprivate:\n\tchar* m_data {};\n\tint m_length {};\n\npublic:\n\tMyString(const char* data = nullptr, int length = 0 )\n\t\t: m_length { std::max(length, 0) }\n\t{\n\t\tif (length)\n\t\t{\n\t\t\tm_data = new char[static_cast<std::size_t>(length)];\n\t\t\tstd::copy_n(data, length, m_data); // copy length elements of data into m_data\n\t\t}\n\t}\n\t~MyString()\n\t{\n\t\tdelete[] m_data;\n\t}\n\n\tMyString(const MyString&) = default; // some compilers (gcc) warn if you have pointer members but no declared copy constructor\n\n\t// Overloaded assignment\n\tMyString& operator= (const MyString& str);\n\n\tfriend std::ostream& operator<<(std::ostream& out, const MyString& s);\n};\n\nstd::ostream& operator<<(std::ostream& out, const MyString& s)\n{\n\tout << s.m_data;\n\treturn out;\n}\n\n// A simplistic implementation of operator= (do not use)\nMyString& MyString::operator= (const MyString& str)\n{\n\t// if data exists in the current string, delete it\n\tif (m_data) delete[] m_data;\n\n\tm_length = str.m_length;\n\tm_data = nullptr;\n\n\t// allocate a new array of the appropriate length\n\tif (m_length)\n\t\tm_data = new char[static_cast<std::size_t>(str.m_length)];\n\n\tstd::copy_n(str.m_data, m_length, m_data); // copies m_length elements of str.m_data into m_data\n\n\t// return the existing object so we can chain this operator\n\treturn *this;\n}\n\nint main()\n{\n\tMyString alex(\"Alex\", 5); // Meet Alex\n\tMyString employee;\n\temployee = alex; // Alex is our newest employee\n\tstd::cout << employee; // Say your name, employee\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;algorithm&gt; // for std::max and std::copy_n\n#include &lt;iostream&gt;\n\nclass MyString\n{\nprivate:\n\tchar* m_data {};\n\tint m_length {};\n\npublic:\n\tMyString(const char* data = nullptr, int length = 0 )\n\t\t: m_length { std::max(length, 0) }\n\t{\n\t\tif (length)\n\t\t{\n\t\t\tm_data = new char[static_cast&lt;std::size_t&gt;(length)];\n\t\t\tstd::copy_n(data, length, m_data); // copy length elements of data into m_data\n\t\t}\n\t}\n\t~MyString()\n\t{\n\t\tdelete[] m_data;\n\t}\n\n\tMyString(const MyString&amp;) = default; // some compilers (gcc) warn if you have pointer members but no declared copy constructor\n\n\t// Overloaded assignment\n\tMyString&amp; operator= (const MyString&amp; str);\n\n\tfriend std::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const MyString&amp; s);\n};\n\nstd::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const MyString&amp; s)\n{\n\tout &lt;&lt; s.m_data;\n\treturn out;\n}\n\n// A simplistic implementation of operator= (do not use)\nMyString&amp; MyString::operator= (const MyString&amp; str)\n{\n\t// if data exists in the current string, delete it\n\tif (m_data) delete[] m_data;\n\n\tm_length = str.m_length;\n\tm_data = nullptr;\n\n\t// allocate a new array of the appropriate length\n\tif (m_length)\n\t\tm_data = new char[static_cast&lt;std::size_t&gt;(str.m_length)];\n\n\tstd::copy_n(str.m_data, m_length, m_data); // copies m_length elements of str.m_data into m_data\n\n\t// return the existing object so we can chain this operator\n\treturn *this;\n}\n\nint main()\n{\n\tMyString alex(\"Alex\", 5); // Meet Alex\n\tMyString employee;\n\temployee = alex; // Alex is our newest employee\n\tstd::cout &lt;&lt; employee; // Say your name, employee\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b19",
      "type": "paragraph",
      "html": "<p>First, run the program as it is. You’ll see that the program prints “Alex” as it should.</p>",
      "preview": "First, run the program as it is. You’ll see that the program prints “Alex” as it",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b20",
      "type": "paragraph",
      "html": "<p>Now run the following program:</p>",
      "preview": "Now run the following program:",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b21",
      "type": "code",
      "language": "cpp",
      "code": "int main()\n{\n    MyString alex { \"Alex\", 5 }; // Meet Alex\n    alex = alex; // Alex is himself\n    std::cout << alex; // Say your name, Alex\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int main()\n{\n    MyString alex { \"Alex\", 5 }; // Meet Alex\n    alex = alex; // Alex is himself\n    std::cout &lt;&lt; alex; // Say your name, Alex\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b22",
      "type": "paragraph",
      "html": "<p>You’ll probably get garbage output. What happened?</p>",
      "preview": "You’ll probably get garbage output. What happened?",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b23",
      "type": "paragraph",
      "html": "<p>Consider what happens in the overloaded operator= when the implicit object AND the passed in parameter (str) are both variable alex. In this case, m_data is the same as str.m_data. The first thing that happens is that the function checks to see if the implicit object already has a string. If so, it needs to delete it, so we don’t end up with a memory leak. In this case, m_data is allocated, so the function deletes m_data. But because str is the same as *this, the string that we wanted to copy has been deleted and m_data (and str.m_data) are dangling.</p>",
      "preview": "Consider what happens in the overloaded operator= when the implicit object AND t",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b24",
      "type": "paragraph",
      "html": "<p>Later on, we allocate new memory to m_data (and str.m_data). So when we subsequently copy the data from str.m_data into m_data, we’re copying garbage, because str.m_data was never initialized.</p>",
      "preview": "Later on, we allocate new memory to m_data (and str.m_data). So when we subseque",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b25",
      "type": "section",
      "title": "Detecting and handling self-assignment",
      "html": "<h3 class=\"cpp-section-heading\">Detecting and handling self-assignment</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-assignment-operator_b26",
      "type": "paragraph",
      "html": "<p>Fortunately, we can detect when self-assignment occurs. Here’s an updated implementation of our overloaded operator= for the MyString class:</p>",
      "preview": "Fortunately, we can detect when self-assignment occurs. Here’s an updated implem",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b27",
      "type": "code",
      "language": "cpp",
      "code": "MyString& MyString::operator= (const MyString& str)\n{\n\t// self-assignment check\n\tif (this == &str)\n\t\treturn *this;\n\n\t// if data exists in the current string, delete it\n\tif (m_data) delete[] m_data;\n\n\tm_length = str.m_length;\n\tm_data = nullptr;\n\n\t// allocate a new array of the appropriate length\n\tif (m_length)\n\t\tm_data = new char[static_cast<std::size_t>(str.m_length)];\n\n\tstd::copy_n(str.m_data, m_length, m_data); // copies m_length elements of str.m_data into m_data\n\n\t// return the existing object so we can chain this operator\n\treturn *this;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">MyString&amp; MyString::operator= (const MyString&amp; str)\n{\n\t// self-assignment check\n\tif (this == &amp;str)\n\t\treturn *this;\n\n\t// if data exists in the current string, delete it\n\tif (m_data) delete[] m_data;\n\n\tm_length = str.m_length;\n\tm_data = nullptr;\n\n\t// allocate a new array of the appropriate length\n\tif (m_length)\n\t\tm_data = new char[static_cast&lt;std::size_t&gt;(str.m_length)];\n\n\tstd::copy_n(str.m_data, m_length, m_data); // copies m_length elements of str.m_data into m_data\n\n\t// return the existing object so we can chain this operator\n\treturn *this;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b28",
      "type": "paragraph",
      "html": "<p>By checking if the address of our implicit object is the same as the address of the object being passed in as a parameter, we can have our assignment operator just return immediately without doing any other work.</p>",
      "preview": "By checking if the address of our implicit object is the same as the address of ",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b29",
      "type": "paragraph",
      "html": "<p>Because this is just a pointer comparison, it should be fast, and does not require operator== to be overloaded.</p>",
      "preview": "Because this is just a pointer comparison, it should be fast, and does not requi",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b30",
      "type": "section",
      "title": "When not to handle self-assignment",
      "html": "<h3 class=\"cpp-section-heading\">When not to handle self-assignment</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-assignment-operator_b31",
      "type": "paragraph",
      "html": "<p>Typically the self-assignment check is skipped for copy constructors. Because the object being copy constructed is newly created, the only case where the newly created object can be equal to the object being copied is when you try to initialize a newly defined object with itself:</p>",
      "preview": "Typically the self-assignment check is skipped for copy constructors. Because th",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b32",
      "type": "code",
      "language": "cpp",
      "code": "someClass c { c };",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">someClass c { c };</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b33",
      "type": "paragraph",
      "html": "<p>In such cases, your compiler should warn you that <code>c</code> is an uninitialized variable.</p>",
      "preview": "In such cases, your compiler should warn you that c is an uninitialized variable",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b34",
      "type": "paragraph",
      "html": "<p>Second, the self-assignment check may be omitted in classes that can naturally handle self-assignment. Consider this Fraction class assignment operator that has a self-assignment guard:</p>",
      "preview": "Second, the self-assignment check may be omitted in classes that can naturally h",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b35",
      "type": "code",
      "language": "cpp",
      "code": "// A better implementation of operator=\nFraction& Fraction::operator= (const Fraction& fraction)\n{\n    // self-assignment guard\n    if (this == &fraction)\n        return *this;\n\n    // do the copy\n    m_numerator = fraction.m_numerator; // can handle self-assignment\n    m_denominator = fraction.m_denominator; // can handle self-assignment\n\n    // return the existing object so we can chain this operator\n    return *this;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">// A better implementation of operator=\nFraction&amp; Fraction::operator= (const Fraction&amp; fraction)\n{\n    // self-assignment guard\n    if (this == &amp;fraction)\n        return *this;\n\n    // do the copy\n    m_numerator = fraction.m_numerator; // can handle self-assignment\n    m_denominator = fraction.m_denominator; // can handle self-assignment\n\n    // return the existing object so we can chain this operator\n    return *this;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b36",
      "type": "paragraph",
      "html": "<p>If the self-assignment guard did not exist, this function would still operate correctly during a self-assignment (because all of the operations done by the function can handle self-assignment properly).</p>",
      "preview": "If the self-assignment guard did not exist, this function would still operate co",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b37",
      "type": "paragraph",
      "html": "<p>Because self-assignment is a rare event, some prominent C++ gurus recommend omitting the self-assignment guard even in classes that would benefit from it. We do not recommend this, as we believe it’s a better practice to code defensively and then selectively optimize later.</p>",
      "preview": "Because self-assignment is a rare event, some prominent C++ gurus recommend omit",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b38",
      "type": "section",
      "title": "The copy and swap idiom",
      "html": "<h3 class=\"cpp-section-heading\">The copy and swap idiom</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-assignment-operator_b39",
      "type": "paragraph",
      "html": "<p>A better way to handle self-assignment issues is via what’s called the copy and swap idiom. There’s a great writeup of how this idiom works <a href=\"https://stackoverflow.com/questions/3279543/what-is-the-copy-and-swap-idiom\">on Stack Overflow</a>.</p>",
      "preview": "A better way to handle self-assignment issues is via what’s called the copy and ",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b40",
      "type": "section",
      "title": "The implicit copy assignment operator",
      "html": "<h3 class=\"cpp-section-heading\">The implicit copy assignment operator</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-the-assignment-operator_b41",
      "type": "paragraph",
      "html": "<p>Unlike other operators, the compiler will provide an implicit public copy assignment operator for your class if you do not provide a user-defined one. This assignment operator does memberwise assignment (which is essentially the same as the memberwise initialization that default copy constructors do).</p>",
      "preview": "Unlike other operators, the compiler will provide an implicit public copy assign",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b42",
      "type": "paragraph",
      "html": "<p>Just like other constructors and operators, you can prevent assignments from being made by making your copy assignment operator private or using the delete keyword:</p>",
      "preview": "Just like other constructors and operators, you can prevent assignments from bei",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b43",
      "type": "code",
      "language": "cpp",
      "code": "#include <cassert>\n#include <iostream>\n\nclass Fraction\n{\nprivate:\n\tint m_numerator { 0 };\n\tint m_denominator { 1 };\n\npublic:\n    // Default constructor\n    Fraction(int numerator = 0, int denominator = 1)\n        : m_numerator { numerator }, m_denominator { denominator }\n    {\n        assert(denominator != 0);\n    }\n\n\t// Copy constructor\n\tFraction(const Fraction &copy) = delete;\n\n\t// Overloaded assignment\n\tFraction& operator= (const Fraction& fraction) = delete; // no copies through assignment!\n\n\tfriend std::ostream& operator<<(std::ostream& out, const Fraction& f1);\n        \n};\n\nstd::ostream& operator<<(std::ostream& out, const Fraction& f1)\n{\n\tout << f1.m_numerator << '/' << f1.m_denominator;\n\treturn out;\n}\n\nint main()\n{\n    Fraction fiveThirds { 5, 3 };\n    Fraction f;\n    f = fiveThirds; // compile error, operator= has been deleted\n    std::cout << f;\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt;\n#include &lt;iostream&gt;\n\nclass Fraction\n{\nprivate:\n\tint m_numerator { 0 };\n\tint m_denominator { 1 };\n\npublic:\n    // Default constructor\n    Fraction(int numerator = 0, int denominator = 1)\n        : m_numerator { numerator }, m_denominator { denominator }\n    {\n        assert(denominator != 0);\n    }\n\n\t// Copy constructor\n\tFraction(const Fraction &amp;copy) = delete;\n\n\t// Overloaded assignment\n\tFraction&amp; operator= (const Fraction&amp; fraction) = delete; // no copies through assignment!\n\n\tfriend std::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const Fraction&amp; f1);\n        \n};\n\nstd::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const Fraction&amp; f1)\n{\n\tout &lt;&lt; f1.m_numerator &lt;&lt; '/' &lt;&lt; f1.m_denominator;\n\treturn out;\n}\n\nint main()\n{\n    Fraction fiveThirds { 5, 3 };\n    Fraction f;\n    f = fiveThirds; // compile error, operator= has been deleted\n    std::cout &lt;&lt; f;\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b44",
      "type": "paragraph",
      "html": "<p>Note that if your class has const members, the compiler will instead define the implicit <code>operator=</code> as deleted. This is because const members can’t be assigned, so the compiler will assume your class should not be assignable.</p>",
      "preview": "Note that if your class has const members, the compiler will instead define the ",
      "canCheck": true
    },
    {
      "id": "overloading-the-assignment-operator_b45",
      "type": "paragraph",
      "html": "<p>If you want a class with const members to be assignable (for all members that aren’t const), you will need to explicitly overload <code>operator=</code> and manually assign each non-const member.</p>",
      "preview": "If you want a class with const members to be assignable (for all members that ar",
      "canCheck": true
    }
  ]
}