{
  "index": 352,
  "chapterId": "D",
  "number": "22.4",
  "title": "22.4 — std::string character access and conversion to C-style arrays",
  "url": "https://www.learncpp.com/cpp-tutorial/stdstring-character-access-and-conversion-to-c-style-arrays/",
  "slug": "stdstring-character-access-and-conversion-to-c-style-arrays",
  "scrapedAt": "2026-09-15T16:31:44.131Z",
  "totalBlocks": 11,
  "totalCheckpoints": 11,
  "blocks": [
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b0",
      "type": "paragraph",
      "html": "<p><strong>Character access</strong></p>",
      "preview": "Character access",
      "canCheck": true,
      "html_cs": "<p><strong>Přístup k postavám</strong></p>"
    },
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b1",
      "type": "paragraph",
      "html": "<p>There are two almost identical ways to access characters in a string. The easier to use and faster version is the overloaded operator[]:</p>",
      "preview": "There are two almost identical ways to access characters in a string. The easier",
      "canCheck": true,
      "html_cs": "<p>Existují dva téměř identické způsoby přístupu ke znakům v řetězci. Jednodušší a rychlejší verze je přetížený operátor[]:</p>"
    },
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b2",
      "type": "paragraph",
      "html": "<div class=\"cpp-table-wrapper\"><p></p><p></p><table class=\"cpp-table\"><tbody><tr><td><b>char&amp; string::operator[] (size_type nIndex)</b><br><b>const char&amp; string::operator[] (size_type nIndex) const</b><p></p><ul><li>Both of these functions return the character with index nIndex</li><li>Passing an invalid index results in undefined behavior</li><li>Because char&amp; is the return type, you can use this to edit characters in the array</li></ul><p>Sample code:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">std::string sSource{ \"abcdefg\" };\nstd::cout &lt;&lt; sSource[5] &lt;&lt; '\\n';\nsSource[5] = 'X';\nstd::cout &lt;&lt; sSource &lt;&lt; '\\n';</code></pre><p>Output:</p><pre>f\nabcdeXg\n</pre></td></tr></tbody></table></div>",
      "preview": "char& string::operator[] (size_type nIndex)const char& string::operator[] (size_",
      "canCheck": true,
      "html_cs": "<div class=\"cpp-table-wrapper\"><p></p><p></p><table class=\"cpp-table\"><tbody><tr><td><b>char& string::operator[] (size_type nIndex)</b><br><b>const char& string::operator[] (size_type nIndex funkce/vrátí znak const>Boul s indexem <b><p) nIndex</li><li>Předání neplatného indexu vede k nedefinovanému chování</li><li>Protože char& je návratový typ, můžete jej použít k úpravě znaků v poli</li></ul><p>Ukázkový kód:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-string\"}scourg{101}\nstd::cout << sZdroj[5] << '\\n';\nsSource[5] = 'X';\nstd::cout << sSource << '\\n';</code></pre><p>Výstup:</p><pre>f\nabcdeXg\n</pre></td></tr></tbody></table></div>"
    },
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b3",
      "type": "paragraph",
      "html": "<p>There is also a non-operator version. This version is slower since it uses exceptions to check if the nIndex is valid. If you are not sure whether nIndex is valid, you should use this version to access the array:</p>",
      "preview": "There is also a non-operator version. This version is slower since it uses excep",
      "canCheck": true,
      "html_cs": "<p>Existuje také verze bez operátora. Tato verze je pomalejší, protože používá výjimky ke kontrole platnosti nIndexu. Pokud si nejste jisti, zda je nIndex platný, měli byste pro přístup k poli použít tuto verzi:</p>"
    },
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b4",
      "type": "paragraph",
      "html": "<div class=\"cpp-table-wrapper\"><p></p><p></p><table class=\"cpp-table\"><tbody><tr><td><b>char&amp; string::at (size_type nIndex)</b><br><b>const char&amp; string::at (size_type nIndex) const</b><p></p><ul><li>Both of these functions return the character with index nIndex</li><li>Passing an invalid index results in an out_of_range exception</li><li>Because char&amp; is the return type, you can use this to edit characters in the array</li></ul><p>Sample code:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">std::string sSource{ \"abcdefg\" };\nstd::cout &lt;&lt; sSource.at(5) &lt;&lt; '\\n';\nsSource.at(5) = 'X';\nstd::cout &lt;&lt; sSource &lt;&lt; '\\n';</code></pre><p>Output:</p><pre>f\nabcdeXg\n</pre></td></tr></tbody></table></div>",
      "preview": "char& string::at (size_type nIndex)const char& string::at (size_type nIndex) con",
      "canCheck": true,
      "html_cs": "<div class=\"cpp-table-wrapper\"><p></p><p></p><table class=\"cpp-table\"><tbody><tr><td><b>char& string::at (size_type nIndex)</b><br><b>const char& string::at (size_type nIndex) const</b><p></p><ul index) const</b><p></p><ul nIndex</li><li>Předání neplatného indexu má za následek výjimku mimo rozsah</li><li>Protože char& je návratový typ, můžete jej použít k úpravě znaků v poli</li></ul><p>Ukázkový kód:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-cdef:string\"sabd \"st\nstd::cout << sSource.at(5) << '\\n';\nsSource.at(5) = 'X';\nstd::cout << sSource << '\\n';</code></pre><p>Výstup:</p><pre>f\nabcdeXg\n</pre></td></tr></tbody></table></div>"
    },
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b5",
      "type": "paragraph",
      "html": "<p><strong>Conversion to C-style arrays</strong></p>",
      "preview": "Conversion to C-style arrays",
      "canCheck": true,
      "html_cs": "<p><strong>Konverze na pole ve stylu C</strong></p>"
    },
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b6",
      "type": "paragraph",
      "html": "<p>Many functions (including all C functions) expect strings to be formatted as C-style strings rather than std::string. For this reason, std::string provides 3 different ways to convert std::string to C-style strings.</p>",
      "preview": "Many functions (including all C functions) expect strings to be formatted as C-s",
      "canCheck": true,
      "html_cs": "<p>Mnoho funkcí (včetně všech funkcí C) očekává, že řetězce budou formátovány jako řetězce ve stylu C, nikoli std::string. Z tohoto důvodu nabízí std::string 3 různé způsoby převodu std::string na řetězce ve stylu C.</p>"
    },
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b7",
      "type": "paragraph",
      "html": "<div class=\"cpp-table-wrapper\"><p></p><p></p><table class=\"cpp-table\"><tbody><tr><td><b>const char* string::c_str () const</b><p></p><ul><li>Returns the contents of the string as a const C-style string</li><li>A null terminator is appended</li><li>The C-style string is owned by the std::string and should not be deleted</li></ul><p>Sample code:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cstring&gt;\n\nstd::string sSource{ \"abcdefg\" };\nstd::cout &lt;&lt; std::strlen(sSource.c_str());</code></pre><p>Output:</p><pre>7\n</pre></td></tr></tbody></table></div>",
      "preview": "const char* string::c_str () constReturns the contents of the string as a const ",
      "canCheck": true,
      "html_cs": "<div class=\"cpp-table-wrapper\"><p></p><p></p><table class=\"cpp-table\"><tbody><tr><td><b>const char* string::c_str () const</b><p></p><ul><li>Vrátí obsah řetězce jako řetězec ve stylu const C</li><li>Aplikace je vlastněna null terminátorem C><li std::string a neměl by být smazán</li></ul><p>Ukázkový kód:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include <cstring>\n\nstd::string sSource{ \"abcdefg\" };\nstd::cout << std::strlen(sSource.c_str());</code></pre><p>Výstup:</p><pre>7\n</pre></td></tr></tbody></table></div>"
    },
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b8",
      "type": "paragraph",
      "html": "<div class=\"cpp-table-wrapper\"><p></p><p></p><table class=\"cpp-table\"><tbody><tr><td><b>const char* string::data () const</b><p></p><ul><li>Returns the contents of the string as a const C-style string</li><li>A null terminator is appended. This function performs the same action as <code>c_str()</code></li><li>The C-style string is owned by the std::string and should not be deleted</li></ul><p>Sample code:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cstring&gt;\n\nstd::string sSource{ \"abcdefg\" };\nconst char* szString{ \"abcdefg\" };\n// memcmp compares the first n characters of two C-style strings and returns 0 if they are equal\nif (std::memcmp(sSource.data(), szString, sSource.length()) == 0)\n    std::cout &lt;&lt; \"The strings are equal\";\nelse\n    std::cout &lt;&lt; \"The strings are not equal\";</code></pre><p>Output:</p><pre>The strings are equal\n</pre></td></tr></tbody></table></div>",
      "preview": "const char* string::data () constReturns the contents of the string as a const C",
      "canCheck": true,
      "html_cs": "<div class=\"cpp-table-wrapper\"><p></p><p></p><table class=\"cpp-table\"><tbody><tr><td><b>const char* string::data () const</b><p></p><ul><li>Vrátí obsah řetězce jako řetězec ve stylu const C</li><li>Připojen je ukončovací znak null. Tato funkce provádí stejnou akci jako <code>c_str()</code></li><li>Řetězec ve stylu C je vlastněn std::string a neměl by být smazán</li></ul><p>Ukázkový kód:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include <\n\nstd::string sSource{ \"abcdefg\" };\nconst char* szString{ \"abcdefg\" };\n// memcmp porovná prvních n znaků dvou řetězců ve stylu C a vrátí 0, pokud jsou stejné\nif (std::memcmp(sSource.data(), szString, sSource.length()) == 0)\n    std::cout << \"Řetězce jsou stejné\";\njinak\n    std::cout << \"Řetězce nejsou stejné\";</code></pre><p>Výstup:</p><pre>Řetězce jsou stejné\n</pre></td></tr></tbody></table></div>"
    },
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b9",
      "type": "paragraph",
      "html": "<div class=\"cpp-table-wrapper\"><p></p><p></p><table class=\"cpp-table\"><tbody><tr><td><b>size_type string::copy(char* szBuf, size_type nLength, size_type nIndex = 0) const</b><p></p><ul><li>Both flavors copy at most nLength characters of the string to szBuf, beginning with character nIndex</li><li>The number of characters copied is returned</li><li>No null is appended. It is up to the caller to ensure szBuf is initialized to NULL or terminate the string using the returned length</li><li>The caller is responsible for not overflowing szBuf</li></ul><p>Sample code:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">std::string sSource{ \"sphinx of black quartz, judge my vow\" };\n\nchar szBuf[20];\nint nLength{ static_cast&lt;int&gt;(sSource.copy(szBuf, 5, 10)) };\nszBuf[nLength] = '\\0';  // Make sure we terminate the string in the buffer\n\nstd::cout &lt;&lt; szBuf &lt;&lt; '\\n';</code></pre><p>Output:</p><pre>black\n</pre></td></tr></tbody></table></div>",
      "preview": "size_type string::copy(char* szBuf, size_type nLength, size_type nIndex = 0) con",
      "canCheck": true,
      "html_cs": "<div class=\"cpp-table-wrapper\"><p></p><p></p><table class=\"cpp-table\"><tbody><tr><td><b>size_type string::copy(char* szBuf, size_type nLength, size_type nIndex = 0) const</b><engp></p><ul><li>zkopírujte maximálně szL oba znaky od začátku příchuti se znakem nIndex</li><li>Je vrácen počet zkopírovaných znaků</li><li>Není připojena žádná hodnota null. Je na volajícím, aby zajistil, že szBuf je inicializován na hodnotu NULL nebo ukončí řetězec pomocí vrácené délky</li><li>Volající je odpovědný za to, aby nepřetečoval szBuf</li></ul><p>Ukázkový kód:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-string\">černá sphinx, soudce: mysfour: slib\" };\n\nchar szBuf[20];\nint nLength{ static_cast<int>(sSource.copy(szBuf, 5, 10)) };\nszBuf[nLength] = '\\0';  // Ujistěte se, že jsme ukončili řetězec ve vyrovnávací paměti\n\nstd::cout << szBuf << '\\n';</code></pre><p>Výstup:</p><pre>černý\n</pre></td></tr></tbody></table></div>"
    },
    {
      "id": "stdstring-character-access-and-conversion-to-c-style-arrays_b10",
      "type": "paragraph",
      "html": "<p>This function should be avoided where possible as it is relatively dangerous (as it is up to the caller to provide null-termination and avoid buffer overflows).</p>",
      "preview": "This function should be avoided where possible as it is relatively dangerous (as",
      "canCheck": true,
      "html_cs": "<p>Této funkci je třeba se vyhnout, kde je to možné, protože je relativně nebezpečná (protože je na volajícím, aby poskytl nulové ukončení a zabránil přetečení vyrovnávací paměti).</p>"
    }
  ],
  "title_cs": "22.4 — std::string character access and conversion to C-style arrays"
}