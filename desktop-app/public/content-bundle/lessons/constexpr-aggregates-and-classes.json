{
  "index": 199,
  "chapterId": "14",
  "number": "14.17",
  "title": "14.17 — Constexpr aggregates and classes",
  "url": "https://www.learncpp.com/cpp-tutorial/constexpr-aggregates-and-classes/",
  "slug": "constexpr-aggregates-and-classes",
  "scrapedAt": "2026-09-15T16:30:19.461Z",
  "totalBlocks": 62,
  "totalCheckpoints": 55,
  "blocks": [
    {
      "id": "constexpr-aggregates-and-classes_b0",
      "type": "paragraph",
      "html": "<p>In lesson <a href=\"https://www.learncpp.com/cpp-tutorial/constexpr-functions/\">F.1 -- Constexpr functions</a>, we covered constexpr functions, which are functions that may be evaluated at either compile-time or runtime. For example:</p>",
      "preview": "In lesson F.1 -- Constexpr functions, we covered constexpr functions, which are ",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b1",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nconstexpr int greater(int x, int y)\n{\n    return (x > y ? x : y);\n}\n\nint main()\n{\n    std::cout << greater(5, 6) << '\\n'; // greater(5, 6) may be evaluated at compile-time or runtime\n\n    constexpr int g { greater(5, 6) };  // greater(5, 6) must be evaluated at compile-time\n    std::cout << g << '\\n';             // prints 6\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nconstexpr int greater(int x, int y)\n{\n    return (x &gt; y ? x : y);\n}\n\nint main()\n{\n    std::cout &lt;&lt; greater(5, 6) &lt;&lt; '\\n'; // greater(5, 6) may be evaluated at compile-time or runtime\n\n    constexpr int g { greater(5, 6) };  // greater(5, 6) must be evaluated at compile-time\n    std::cout &lt;&lt; g &lt;&lt; '\\n';             // prints 6\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b2",
      "type": "paragraph",
      "html": "<p>In this example, <code>greater()</code> is a constexpr function, and <code>greater(5, 6)</code> is a constant expression, which may be evaluated at either compile-time or runtime. Because <code>std::cout &lt;&lt; greater(5, 6)</code> calls <code>greater(5, 6)</code> in a non-constexpr context, the compiler is free to choose whether to evaluate <code>greater(5, 6</code>) at compile-time or runtime. When <code>greater(5, 6)</code> is used to initialize constexpr variable <code>g</code>, <code>greater(5, 6)</code> is called in a constexpr context, and must be evaluated at compile-time.</p>",
      "preview": "In this example, greater() is a constexpr function, and greater(5, 6) is a const",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b3",
      "type": "paragraph",
      "html": "<p>Now consider the following similar example:</p>",
      "preview": "Now consider the following similar example:",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b4",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nstruct Pair\n{\n    int m_x {};\n    int m_y {};\n\n    int greater() const\n    {\n        return (m_x > m_y  ? m_x : m_y);\n    }\n};\n\nint main()\n{\n    Pair p { 5, 6 };                  // inputs are constexpr values\n    std::cout << p.greater() << '\\n'; // p.greater() evaluates at runtime\n\n    constexpr int g { p.greater() };  // compile error: greater() not constexpr\n    std::cout << g << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nstruct Pair\n{\n    int m_x {};\n    int m_y {};\n\n    int greater() const\n    {\n        return (m_x &gt; m_y  ? m_x : m_y);\n    }\n};\n\nint main()\n{\n    Pair p { 5, 6 };                  // inputs are constexpr values\n    std::cout &lt;&lt; p.greater() &lt;&lt; '\\n'; // p.greater() evaluates at runtime\n\n    constexpr int g { p.greater() };  // compile error: greater() not constexpr\n    std::cout &lt;&lt; g &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b5",
      "type": "paragraph",
      "html": "<p>In this version, we have an aggregate struct named <code>Pair</code>, and <code>greater()</code> is now a member function. However, because member function <code>greater()</code> is not constexpr, <code>p.greater()</code> is not a constant expression. When <code>std::cout &lt;&lt; p.greater()</code> calls <code>p.greater()</code> (in a non-constexpr context), <code>p.greater()</code> will be evaluated at runtime. However, when we try to use <code>p.greater()</code> to initialize constexpr variable <code>g</code>, we get a compile error, as <code>p.greater()</code> cannot be evaluated at compile-time.</p>",
      "preview": "In this version, we have an aggregate struct named Pair, and greater() is now a ",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b6",
      "type": "paragraph",
      "html": "<p>Since the inputs to <code>p</code> are constexpr values (<code>5</code> and <code>6</code>), it seems like <code>p.greater()</code> should be capable of being evaluated at compile-time. But how do we do that?</p>",
      "preview": "Since the inputs to p are constexpr values (5 and 6), it seems like p.greater() ",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b7",
      "type": "section",
      "title": "Constexpr member functions",
      "html": "<h3 class=\"cpp-section-heading\">Constexpr member functions</h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-aggregates-and-classes_b8",
      "type": "paragraph",
      "html": "<p>Just like non-member functions, member functions can be made constexpr via use of the <code>constexpr</code> keyword. Constexpr member functions can be evaluated at either compile-time or runtime.</p>",
      "preview": "Just like non-member functions, member functions can be made constexpr via use o",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b9",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nstruct Pair\n{\n    int m_x {};\n    int m_y {};\n\n    constexpr int greater() const // can evaluate at either compile-time or runtime\n    {\n        return (m_x > m_y  ? m_x : m_y);\n    }\n};\n\nint main()\n{\n    Pair p { 5, 6 };\n    std::cout << p.greater() << '\\n'; // okay: p.greater() evaluates at runtime\n\n    constexpr int g { p.greater() };  // compile error: p not constexpr\n    std::cout << g << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nstruct Pair\n{\n    int m_x {};\n    int m_y {};\n\n    constexpr int greater() const // can evaluate at either compile-time or runtime\n    {\n        return (m_x &gt; m_y  ? m_x : m_y);\n    }\n};\n\nint main()\n{\n    Pair p { 5, 6 };\n    std::cout &lt;&lt; p.greater() &lt;&lt; '\\n'; // okay: p.greater() evaluates at runtime\n\n    constexpr int g { p.greater() };  // compile error: p not constexpr\n    std::cout &lt;&lt; g &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b10",
      "type": "paragraph",
      "html": "<p>In this example, we’ve made <code>greater()</code> a constexpr function, so the compiler can evaluate it at either runtime or compile-time.</p>",
      "preview": "In this example, we’ve made greater() a constexpr function, so the compiler can ",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b11",
      "type": "paragraph",
      "html": "<p>When we call <code>p.greater()</code> in runtime expression <code>std::cout &lt;&lt; p.greater()</code>, it evaluates at runtime.</p>",
      "preview": "When we call p.greater() in runtime expression std::cout << p.greater(), it eval",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b12",
      "type": "paragraph",
      "html": "<p>However, when <code>p.greater()</code> is used to initialize constexpr variable <code>g</code>, we get a compiler error. Although <code>greater()</code> is now constexpr, <code>p</code> is still not constexpr, therefore <code>p.greater()</code> is not a constant expression.</p>",
      "preview": "However, when p.greater() is used to initialize constexpr variable g, we get a c",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b13",
      "type": "section",
      "title": "Constexpr aggregates",
      "html": "<h3 class=\"cpp-section-heading\">Constexpr aggregates</h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-aggregates-and-classes_b14",
      "type": "paragraph",
      "html": "<p>Okay, so if we need <code>p</code> to be constexpr, let’s just make it constexpr:</p>",
      "preview": "Okay, so if we need p to be constexpr, let’s just make it constexpr:",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b15",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nstruct Pair // Pair is an aggregate\n{\n    int m_x {};\n    int m_y {};\n\n    constexpr int greater() const\n    {\n        return (m_x > m_y  ? m_x : m_y);\n    }\n};\n\nint main()\n{\n    constexpr Pair p { 5, 6 };        // now constexpr\n    std::cout << p.greater() << '\\n'; // p.greater() evaluates at runtime or compile-time\n\n    constexpr int g { p.greater() };  // p.greater() must evaluate at compile-time\n    std::cout << g << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nstruct Pair // Pair is an aggregate\n{\n    int m_x {};\n    int m_y {};\n\n    constexpr int greater() const\n    {\n        return (m_x &gt; m_y  ? m_x : m_y);\n    }\n};\n\nint main()\n{\n    constexpr Pair p { 5, 6 };        // now constexpr\n    std::cout &lt;&lt; p.greater() &lt;&lt; '\\n'; // p.greater() evaluates at runtime or compile-time\n\n    constexpr int g { p.greater() };  // p.greater() must evaluate at compile-time\n    std::cout &lt;&lt; g &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b16",
      "type": "paragraph",
      "html": "<p>Since <code>Pair</code> is an aggregate, and aggregates implicitly support constexpr, we’re done. This works! Since <code>p</code> is a constexpr type, and <code>greater()</code> is a constexpr member function, <code>p.greater()</code> is a constant expression and can be used in places where only constant expressions are allowed.</p>",
      "preview": "Since Pair is an aggregate, and aggregates implicitly support constexpr, we’re d",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b17",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>We covered aggregates in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/struct-aggregate-initialization/\">13.8 -- Struct aggregate initialization</a>.</p></div>",
      "preview": "Related contentWe covered aggregates in lesson 13.8 -- Struct aggregate initialization.",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b18",
      "type": "section",
      "title": "Constexpr class objects and constexpr constructors",
      "html": "<h3 class=\"cpp-section-heading\">Constexpr class objects and constexpr constructors</h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-aggregates-and-classes_b19",
      "type": "paragraph",
      "html": "<p>Now let’s make our <code>Pair</code> a non-aggregate:</p>",
      "preview": "Now let’s make our Pair a non-aggregate:",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b20",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nclass Pair // Pair is no longer an aggregate\n{\nprivate:\n    int m_x {};\n    int m_y {};\n\npublic:\n    Pair(int x, int y): m_x { x }, m_y { y } {}\n\n    constexpr int greater() const\n    {\n        return (m_x > m_y  ? m_x : m_y);\n    }\n};\n\nint main()\n{\n    constexpr Pair p { 5, 6 };       // compile error: p is not a literal type\n    std::cout << p.greater() << '\\n';\n\n    constexpr int g { p.greater() };\n    std::cout << g << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nclass Pair // Pair is no longer an aggregate\n{\nprivate:\n    int m_x {};\n    int m_y {};\n\npublic:\n    Pair(int x, int y): m_x { x }, m_y { y } {}\n\n    constexpr int greater() const\n    {\n        return (m_x &gt; m_y  ? m_x : m_y);\n    }\n};\n\nint main()\n{\n    constexpr Pair p { 5, 6 };       // compile error: p is not a literal type\n    std::cout &lt;&lt; p.greater() &lt;&lt; '\\n';\n\n    constexpr int g { p.greater() };\n    std::cout &lt;&lt; g &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b21",
      "type": "paragraph",
      "html": "<p>This example is almost identical to the prior one, except <code>Pair</code> is no longer an aggregate (due to having private data members and a constructor).</p>",
      "preview": "This example is almost identical to the prior one, except Pair is no longer an a",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b22",
      "type": "paragraph",
      "html": "<p>When we compile this program, we get a compiler error about <code>Pair</code> not being a “literal type”. Say what?</p>",
      "preview": "When we compile this program, we get a compiler error about Pair not being a “li",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b23",
      "type": "paragraph",
      "html": "<p>In C++, a <strong>literal type</strong> is any type for which it might be possible to create an object within a constant expression. Put another way, an object can’t be constexpr unless the type qualifies as a literal type. And our non-aggregate <code>Pair</code> does not qualify.</p>",
      "preview": "In C++, a literal type is any type for which it might be possible to create an o",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b24",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Nomenclature</p><p>A literal and a literal type are distinct (but related) things. A literal is a constexpr value that is inserted into the source code. A literal type is a type that can be used as the type of a constexpr value. A literal always has a literal type. However, a value or object with a literal type need not be a literal.</p></div>",
      "preview": "NomenclatureA literal and a literal type are distinct (but related) things. A literal is a constexpr",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b25",
      "type": "paragraph",
      "html": "<p>The definition of a literal type is complex, and a summary can be found on <a href=\"https://en.cppreference.com/w/cpp/named_req/LiteralType\">cppreference</a>. However, it’s worth noting that literal types include:</p>",
      "preview": "The definition of a literal type is complex, and a summary can be found on cppre",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b26",
      "type": "list",
      "html": "<ul><li>Scalar types (those holding a single value, such as fundamental types and pointers)</li><li>Reference types</li><li>Most aggregates</li><li>Classes that have a constexpr constructor</li></ul>",
      "preview": "Scalar types (those holding a single value, such as fundamental types and pointe",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b27",
      "type": "paragraph",
      "html": "<p>And now we see why our <code>Pair</code> isn’t a literal type. When a class object is instantiated, the compiler will call the constructor function to initialize the object. And the constructor function in our <code>Pair</code> class is not constexpr, so it can’t be invoked at compile-time. Therefore, <code>Pair</code> objects cannot be constexpr.</p>",
      "preview": "And now we see why our Pair isn’t a literal type. When a class object is instant",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b28",
      "type": "paragraph",
      "html": "<p>The fix for this is simple: we just make our constructor <code>constexpr</code> as well:</p>",
      "preview": "The fix for this is simple: we just make our constructor constexpr as well:",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b29",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nclass Pair\n{\nprivate:\n    int m_x {};\n    int m_y {};\n\npublic:\n    constexpr Pair(int x, int y): m_x { x }, m_y { y } {} // now constexpr\n\n    constexpr int greater() const\n    {\n        return (m_x > m_y  ? m_x : m_y);\n    }\n};\n\nint main()\n{\n    constexpr Pair p { 5, 6 };\n    std::cout << p.greater() << '\\n';\n\n    constexpr int g { p.greater() };\n    std::cout << g << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nclass Pair\n{\nprivate:\n    int m_x {};\n    int m_y {};\n\npublic:\n    constexpr Pair(int x, int y): m_x { x }, m_y { y } {} // now constexpr\n\n    constexpr int greater() const\n    {\n        return (m_x &gt; m_y  ? m_x : m_y);\n    }\n};\n\nint main()\n{\n    constexpr Pair p { 5, 6 };\n    std::cout &lt;&lt; p.greater() &lt;&lt; '\\n';\n\n    constexpr int g { p.greater() };\n    std::cout &lt;&lt; g &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b30",
      "type": "paragraph",
      "html": "<p>This works as expected, just like our aggregate version of <code>Pair</code> did.</p>",
      "preview": "This works as expected, just like our aggregate version of Pair did.",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b31",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>If you want your class to be able to be evaluated at compile-time, make your member functions and constructor constexpr.</p></div>",
      "preview": "Best practiceIf you want your class to be able to be evaluated at compile-time, make your member fun",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b32",
      "type": "paragraph",
      "html": "<p>Implicitly defined constructors are constexpr if they can be defined as such. Explicitly defaulted constructors must be explicitly defined as constexpr.</p>",
      "preview": "Implicitly defined constructors are constexpr if they can be defined as such. Ex",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b33",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Tip</p><p>Constexpr is part of the interface of the class, and removing it later will break callers who are calling the function in a constant context.</p></div>",
      "preview": "TipConstexpr is part of the interface of the class, and removing it later will break callers who are",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b34",
      "type": "section",
      "title": "Constexpr members may be needed with non-constexpr/non-const objects",
      "html": "<h3 class=\"cpp-section-heading\">Constexpr members may be needed with non-constexpr/non-const objects</h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-aggregates-and-classes_b35",
      "type": "paragraph",
      "html": "<p>In the above example, since the initializer of constexpr variable <code>g</code> must be a constant expression, it’s clear that <code>p.greater()</code> must be a constant expression, and therefore <code>p</code>, the <code>Pair</code> constructor, and <code>greater()</code> must all be constexpr.</p>",
      "preview": "In the above example, since the initializer of constexpr variable g must be a co",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b36",
      "type": "paragraph",
      "html": "<p>However, if we replace <code>p.greater()</code> with a constexpr function, things get a little less obvious:</p>",
      "preview": "However, if we replace p.greater() with a constexpr function, things get a littl",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b37",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nclass Pair\n{\nprivate:\n    int m_x {};\n    int m_y {};\n\npublic:\n    constexpr Pair(int x, int y): m_x { x }, m_y { y } {}\n\n    constexpr int greater() const\n    {\n        return (m_x > m_y  ? m_x : m_y);\n    }\n};\n\nconstexpr int init()\n{\n    Pair p { 5, 6 };    // requires constructor to be constexpr when evaluated at compile-time\n    return p.greater(); // requires greater() to be constexpr when evaluated at compile-time\n}\n\nint main()\n{\n    constexpr int g { init() }; // init() evaluated in compile-time context\n    std::cout << g << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nclass Pair\n{\nprivate:\n    int m_x {};\n    int m_y {};\n\npublic:\n    constexpr Pair(int x, int y): m_x { x }, m_y { y } {}\n\n    constexpr int greater() const\n    {\n        return (m_x &gt; m_y  ? m_x : m_y);\n    }\n};\n\nconstexpr int init()\n{\n    Pair p { 5, 6 };    // requires constructor to be constexpr when evaluated at compile-time\n    return p.greater(); // requires greater() to be constexpr when evaluated at compile-time\n}\n\nint main()\n{\n    constexpr int g { init() }; // init() evaluated in compile-time context\n    std::cout &lt;&lt; g &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b38",
      "type": "paragraph",
      "html": "<p>Remember that a constexpr function can evaluate at either runtime or compile-time. And when a constexpr function evaluates at compile-time, it can only call functions capable of evaluating at compile-time. In the case of a class type, that means constexpr member functions.</p>",
      "preview": "Remember that a constexpr function can evaluate at either runtime or compile-tim",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b39",
      "type": "paragraph",
      "html": "<p>Since <code>g</code> is constexpr, <code>init()</code> must be evaluated at compile-time. Within the <code>init()</code> function, we define <code>p</code> as non-constexpr/non-const (because we can, not because we should). Even though <code>p</code> is not defined as constexpr, <code>p</code> still needs to be created at compile-time, and therefore requires a constexpr <code>Pair</code> constructor. Similarly, in order for <code>p.greater()</code> to evaluate at compile-time, <code>greater()</code> must be a constexpr member function. If either the <code>Pair</code> constructor or <code>greater()</code> were not constexpr, the compiler would error.</p>",
      "preview": "Since g is constexpr, init() must be evaluated at compile-time. Within the init(",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b40",
      "type": "callout",
      "calloutType": "insight",
      "html": "<div class=\"cpp-note cpp-lightbluebackground\"><p class=\"cpp-note-title cpp-bottomline\">Key insight</p><p>When a constexpr function is evaluating in a compile-time context, only constexpr functions can be called.</p></div>",
      "preview": "Key insightWhen a constexpr function is evaluating in a compile-time context, only constexpr functio",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b41",
      "type": "section",
      "title": "Constexpr member functions may be const or non-const C++14",
      "html": "<h3 class=\"cpp-section-heading\">Constexpr member functions may be const or non-const <span class=\"cpp-section-pill cpp-section-standard\">C++14</span></h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-aggregates-and-classes_b42",
      "type": "paragraph",
      "html": "<p>In C++11, non-static constexpr member functions are implicitly const (except constructors).</p>",
      "preview": "In C++11, non-static constexpr member functions are implicitly const (except con",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b43",
      "type": "paragraph",
      "html": "<p>However, as of C++14, constexpr member functions are no longer implicitly const. This means that if you want a constexpr function to be a const function, you must explicitly mark it as such.</p>",
      "preview": "However, as of C++14, constexpr member functions are no longer implicitly const.",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b44",
      "type": "section",
      "title": "Constexpr non-const member functions can change data members Optional",
      "html": "<h3 class=\"cpp-section-heading\">Constexpr non-const member functions can change data members <span class=\"cpp-section-pill cpp-section-optional\">Optional</span></h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-aggregates-and-classes_b45",
      "type": "paragraph",
      "html": "<p>A constexpr non-const member function can change data members of the class, so long as the implicit object isn’t const. This is true even if the function is evaluating at compile-time.</p>",
      "preview": "A constexpr non-const member function can change data members of the class, so l",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b46",
      "type": "paragraph",
      "html": "<p>Here’s a contrived example of this:</p>",
      "preview": "Here’s a contrived example of this:",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b47",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nclass Pair\n{\nprivate:\n    int m_x {};\n    int m_y {};\n\npublic:\n    constexpr Pair(int x, int y): m_x { x }, m_y { y } {}\n\n    constexpr int greater() const // constexpr and const\n    {\n        return (m_x > m_y  ? m_x : m_y);\n    }\n\n    constexpr void reset() // constexpr but non-const\n    {\n        m_x = m_y = 0; // non-const member function can change members\n    }\n\n    constexpr const int& getX() const { return m_x; }\n};\n\n// This function is constexpr\nconstexpr Pair zero()\n{\n    Pair p { 1, 2 }; // p is non-const\n    p.reset();       // okay to call non-const member function on non-const object\n    return p;\n}\n\nint main()\n{\n    Pair p1 { 3, 4 };\n    p1.reset();                     // okay to call non-const member function on non-const object\n    std::cout << p1.getX() << '\\n'; // prints 0\n    \n    Pair p2 { zero() };             // zero() will be evaluated at runtime\n    p2.reset();                     // okay to call non-const member function on non-const object\n    std::cout << p2.getX() << '\\n'; // prints 0\n\n    constexpr Pair p3 { zero() };   // zero() will be evaluated at compile-time\n//    p3.reset();                   // Compile error: can't call non-const member function on const object\n    std::cout << p3.getX() << '\\n'; // prints 0\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nclass Pair\n{\nprivate:\n    int m_x {};\n    int m_y {};\n\npublic:\n    constexpr Pair(int x, int y): m_x { x }, m_y { y } {}\n\n    constexpr int greater() const // constexpr and const\n    {\n        return (m_x &gt; m_y  ? m_x : m_y);\n    }\n\n    constexpr void reset() // constexpr but non-const\n    {\n        m_x = m_y = 0; // non-const member function can change members\n    }\n\n    constexpr const int&amp; getX() const { return m_x; }\n};\n\n// This function is constexpr\nconstexpr Pair zero()\n{\n    Pair p { 1, 2 }; // p is non-const\n    p.reset();       // okay to call non-const member function on non-const object\n    return p;\n}\n\nint main()\n{\n    Pair p1 { 3, 4 };\n    p1.reset();                     // okay to call non-const member function on non-const object\n    std::cout &lt;&lt; p1.getX() &lt;&lt; '\\n'; // prints 0\n    \n    Pair p2 { zero() };             // zero() will be evaluated at runtime\n    p2.reset();                     // okay to call non-const member function on non-const object\n    std::cout &lt;&lt; p2.getX() &lt;&lt; '\\n'; // prints 0\n\n    constexpr Pair p3 { zero() };   // zero() will be evaluated at compile-time\n//    p3.reset();                   // Compile error: can't call non-const member function on const object\n    std::cout &lt;&lt; p3.getX() &lt;&lt; '\\n'; // prints 0\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b48",
      "type": "paragraph",
      "html": "<p>As we work through this example, remember:</p>",
      "preview": "As we work through this example, remember:",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b49",
      "type": "list",
      "html": "<ul><li>A non-const member function can modify members of non-const objects.</li><li>A constexpr member function can be called in either runtime contexts or compile-time contexts.</li></ul>",
      "preview": "A non-const member function can modify members of non-const objects.A constexpr ",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b50",
      "type": "paragraph",
      "html": "<p>These two things work independently.</p>",
      "preview": "These two things work independently.",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b51",
      "type": "paragraph",
      "html": "<p>In the case of <code>p1</code>, <code>p1</code> is non-const. Therefore, we are allowed to call non-const member function <code>p1.reset()</code> to modify <code>p1</code>. The fact that <code>reset()</code> is constexpr doesn’t matter here because nothing we’re doing requires compile-time evaluation.</p>",
      "preview": "In the case of p1, p1 is non-const. Therefore, we are allowed to call non-const ",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b52",
      "type": "paragraph",
      "html": "<p>The <code>p2</code> case is similar. In this case, the initializer to <code>p2</code> is a function call to <code>zero()</code>. Even though <code>zero()</code> is a constexpr function, in this case it is invoked in a runtime context, and acts just like a normal function. Within <code>zero()</code>, we instantiate non-const <code>p</code>, call non-const member function <code>p.reset()</code> on it, and then return <code>p</code>. The returned <code>Pair</code> is used as the initializer for <code>p2</code>. The fact that <code>zero()</code> and <code>reset()</code> are constexpr don’t matter in this case, because nothing we’re doing requires compile-time evaluation.</p>",
      "preview": "The p2 case is similar. In this case, the initializer to p2 is a function call t",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b53",
      "type": "paragraph",
      "html": "<p>The <code>p3</code> case is the interesting one. Because <code>p3</code> is constexpr, it must have a constant expression initializer. Therefore, this call to <code>zero()</code> must evaluate at compile-time. And because we’re evaluating in a compile-time context, we can only call constexpr functions. Inside <code>zero()</code>, <code>p</code> is non-const (which is allowed, even though we’re evaluating at compile-time). However, because we’re in a compile-time context, the constructor used to create <code>p</code> must be constexpr. And just like the <code>p2</code> case, we’re allowed to call non-const member function <code>p.reset()</code> on non-const object <code>p</code>. But because we’re in a compile-time context, the <code>reset()</code> member function must be constexpr. The function then returns <code>p</code>, which is used to initialize <code>p3</code>.</p>",
      "preview": "The p3 case is the interesting one. Because p3 is constexpr, it must have a cons",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b54",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Author’s note</p><p>Yes, we used a non-const object to initialize a constexpr object. If this breaks your brain, it’s probably because you haven’t fully separated const from constexpr.</p><p>There is no requirement that a constexpr variable be initialized with a const value. It may seem that way because most of the time we initialize constexpr variable using literals (which are const) or other constexpr variables (which are implicitly const), and because the terms <code>const</code> and <code>constexpr</code> have similar names.</p><p>The requirement is actually that a constexpr variable be initialized with a constant expression. For functions (and operators), constexpr does not imply const, and constexpr functions (and operators) can make use of non-const objects and even return them.</p><p>The important thing isn’t the const, its that the compiler can determine the value of the object at compile-time. And in the case of constexpr functions, that’s possible even when they return a non-const object!</p></div>",
      "preview": "Author’s noteYes, we used a non-const object to initialize a constexpr object. If this breaks your b",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b55",
      "type": "section",
      "title": "Constexpr functions that return const references (or pointers) Optional",
      "html": "<h3 class=\"cpp-section-heading\">Constexpr functions that return const references (or pointers) <span class=\"cpp-section-pill cpp-section-optional\">Optional</span></h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-aggregates-and-classes_b56",
      "type": "paragraph",
      "html": "<p>Normally you won’t see <code>constexpr</code> and <code>const</code> used right next to each other, but one case where this does happen is when you have a constexpr member function that returns a const reference (or (const) pointer-to-const).</p>",
      "preview": "Normally you won’t see constexpr and const used right next to each other, but on",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b57",
      "type": "paragraph",
      "html": "<p>In our <code>Pair</code> class above, <code>getX()</code> is a constexpr member function that returns a const reference:</p>",
      "preview": "In our Pair class above, getX() is a constexpr member function that returns a co",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b58",
      "type": "code",
      "language": "cpp",
      "code": "    constexpr const int& getX() const { return m_x; }",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">    constexpr const int&amp; getX() const { return m_x; }</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b59",
      "type": "paragraph",
      "html": "<p>That’s a lot of const-ing!</p>",
      "preview": "That’s a lot of const-ing!",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b60",
      "type": "paragraph",
      "html": "<p>The <code>constexpr</code> indicates that the member function can be evaluated at compile-time. The <code>const int&amp;</code> is the return type of the function. The rightmost <code>const</code> means the member-function itself is const so it can be called on const objects.</p>",
      "preview": "The constexpr indicates that the member function can be evaluated at compile-tim",
      "canCheck": true
    },
    {
      "id": "constexpr-aggregates-and-classes_b61",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">As an aside…</p><p>A member function that returned a const pointer to const instead might look something like this:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">constexpr const int* const getXPtr() const { return &amp;m_x; }</code></pre><p>Isn’t it beautiful? No? Okay, fine.</p></div>",
      "preview": "As an aside…A member function that returned a const pointer to const instead might look something li",
      "canCheck": true
    }
  ]
}