{
  "index": 270,
  "chapterId": "21",
  "number": "21.x",
  "title": "21.x — Chapter 21 summary and quiz",
  "url": "https://www.learncpp.com/cpp-tutorial/chapter-21-summary-and-quiz/",
  "slug": "chapter-21-summary-and-quiz",
  "scrapedAt": "2026-09-15T16:30:56.726Z",
  "totalBlocks": 55,
  "totalCheckpoints": 49,
  "blocks": [
    {
      "id": "chapter-21-summary-and-quiz_b0",
      "type": "paragraph",
      "html": "<p>In this chapter, we explored topics related to operator overloading, as well as overloaded typecasts, and topics related to the copy constructor.</p>",
      "preview": "In this chapter, we explored topics related to operator overloading, as well as ",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b1",
      "type": "section",
      "title": "Summary",
      "html": "<h3 class=\"cpp-section-heading\">Summary</h3>",
      "canCheck": false
    },
    {
      "id": "chapter-21-summary-and-quiz_b2",
      "type": "paragraph",
      "html": "<p>Operator overloading is a variant of function overloading that lets you overload operators for your classes. When operators are overloaded, the intent of the operators should be kept as close to the original intent of the operators as possible. If the meaning of an operator when applied to a custom class is not clear and intuitive, use a named function instead.</p>",
      "preview": "Operator overloading is a variant of function overloading that lets you overload",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b3",
      "type": "paragraph",
      "html": "<p>Operators can be overloaded as a normal function, a friend function, or a member function. The following rules of thumb can help you determine which form is best for a given situation:</p>",
      "preview": "Operators can be overloaded as a normal function, a friend function, or a member",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b4",
      "type": "list",
      "html": "<ul><li>If you’re overloading assignment (=), subscript ([]), function call (()), or member selection (-&gt;), do so as a member function.</li><li>If you’re overloading a unary operator, do so as a member function.</li><li>If you’re overloading a binary operator that modifies its left operand (e.g. operator+=), do so as a member function if you can.</li><li>If you’re overloading a binary operator that does not modify its left operand (e.g. operator+), do so as a normal function or friend function.</li></ul>",
      "preview": "If you’re overloading assignment (=), subscript ([]), function call (()), or mem",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b5",
      "type": "paragraph",
      "html": "<p>Typecasts can be overloaded to provide conversion functions, which can be used to explicitly or implicitly convert your class into another type.</p>",
      "preview": "Typecasts can be overloaded to provide conversion functions, which can be used t",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b6",
      "type": "paragraph",
      "html": "<p>A copy constructor is a special type of constructor used to initialize an object from another object of the same type. Copy constructors are used for direct/uniform initialization from an object of the same type, copy initialization (Fraction f = Fraction(5,3)), and when passing or returning a parameter by value.</p>",
      "preview": "A copy constructor is a special type of constructor used to initialize an object",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b7",
      "type": "paragraph",
      "html": "<p>If you do not supply a copy constructor, the compiler will create one for you. Compiler-provided copy constructors will use memberwise initialization, meaning each member of the copy is initialized from the original member. The copy constructor may be elided for optimization purposes, even if it has side-effects, so do not rely on your copy constructor actually executing.</p>",
      "preview": "If you do not supply a copy constructor, the compiler will create one for you. C",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b8",
      "type": "paragraph",
      "html": "<p>Constructors are considered converting constructors by default, meaning that the compiler will use them to implicitly convert objects of other types into objects of your class. You can avoid this by using the explicit keyword in front of your constructor. You can also delete functions within your class, including the copy constructor and overloaded assignment operator if desired. This will cause a compiler error if a deleted function would be called.</p>",
      "preview": "Constructors are considered converting constructors by default, meaning that the",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b9",
      "type": "paragraph",
      "html": "<p>The assignment operator can be overloaded to allow assignment to your class. If you do not provide an overloaded assignment operator, the compiler will create one for you. Overloaded assignment operators should always include a self-assignment check (unless it’s handled naturally, or you’re using the copy and swap idiom).</p>",
      "preview": "The assignment operator can be overloaded to allow assignment to your class. If ",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b10",
      "type": "paragraph",
      "html": "<p>New programmers often mix up when the assignment operator vs copy constructor are used, but it’s fairly straightforward:</p>",
      "preview": "New programmers often mix up when the assignment operator vs copy constructor ar",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b11",
      "type": "list",
      "html": "<ul><li>If a new object has to be created before the copying can occur, the copy constructor is used (note: this includes passing or returning objects by value).</li><li>If a new object does not have to be created before the copying can occur, the assignment operator is used.</li></ul>",
      "preview": "If a new object has to be created before the copying can occur, the copy constru",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b12",
      "type": "paragraph",
      "html": "<p>By default, the copy constructor and assignment operators provided by the compiler do a memberwise initialization or assignment, which is a shallow copy. If your class dynamically allocates memory, this will likely lead to problems, as multiple objects will end up pointing to the same allocated memory. In this case, you’ll need to explicitly define these in order to do a deep copy. Even better, avoid doing your own memory management if you can and use classes from the standard library.</p>",
      "preview": "By default, the copy constructor and assignment operators provided by the compil",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b13",
      "type": "section",
      "title": "Quiz time",
      "html": "<h3 class=\"cpp-section-heading\">Quiz time</h3>",
      "canCheck": false
    },
    {
      "id": "chapter-21-summary-and-quiz_b14",
      "type": "quiz",
      "questionTitle": "Question #1",
      "questionHtml": "a) <code>point + point</code>",
      "hintHtml": "",
      "solutionHtml": "<code>operator=</code> must be implemented as a member function.",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b15",
      "type": "list",
      "html": "<ol start=\"1\"><li>Assuming <code>Point</code> is a class and <code>point</code> is an instance of that class, should you use a normal/friend or member function overload for the following operators?</li></ol>",
      "preview": "Assuming Point is a class and point is an instance of that class, should you use",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b16",
      "type": "paragraph",
      "html": "<p>b) <code>-point</code></p>",
      "preview": "b) -point",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b17",
      "type": "paragraph",
      "html": "<p>c) <code>std::cout &lt;&lt; point</code></p>",
      "preview": "c) std::cout << point",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b18",
      "type": "paragraph",
      "html": "<p>d) <code>point = 5;</code></p>",
      "preview": "d) point = 5;",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b19",
      "type": "quiz",
      "questionTitle": "Question #2",
      "questionHtml": "Write a class named Average that will keep track of the average of all integers passed to it. Use two members: The first one should be type <code>std::int32_t</code>, and used to keep track of the sum of all the numbers you’ve seen so far. The second should keep track of how many numbers you’ve seen so far. You can divide them to find your average.",
      "hintHtml": "",
      "solutionHtml": "<p><code>int</code> may be 16-bits on some platforms, which would mean our <code>Average</code> object could only have a maximum numerator value of 32,767. Using <code>std::int32_t</code> guarantees a 32-bit integral value, which gives us much more range to work with.</p>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b20",
      "type": "paragraph",
      "html": "<p>a) Write all of the functions necessary for the following program to run:</p>",
      "preview": "a) Write all of the functions necessary for the following program to run:",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b21",
      "type": "code",
      "language": "cpp",
      "code": "int main()\n{\n\tAverage avg{};\n\tstd::cout << avg << '\\n';\n\t\n\tavg += 4;\n\tstd::cout << avg << '\\n'; // 4 / 1 = 4\n\t\n\tavg += 8;\n\tstd::cout << avg << '\\n'; // (4 + 8) / 2 = 6\n\n\tavg += 24;\n\tstd::cout << avg << '\\n'; // (4 + 8 + 24) / 3 = 12\n\n\tavg += -10;\n\tstd::cout << avg << '\\n'; // (4 + 8 + 24 - 10) / 4 = 6.5\n\n\t(avg += 6) += 10; // 2 calls chained together\n\tstd::cout << avg << '\\n'; // (4 + 8 + 24 - 10 + 6 + 10) / 6 = 7\n\n\tAverage copy{ avg };\n\tstd::cout << copy << '\\n';\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int main()\n{\n\tAverage avg{};\n\tstd::cout &lt;&lt; avg &lt;&lt; '\\n';\n\t\n\tavg += 4;\n\tstd::cout &lt;&lt; avg &lt;&lt; '\\n'; // 4 / 1 = 4\n\t\n\tavg += 8;\n\tstd::cout &lt;&lt; avg &lt;&lt; '\\n'; // (4 + 8) / 2 = 6\n\n\tavg += 24;\n\tstd::cout &lt;&lt; avg &lt;&lt; '\\n'; // (4 + 8 + 24) / 3 = 12\n\n\tavg += -10;\n\tstd::cout &lt;&lt; avg &lt;&lt; '\\n'; // (4 + 8 + 24 - 10) / 4 = 6.5\n\n\t(avg += 6) += 10; // 2 calls chained together\n\tstd::cout &lt;&lt; avg &lt;&lt; '\\n'; // (4 + 8 + 24 - 10 + 6 + 10) / 6 = 7\n\n\tAverage copy{ avg };\n\tstd::cout &lt;&lt; copy &lt;&lt; '\\n';\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b22",
      "type": "paragraph",
      "html": "<p>and produce the result:</p>",
      "preview": "and produce the result:",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b23",
      "type": "output",
      "text": "0\n4\n6\n12\n6.5\n7\n7\n",
      "html": "<pre>0\n4\n6\n12\n6.5\n7\n7\n</pre>",
      "canCheck": false
    },
    {
      "id": "chapter-21-summary-and-quiz_b24",
      "type": "paragraph",
      "html": "<p>b) Should you provide a user-defined copy constructor or assignment operator for this class?</p>",
      "preview": "b) Should you provide a user-defined copy constructor or assignment operator for",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b25",
      "type": "paragraph",
      "html": "<p>c) Why use <code>std::int32_t</code> instead of <code>int</code>?</p>",
      "preview": "c) Why use std::int32_t instead of int?",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b26",
      "type": "quiz",
      "questionTitle": "Question #3",
      "questionHtml": "Write your own integer array class named <code>IntArray</code> from scratch (do not use std::array or std::vector). Users should pass in the size of the array when it is created, and the array should be dynamically allocated. Use assert statements to guard against bad data. Create any constructors or overloaded operators needed to make the following program operate correctly:",
      "hintHtml": "",
      "solutionHtml": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;cassert&gt; // for assert\n\nclass IntArray\n{\nprivate:\n\tint m_length{ 0 };\n\tint* m_array{ nullptr };\n\npublic:\n\texplicit IntArray(int length)\n\t\t: m_length{ length }\n\t{\n\t\tassert(length &gt; 0 &amp;&amp; \"IntArray length should be a positive integer\");\n\n\t\tm_array = new int[static_cast&lt;std::size_t&gt;(m_length)] {};\n\t}\n\n\t// Copy constructor that does a deep copy\n\tIntArray(const IntArray&amp; array)\n\t\t: m_length{ array.m_length }\n\t{\n\t\t// Allocate a new array\n\t\tm_array = new int[static_cast&lt;std::size_t&gt;(m_length)] {};\n\n\t\t// Copy elements from original array to new array\n\t\tfor (int count{ 0 }; count &lt; array.m_length; ++count)\n\t\t\tm_array[count] = array.m_array[count];\n\t}\n\n\t~IntArray()\n\t{\n\t\tdelete[] m_array;\n\t}\n\n\t// If you're getting crazy values here you probably forgot to do a deep copy in your copy constructor\n\tfriend std::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const IntArray&amp; array)\n\t{\n\t\tfor (int count{ 0 }; count &lt; array.m_length; ++count)\n\t\t{\n\t\t\tout &lt;&lt; array.m_array[count] &lt;&lt; ' ';\n\t\t}\n\t\treturn out;\n\t}\n\n\tint&amp; operator[] (const int index)\n\t{\n\t\tassert(index &gt;= 0);\n\t\tassert(index &lt; m_length);\n\t\treturn m_array[index];\n\t}\n\n\t// Assignment operator that does a deep copy\n\tIntArray&amp; operator= (const IntArray&amp; array)\n\t{\n\t\t// self-assignment guard\n\t\tif (this == &amp;array)\n\t\t\treturn *this;\n\n\t\t// If this array already exists, delete it so we don't leak memory\n\t\tdelete[] m_array;\n\n\t\tm_length = array.m_length;\n\n\t\t// Allocate a new array\n\t\tm_array = new int[static_cast&lt;std::size_t&gt;(m_length)] {};\n\n\t\t// Copy elements from original array to new array\n\t\tfor (int count{ 0 }; count &lt; array.m_length; ++count)\n\t\t\tm_array[count] = array.m_array[count];\n\n\t\treturn *this;\n\t}\n\n};\n\nIntArray fillArray()\n{\n\tIntArray a(5);\n\ta[0] = 5;\n\ta[1] = 8;\n\ta[2] = 2;\n\ta[3] = 3;\n\ta[4] = 6;\n\n\treturn a;\n}\n\nint main()\n{\n\tIntArray a{ fillArray() };\n\n\t// If you're getting crazy values here you probably forgot to do a deep copy in your copy constructor\n\tstd::cout &lt;&lt; a &lt;&lt; '\\n';\n\n\tauto&amp; ref{ a }; // we're using this reference to avoid compiler self-assignment errors\n\ta = ref;\n\n\tIntArray b(1);\n\tb = a;\n\n\ta[4] = 7; // change value in a, b should not change\n\n\t// If you're getting crazy values here you probably forgot to do self-assignment check\n\t// If b ends in 7, you probably forgot to do a deep copy in your copy assignment\n\tstd::cout &lt;&lt; b &lt;&lt; '\\n';\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b27",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nIntArray fillArray()\n{\n\tIntArray a(5);\n\n\ta[0] = 5;\n\ta[1] = 8;\n\ta[2] = 2;\n\ta[3] = 3;\n\ta[4] = 6;\n\n\treturn a;\n}\n\nint main()\n{\n\tIntArray a{ fillArray() };\n\n\tstd::cout << a << '\\n';\n\n\tauto& ref{ a }; // we're using this reference to avoid compiler self-assignment errors\n\ta = ref;\n\n\tIntArray b(1);\n\tb = a;\n\n\ta[4] = 7;\n\n\tstd::cout << b << '\\n';\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nIntArray fillArray()\n{\n\tIntArray a(5);\n\n\ta[0] = 5;\n\ta[1] = 8;\n\ta[2] = 2;\n\ta[3] = 3;\n\ta[4] = 6;\n\n\treturn a;\n}\n\nint main()\n{\n\tIntArray a{ fillArray() };\n\n\tstd::cout &lt;&lt; a &lt;&lt; '\\n';\n\n\tauto&amp; ref{ a }; // we're using this reference to avoid compiler self-assignment errors\n\ta = ref;\n\n\tIntArray b(1);\n\tb = a;\n\n\ta[4] = 7;\n\n\tstd::cout &lt;&lt; b &lt;&lt; '\\n';\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b28",
      "type": "paragraph",
      "html": "<p>This program should print:</p>",
      "preview": "This program should print:",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b29",
      "type": "output",
      "text": "5 8 2 3 6\n5 8 2 3 6\n",
      "html": "<pre>5 8 2 3 6\n5 8 2 3 6\n</pre>",
      "canCheck": false
    },
    {
      "id": "chapter-21-summary-and-quiz_b30",
      "type": "quiz",
      "questionTitle": "Question #4",
      "questionHtml": "Extra credit: This one is a little more tricky.",
      "hintHtml": "",
      "solutionHtml": "",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b31",
      "type": "paragraph",
      "html": "<p>A floating point number is a number with a decimal where the number of digits after the decimal can be variable. A fixed point number is a number with a fractional component where the number of digits in the fractional portion is fixed.</p>",
      "preview": "A floating point number is a number with a decimal where the number of digits af",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b32",
      "type": "paragraph",
      "html": "<p>In this quiz, we’re going to write a class to implement a fixed point number with two fractional digits (e.g. 12.34, 3.00, or 1278.99). Assume that the range of the class should be -32768.99 to 32767.99, that the fractional component should hold any two digits, that we don’t want precision errors, and that we want to conserve space.</p>",
      "preview": "In this quiz, we’re going to write a class to implement a fixed point number wit",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b33",
      "type": "quiz",
      "questionTitle": "> Step #1",
      "questionHtml": "What type of member variable(s) do you think we should use to implement our fixed point number with 2 digits after the decimal? (Make sure you read the answer before proceeding with the next questions)",
      "hintHtml": "",
      "solutionHtml": "<p>There are many different ways to implement a fixed point number. Because a fixed point number is essentially a subcase of a floating point number (where the number of digits after the decimal is fixed instead of variable), using a floating point number might seem like an obvious choice. But floating point numbers have precision issues. With a fixed number of decimal digits, we can reasonably enumerate all the possible fractional values (in our case, .00 to .99), so using a data type that has precision issues isn’t the best choice.</p><p>A better solution would be to use a 16-bit signed integer to hold the non-fractional part of the number, and an 8-bit signed integer to hold the fractional component.</p>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b34",
      "type": "quiz",
      "questionTitle": "> Step #2",
      "questionHtml": "Write a class named <code>FixedPoint2</code> that implements the recommended solution from the previous question. If either (or both) of the non-fractional and fractional part of the number are negative, the number should be treated as negative. Provide the overloaded operators and constructors required for the following program to run. For now, don’t worry about the fractional portion being out of bounds (&gt;99 or &lt;-99).",
      "hintHtml": "",
      "solutionHtml": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt;\n#include &lt;cstdint&gt; // for fixed width integers\n#include &lt;iostream&gt;\n\nclass FixedPoint2\n{\nprivate:\n\tstd::int16_t m_base{}; // here's our non-fractional part\n\tstd::int8_t m_decimal{}; // here's our factional part\n\npublic:\n\tFixedPoint2(std::int16_t base = 0, std::int8_t decimal = 0)\n\t\t: m_base{ base }, m_decimal{ decimal }\n\t{\n\t\t// If either (or both) of the non-fractional and fractional part of the number are negative, the number should be treated as negative\n\t\tif (m_base &lt; 0 || m_decimal &lt; 0)\n\t\t{\n\t\t\t// Make sure base is negative\n\t\t\tif (m_base &gt; 0)\n\t\t\t\tm_base = -m_base;\n\t\t\t// Make sure decimal is negative\n\t\t\tif (m_decimal &gt; 0)\n\t\t\t\tm_decimal = -m_decimal;\n\t\t}\n\t}\n\n\texplicit operator double() const\n\t{\n\t\treturn m_base + (static_cast&lt;double&gt;(m_decimal) / 100);\n\t}\n};\n\n// This doesn't require access to the internals of the class, so it can be defined outside the class\nstd::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const FixedPoint2&amp; fp)\n{\n\tout &lt;&lt; static_cast&lt;double&gt;(fp);\n\treturn out;\n}\n\nint main()\n{\n\tFixedPoint2 a{ 34, 56 };\n\tstd::cout &lt;&lt; a &lt;&lt; '\\n';\n\tstd::cout &lt;&lt; static_cast&lt;double&gt;(a) &lt;&lt; '\\n';\n\tassert(static_cast&lt;double&gt;(a) == 34.56);\n\n\tFixedPoint2 b{ -2, 8 };\n\tassert(static_cast&lt;double&gt;(b) == -2.08);\n\n\tFixedPoint2 c{ 2, -8 };\n\tassert(static_cast&lt;double&gt;(c) == -2.08);\n\n\tFixedPoint2 d{ -2, -8 };\n\tassert(static_cast&lt;double&gt;(d) == -2.08);\n\n\tFixedPoint2 e{ 0, -5 };\n\tassert(static_cast&lt;double&gt;(e) == -0.05);\n\n\tFixedPoint2 f{ 0, 10 };\n\tassert(static_cast&lt;double&gt;(f) == 0.1);\n    \n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b35",
      "type": "code",
      "language": "cpp",
      "code": "#include <cassert>\n#include <iostream>\n\nint main()\n{\n\tFixedPoint2 a{ 34, 56 };\n\tstd::cout << a << '\\n';\n\tstd::cout << static_cast<double>(a) << '\\n';\n\tassert(static_cast<double>(a) == 34.56);\n\n\tFixedPoint2 b{ -2, 8 };\n\tassert(static_cast<double>(b) == -2.08);\n\n\tFixedPoint2 c{ 2, -8 };\n\tassert(static_cast<double>(c) == -2.08);\n\n\tFixedPoint2 d{ -2, -8 };\n\tassert(static_cast<double>(d) == -2.08);\n\n\tFixedPoint2 e{ 0, -5 };\n\tassert(static_cast<double>(e) == -0.05);\n\n\tFixedPoint2 f{ 0, 10 };\n\tassert(static_cast<double>(f) == 0.1);\n    \n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt;\n#include &lt;iostream&gt;\n\nint main()\n{\n\tFixedPoint2 a{ 34, 56 };\n\tstd::cout &lt;&lt; a &lt;&lt; '\\n';\n\tstd::cout &lt;&lt; static_cast&lt;double&gt;(a) &lt;&lt; '\\n';\n\tassert(static_cast&lt;double&gt;(a) == 34.56);\n\n\tFixedPoint2 b{ -2, 8 };\n\tassert(static_cast&lt;double&gt;(b) == -2.08);\n\n\tFixedPoint2 c{ 2, -8 };\n\tassert(static_cast&lt;double&gt;(c) == -2.08);\n\n\tFixedPoint2 d{ -2, -8 };\n\tassert(static_cast&lt;double&gt;(d) == -2.08);\n\n\tFixedPoint2 e{ 0, -5 };\n\tassert(static_cast&lt;double&gt;(e) == -0.05);\n\n\tFixedPoint2 f{ 0, 10 };\n\tassert(static_cast&lt;double&gt;(f) == 0.1);\n    \n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b36",
      "type": "paragraph",
      "html": "<p>This program should produce the result:</p>",
      "preview": "This program should produce the result:",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b37",
      "type": "output",
      "text": "34.56\n34.56\n",
      "html": "<pre>34.56\n34.56\n</pre>",
      "canCheck": false
    },
    {
      "id": "chapter-21-summary-and-quiz_b38",
      "type": "paragraph",
      "html": "<p>Hint: To output your number, static_cast it to a double.</p>",
      "preview": "Hint: To output your number, static_cast it to a double.",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b39",
      "type": "quiz",
      "questionTitle": "> Step #3",
      "questionHtml": "Now let’s handle the case where the fractional portion is out of bounds. We have two reasonable strategies here:",
      "hintHtml": "",
      "solutionHtml": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt;\n#include &lt;cstdint&gt; // for fixed width integers\n#include &lt;iostream&gt;\n\nclass FixedPoint2\n{\nprivate:\n\tstd::int16_t m_base{}; // here's our non-fractional part\n\tstd::int8_t m_decimal{}; // here's our factional part\n\npublic:\n\tFixedPoint2(std::int16_t base = 0, std::int8_t decimal = 0)\n\t\t: m_base{ base }, m_decimal{ decimal }\n\t{\n\t\t// If either (or both) of the non-fractional and fractional part of the number are negative, the number should be treated as negative\n\t\tif (m_base &lt; 0 || m_decimal &lt; 0)\n\t\t{\n\t\t\t// Make sure base is negative\n\t\t\tif (m_base &gt; 0)\n\t\t\t\tm_base = -m_base;\n\t\t\t// Make sure decimal is negative\n\t\t\tif (m_decimal &gt; 0)\n\t\t\t\tm_decimal = -m_decimal;\n\t\t}\n\n\t\t// If decimal is out of bounds (in either direction),\n\t\t// adjust the decimal so it's in bounds,\n\t\t// and adjust base to account for the number of units removed from the decimal\n\t\t// h/t to reader David Pinheiro for simplifying this\n\t\tm_base += m_decimal / 100;    // integer division\n\t\tm_decimal = m_decimal % 100;  // integer remainder\n\t}\n\n\texplicit operator double() const\n\t{\n\t\treturn m_base + (static_cast&lt;double&gt;(m_decimal) / 100);\n\t}\n\n\tfriend bool testDecimal(const FixedPoint2 &amp;fp);\n};\n\n// This doesn't require access to the internals of the class, so it can be defined outside the class\nstd::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const FixedPoint2&amp; fp)\n{\n\tout &lt;&lt; static_cast&lt;double&gt;(fp);\n\treturn out;\n}\n\n// You will need to make testDecimal a friend of FixedPoint2\n// so the function can access the private members of FixedPoint2\nbool testDecimal(const FixedPoint2 &amp;fp)\n{\n\tif (fp.m_base &gt;= 0)\n\t\treturn fp.m_decimal &gt;= 0 &amp;&amp; fp.m_decimal &lt; 100;\n\telse\n\t\treturn fp.m_decimal &lt;= 0 &amp;&amp; fp.m_decimal &gt; -100;\n}\n\nint main()\n{\n\tFixedPoint2 a{ 1, 104 };\n\tstd::cout &lt;&lt; a &lt;&lt; '\\n';\n\tstd::cout &lt;&lt; static_cast&lt;double&gt;(a) &lt;&lt; '\\n';\n\tassert(static_cast&lt;double&gt;(a) == 2.04);\n\tassert(testDecimal(a));\n\n\tFixedPoint2 b{ 1, -104 };\n\tassert(static_cast&lt;double&gt;(b) == -2.04);\n\tassert(testDecimal(b));\n\n\tFixedPoint2 c{ -1, 104 };\n\tassert(static_cast&lt;double&gt;(c) == -2.04);\n\tassert(testDecimal(c));\n\n\tFixedPoint2 d{ -1, -104 };\n\tassert(static_cast&lt;double&gt;(d) == -2.04);\n\tassert(testDecimal(d));\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b40",
      "type": "list",
      "html": "<ul><li>Clamp the fraction portion (if &gt;99, set to 99).</li><li>Treat overflow as relevant (if &gt;99, reduce by 100 and add 1 to base).</li></ul>",
      "preview": "Clamp the fraction portion (if >99, set to 99).Treat overflow as relevant (if >9",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b41",
      "type": "paragraph",
      "html": "<p>In this exercise, we’ll treat decimal overflow as relevant, as this will be useful in the next step.</p>",
      "preview": "In this exercise, we’ll treat decimal overflow as relevant, as this will be usef",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b42",
      "type": "paragraph",
      "html": "<p>The following should run:</p>",
      "preview": "The following should run:",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b43",
      "type": "code",
      "language": "cpp",
      "code": "#include <cassert>\n#include <iostream>\n\n// You will need to make testDecimal a friend of FixedPoint2\n// so the function can access the private members of FixedPoint2\nbool testDecimal(const FixedPoint2 &fp)\n{\n    if (fp.m_base >= 0)\n        return fp.m_decimal >= 0 && fp.m_decimal < 100;\n    else\n        return fp.m_decimal <= 0 && fp.m_decimal > -100;\n}\n\nint main()\n{\n\tFixedPoint2 a{ 1, 104 };\n\tstd::cout << a << '\\n';\n\tstd::cout << static_cast<double>(a) << '\\n';\n\tassert(static_cast<double>(a) == 2.04);\n\tassert(testDecimal(a));\n\n\tFixedPoint2 b{ 1, -104 };\n\tassert(static_cast<double>(b) == -2.04);\n\tassert(testDecimal(b));\n\n\tFixedPoint2 c{ -1, 104 };\n\tassert(static_cast<double>(c) == -2.04);\n\tassert(testDecimal(c));\n\n\tFixedPoint2 d{ -1, -104 };\n\tassert(static_cast<double>(d) == -2.04);\n\tassert(testDecimal(d));\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt;\n#include &lt;iostream&gt;\n\n// You will need to make testDecimal a friend of FixedPoint2\n// so the function can access the private members of FixedPoint2\nbool testDecimal(const FixedPoint2 &amp;fp)\n{\n    if (fp.m_base &gt;= 0)\n        return fp.m_decimal &gt;= 0 &amp;&amp; fp.m_decimal &lt; 100;\n    else\n        return fp.m_decimal &lt;= 0 &amp;&amp; fp.m_decimal &gt; -100;\n}\n\nint main()\n{\n\tFixedPoint2 a{ 1, 104 };\n\tstd::cout &lt;&lt; a &lt;&lt; '\\n';\n\tstd::cout &lt;&lt; static_cast&lt;double&gt;(a) &lt;&lt; '\\n';\n\tassert(static_cast&lt;double&gt;(a) == 2.04);\n\tassert(testDecimal(a));\n\n\tFixedPoint2 b{ 1, -104 };\n\tassert(static_cast&lt;double&gt;(b) == -2.04);\n\tassert(testDecimal(b));\n\n\tFixedPoint2 c{ -1, 104 };\n\tassert(static_cast&lt;double&gt;(c) == -2.04);\n\tassert(testDecimal(c));\n\n\tFixedPoint2 d{ -1, -104 };\n\tassert(static_cast&lt;double&gt;(d) == -2.04);\n\tassert(testDecimal(d));\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b44",
      "type": "paragraph",
      "html": "<p>And produce the output:</p>",
      "preview": "And produce the output:",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b45",
      "type": "output",
      "text": "2.04\n2.04\n",
      "html": "<pre>2.04\n2.04\n</pre>",
      "canCheck": false
    },
    {
      "id": "chapter-21-summary-and-quiz_b46",
      "type": "quiz",
      "questionTitle": "> Step #4",
      "questionHtml": "Now add a constructor that takes a double. The follow program should run:",
      "hintHtml": "Hint: You can round a number (on the left of the decimal) by using the <code>std::round()</code> function (included in header <code>&lt;cmath&gt;</code>), and take the floor of the number (towards zero) using <code>std::trunc()</code>.",
      "solutionHtml": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt;\n#include &lt;cmath&gt;\n#include &lt;cstdint&gt; // for fixed width integers\n#include &lt;iostream&gt;\n\nclass FixedPoint2\n{\nprivate:\n\tstd::int16_t m_base{}; // here's our non-fractional part\n\tstd::int8_t m_decimal{}; // here's our factional part\n\npublic:\n\tFixedPoint2(std::int16_t base = 0, std::int8_t decimal = 0)\n\t\t: m_base{ base }, m_decimal{ decimal }\n\t{\n\t\t// If either (or both) of the non-fractional and fractional part of the number are negative, the number should be treated as negative\n\t\tif (m_base &lt; 0 || m_decimal &lt; 0)\n\t\t{\n\t\t\t// Make sure base is negative\n\t\t\tif (m_base &gt; 0)\n\t\t\t\tm_base = -m_base;\n\t\t\t// Make sure decimal is negative\n\t\t\tif (m_decimal &gt; 0)\n\t\t\t\tm_decimal = -m_decimal;\n\t\t}\n\n\t\t// If decimal is out of bounds (in either direction),\n\t\t// adjust the decimal so it's in bounds,\n\t\t// and adjust base to account for the number of units removed from the decimal\n\t\t// h/t to reader David Pinheiro for simplifying this\n\t\tm_base += m_decimal / 100;    // integer division\n\t\tm_decimal = m_decimal % 100;  // integer remainder\n\t}\n\n\t// We'll delegate to the prior constructor so we don't have to duplicate the negative number and overflow handling logic\n\tFixedPoint2(double d) :\n\t\tFixedPoint2(\n\t\t\tstatic_cast&lt;std::int16_t&gt;(std::trunc(d)),\n\t\t\tstatic_cast&lt;std::int8_t&gt;(std::round(d * 100) - std::trunc(d) * 100)\n\t\t)\n\t{\n\t}\n\n\texplicit operator double() const\n\t{\n\t\treturn m_base + (static_cast&lt;double&gt;(m_decimal) / 100);\n\t}\n};\n\n// This doesn't require access to the internals of the class, so it can be defined outside the class\nstd::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const FixedPoint2&amp; fp)\n{\n\tout &lt;&lt; static_cast&lt;double&gt;(fp);\n\treturn out;\n}\n\nint main()\n{\n\tFixedPoint2 a{ 0.01 };\n\tstd::cout &lt;&lt; a &lt;&lt; '\\n';\n\tassert(static_cast&lt;double&gt;(a) == 0.01);\n\n\tFixedPoint2 b{ -0.01 };\n\tassert(static_cast&lt;double&gt;(b) == -0.01);\n\n\tFixedPoint2 c{ 1.9 }; // make sure we handle single digit decimal\n\tassert(static_cast&lt;double&gt;(c) == 1.9);\n    \n\tFixedPoint2 d{ 5.01 }; // stored as 5.0099999... so we'll need to round this\n\tassert(static_cast&lt;double&gt;(d) == 5.01);\n\n\tFixedPoint2 e{ -5.01 }; // stored as -5.0099999... so we'll need to round this\n\tassert(static_cast&lt;double&gt;(e) == -5.01);\n\n\t// Handle case where the argument's decimal rounds to 100 (need to increase base by 1)\n\tFixedPoint2 f { 106.9978 }; // should be stored with base 107 and decimal 0\n\tassert(static_cast&lt;double&gt;(f) == 107.0);\n\n\t// Handle case where the argument's decimal rounds to -100 (need to decrease base by 1)\n\tFixedPoint2 g { -106.9978 }; // should be stored with base -107 and decimal 0\n\tassert(static_cast&lt;double&gt;(g) == -107.0);\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b47",
      "type": "code",
      "language": "cpp",
      "code": "#include <cassert>\n#include <iostream>\n\nint main()\n{\n\tFixedPoint2 a{ 0.01 };\n\tassert(static_cast<double>(a) == 0.01);\n\n\tFixedPoint2 b{ -0.01 };\n\tassert(static_cast<double>(b) == -0.01);\n\n\tFixedPoint2 c{ 1.9 }; // make sure we handle single digit decimal\n\tassert(static_cast<double>(c) == 1.9);\n    \n\tFixedPoint2 d{ 5.01 }; // stored as 5.0099999... so we'll need to round this\n\tassert(static_cast<double>(d) == 5.01);\n\n\tFixedPoint2 e{ -5.01 }; // stored as -5.0099999... so we'll need to round this\n\tassert(static_cast<double>(e) == -5.01);\n\n\t// Handle case where the argument's decimal rounds to 100 (need to increase base by 1)\n\tFixedPoint2 f { 106.9978 }; // should be stored with base 107 and decimal 0\n\tassert(static_cast<double>(f) == 107.0);\n\n\t// Handle case where the argument's decimal rounds to -100 (need to decrease base by 1)\n\tFixedPoint2 g { -106.9978 }; // should be stored with base -107 and decimal 0\n\tassert(static_cast<double>(g) == -107.0);\n\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt;\n#include &lt;iostream&gt;\n\nint main()\n{\n\tFixedPoint2 a{ 0.01 };\n\tassert(static_cast&lt;double&gt;(a) == 0.01);\n\n\tFixedPoint2 b{ -0.01 };\n\tassert(static_cast&lt;double&gt;(b) == -0.01);\n\n\tFixedPoint2 c{ 1.9 }; // make sure we handle single digit decimal\n\tassert(static_cast&lt;double&gt;(c) == 1.9);\n    \n\tFixedPoint2 d{ 5.01 }; // stored as 5.0099999... so we'll need to round this\n\tassert(static_cast&lt;double&gt;(d) == 5.01);\n\n\tFixedPoint2 e{ -5.01 }; // stored as -5.0099999... so we'll need to round this\n\tassert(static_cast&lt;double&gt;(e) == -5.01);\n\n\t// Handle case where the argument's decimal rounds to 100 (need to increase base by 1)\n\tFixedPoint2 f { 106.9978 }; // should be stored with base 107 and decimal 0\n\tassert(static_cast&lt;double&gt;(f) == 107.0);\n\n\t// Handle case where the argument's decimal rounds to -100 (need to decrease base by 1)\n\tFixedPoint2 g { -106.9978 }; // should be stored with base -107 and decimal 0\n\tassert(static_cast&lt;double&gt;(g) == -107.0);\n\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b48",
      "type": "paragraph",
      "html": "<p>Recommendation: This one will be a bit tricky. Do this one in three steps. First, solve for the cases where the double parameter is representable directly (variables <code>a</code> through <code>c</code> above). Then, update your code to handle the cases where the double parameter has a rounding error (variables <code>d</code> &amp; <code>e</code>). Variables <code>f</code> and <code>g</code> should be handled by the overflow handling we added in the prior step.</p>",
      "preview": "Recommendation: This one will be a bit tricky. Do this one in three steps. First",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b49",
      "type": "paragraph",
      "html": "<p>For all cases: <a class=\"hint_link_show\" href=\"javascript:void(0)\" onclick=\"cppHintToggle(document.getElementById('cpp_hint_id_0'),this,'Show Hint','')\">Show Hint</a></p>",
      "preview": "For all cases: Show Hint",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b50",
      "type": "paragraph",
      "html": "<p>For variables <code>a</code> through <code>c</code>: <a class=\"hint_link_show\" href=\"javascript:void(0)\" onclick=\"cppHintToggle(document.getElementById('cpp_hint_id_1'),this,'Show Hint','')\">Show Hint</a></p>",
      "preview": "For variables a through c: Show Hint",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b51",
      "type": "paragraph",
      "html": "<p>For variables <code>d</code> &amp; <code>e</code>: <a class=\"hint_link_show\" href=\"javascript:void(0)\" onclick=\"cppHintToggle(document.getElementById('cpp_hint_id_2'),this,'Show Hint','')\">Show Hint</a></p>",
      "preview": "For variables d & e: Show Hint",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b52",
      "type": "quiz",
      "questionTitle": "> Step #5",
      "questionHtml": "Overload <code>operator==</code>, <code>operator&gt;&gt;</code>, <code>operator-</code> (unary), and <code>operator+</code> (binary).",
      "hintHtml": "Hint: For <code>operator&gt;&gt;</code>, use your double constructor to create an anonymous object of type <code>FixedPoint2</code>, and assign it to your <code>FixedPoint2</code> function parameter.",
      "solutionHtml": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt;\n#include &lt;cmath&gt;\n#include &lt;cstdint&gt; // for fixed width integers\n#include &lt;iostream&gt;\n\nclass FixedPoint2\n{\nprivate:\n\tstd::int16_t m_base{}; // here's our non-fractional part\n\tstd::int8_t m_decimal{}; // here's our factional part\n\npublic:\n\tFixedPoint2(std::int16_t base = 0, std::int8_t decimal = 0)\n\t\t: m_base{ base }, m_decimal{ decimal }\n\t{\n\t\t// If either (or both) of the non-fractional and fractional part of the number are negative, the number should be treated as negative\n\t\tif (m_base &lt; 0 || m_decimal &lt; 0)\n\t\t{\n\t\t\t// Make sure base is negative\n\t\t\tif (m_base &gt; 0)\n\t\t\t\tm_base = -m_base;\n\t\t\t// Make sure decimal is negative\n\t\t\tif (m_decimal &gt; 0)\n\t\t\t\tm_decimal = -m_decimal;\n\t\t}\n\n\t\t// If decimal is out of bounds (in either direction),\n\t\t// adjust the decimal so it's in bounds,\n\t\t// and adjust base to account for the number of units removed from the decimal\n\t\t// h/t to reader David Pinheiro for simplifying this\n\t\tm_base += m_decimal / 100;    // integer division\n\t\tm_decimal = m_decimal % 100;  // integer remainder\n\t}\n\n\tFixedPoint2(double d) :\n\t\tFixedPoint2(\n\t\t\tstatic_cast&lt;std::int16_t&gt;(std::trunc(d)),\n\t\t\tstatic_cast&lt;std::int8_t&gt;(std::round(d * 100) - std::trunc(d) * 100)\n\t\t)\n\t{\n\t}\n\n\texplicit operator double() const\n\t{\n\t\treturn m_base + (static_cast&lt;double&gt;(m_decimal) / 100);\n\t}\n\n\tfriend bool operator==(const FixedPoint2&amp; fp1, const FixedPoint2&amp; fp2)\n\t{\n\t\treturn fp1.m_base == fp2.m_base &amp;&amp; fp1.m_decimal == fp2.m_decimal;\n\t}\n\n\tFixedPoint2 operator-() const\n\t{\n\t\t// Cast to double, make the double negative, then convert back to FixedPoint2\n\t\t// h/t to reader EmtyC for this version\n\t\treturn FixedPoint2{ -static_cast&lt;double&gt;(*this) };\n\t}\n};\n\n// This doesn't require access to the internals of the class, so it can be defined outside the class\nstd::ostream&amp; operator&lt;&lt;(std::ostream&amp; out, const FixedPoint2&amp; fp)\n{\n\tout &lt;&lt; static_cast&lt;double&gt;(fp);\n\treturn out;\n}\n\nstd::istream&amp; operator&gt;&gt;(std::istream&amp; in, FixedPoint2&amp; fp)\n{\n\tdouble d{};\n\tin &gt;&gt; d;\n\tfp = FixedPoint2{ d };\n\n\treturn in;\n}\n\nFixedPoint2 operator+(const FixedPoint2&amp; fp1, const FixedPoint2&amp; fp2)\n{\n\treturn FixedPoint2{ static_cast&lt;double&gt;(fp1) + static_cast&lt;double&gt;(fp2) };\n}\n\nint main()\n{\n\tassert(FixedPoint2{ 0.75 } == FixedPoint2{ 0.75 });    // Test equality true\n\tassert(!(FixedPoint2{ 0.75 } == FixedPoint2{ 0.76 })); // Test equality false\n    \n\t// Test additional cases -- h/t to reader Sharjeel Safdar for these test cases\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ 1.23 } == FixedPoint2{ 1.98 });    // both positive, no decimal overflow\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ 1.50 } == FixedPoint2{ 2.25 });    // both positive, with decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ -1.23 } == FixedPoint2{ -1.98 }); // both negative, no decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ -1.50 } == FixedPoint2{ -2.25 }); // both negative, with decimal overflow\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ -1.23 } == FixedPoint2{ -0.48 });  // second negative, no decimal overflow\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ -1.50 } == FixedPoint2{ -0.75 });  // second negative, possible decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ 1.23 } == FixedPoint2{ 0.48 });   // first negative, no decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ 1.50 } == FixedPoint2{ 0.75 });   // first negative, possible decimal overflow\n    \n\tFixedPoint2 a{ -0.48 };\n\tassert(static_cast&lt;double&gt;(a) == -0.48);\n\tassert(static_cast&lt;double&gt;(-a) == 0.48);\n\n\tstd::cout &lt;&lt; \"Enter a number: \"; // enter 5.678\n\tstd::cin &gt;&gt; a;\n\tstd::cout &lt;&lt; \"You entered: \" &lt;&lt; a &lt;&lt; '\\n';\n\tassert(static_cast&lt;double&gt;(a) == 5.68);\n\t\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b53",
      "type": "paragraph",
      "html": "<p>The following program should run:</p>",
      "preview": "The following program should run:",
      "canCheck": true
    },
    {
      "id": "chapter-21-summary-and-quiz_b54",
      "type": "code",
      "language": "cpp",
      "code": "#include <cassert>\n#include <iostream>\n\nint main()\n{\n\tassert(FixedPoint2{ 0.75 } == FixedPoint2{ 0.75 });    // Test equality true\n\tassert(!(FixedPoint2{ 0.75 } == FixedPoint2{ 0.76 })); // Test equality false\n    \n\t// Test additional cases -- h/t to reader Sharjeel Safdar for these test cases\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ 1.23 } == FixedPoint2{ 1.98 });    // both positive, no decimal overflow\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ 1.50 } == FixedPoint2{ 2.25 });    // both positive, with decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ -1.23 } == FixedPoint2{ -1.98 }); // both negative, no decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ -1.50 } == FixedPoint2{ -2.25 }); // both negative, with decimal overflow\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ -1.23 } == FixedPoint2{ -0.48 });  // second negative, no decimal overflow\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ -1.50 } == FixedPoint2{ -0.75 });  // second negative, possible decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ 1.23 } == FixedPoint2{ 0.48 });   // first negative, no decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ 1.50 } == FixedPoint2{ 0.75 });   // first negative, possible decimal overflow\n    \n\tFixedPoint2 a{ -0.48 };\n\tassert(static_cast<double>(a) == -0.48);\n\tassert(static_cast<double>(-a) == 0.48);\n\n\tstd::cout << \"Enter a number: \"; // enter 5.678\n\tstd::cin >> a;\n\tstd::cout << \"You entered: \" << a << '\\n';\n\tassert(static_cast<double>(a) == 5.68);\n\t\n\treturn 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;cassert&gt;\n#include &lt;iostream&gt;\n\nint main()\n{\n\tassert(FixedPoint2{ 0.75 } == FixedPoint2{ 0.75 });    // Test equality true\n\tassert(!(FixedPoint2{ 0.75 } == FixedPoint2{ 0.76 })); // Test equality false\n    \n\t// Test additional cases -- h/t to reader Sharjeel Safdar for these test cases\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ 1.23 } == FixedPoint2{ 1.98 });    // both positive, no decimal overflow\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ 1.50 } == FixedPoint2{ 2.25 });    // both positive, with decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ -1.23 } == FixedPoint2{ -1.98 }); // both negative, no decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ -1.50 } == FixedPoint2{ -2.25 }); // both negative, with decimal overflow\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ -1.23 } == FixedPoint2{ -0.48 });  // second negative, no decimal overflow\n\tassert(FixedPoint2{ 0.75 } + FixedPoint2{ -1.50 } == FixedPoint2{ -0.75 });  // second negative, possible decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ 1.23 } == FixedPoint2{ 0.48 });   // first negative, no decimal overflow\n\tassert(FixedPoint2{ -0.75 } + FixedPoint2{ 1.50 } == FixedPoint2{ 0.75 });   // first negative, possible decimal overflow\n    \n\tFixedPoint2 a{ -0.48 };\n\tassert(static_cast&lt;double&gt;(a) == -0.48);\n\tassert(static_cast&lt;double&gt;(-a) == 0.48);\n\n\tstd::cout &lt;&lt; \"Enter a number: \"; // enter 5.678\n\tstd::cin &gt;&gt; a;\n\tstd::cout &lt;&lt; \"You entered: \" &lt;&lt; a &lt;&lt; '\\n';\n\tassert(static_cast&lt;double&gt;(a) == 5.68);\n\t\n\treturn 0;\n}</code></pre>",
      "canCheck": true
    }
  ]
}