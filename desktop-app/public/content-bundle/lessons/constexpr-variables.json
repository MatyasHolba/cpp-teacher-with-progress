{
  "index": 68,
  "chapterId": "5",
  "number": "5.6",
  "title": "5.6 — Constexpr variables",
  "url": "https://www.learncpp.com/cpp-tutorial/constexpr-variables/",
  "slug": "constexpr-variables",
  "scrapedAt": "2026-09-15T16:29:10.983Z",
  "totalBlocks": 40,
  "totalCheckpoints": 34,
  "blocks": [
    {
      "id": "constexpr-variables_b0",
      "type": "paragraph",
      "html": "<p>In the previous lesson <a href=\"https://www.learncpp.com/cpp-tutorial/constant-expressions/#whywecare\">5.5 -- Constant expressions</a>, we defined what a constant expression is, discussed why constant expressions are desirable, and concluded with when constant expressions actually evaluate at compile-time.</p>",
      "preview": "In the previous lesson 5.5 -- Constant expressions, we defined what a constant e",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b1",
      "type": "paragraph",
      "html": "<p>In this lesson, we’ll take a closer look at how we create variables that can be used in constant expressions in modern C++. We’ll also explore our first method for ensuring that code actually executes at compile-time.</p>",
      "preview": "In this lesson, we’ll take a closer look at how we create variables that can be ",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b2",
      "type": "section",
      "title": "The compile-time const challenge",
      "html": "<h3 class=\"cpp-section-heading\">The compile-time <code>const</code> challenge</h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-variables_b3",
      "type": "paragraph",
      "html": "<p>In the prior lesson, we noted that one way to create a variable that can be used in a constant expression is to use the <code>const</code> keyword. A <code>const</code> variable with an integral type and a constant expression initializer can be used in a constant expression. All other <code>const</code> variables cannot be used in constant expressions.</p>",
      "preview": "In the prior lesson, we noted that one way to create a variable that can be used",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b4",
      "type": "paragraph",
      "html": "<p>However, the use of <code>const</code> to create variables that can be used in constant expressions has a few challenges.</p>",
      "preview": "However, the use of const to create variables that can be used in constant expre",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b5",
      "type": "paragraph",
      "html": "<p>First, use of <code>const</code> does not make it immediately clear whether the variable is usable in a constant expression or not. In some cases, we can figure it out fairly easily:</p>",
      "preview": "First, use of const does not make it immediately clear whether the variable is u",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b6",
      "type": "code",
      "language": "cpp",
      "code": "int a { 5 };       // not const at all\nconst int b { a }; // clearly not a constant expression (since initializer is non-const)\nconst int c { 5 }; // clearly a constant expression (since initializer is a constant expression)",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int a { 5 };       // not const at all\nconst int b { a }; // clearly not a constant expression (since initializer is non-const)\nconst int c { 5 }; // clearly a constant expression (since initializer is a constant expression)</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b7",
      "type": "paragraph",
      "html": "<p>In other cases, it can be quite difficult:</p>",
      "preview": "In other cases, it can be quite difficult:",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b8",
      "type": "code",
      "language": "cpp",
      "code": "const int d { someVar };    // not obvious whether d is usable in a constant expression or not\nconst int e { getValue() }; // not obvious whether e is usable in a constant expression or not",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">const int d { someVar };    // not obvious whether d is usable in a constant expression or not\nconst int e { getValue() }; // not obvious whether e is usable in a constant expression or not</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b9",
      "type": "paragraph",
      "html": "<p>In the above example, variables <code>d</code> and <code>e</code> may or may not be usable in a constant expressions, depending on how <code>someVar</code> and <code>getValue()</code> are defined. That means we have to go inspect the definitions of those initializers and infer what case we’re in. And that may not even be sufficient -- if <code>someVar</code> is const and initialized with a variable or a function call, we’ll have to go inspect the definition of its initializer too!</p>",
      "preview": "In the above example, variables d and e may or may not be usable in a constant e",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b10",
      "type": "paragraph",
      "html": "<p>Second, use of <code>const</code> does not provide a way to inform the compiler that we require a variable that is usable in a constant expression (and that it should halt compilation if it isn’t). Instead, it will just silently create a variable that can only be used in runtime expressions.</p>",
      "preview": "Second, use of const does not provide a way to inform the compiler that we requi",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b11",
      "type": "paragraph",
      "html": "<p>Third, the use of <code>const</code> to create compile-time constant variables does not extend to non-integral variables. And there are many cases where we would like non-integral variables to be compile-time constants too.</p>",
      "preview": "Third, the use of const to create compile-time constant variables does not exten",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b12",
      "type": "section",
      "title": "The constexpr keyword",
      "html": "<h3 class=\"cpp-section-heading\">The <code>constexpr</code> keyword</h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-variables_b13",
      "type": "paragraph",
      "html": "<p>Fortunately, we can enlist the compiler’s help to ensure we get a compile-time constant variable where we desire one. To do so, we use the <code>constexpr</code> keyword (which is shorthand for “constant expression”) instead of <code>const</code> in a variable’s declaration. A <strong>constexpr</strong> variable is always a compile-time constant. As a result, a constexpr variable must be initialized with a constant expression, otherwise a compilation error will result.</p>",
      "preview": "Fortunately, we can enlist the compiler’s help to ensure we get a compile-time c",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b14",
      "type": "paragraph",
      "html": "<p>For example:</p>",
      "preview": "For example:",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b15",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\n// The return value of a non-constexpr function is not constexpr\nint five()\n{\n    return 5;\n}\n\nint main()\n{\n    constexpr double gravity { 9.8 }; // ok: 9.8 is a constant expression\n    constexpr int sum { 4 + 5 };      // ok: 4 + 5 is a constant expression\n    constexpr int something { sum };  // ok: sum is a constant expression\n\n    std::cout << \"Enter your age: \";\n    int age{};\n    std::cin >> age;\n\n    constexpr int myAge { age };      // compile error: age is not a constant expression\n    constexpr int f { five() };       // compile error: return value of five() is not constexpr\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\n// The return value of a non-constexpr function is not constexpr\nint five()\n{\n    return 5;\n}\n\nint main()\n{\n    constexpr double gravity { 9.8 }; // ok: 9.8 is a constant expression\n    constexpr int sum { 4 + 5 };      // ok: 4 + 5 is a constant expression\n    constexpr int something { sum };  // ok: sum is a constant expression\n\n    std::cout &lt;&lt; \"Enter your age: \";\n    int age{};\n    std::cin &gt;&gt; age;\n\n    constexpr int myAge { age };      // compile error: age is not a constant expression\n    constexpr int f { five() };       // compile error: return value of five() is not constexpr\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b16",
      "type": "paragraph",
      "html": "<p>Because functions normally execute at runtime, the return value of a function is not constexpr (even when the return expression is a constant expression). This is why <code>five()</code> is not a legal initialization value for <code>constexpr int f</code>.</p>",
      "preview": "Because functions normally execute at runtime, the return value of a function is",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b17",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>We talk about functions whose return values can be used in constant expressions in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/constexpr-functions/\">F.1 -- Constexpr functions</a>.</p></div>",
      "preview": "Related contentWe talk about functions whose return values can be used in constant expressions in le",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b18",
      "type": "paragraph",
      "html": "<p>Additionally, <code>constexpr</code> works for variables with non-integral types:</p>",
      "preview": "Additionally, constexpr works for variables with non-integral types:",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b19",
      "type": "code",
      "language": "cpp",
      "code": "constexpr double d { 1.2 }; // d can be used in constant expressions!",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">constexpr double d { 1.2 }; // d can be used in constant expressions!</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b20",
      "type": "section",
      "title": "The meaning of const vs constexpr for variables",
      "html": "<h3 class=\"cpp-section-heading\">The meaning of const vs constexpr for variables</h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-variables_b21",
      "type": "paragraph",
      "html": "<p>For variables:</p>",
      "preview": "For variables:",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b22",
      "type": "list",
      "html": "<ul><li><code>const</code> means that the value of an object cannot be changed after initialization. The value of the initializer may be known at compile-time or runtime. The const object can be evaluated at runtime.</li><li><code>constexpr</code> means that the object can be used in a constant expression. The value of the initializer must be known at compile-time. The constexpr object can be evaluated at runtime or compile-time.</li></ul>",
      "preview": "const means that the value of an object cannot be changed after initialization. ",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b23",
      "type": "paragraph",
      "html": "<p>Constexpr variables are implicitly const. Const variables are not implicitly constexpr (except for const integral variables with a constant expression initializer). Although a variable can be defined as both <code>constexpr</code> and <code>const</code>, in most cases this is redundant, and we only need to use either <code>const</code> or <code>constexpr</code>.</p>",
      "preview": "Constexpr variables are implicitly const. Const variables are not implicitly con",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b24",
      "type": "paragraph",
      "html": "<p>Unlike <code>const</code>, <code>constexpr</code> is not part of an object’s type. Therefore a variable defined as <code>constexpr int</code> actually has type <code>const int</code> (due to the implicit <code>const</code> that <code>constexpr</code> provides for objects).</p>",
      "preview": "Unlike const, constexpr is not part of an object’s type. Therefore a variable de",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b25",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Any constant variable whose initializer is a constant expression should be declared as <code>constexpr</code>.</p><p>Any constant variable whose initializer is not a constant expression (making it a runtime constant) should be declared as <code>const</code>.</p><p>Caveat: In the future we will discuss some types that are not fully compatible with <code>constexpr</code> (including <code>std::string</code>, <code>std::vector</code>, and other types that use dynamic memory allocation). For constant objects of these types, either use <code>const</code> instead of <code>constexpr</code>, or pick a different type that is constexpr compatible (e.g. <code>std::string_view</code> or <code>std::array</code>).</p></div>",
      "preview": "Best practiceAny constant variable whose initializer is a constant expression should be declared as ",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b26",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Nomenclature</p><p>The term <code>constexpr</code> is a portmanteau of “constant expression”. This name was picked because constexpr objects (and functions) can be used in constant expressions.</p><p>Formally, the keyword <code>constexpr</code> applies only to objects and functions. Conventionally, the term <code>constexpr</code> is used as shorthand for any constant expression (such as <code>1 + 2</code>).</p></div>",
      "preview": "NomenclatureThe term constexpr is a portmanteau of “constant expression”. This name was picked becau",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b27",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Author’s note</p><p>Some of the examples on this site were written prior to the best practice to use <code>constexpr</code> -- as a result, you will note that some examples do not follow the above best practice. We are currently in the process of updating non-compliant examples as we run across them.</p></div>",
      "preview": "Author’s noteSome of the examples on this site were written prior to the best practice to use conste",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b28",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">For advanced readers</p><p>In C and C++, the declaration of an array object (an object can hold multiple values) requires the length of the array (the number of values that it can hold) be known at compile-time (so the compiler can ensure the correct amount of memory is allocated for array objects).</p><p>Since literals are known at compile-time, they can be used as an array length:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int arr[5]; // an array of 5 int values, length of 5 is known at compile-time</code></pre><p>In many cases, it would be preferable to use a symbolic constant as an array length (e.g. to avoid magic numbers and make the array length easier to change if it is used in multiple places). In C, this can be done via a preprocessor macro, or via an enumerator, but not via a const variable (excluding VLA’s, which have other downsides). C++, looking to improve on this situation, wanted to allow the use of const variables instead of macros. But the value of variables was generally assumed to be known only at runtime, which made them ineligible to be used as array lengths.</p><p>To solve this problem, the C++ language standard added an exemption so that const integral types with a constant expression initializer would be treated as values known at compile-time, and thus be usable as array lengths:</p><pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">const int arrLen = 5;\nint arr[arrLen]; // ok: array of 5 ints</code></pre><p>When C++11 introduced constant expressions, it made sense for a const int with a constant expression initializer to be grandfathered into that definition. The committee discussed whether other types should be included as well, but ultimately decided not to.</p></div>",
      "preview": "For advanced readersIn C and C++, the declaration of an array object (an object can hold multiple va",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b29",
      "type": "section",
      "title": "Const and constexpr function parameters",
      "html": "<h3 class=\"cpp-section-heading\">Const and constexpr function parameters</h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-variables_b30",
      "type": "paragraph",
      "html": "<p>Normal function calls are evaluated at runtime, with the supplied arguments being used to initialize the function’s parameters. Because the initialization of function parameters happens at runtime, this leads to two consequences:</p>",
      "preview": "Normal function calls are evaluated at runtime, with the supplied arguments bein",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b31",
      "type": "list",
      "html": "<ol start=\"1\"><li><code>const</code> function parameters are treated as runtime constants (even when the supplied argument is a compile-time constant).</li><li>Function parameters cannot be declared as <code>constexpr</code>, since their initialization value isn’t determined until runtime.</li></ol>",
      "preview": "const function parameters are treated as runtime constants (even when the suppli",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b32",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>We discuss functions that can be evaluated at compile-time (and thus be used in constant expressions) below.</p><p>C++ also supports a way to pass compile-time constants to a function. We discuss these in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/non-type-template-parameters/\">11.9 -- Non-type template parameters</a>.</p></div>",
      "preview": "Related contentWe discuss functions that can be evaluated at compile-time (and thus be used in const",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b33",
      "type": "section",
      "title": "Nomenclature recap",
      "html": "<h3 class=\"cpp-section-heading\">Nomenclature recap</h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-variables_b34",
      "type": "paragraph",
      "html": "<div class=\"cpp-table-wrapper\"><table class=\"cpp-table\"><tbody><tr><th>Term</th><th>Definition</th></tr><tr><td>Compile-time constant</td><td>A value or non-modifiable object whose value must be known at compile time (e.g. literals and constexpr variables).</td></tr><tr><td>Constexpr</td><td>Keyword that declares objects as compile-time constants (and functions that can be evaluated at compile-time). Informally, shorthand for “constant expression”.</td></tr><tr><td>Constant expression</td><td>An expression that contains only compile-time constants and operators/functions that support compile-time evaluation.</td></tr><tr><td>Runtime expression</td><td>An expression that is not a constant expression.</td></tr><tr><td>Runtime constant</td><td>A value or non-modifiable object that is not a compile-time constant.</td></tr></tbody></table></div>",
      "preview": "TermDefinitionCompile-time constantA value or non-modifiable object whose value ",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b35",
      "type": "section",
      "title": "A brief introduction to constexpr functions",
      "html": "<h3 class=\"cpp-section-heading\">A brief introduction to constexpr functions</h3>",
      "canCheck": false
    },
    {
      "id": "constexpr-variables_b36",
      "type": "paragraph",
      "html": "<p>A <strong>constexpr function</strong> is a function that can be called in a constant expression. A constexpr function must evaluate at compile-time when the constant expression it is part of must evaluate at compile time (e.g. in the initializer of a constexpr variable). Otherwise, a constexpr function may be evaluated at either compile-time (if eligible) or runtime. To be eligible for compile-time execution, all arguments must be constant expressions.</p>",
      "preview": "A constexpr function is a function that can be called in a constant expression. ",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b37",
      "type": "paragraph",
      "html": "<p>To make a constexpr function, the <code>constexpr</code> keyword is placed in the function declaration before the return type:</p>",
      "preview": "To make a constexpr function, the constexpr keyword is placed in the function de",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b38",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint max(int x, int y) // this is a non-constexpr function\n{\n    if (x > y)\n        return x;\n    else\n        return y;\n}\n\nconstexpr int cmax(int x, int y) // this is a constexpr function\n{\n    if (x > y)\n        return x;\n    else\n        return y;\n}\n\nint main()\n{\n    int m1 { max(5, 6) };            // ok\n    const int m2 { max(5, 6) };      // ok\n    constexpr int m3 { max(5, 6) };  // compile error: max(5, 6) not a constant expression\n\n    int m4 { cmax(5, 6) };           // ok: may evaluate at compile-time or runtime\n    const int m5 { cmax(5, 6) };     // ok: may evaluate at compile-time or runtime\n    constexpr int m6 { cmax(5, 6) }; // okay: must evaluate at compile-time\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint max(int x, int y) // this is a non-constexpr function\n{\n    if (x &gt; y)\n        return x;\n    else\n        return y;\n}\n\nconstexpr int cmax(int x, int y) // this is a constexpr function\n{\n    if (x &gt; y)\n        return x;\n    else\n        return y;\n}\n\nint main()\n{\n    int m1 { max(5, 6) };            // ok\n    const int m2 { max(5, 6) };      // ok\n    constexpr int m3 { max(5, 6) };  // compile error: max(5, 6) not a constant expression\n\n    int m4 { cmax(5, 6) };           // ok: may evaluate at compile-time or runtime\n    const int m5 { cmax(5, 6) };     // ok: may evaluate at compile-time or runtime\n    constexpr int m6 { cmax(5, 6) }; // okay: must evaluate at compile-time\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "constexpr-variables_b39",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Author’s note</p><p>We used to discuss constexpr functions in detail in this chapter, but feedback from readers indicated that the topic was too long and nuanced to present this early in the tutorial series. As a result, we’ve moved the full discussion back to lesson <a href=\"https://www.learncpp.com/cpp-tutorial/constexpr-functions/\">F.1 -- Constexpr functions</a>.</p><p>The key thing to take away from this introduction is that a constexpr function may be called in constant expressions.</p><p>You will see constexpr functions used in some future examples (where appropriate), but we will not expect you to understand them further or write your own constexpr functions until we’ve formally covered the topic.</p></div>",
      "preview": "Author’s noteWe used to discuss constexpr functions in detail in this chapter, but feedback from rea",
      "canCheck": true
    }
  ]
}