{
  "index": 302,
  "chapterId": "25",
  "number": "25.5",
  "title": "25.5 — Early binding and late binding",
  "url": "https://www.learncpp.com/cpp-tutorial/early-binding-and-late-binding/",
  "slug": "early-binding-and-late-binding",
  "scrapedAt": "2026-09-15T16:31:15.153Z",
  "totalBlocks": 34,
  "totalCheckpoints": 31,
  "blocks": [
    {
      "id": "early-binding-and-late-binding_b0",
      "type": "paragraph",
      "html": "<p>In this lesson and the next, we are going to take a closer look at how virtual functions are implemented. While this information is not strictly necessary to effectively use virtual functions, it is interesting. Nevertheless, you can consider both sections optional reading.</p>",
      "preview": "In this lesson and the next, we are going to take a closer look at how virtual f",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b1",
      "type": "paragraph",
      "html": "<p>When a C++ program is executed, it executes sequentially, beginning at the top of <code>main()</code>. When a function call is encountered, the point of execution jumps to the beginning of the function being called. How does the CPU know to do this?</p>",
      "preview": "When a C++ program is executed, it executes sequentially, beginning at the top o",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b2",
      "type": "paragraph",
      "html": "<p>When a program is compiled, the compiler converts each statement in your C++ program into one or more lines of machine language. Each line of machine language is given its own unique sequential address. This is no different for functions -- when a function is encountered, it is converted into machine language and given the next available address. Thus, each function ends up with a unique address.</p>",
      "preview": "When a program is compiled, the compiler converts each statement in your C++ pro",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b3",
      "type": "section",
      "title": "Binding and dispatching",
      "html": "<h3 class=\"cpp-section-heading\">Binding and dispatching</h3>",
      "canCheck": false
    },
    {
      "id": "early-binding-and-late-binding_b4",
      "type": "paragraph",
      "html": "<p>Our programs contain many names (identifiers, keywords, etc…). Each name has a set of associated properties: for example, if the name represents a variable, that variable has a type, a value, a memory address, etc…</p>",
      "preview": "Our programs contain many names (identifiers, keywords, etc…). Each name has a s",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b5",
      "type": "paragraph",
      "html": "<p>For example, when we say <code>int x</code>, we’re telling the compiler to associate the name <code>x</code> with the type <code>int</code>. Later if we say <code>x = 5</code>, the compiler can use this association to type check the assignment to ensure it is valid.</p>",
      "preview": "For example, when we say int x, we’re telling the compiler to associate the name",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b6",
      "type": "paragraph",
      "html": "<p>In general programming, <strong>binding</strong> is the process of associating names with such properties. <strong>Function binding</strong> (or <strong>method binding</strong>) is the process that determines what function definition is associated with a function call. The process of actually invoking a bound function is called <strong>dispatching</strong>.</p>",
      "preview": "In general programming, binding is the process of associating names with such pr",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b7",
      "type": "paragraph",
      "html": "<p>In C++, the term binding is used more casually (and dispatching is usually considered part of binding). We’ll explore the C++ use of the terms below.</p>",
      "preview": "In C++, the term binding is used more casually (and dispatching is usually consi",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b8",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Nomenclature</p><p>Binding is an overloaded term. In other contexts, binding may refer to:</p><ul><li>The binding of a reference to an object</li><li><code>std::bind</code></li><li>Language binding</li></ul></div>",
      "preview": "NomenclatureBinding is an overloaded term. In other contexts, binding may refer to:The binding of a ",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b9",
      "type": "section",
      "title": "Early binding",
      "html": "<h3 class=\"cpp-section-heading\">Early binding</h3>",
      "canCheck": false
    },
    {
      "id": "early-binding-and-late-binding_b10",
      "type": "paragraph",
      "html": "<p>Most of the function calls the compiler encounters will be direct function calls. A direct function call is a statement that directly calls a function. For example:</p>",
      "preview": "Most of the function calls the compiler encounters will be direct function calls",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b11",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nstruct Foo\n{\n    void printValue(int value)\n    {\n        std::cout << value;\n    }\n};\n\nvoid printValue(int value)\n{\n    std::cout << value;\n}\n\nint main()\n{\n    printValue(5);   // direct function call to printValue(int)\n\n    Foo f{};\n    f.printValue(5); // direct function call to Foo::printValue(int)\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nstruct Foo\n{\n    void printValue(int value)\n    {\n        std::cout &lt;&lt; value;\n    }\n};\n\nvoid printValue(int value)\n{\n    std::cout &lt;&lt; value;\n}\n\nint main()\n{\n    printValue(5);   // direct function call to printValue(int)\n\n    Foo f{};\n    f.printValue(5); // direct function call to Foo::printValue(int)\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b12",
      "type": "paragraph",
      "html": "<p>In C++, when a direct call is made to a non-member function or a non-virtual member function, the compiler can determine which function definition should be matched to the call. This is sometimes called <strong>early binding</strong> (or <strong>static binding</strong>), as it can be performed at compile-time. The compiler (or linker) can then generate machine language instructions that tells the CPU to jump directly to the address of the function.</p>",
      "preview": "In C++, when a direct call is made to a non-member function or a non-virtual mem",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b13",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">For advanced readers</p><p>If we look at the assembly code generated for the call to <code>printValue(5)</code> (using clang x86-64), we see something like this:</p><pre>        mov     edi, 5           ; copy argument 5 into edi register in preparation for function call\n        call    printValue(int)  ; directly call printValue(int)\n</pre><p>You can clearly see that this is a direct function call to printValue(int).</p></div>",
      "preview": "For advanced readersIf we look at the assembly code generated for the call to printValue(5) (using c",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b14",
      "type": "paragraph",
      "html": "<p>Calls to overloaded functions and function templates can also be resolved at compile-time:</p>",
      "preview": "Calls to overloaded functions and function templates can also be resolved at com",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b15",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\ntemplate <typename T>\nvoid printValue(T value)\n{\n    std::cout << value << '\\n';\n}\n\nvoid printValue(double value)\n{\n    std::cout << value << '\\n';\n}\n\nvoid printValue(int value)\n{\n    std::cout << value << '\\n';\n}\n\nint main()\n{\n    printValue(5);   // direct function call to printValue(int)\n    printValue<>(5); // direct function call to printValue<int>(int)\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\ntemplate &lt;typename T&gt;\nvoid printValue(T value)\n{\n    std::cout &lt;&lt; value &lt;&lt; '\\n';\n}\n\nvoid printValue(double value)\n{\n    std::cout &lt;&lt; value &lt;&lt; '\\n';\n}\n\nvoid printValue(int value)\n{\n    std::cout &lt;&lt; value &lt;&lt; '\\n';\n}\n\nint main()\n{\n    printValue(5);   // direct function call to printValue(int)\n    printValue&lt;&gt;(5); // direct function call to printValue&lt;int&gt;(int)\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b16",
      "type": "paragraph",
      "html": "<p>Let’s take a look at a simple calculator program that uses early binding:</p>",
      "preview": "Let’s take a look at a simple calculator program that uses early binding:",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b17",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint add(int x, int y)\n{\n    return x + y;\n}\n\nint subtract(int x, int y)\n{\n    return x - y;\n}\n\nint multiply(int x, int y)\n{\n    return x * y;\n}\n\nint main()\n{\n    int x{};\n    std::cout << \"Enter a number: \";\n    std::cin >> x;\n\n    int y{};\n    std::cout << \"Enter another number: \";\n    std::cin >> y;\n\n    int op{};\n    std::cout << \"Enter an operation (0=add, 1=subtract, 2=multiply): \";\n    std::cin >> op;\n\n    int result {};\n    switch (op)\n    {\n        // call the target function directly using early binding\n        case 0: result = add(x, y); break;\n        case 1: result = subtract(x, y); break;\n        case 2: result = multiply(x, y); break;\n        default:\n            std::cout << \"Invalid operator\\n\";\n            return 1;\n    }\n\n    std::cout << \"The answer is: \" << result << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint add(int x, int y)\n{\n    return x + y;\n}\n\nint subtract(int x, int y)\n{\n    return x - y;\n}\n\nint multiply(int x, int y)\n{\n    return x * y;\n}\n\nint main()\n{\n    int x{};\n    std::cout &lt;&lt; \"Enter a number: \";\n    std::cin &gt;&gt; x;\n\n    int y{};\n    std::cout &lt;&lt; \"Enter another number: \";\n    std::cin &gt;&gt; y;\n\n    int op{};\n    std::cout &lt;&lt; \"Enter an operation (0=add, 1=subtract, 2=multiply): \";\n    std::cin &gt;&gt; op;\n\n    int result {};\n    switch (op)\n    {\n        // call the target function directly using early binding\n        case 0: result = add(x, y); break;\n        case 1: result = subtract(x, y); break;\n        case 2: result = multiply(x, y); break;\n        default:\n            std::cout &lt;&lt; \"Invalid operator\\n\";\n            return 1;\n    }\n\n    std::cout &lt;&lt; \"The answer is: \" &lt;&lt; result &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b18",
      "type": "paragraph",
      "html": "<p>Because <code>add()</code>, <code>subtract()</code>, and <code>multiply()</code> are all direct function calls to non-member functions, the compiler will match these function calls to their respective function definitions at compile-time.</p>",
      "preview": "Because add(), subtract(), and multiply() are all direct function calls to non-m",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b19",
      "type": "paragraph",
      "html": "<p>Note that because of the switch statement, which function is actually called is not determined until runtime. However, that is a path of execution issue, not a binding issue.</p>",
      "preview": "Note that because of the switch statement, which function is actually called is ",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b20",
      "type": "section",
      "title": "Late binding",
      "html": "<h3 class=\"cpp-section-heading\">Late binding</h3>",
      "canCheck": false
    },
    {
      "id": "early-binding-and-late-binding_b21",
      "type": "paragraph",
      "html": "<p>In some cases, a function call can’t be resolved until runtime. In C++, this is sometimes known as <strong>late binding</strong> (or in the case of virtual function resolution, <strong>dynamic dispatch</strong>).</p>",
      "preview": "In some cases, a function call can’t be resolved until runtime. In C++, this is ",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b22",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Author’s note</p><p>In general programming terminology, the term “late binding” usually means that the function being called can’t be determined based on static type information alone, but must be resolved using dynamic type information.</p><p>In C++, the term tends to be used more loosely to mean any function call where the actual function being called is not known by the compiler or linker at the point where the function call is actually being made.</p></div>",
      "preview": "Author’s noteIn general programming terminology, the term “late binding” usually means that the func",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b23",
      "type": "paragraph",
      "html": "<p>In C++, one way to get late binding is to use function pointers. To review function pointers briefly, a function pointer is a type of pointer that points to a function instead of a variable. The function that a function pointer points to can be called by using the function call operator <code>()</code> on the pointer.</p>",
      "preview": "In C++, one way to get late binding is to use function pointers. To review funct",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b24",
      "type": "paragraph",
      "html": "<p>For example, the following code calls the <code>printValue()</code> function through a function pointer:</p>",
      "preview": "For example, the following code calls the printValue() function through a functi",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b25",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nvoid printValue(int value)\n{\n    std::cout << value << '\\n';\n}\n\nint main()\n{\n    auto fcn { printValue }; // create a function pointer and make it point to function printValue\n    fcn(5);                  // invoke printValue indirectly through the function pointer\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nvoid printValue(int value)\n{\n    std::cout &lt;&lt; value &lt;&lt; '\\n';\n}\n\nint main()\n{\n    auto fcn { printValue }; // create a function pointer and make it point to function printValue\n    fcn(5);                  // invoke printValue indirectly through the function pointer\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b26",
      "type": "paragraph",
      "html": "<p>Calling a function via a function pointer is also known as an indirect function call. At the point where <code>fcn(5)</code> is actually called, the compiler does not know at compile-time what function is being called. Instead, at runtime, an indirect function call is made to whatever function exists at the address held by the function pointer.</p>",
      "preview": "Calling a function via a function pointer is also known as an indirect function ",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b27",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">For advanced readers</p><p>If we look at the assembly code generated for the call to <code>fcn(5)</code> (using clang x86-64), we see something like this:</p><pre>        lea     rax, [rip + printValue(int)] ; determine address of printValue and place into rax register\n        mov     qword ptr [rbp - 8], rax     ; move value in rax register into memory associated with variable fcn\n\n        mov     edi, 5                       ; copy argument 5 into edi register in preparation for function call\n        call    qword ptr [rbp - 8]          ; invoke the function at the address held by variable fcn\n</pre><p>You can clearly see that this is a indirect function call to printValue(int) via its address.</p></div>",
      "preview": "For advanced readersIf we look at the assembly code generated for the call to fcn(5) (using clang x8",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b28",
      "type": "paragraph",
      "html": "<p>The following calculator program is functionally identical to the calculator example above, except it uses a function pointer instead of a direct function call:</p>",
      "preview": "The following calculator program is functionally identical to the calculator exa",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b29",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint add(int x, int y)\n{\n    return x + y;\n}\n\nint subtract(int x, int y)\n{\n    return x - y;\n}\n\nint multiply(int x, int y)\n{\n    return x * y;\n}\n\nint main()\n{\n    int x{};\n    std::cout << \"Enter a number: \";\n    std::cin >> x;\n\n    int y{};\n    std::cout << \"Enter another number: \";\n    std::cin >> y;\n\n    int op{};\n    std::cout << \"Enter an operation (0=add, 1=subtract, 2=multiply): \";\n    std::cin >> op;\n\n    using FcnPtr = int (*)(int, int); // alias ugly function pointer type\n    FcnPtr fcn { nullptr }; // create a function pointer object, set to nullptr initially\n\n    // Set fcn to point to the function the user chose\n    switch (op)\n    {\n        case 0: fcn = add; break;\n        case 1: fcn = subtract; break;\n        case 2: fcn = multiply; break;\n        default:\n            std::cout << \"Invalid operator\\n\";\n            return 1;\n    }\n\n    // Call the function that fcn is pointing to with x and y as parameters\n    std::cout << \"The answer is: \" << fcn(x, y) << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint add(int x, int y)\n{\n    return x + y;\n}\n\nint subtract(int x, int y)\n{\n    return x - y;\n}\n\nint multiply(int x, int y)\n{\n    return x * y;\n}\n\nint main()\n{\n    int x{};\n    std::cout &lt;&lt; \"Enter a number: \";\n    std::cin &gt;&gt; x;\n\n    int y{};\n    std::cout &lt;&lt; \"Enter another number: \";\n    std::cin &gt;&gt; y;\n\n    int op{};\n    std::cout &lt;&lt; \"Enter an operation (0=add, 1=subtract, 2=multiply): \";\n    std::cin &gt;&gt; op;\n\n    using FcnPtr = int (*)(int, int); // alias ugly function pointer type\n    FcnPtr fcn { nullptr }; // create a function pointer object, set to nullptr initially\n\n    // Set fcn to point to the function the user chose\n    switch (op)\n    {\n        case 0: fcn = add; break;\n        case 1: fcn = subtract; break;\n        case 2: fcn = multiply; break;\n        default:\n            std::cout &lt;&lt; \"Invalid operator\\n\";\n            return 1;\n    }\n\n    // Call the function that fcn is pointing to with x and y as parameters\n    std::cout &lt;&lt; \"The answer is: \" &lt;&lt; fcn(x, y) &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b30",
      "type": "paragraph",
      "html": "<p>In this example, instead of calling the <code>add()</code>, <code>subtract()</code>, or <code>multiply()</code> function directly, we’ve instead set <code>fcn</code> to point at the function we wish to call. Then we call the function through the pointer.</p>",
      "preview": "In this example, instead of calling the add(), subtract(), or multiply() functio",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b31",
      "type": "paragraph",
      "html": "<p>The compiler is unable to use early binding to resolve the function call <code>fcn(x, y)</code> because it can not tell which function <code>fcn</code> will be pointing to at compile time!</p>",
      "preview": "The compiler is unable to use early binding to resolve the function call fcn(x, ",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b32",
      "type": "paragraph",
      "html": "<p>Late binding is slightly less efficient since it involves an extra level of indirection. With early binding, the CPU can jump directly to the function’s address. With late binding, the program has to read the address held in the pointer and then jump to that address. This involves one extra step, making it slightly slower. However, the advantage of late binding is that it is more flexible than early binding, because decisions about what function to call do not need to be made until runtime.</p>",
      "preview": "Late binding is slightly less efficient since it involves an extra level of indi",
      "canCheck": true
    },
    {
      "id": "early-binding-and-late-binding_b33",
      "type": "paragraph",
      "html": "<p>In the next lesson, we’ll take a look at how late binding is used to implement virtual functions.</p>",
      "preview": "In the next lesson, we’ll take a look at how late binding is used to implement v",
      "canCheck": true
    }
  ]
}