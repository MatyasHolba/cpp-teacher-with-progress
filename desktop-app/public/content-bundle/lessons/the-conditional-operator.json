{
  "index": 78,
  "chapterId": "6",
  "number": "6.6",
  "title": "6.6 — The conditional operator",
  "url": "https://www.learncpp.com/cpp-tutorial/the-conditional-operator/",
  "slug": "the-conditional-operator",
  "scrapedAt": "2026-09-15T16:29:16.940Z",
  "totalBlocks": 71,
  "totalCheckpoints": 61,
  "blocks": [
    {
      "id": "the-conditional-operator_b0",
      "type": "paragraph",
      "html": "<div class=\"cpp-table-wrapper\"><table class=\"cpp-table\"><tbody><tr><th>Operator</th><th>Symbol</th><th>Form</th><th>Meaning</th></tr><tr><td>Conditional</td><td>?:</td><td><nobr>c ? x : y</nobr></td><td>If conditional <code>c</code> is <code>true</code> then evaluate <code>x</code>, otherwise evaluate <code>y</code></td></tr></tbody></table></div>",
      "preview": "OperatorSymbolFormMeaningConditional?:c ? x : yIf conditional c is true then eva",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b1",
      "type": "paragraph",
      "html": "<p>The <strong>conditional operator</strong> (<code>?:</code>) (also sometimes called the <strong>arithmetic if</strong> operator) is a ternary operator (an operator that takes 3 operands). Because it has historically been C++’s only ternary operator, it’s also sometimes referred to as “the ternary operator”.</p>",
      "preview": "The conditional operator (?:) (also sometimes called the arithmetic if operator)",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b2",
      "type": "paragraph",
      "html": "<p>The <code>?:</code> operator provides a shorthand method for doing a particular type of if-else statement.</p>",
      "preview": "The ?: operator provides a shorthand method for doing a particular type of if-el",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b3",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>We cover if-else statements in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/introduction-to-if-statements/\">4.10 -- Introduction to if statements</a>.</p></div>",
      "preview": "Related contentWe cover if-else statements in lesson 4.10 -- Introduction to if statements.",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b4",
      "type": "paragraph",
      "html": "<p>To recap, an if-else statement takes the following form:</p>",
      "preview": "To recap, an if-else statement takes the following form:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b5",
      "type": "output",
      "text": "if (condition)\n    statement1;\nelse\n    statement2;\n",
      "html": "<pre>if (condition)\n    statement1;\nelse\n    statement2;\n</pre>",
      "canCheck": false
    },
    {
      "id": "the-conditional-operator_b6",
      "type": "paragraph",
      "html": "<p>If <code>condition</code> evaluates to <code>true</code>, then <code>statement1</code> is executed, otherwise <code>statement2</code> is executed. The <code>else</code> and <code>statement2</code> are optional.</p>",
      "preview": "If condition evaluates to true, then statement1 is executed, otherwise statement",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b7",
      "type": "paragraph",
      "html": "<p>The <code>?:</code> operator takes the following form:</p>",
      "preview": "The ?: operator takes the following form:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b8",
      "type": "output",
      "text": "condition ? expression1 : expression2;\n",
      "html": "<pre>condition ? expression1 : expression2;\n</pre>",
      "canCheck": false
    },
    {
      "id": "the-conditional-operator_b9",
      "type": "paragraph",
      "html": "<p>If <code>condition</code> evaluates to <code>true</code>, then <code>expression1</code> is evaluated, otherwise <code>expression2</code> is evaluated. The <code>:</code> and <code>expression2</code> are not optional.</p>",
      "preview": "If condition evaluates to true, then expression1 is evaluated, otherwise express",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b10",
      "type": "paragraph",
      "html": "<p>Consider an if-else statement that looks like this:</p>",
      "preview": "Consider an if-else statement that looks like this:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b11",
      "type": "code",
      "language": "cpp",
      "code": "if (x > y)\n    max = x;\nelse\n    max = y;",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">if (x &gt; y)\n    max = x;\nelse\n    max = y;</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b12",
      "type": "paragraph",
      "html": "<p>This can be rewritten as:</p>",
      "preview": "This can be rewritten as:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b13",
      "type": "code",
      "language": "cpp",
      "code": "max = ((x > y) ? x : y);",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">max = ((x &gt; y) ? x : y);</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b14",
      "type": "paragraph",
      "html": "<p>In such cases, the conditional operator can help compact code without losing readability.</p>",
      "preview": "In such cases, the conditional operator can help compact code without losing rea",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b15",
      "type": "section",
      "title": "An example",
      "html": "<h3 class=\"cpp-section-heading\">An example</h3>",
      "canCheck": false
    },
    {
      "id": "the-conditional-operator_b16",
      "type": "paragraph",
      "html": "<p>Consider the following example:</p>",
      "preview": "Consider the following example:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b17",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint getValue()\n{\n    std::cout << \"Enter a number: \";\n    int x{};\n    std::cin >> x;\n    return x;\n}\n\nint main()\n{\n    int x { getValue() };\n    int y { getValue() };\n    int max { (x > y) ? x : y };\n    std::cout << \"The max of \" << x <<\" and \" << y << \" is \" << max << \".\\n\";\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint getValue()\n{\n    std::cout &lt;&lt; \"Enter a number: \";\n    int x{};\n    std::cin &gt;&gt; x;\n    return x;\n}\n\nint main()\n{\n    int x { getValue() };\n    int y { getValue() };\n    int max { (x &gt; y) ? x : y };\n    std::cout &lt;&lt; \"The max of \" &lt;&lt; x &lt;&lt;\" and \" &lt;&lt; y &lt;&lt; \" is \" &lt;&lt; max &lt;&lt; \".\\n\";\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b18",
      "type": "paragraph",
      "html": "<p>First, let’s enter <code>5</code> and <code>7</code> as input (so <code>x</code> is <code>5</code>, and <code>y</code> is <code>7</code>). When <code>max</code> is initialized, the expression <code>(5 &gt; 7) ? 5 : 7</code> is evaluated. Since <code>5 &gt; 7</code> is false, this yields <code>false ? 5 : 7</code>, which evaluates to <code>7</code>. The program prints:</p>",
      "preview": "First, let’s enter 5 and 7 as input (so x is 5, and y is 7). When max is initial",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b19",
      "type": "output",
      "text": "The max of 5 and 7 is 7.\n",
      "html": "<pre>The max of 5 and 7 is 7.\n</pre>",
      "canCheck": false
    },
    {
      "id": "the-conditional-operator_b20",
      "type": "paragraph",
      "html": "<p>Now let’s enter <code>7</code> and <code>5</code> as input (so <code>x</code> is <code>7</code>, and <code>y</code> is <code>5</code>). In this case, we get <code>(7 &gt; 5) ? 7 : 5</code>, which is <code>true ? 7 : 5</code>, which evaluates to <code>7</code>. The program prints:</p>",
      "preview": "Now let’s enter 7 and 5 as input (so x is 7, and y is 5). In this case, we get (",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b21",
      "type": "output",
      "text": "The max of 7 and 5 is 7.\n",
      "html": "<pre>The max of 7 and 5 is 7.\n</pre>",
      "canCheck": false
    },
    {
      "id": "the-conditional-operator_b22",
      "type": "section",
      "title": "The conditional operator evaluates as part of an expression",
      "html": "<h3 class=\"cpp-section-heading\">The conditional operator evaluates as part of an expression</h3>",
      "canCheck": false
    },
    {
      "id": "the-conditional-operator_b23",
      "type": "paragraph",
      "html": "<p>Since the conditional operator is evaluated as part of an expression, it can be used anywhere an expression is accepted. In cases where the operands of the conditional operator are constant expressions, the conditional operator can be used in a constant expression.</p>",
      "preview": "Since the conditional operator is evaluated as part of an expression, it can be ",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b24",
      "type": "paragraph",
      "html": "<p>This allows the conditional operator to be used in places where statements cannot be used.</p>",
      "preview": "This allows the conditional operator to be used in places where statements canno",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b25",
      "type": "paragraph",
      "html": "<p>For example, when initializing a variable:</p>",
      "preview": "For example, when initializing a variable:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b26",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n    constexpr bool inBigClassroom { false };\n    constexpr int classSize { inBigClassroom ? 30 : 20 };\n    std::cout << \"The class size is: \" << classSize << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n    constexpr bool inBigClassroom { false };\n    constexpr int classSize { inBigClassroom ? 30 : 20 };\n    std::cout &lt;&lt; \"The class size is: \" &lt;&lt; classSize &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b27",
      "type": "paragraph",
      "html": "<p>There’s no direct if-else replacement for this.</p>",
      "preview": "There’s no direct if-else replacement for this.",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b28",
      "type": "paragraph",
      "html": "<p>You might think to try something like this:</p>",
      "preview": "You might think to try something like this:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b29",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n    constexpr bool inBigClassroom { false };\n\n    if (inBigClassroom)\n        constexpr int classSize { 30 };\n    else\n        constexpr int classSize { 20 }; \n\n    std::cout << \"The class size is: \" << classSize << '\\n'; // Compile error: classSize not defined\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n    constexpr bool inBigClassroom { false };\n\n    if (inBigClassroom)\n        constexpr int classSize { 30 };\n    else\n        constexpr int classSize { 20 }; \n\n    std::cout &lt;&lt; \"The class size is: \" &lt;&lt; classSize &lt;&lt; '\\n'; // Compile error: classSize not defined\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b30",
      "type": "paragraph",
      "html": "<p>However, this won’t compile, and you’ll get an error message that <code>classSize</code> isn’t defined. Much like how variables defined inside functions die at the end of the function, variables defined inside an if-statement or else-statement die at the end of the if-statement or else-statement. Thus, <code>classSize</code> has already been destroyed by the time we try to print it.</p>",
      "preview": "However, this won’t compile, and you’ll get an error message that classSize isn’",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b31",
      "type": "paragraph",
      "html": "<p>If you want to use an if-else, you’d have to do something like this:</p>",
      "preview": "If you want to use an if-else, you’d have to do something like this:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b32",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint getClassSize(bool inBigClassroom)\n{\n    if (inBigClassroom)\n        return 30;\n    else\n        return 20;\n}\n\nint main()\n{\n    const int classSize { getClassSize(false) };\n    std::cout << \"The class size is: \" << classSize << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint getClassSize(bool inBigClassroom)\n{\n    if (inBigClassroom)\n        return 30;\n    else\n        return 20;\n}\n\nint main()\n{\n    const int classSize { getClassSize(false) };\n    std::cout &lt;&lt; \"The class size is: \" &lt;&lt; classSize &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b33",
      "type": "paragraph",
      "html": "<p>This one works because <code>getClassSize(false)</code> is an expression, and the if-else logic is inside a function (where we can use statements). But this is a lot of extra code when we could just use the conditional operator instead.</p>",
      "preview": "This one works because getClassSize(false) is an expression, and the if-else log",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b34",
      "type": "section",
      "title": "Parenthesizing the conditional operator",
      "html": "<h3 class=\"cpp-section-heading\">Parenthesizing the conditional operator</h3>",
      "canCheck": false
    },
    {
      "id": "the-conditional-operator_b35",
      "type": "paragraph",
      "html": "<p>Because C++ prioritizes the evaluation of most operators above the evaluation of the conditional operator, it’s quite easy to write expressions using the conditional operator that don’t evaluate as expected.</p>",
      "preview": "Because C++ prioritizes the evaluation of most operators above the evaluation of",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b36",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>We cover the way that C++ prioritizes the evaluation of operators in future lesson <a href=\"https://www.learncpp.com/cpp-tutorial/operator-precedence-and-associativity/\">6.1 -- Operator precedence and associativity</a>.</p></div>",
      "preview": "Related contentWe cover the way that C++ prioritizes the evaluation of operators in future lesson 6.",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b37",
      "type": "paragraph",
      "html": "<p>For example:</p>",
      "preview": "For example:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b38",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n    int x { 2 };\n    int y { 1 };\n    int z { 10 - x > y ? x : y };\n    std::cout << z;\n    \n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n    int x { 2 };\n    int y { 1 };\n    int z { 10 - x &gt; y ? x : y };\n    std::cout &lt;&lt; z;\n    \n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b39",
      "type": "paragraph",
      "html": "<p>You might expect this to evaluate as <code>10 - (x &gt; y ? x : y)</code> (which would evaluate to <code>8</code>) but it actually evaluates as <code>(10 - x) &gt; y ? x : y</code> (which evaluates to <code>2</code>).</p>",
      "preview": "You might expect this to evaluate as 10 - (x > y ? x : y) (which would evaluate ",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b40",
      "type": "paragraph",
      "html": "<p>Here’s another example that exhibits a common mistake:</p>",
      "preview": "Here’s another example that exhibits a common mistake:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b41",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n    int x { 2 };\n    std::cout << (x < 0) ? \"negative\" : \"non-negative\";\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n    int x { 2 };\n    std::cout &lt;&lt; (x &lt; 0) ? \"negative\" : \"non-negative\";\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b42",
      "type": "paragraph",
      "html": "<p>You might expect this to print <code>non-negative</code>, but it actually prints <code>0</code>.</p>",
      "preview": "You might expect this to print non-negative, but it actually prints 0.",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b43",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Optional reading</p><p>Here’s what’s happening in the above example. First, <code>x &lt; 0</code> evaluates to <code>false</code>. The partially evaluated expression is now <code>std::cout &lt;&lt; false ? \"negative\" : \"non-negative\"</code>. Because <code>operator&lt;&lt;</code> has higher precedence than <code>operator?:</code>, this expression evaluates as if it were written as <code>(std::cout &lt;&lt; false) ? \"negative\" : \"non-negative\"</code>. Thus <code>std::cout &lt;&lt; false</code> is evaluated, which prints <code>0</code> (and returns <code>std::cout</code>).</p><p>The partially evaluated expression is now <code>std::cout ? \"negative\" : \"non-negative\"</code>. Since <code>std::cout</code> is all that is remaining in the condition, the compiler will try to convert it to a <code>bool</code> so the condition can be resolved. Perhaps surprisingly, <code>std::cout</code> has a defined conversion to <code>bool</code>, which will most likely return <code>false</code>. Assuming it returns <code>false</code>, we now have <code>false ? \"negative\" : \"non-negative\"</code>, which evaluates to <code>\"non-negative\"</code>. So our fully evaluated statement is <code>\"non-negative\";</code>. An expression statement that is just a literal (in this case, a string literal) has no effect, so we’re done.</p></div>",
      "preview": "Optional readingHere’s what’s happening in the above example. First, x < 0 evaluates to false. The p",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b44",
      "type": "paragraph",
      "html": "<p>To avoid such evaluation prioritization issues, the conditional operator should be parenthesized as follows:</p>",
      "preview": "To avoid such evaluation prioritization issues, the conditional operator should ",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b45",
      "type": "list",
      "html": "<ul><li>Parenthesize the entire conditional operation (including operands) when used in a compound expression (an expression with other operators).</li><li>For readability, consider parenthesizing the condition if it contains any operators (other than the function call operator).</li></ul>",
      "preview": "Parenthesize the entire conditional operation (including operands) when used in ",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b46",
      "type": "paragraph",
      "html": "<p>The operands of the conditional operator do not need to be parenthesized.</p>",
      "preview": "The operands of the conditional operator do not need to be parenthesized.",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b47",
      "type": "paragraph",
      "html": "<p>Let’s take a look at some statements containing the conditional operator and how they should be parenthesized:</p>",
      "preview": "Let’s take a look at some statements containing the conditional operator and how",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b48",
      "type": "code",
      "language": "cpp",
      "code": "return isStunned ? 0 : movesLeft;           // not used in compound expression, condition contains no operators\nint z { (x > y) ? x : y };                  // not used in compound expression, condition contains operators\nstd::cout << (isAfternoon() ? \"PM\" : \"AM\"); // used in compound expression, condition contains no operators (function call operator excluded)\nstd::cout << ((x > y) ? x : y);             // used in compound expression, condition contains operators",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">return isStunned ? 0 : movesLeft;           // not used in compound expression, condition contains no operators\nint z { (x &gt; y) ? x : y };                  // not used in compound expression, condition contains operators\nstd::cout &lt;&lt; (isAfternoon() ? \"PM\" : \"AM\"); // used in compound expression, condition contains no operators (function call operator excluded)\nstd::cout &lt;&lt; ((x &gt; y) ? x : y);             // used in compound expression, condition contains operators</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b49",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Parenthesize the entire conditional operation (including operands) when used in a compound expression.</p><p>For readability, consider parenthesizing the condition if it contains any operators (other than the function call operator).</p></div>",
      "preview": "Best practiceParenthesize the entire conditional operation (including operands) when used in a compo",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b50",
      "type": "section",
      "title": "The type of the expressions must match or be convertible",
      "html": "<h3 class=\"cpp-section-heading\">The type of the expressions must match or be convertible</h3>",
      "canCheck": false
    },
    {
      "id": "the-conditional-operator_b51",
      "type": "paragraph",
      "html": "<p>To comply with C++’s type checking rules, one of the following must be true:</p>",
      "preview": "To comply with C++’s type checking rules, one of the following must be true:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b52",
      "type": "list",
      "html": "<ul><li>The type of the second and third operand must match.</li><li>The compiler must be able to find a way to convert one or both of the second and third operands to matching types. The conversion rules the compiler uses are fairly complex and may yield surprising results in some cases.</li></ul>",
      "preview": "The type of the second and third operand must match.The compiler must be able to",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b53",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">For advanced readers</p><p>Alternatively, one or both of the second and third operands is allowed to be a throw expression. We cover <code>throw</code> in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/basic-exception-handling/\">27.2 -- Basic exception handling</a>.</p></div>",
      "preview": "For advanced readersAlternatively, one or both of the second and third operands is allowed to be a t",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b54",
      "type": "paragraph",
      "html": "<p>For example:</p>",
      "preview": "For example:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b55",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n    std::cout << (true ? 1 : 2) << '\\n';    // okay: both operands have matching type int\n\n    std::cout << (false ? 1 : 2.2) << '\\n'; // okay: int value 1 converted to double\n\n    std::cout << (true ? -1 : 2u) << '\\n';  // surprising result: -1 converted to unsigned int, result out of range\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n    std::cout &lt;&lt; (true ? 1 : 2) &lt;&lt; '\\n';    // okay: both operands have matching type int\n\n    std::cout &lt;&lt; (false ? 1 : 2.2) &lt;&lt; '\\n'; // okay: int value 1 converted to double\n\n    std::cout &lt;&lt; (true ? -1 : 2u) &lt;&lt; '\\n';  // surprising result: -1 converted to unsigned int, result out of range\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b56",
      "type": "paragraph",
      "html": "<p>Assuming 4 byte integers, the above prints:</p>",
      "preview": "Assuming 4 byte integers, the above prints:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b57",
      "type": "output",
      "text": "1\n2.2\n4294967295\n",
      "html": "<pre>1\n2.2\n4294967295\n</pre>",
      "canCheck": false
    },
    {
      "id": "the-conditional-operator_b58",
      "type": "paragraph",
      "html": "<p>In general, it’s okay to mix operands with fundamental types (excluding mixing signed and unsigned values). If either operand is not a fundamental type, it’s generally best to explicitly convert one or both operands to a matching type yourself so you know exactly what you’ll get.</p>",
      "preview": "In general, it’s okay to mix operands with fundamental types (excluding mixing s",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b59",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>The surprising case above related to mixing signed and unsigned values is due to the arithmetic conversion rules, which we cover in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/arithmetic-conversions/\">10.5 -- Arithmetic conversions</a>.</p></div>",
      "preview": "Related contentThe surprising case above related to mixing signed and unsigned values is due to the ",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b60",
      "type": "paragraph",
      "html": "<p>If the compiler can’t find a way to convert the second and third operands to a matching type, a compile error will result:</p>",
      "preview": "If the compiler can’t find a way to convert the second and third operands to a m",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b61",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n    constexpr int x{ 5 };\n    std::cout << ((x != 5) ? x : \"x is 5\"); // compile error: compiler can't find common type for constexpr int and C-style string literal\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n    constexpr int x{ 5 };\n    std::cout &lt;&lt; ((x != 5) ? x : \"x is 5\"); // compile error: compiler can't find common type for constexpr int and C-style string literal\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b62",
      "type": "paragraph",
      "html": "<p>In the above example, one of the expressions is an integer, and the other is a C-style string literal. The compiler will not be able to find a matching type on its own, so a compile error will result.</p>",
      "preview": "In the above example, one of the expressions is an integer, and the other is a C",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b63",
      "type": "paragraph",
      "html": "<p>In such cases, you can either do an explicit conversion, or use an if-else statement:</p>",
      "preview": "In such cases, you can either do an explicit conversion, or use an if-else state",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b64",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <string>\n\nint main()\n{\n    int x{ 5 }; // intentionally non-constexpr for this example\n\n    // We can explicitly convert the types to match\n    std::cout << ((x != 5) ? std::to_string(x) : std::string{\"x is 5\"}) << '\\n';\n\n    // Or use an if-else statement\n    if (x != 5)\n        std::cout << x << '\\n';\n    else\n        std::cout << \"x is 5\" << '\\n';\n    \n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;string&gt;\n\nint main()\n{\n    int x{ 5 }; // intentionally non-constexpr for this example\n\n    // We can explicitly convert the types to match\n    std::cout &lt;&lt; ((x != 5) ? std::to_string(x) : std::string{\"x is 5\"}) &lt;&lt; '\\n';\n\n    // Or use an if-else statement\n    if (x != 5)\n        std::cout &lt;&lt; x &lt;&lt; '\\n';\n    else\n        std::cout &lt;&lt; \"x is 5\" &lt;&lt; '\\n';\n    \n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b65",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">For advanced readers</p><p>If <code>x</code> is constexpr, then the condition <code>x != 5</code> is a constant expression. In such cases, using <code>if constexpr</code> should be preferred over <code>if</code>, and your compiler may generate a warning indicating so (which will be promoted to an error if you are treating warnings as errors).</p><p>Since we haven’t covered <code>if constexpr</code> yet (we do so in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/constexpr-if-statements/\">8.4 -- Constexpr if statements</a>), <code>x</code> is non-constexpr in this example to avoid the potential compiler warning.</p></div>",
      "preview": "For advanced readersIf x is constexpr, then the condition x != 5 is a constant expression. In such c",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b66",
      "type": "section",
      "title": "So when should you use the conditional operator?",
      "html": "<h3 class=\"cpp-section-heading\">So when should you use the conditional operator?</h3>",
      "canCheck": false
    },
    {
      "id": "the-conditional-operator_b67",
      "type": "paragraph",
      "html": "<p>The conditional operator is most useful when doing one of the following:</p>",
      "preview": "The conditional operator is most useful when doing one of the following:",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b68",
      "type": "list",
      "html": "<ul><li>Initializing an object with one of two values.</li><li>Assigning one of two values to an object.</li><li>Passing one of two values to a function.</li><li>Returning one of two values from a function.</li><li>Printing one of two values.</li></ul>",
      "preview": "Initializing an object with one of two values.Assigning one of two values to an ",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b69",
      "type": "paragraph",
      "html": "<p>Complicated expressions should generally avoid use of the conditional operator, as they tend to be error prone and hard to read.</p>",
      "preview": "Complicated expressions should generally avoid use of the conditional operator, ",
      "canCheck": true
    },
    {
      "id": "the-conditional-operator_b70",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Prefer to avoid the conditional operator in complicated expressions.</p></div>",
      "preview": "Best practicePrefer to avoid the conditional operator in complicated expressions.",
      "canCheck": true
    }
  ]
}