{
  "index": 178,
  "chapterId": "13",
  "number": "13.13",
  "title": "13.13 — Class templates",
  "url": "https://www.learncpp.com/cpp-tutorial/class-templates/",
  "slug": "class-templates",
  "scrapedAt": "2026-09-15T16:30:08.493Z",
  "totalBlocks": 73,
  "totalCheckpoints": 65,
  "blocks": [
    {
      "id": "class-templates_b0",
      "type": "paragraph",
      "html": "<p>In lesson <a href=\"https://www.learncpp.com/cpp-tutorial/function-templates/\">11.6 -- Function templates</a>, we introduced the challenge of having to create a separate (overloaded) function for each different set of types we want to work with:</p>",
      "preview": "In lesson 11.6 -- Function templates, we introduced the challenge of having to c",
      "canCheck": true
    },
    {
      "id": "class-templates_b1",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\n// function to calculate the greater of two int values\nint max(int x, int y)\n{\n    return (x < y) ? y : x;\n}\n\n// almost identical function to calculate the greater of two double values\n// the only difference is the type information\ndouble max(double x, double y)\n{\n    return (x < y) ? y : x;\n}\n\nint main()\n{\n    std::cout << max(5, 6);     // calls max(int, int)\n    std::cout << '\\n';\n    std::cout << max(1.2, 3.4); // calls max(double, double)\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\n// function to calculate the greater of two int values\nint max(int x, int y)\n{\n    return (x &lt; y) ? y : x;\n}\n\n// almost identical function to calculate the greater of two double values\n// the only difference is the type information\ndouble max(double x, double y)\n{\n    return (x &lt; y) ? y : x;\n}\n\nint main()\n{\n    std::cout &lt;&lt; max(5, 6);     // calls max(int, int)\n    std::cout &lt;&lt; '\\n';\n    std::cout &lt;&lt; max(1.2, 3.4); // calls max(double, double)\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b2",
      "type": "paragraph",
      "html": "<p>The solution to this was to create a function template that the compiler can use to instantiate normal functions for whichever set of types we need:</p>",
      "preview": "The solution to this was to create a function template that the compiler can use",
      "canCheck": true
    },
    {
      "id": "class-templates_b3",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\n// a single function template for max\ntemplate <typename T>\nT max(T x, T y)\n{\n    return (x < y) ? y : x;\n}\n\nint main()\n{\n    std::cout << max(5, 6);     // instantiates and calls max<int>(int, int)\n    std::cout << '\\n';\n    std::cout << max(1.2, 3.4); // instantiates and calls max<double>(double, double)\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\n// a single function template for max\ntemplate &lt;typename T&gt;\nT max(T x, T y)\n{\n    return (x &lt; y) ? y : x;\n}\n\nint main()\n{\n    std::cout &lt;&lt; max(5, 6);     // instantiates and calls max&lt;int&gt;(int, int)\n    std::cout &lt;&lt; '\\n';\n    std::cout &lt;&lt; max(1.2, 3.4); // instantiates and calls max&lt;double&gt;(double, double)\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b4",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Related content</p><p>We cover how function template instantiation works in lesson <a href=\"https://www.learncpp.com/cpp-tutorial/function-template-instantiation/\">11.7 -- Function template instantiation</a>.</p></div>",
      "preview": "Related contentWe cover how function template instantiation works in lesson 11.7 -- Function templat",
      "canCheck": true
    },
    {
      "id": "class-templates_b5",
      "type": "section",
      "title": "Aggregate types have similar challenges",
      "html": "<h3 class=\"cpp-section-heading\">Aggregate types have similar challenges</h3>",
      "canCheck": false
    },
    {
      "id": "class-templates_b6",
      "type": "paragraph",
      "html": "<p>We run into similar challenges with aggregate types (both structs/classes/unions and arrays).</p>",
      "preview": "We run into similar challenges with aggregate types (both structs/classes/unions",
      "canCheck": true
    },
    {
      "id": "class-templates_b7",
      "type": "paragraph",
      "html": "<p>For example, let’s say we’re writing a program where we need to work with pairs of <code>int</code> values, and need to determine which of the two numbers is larger. We might write a program like this:</p>",
      "preview": "For example, let’s say we’re writing a program where we need to work with pairs ",
      "canCheck": true
    },
    {
      "id": "class-templates_b8",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nstruct Pair\n{\n    int first{};\n    int second{};\n};\n\nconstexpr int max(Pair p) // pass by value because Pair is small\n{\n    return (p.first < p.second ? p.second : p.first);\n}\n\nint main()\n{\n    Pair p1{ 5, 6 };\n    std::cout << max(p1) << \" is larger\\n\";\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nstruct Pair\n{\n    int first{};\n    int second{};\n};\n\nconstexpr int max(Pair p) // pass by value because Pair is small\n{\n    return (p.first &lt; p.second ? p.second : p.first);\n}\n\nint main()\n{\n    Pair p1{ 5, 6 };\n    std::cout &lt;&lt; max(p1) &lt;&lt; \" is larger\\n\";\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b9",
      "type": "paragraph",
      "html": "<p>Later, we discover that we also need pairs of <code>double</code> values. So we update our program to the following:</p>",
      "preview": "Later, we discover that we also need pairs of double values. So we update our pr",
      "canCheck": true
    },
    {
      "id": "class-templates_b10",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nstruct Pair\n{\n    int first{};\n    int second{};\n};\n\nstruct Pair // compile error: erroneous redefinition of Pair\n{\n    double first{};\n    double second{};\n};\n\nconstexpr int max(Pair p)\n{\n    return (p.first < p.second ? p.second : p.first);\n}\n\nconstexpr double max(Pair p) // compile error: overloaded function differs only by return type\n{\n    return (p.first < p.second ? p.second : p.first);\n}\n\nint main()\n{\n    Pair p1{ 5, 6 };\n    std::cout << max(p1) << \" is larger\\n\";\n\n    Pair p2{ 1.2, 3.4 };\n    std::cout << max(p2) << \" is larger\\n\";\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nstruct Pair\n{\n    int first{};\n    int second{};\n};\n\nstruct Pair // compile error: erroneous redefinition of Pair\n{\n    double first{};\n    double second{};\n};\n\nconstexpr int max(Pair p)\n{\n    return (p.first &lt; p.second ? p.second : p.first);\n}\n\nconstexpr double max(Pair p) // compile error: overloaded function differs only by return type\n{\n    return (p.first &lt; p.second ? p.second : p.first);\n}\n\nint main()\n{\n    Pair p1{ 5, 6 };\n    std::cout &lt;&lt; max(p1) &lt;&lt; \" is larger\\n\";\n\n    Pair p2{ 1.2, 3.4 };\n    std::cout &lt;&lt; max(p2) &lt;&lt; \" is larger\\n\";\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b11",
      "type": "paragraph",
      "html": "<p>Unfortunately, this program won’t compile, and has a number of problems that need to be addressed.</p>",
      "preview": "Unfortunately, this program won’t compile, and has a number of problems that nee",
      "canCheck": true
    },
    {
      "id": "class-templates_b12",
      "type": "paragraph",
      "html": "<p>First, unlike functions, type definitions can’t be overloaded. The compiler will treat double second definition of <code>Pair</code> as an erroneous redeclaration of the first definition of <code>Pair</code>. Second, although functions can be overloaded, our <code>max(Pair)</code> functions only differ by return type, and overloaded functions can’t be differentiated solely by return type. Third, there is a lot of redundancy here. Each <code>Pair</code> struct is identical (except for the data type) and same with our <code>max(Pair)</code> functions (except for the return type).</p>",
      "preview": "First, unlike functions, type definitions can’t be overloaded. The compiler will",
      "canCheck": true
    },
    {
      "id": "class-templates_b13",
      "type": "paragraph",
      "html": "<p>We could solve the first two issues by giving our <code>Pair</code> structs different names (e.g. <code>PairInt</code> and <code>PairDouble</code>). But then we both have to remember our naming scheme, and essentially clone a bunch of code for each additional pair type we want, which doesn’t solve the redundancy problem.</p>",
      "preview": "We could solve the first two issues by giving our Pair structs different names (",
      "canCheck": true
    },
    {
      "id": "class-templates_b14",
      "type": "paragraph",
      "html": "<p>Fortunately, we can do better.</p>",
      "preview": "Fortunately, we can do better.",
      "canCheck": true
    },
    {
      "id": "class-templates_b15",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Author’s note</p><p>Before proceeding, please review lessons <a href=\"https://www.learncpp.com/cpp-tutorial/function-templates/\">11.6 -- Function templates</a> and <a href=\"https://www.learncpp.com/cpp-tutorial/function-template-instantiation/\">11.7 -- Function template instantiation</a> if you’re hazy on how function templates, template types, or function template instantiation works.</p></div>",
      "preview": "Author’s noteBefore proceeding, please review lessons 11.6 -- Function templates and 11.7 -- Functio",
      "canCheck": true
    },
    {
      "id": "class-templates_b16",
      "type": "section",
      "title": "Class templates",
      "html": "<h3 class=\"cpp-section-heading\">Class templates</h3>",
      "canCheck": false
    },
    {
      "id": "class-templates_b17",
      "type": "paragraph",
      "html": "<p>Much like a function template is a template definition for instantiating functions, a <strong>class template</strong> is a template definition for instantiating class types.</p>",
      "preview": "Much like a function template is a template definition for instantiating functio",
      "canCheck": true
    },
    {
      "id": "class-templates_b18",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">A reminder</p><p>A “class type” is a struct, class, or union type. Although we’ll be demonstrating “class templates” on structs for simplicity, everything here applies equally well to classes.</p></div>",
      "preview": "A reminderA “class type” is a struct, class, or union type. Although we’ll be demonstrating “class t",
      "canCheck": true
    },
    {
      "id": "class-templates_b19",
      "type": "paragraph",
      "html": "<p>As a reminder, here’s our <code>int</code> pair struct definition:</p>",
      "preview": "As a reminder, here’s our int pair struct definition:",
      "canCheck": true
    },
    {
      "id": "class-templates_b20",
      "type": "code",
      "language": "cpp",
      "code": "struct Pair\n{\n    int first{};\n    int second{};\n};",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">struct Pair\n{\n    int first{};\n    int second{};\n};</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b21",
      "type": "paragraph",
      "html": "<p>Let’s rewrite our pair class as a class template:</p>",
      "preview": "Let’s rewrite our pair class as a class template:",
      "canCheck": true
    },
    {
      "id": "class-templates_b22",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\ntemplate <typename T>\nstruct Pair\n{\n    T first{};\n    T second{};\n};\n\nint main()\n{\n    Pair<int> p1{ 5, 6 };        // instantiates Pair<int> and creates object p1\n    std::cout << p1.first << ' ' << p1.second << '\\n';\n\n    Pair<double> p2{ 1.2, 3.4 }; // instantiates Pair<double> and creates object p2\n    std::cout << p2.first << ' ' << p2.second << '\\n';\n\n    Pair<double> p3{ 7.8, 9.0 }; // creates object p3 using prior definition for Pair<double>\n    std::cout << p3.first << ' ' << p3.second << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\ntemplate &lt;typename T&gt;\nstruct Pair\n{\n    T first{};\n    T second{};\n};\n\nint main()\n{\n    Pair&lt;int&gt; p1{ 5, 6 };        // instantiates Pair&lt;int&gt; and creates object p1\n    std::cout &lt;&lt; p1.first &lt;&lt; ' ' &lt;&lt; p1.second &lt;&lt; '\\n';\n\n    Pair&lt;double&gt; p2{ 1.2, 3.4 }; // instantiates Pair&lt;double&gt; and creates object p2\n    std::cout &lt;&lt; p2.first &lt;&lt; ' ' &lt;&lt; p2.second &lt;&lt; '\\n';\n\n    Pair&lt;double&gt; p3{ 7.8, 9.0 }; // creates object p3 using prior definition for Pair&lt;double&gt;\n    std::cout &lt;&lt; p3.first &lt;&lt; ' ' &lt;&lt; p3.second &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b23",
      "type": "paragraph",
      "html": "<p>Just like with function templates, we start a class template definition with a template parameter declaration. We begin with the <code>template</code> keyword. Next, we specify all of the template types that our class template will use inside angled brackets (&lt;&gt;). For each template type that we need, we use the keyword <code>typename</code> (preferred) or <code>class</code> (not preferred), followed by the name of the template type (e.g. <code>T</code>). In this case, since both of our members will be the same type, we only need one template type.</p>",
      "preview": "Just like with function templates, we start a class template definition with a t",
      "canCheck": true
    },
    {
      "id": "class-templates_b24",
      "type": "paragraph",
      "html": "<p>Next, we define our struct like usual, except we can use our template type (<code>T</code>) wherever we want a templated type that will be replaced with a real type later. That’s it! We’re done with the class template definition.</p>",
      "preview": "Next, we define our struct like usual, except we can use our template type (T) w",
      "canCheck": true
    },
    {
      "id": "class-templates_b25",
      "type": "paragraph",
      "html": "<p>Inside main, we can instantiate <code>Pair</code> objects using whatever types we desire. First, we instantiate an object of type <code>Pair&lt;int&gt;</code>. Because a type definition for <code>Pair&lt;int&gt;</code> doesn’t exist yet, the compiler uses the class template to instantiate a struct type definition named <code>Pair&lt;int&gt;</code>, where all occurrences of template type <code>T</code> are replaced by type <code>int</code>.</p>",
      "preview": "Inside main, we can instantiate Pair objects using whatever types we desire. Fir",
      "canCheck": true
    },
    {
      "id": "class-templates_b26",
      "type": "paragraph",
      "html": "<p>Next, we instantiate an object of type <code>Pair&lt;double&gt;</code>, which instantiates a struct type definition named <code>Pair&lt;double&gt;</code> where <code>T</code> is replaced by <code>double</code>. For <code>p3</code>, <code>Pair&lt;double&gt;</code> has already been instantiated, so the compiler will use the prior type definition.</p>",
      "preview": "Next, we instantiate an object of type Pair<double>, which instantiates a struct",
      "canCheck": true
    },
    {
      "id": "class-templates_b27",
      "type": "paragraph",
      "html": "<p>Here’s the same example as above, showing what the compiler actually compiles after all template instantiations are done:</p>",
      "preview": "Here’s the same example as above, showing what the compiler actually compiles af",
      "canCheck": true
    },
    {
      "id": "class-templates_b28",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\n// A declaration for our Pair class template\n// (we don't need the definition any more since it's not used)\ntemplate <typename T>\nstruct Pair;\n\n// Explicitly define what Pair<int> looks like\ntemplate <> // tells the compiler this is a template type with no template parameters\nstruct Pair<int>\n{\n    int first{};\n    int second{};\n};\n\n// Explicitly define what Pair<double> looks like\ntemplate <> // tells the compiler this is a template type with no template parameters\nstruct Pair<double>\n{\n    double first{};\n    double second{};\n};\n\nint main()\n{\n    Pair<int> p1{ 5, 6 };        // instantiates Pair<int> and creates object p1\n    std::cout << p1.first << ' ' << p1.second << '\\n';\n\n    Pair<double> p2{ 1.2, 3.4 }; // instantiates Pair<double> and creates object p2\n    std::cout << p2.first << ' ' << p2.second << '\\n';\n\n    Pair<double> p3{ 7.8, 9.0 }; // creates object p3 using prior definition for Pair<double>\n    std::cout << p3.first << ' ' << p3.second << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\n// A declaration for our Pair class template\n// (we don't need the definition any more since it's not used)\ntemplate &lt;typename T&gt;\nstruct Pair;\n\n// Explicitly define what Pair&lt;int&gt; looks like\ntemplate &lt;&gt; // tells the compiler this is a template type with no template parameters\nstruct Pair&lt;int&gt;\n{\n    int first{};\n    int second{};\n};\n\n// Explicitly define what Pair&lt;double&gt; looks like\ntemplate &lt;&gt; // tells the compiler this is a template type with no template parameters\nstruct Pair&lt;double&gt;\n{\n    double first{};\n    double second{};\n};\n\nint main()\n{\n    Pair&lt;int&gt; p1{ 5, 6 };        // instantiates Pair&lt;int&gt; and creates object p1\n    std::cout &lt;&lt; p1.first &lt;&lt; ' ' &lt;&lt; p1.second &lt;&lt; '\\n';\n\n    Pair&lt;double&gt; p2{ 1.2, 3.4 }; // instantiates Pair&lt;double&gt; and creates object p2\n    std::cout &lt;&lt; p2.first &lt;&lt; ' ' &lt;&lt; p2.second &lt;&lt; '\\n';\n\n    Pair&lt;double&gt; p3{ 7.8, 9.0 }; // creates object p3 using prior definition for Pair&lt;double&gt;\n    std::cout &lt;&lt; p3.first &lt;&lt; ' ' &lt;&lt; p3.second &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b29",
      "type": "paragraph",
      "html": "<p>You can compile this example directly and see that it works as expected!</p>",
      "preview": "You can compile this example directly and see that it works as expected!",
      "canCheck": true
    },
    {
      "id": "class-templates_b30",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">For advanced readers</p><p>The above example makes use of a feature called class template specialization (covered in future lesson <a href=\"https://www.learncpp.com/cpp-tutorial/class-template-specialization/\">26.4 -- Class template specialization</a>). Knowledge of how this feature works is not required at this point.</p></div>",
      "preview": "For advanced readersThe above example makes use of a feature called class template specialization (c",
      "canCheck": true
    },
    {
      "id": "class-templates_b31",
      "type": "section",
      "title": "Using our class template in a function",
      "html": "<h3 class=\"cpp-section-heading\">Using our class template in a function</h3>",
      "canCheck": false
    },
    {
      "id": "class-templates_b32",
      "type": "paragraph",
      "html": "<p>Now let’s return to the challenge of making our <code>max()</code> function work with different types. Because the compiler treats <code>Pair&lt;int&gt;</code> and <code>Pair&lt;double&gt;</code> as separate types, we could use overloaded functions that are differentiated by parameter type:</p>",
      "preview": "Now let’s return to the challenge of making our max() function work with differe",
      "canCheck": true
    },
    {
      "id": "class-templates_b33",
      "type": "code",
      "language": "cpp",
      "code": "constexpr int max(Pair<int> p)\n{\n    return (p.first < p.second ? p.second : p.first);\n}\n\nconstexpr double max(Pair<double> p) // okay: overloaded function differentiated by parameter type\n{\n    return (p.first < p.second ? p.second : p.first);\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">constexpr int max(Pair&lt;int&gt; p)\n{\n    return (p.first &lt; p.second ? p.second : p.first);\n}\n\nconstexpr double max(Pair&lt;double&gt; p) // okay: overloaded function differentiated by parameter type\n{\n    return (p.first &lt; p.second ? p.second : p.first);\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b34",
      "type": "paragraph",
      "html": "<p>While this compiles, it doesn’t solve the redundancy problem. What we really want is a function that can take a pair of any type. In other words, we want a function that takes a parameter of type <code>Pair&lt;T&gt;</code>, where T is a template type parameter. And that means we need a function template for this job!</p>",
      "preview": "While this compiles, it doesn’t solve the redundancy problem. What we really wan",
      "canCheck": true
    },
    {
      "id": "class-templates_b35",
      "type": "paragraph",
      "html": "<p>Here’s a full example, with <code>max()</code> being implemented as a function template:</p>",
      "preview": "Here’s a full example, with max() being implemented as a function template:",
      "canCheck": true
    },
    {
      "id": "class-templates_b36",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\ntemplate <typename T>\nstruct Pair\n{\n    T first{};\n    T second{};\n};\n\ntemplate <typename T>\nconstexpr T max(Pair<T> p)\n{\n    return (p.first < p.second ? p.second : p.first);\n}\n\nint main()\n{\n    Pair<int> p1{ 5, 6 };\n    std::cout << max<int>(p1) << \" is larger\\n\"; // explicit call to max<int>\n\n    Pair<double> p2{ 1.2, 3.4 };\n    std::cout << max(p2) << \" is larger\\n\"; // call to max<double> using template argument deduction (prefer this)\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\ntemplate &lt;typename T&gt;\nstruct Pair\n{\n    T first{};\n    T second{};\n};\n\ntemplate &lt;typename T&gt;\nconstexpr T max(Pair&lt;T&gt; p)\n{\n    return (p.first &lt; p.second ? p.second : p.first);\n}\n\nint main()\n{\n    Pair&lt;int&gt; p1{ 5, 6 };\n    std::cout &lt;&lt; max&lt;int&gt;(p1) &lt;&lt; \" is larger\\n\"; // explicit call to max&lt;int&gt;\n\n    Pair&lt;double&gt; p2{ 1.2, 3.4 };\n    std::cout &lt;&lt; max(p2) &lt;&lt; \" is larger\\n\"; // call to max&lt;double&gt; using template argument deduction (prefer this)\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b37",
      "type": "paragraph",
      "html": "<p>The <code>max()</code> function template is pretty straightforward. Because we want to pass in a <code>Pair&lt;T&gt;</code>, we need the compiler to understand what <code>T</code> is. Therefore, we need to start our function with a template parameter declaration that defines template type T. We can then use <code>T</code> as both our return type, and as the template type for <code>Pair&lt;T&gt;</code>.</p>",
      "preview": "The max() function template is pretty straightforward. Because we want to pass i",
      "canCheck": true
    },
    {
      "id": "class-templates_b38",
      "type": "paragraph",
      "html": "<p>When the <code>max()</code> function is called with a <code>Pair&lt;int&gt;</code> argument, the compiler will instantiate the function <code>int max&lt;int&gt;(Pair&lt;int&gt;)</code> from the function template, where template type <code>T</code> is replaced with <code>int</code>. The following snippet shows what the compiler actually instantiates in such a case:</p>",
      "preview": "When the max() function is called with a Pair<int> argument, the compiler will i",
      "canCheck": true
    },
    {
      "id": "class-templates_b39",
      "type": "code",
      "language": "cpp",
      "code": "template <>\nconstexpr int max(Pair<int> p)\n{\n    return (p.first < p.second ? p.second : p.first);\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">template &lt;&gt;\nconstexpr int max(Pair&lt;int&gt; p)\n{\n    return (p.first &lt; p.second ? p.second : p.first);\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b40",
      "type": "paragraph",
      "html": "<p>As with all calls to a function template, we can either be explicit about the template type argument (e.g. <code>max&lt;int&gt;(p1)</code>) or we can be implicit (e.g. <code>max(p2)</code>) and let the compiler use template argument deduction to determine what the template type argument should be.</p>",
      "preview": "As with all calls to a function template, we can either be explicit about the te",
      "canCheck": true
    },
    {
      "id": "class-templates_b41",
      "type": "section",
      "title": "Class templates with template type and non-template type members",
      "html": "<h3 class=\"cpp-section-heading\">Class templates with template type and non-template type members</h3>",
      "canCheck": false
    },
    {
      "id": "class-templates_b42",
      "type": "paragraph",
      "html": "<p>Class templates can have some members using a template type and other members using a normal (non-template) type. For example:</p>",
      "preview": "Class templates can have some members using a template type and other members us",
      "canCheck": true
    },
    {
      "id": "class-templates_b43",
      "type": "code",
      "language": "cpp",
      "code": "template <typename T>\nstruct Foo\n{\n    T first{};    // first will have whatever type T is replaced with\n    int second{}; // second will always have type int, regardless of what type T is\n};",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">template &lt;typename T&gt;\nstruct Foo\n{\n    T first{};    // first will have whatever type T is replaced with\n    int second{}; // second will always have type int, regardless of what type T is\n};</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b44",
      "type": "paragraph",
      "html": "<p>This works exactly like you’d expect: <code>first</code> will be whatever the template type <code>T</code> is, and <code>second</code> will always be an <code>int</code>.</p>",
      "preview": "This works exactly like you’d expect: first will be whatever the template type T",
      "canCheck": true
    },
    {
      "id": "class-templates_b45",
      "type": "section",
      "title": "Class templates with multiple template types",
      "html": "<h3 class=\"cpp-section-heading\">Class templates with multiple template types</h3>",
      "canCheck": false
    },
    {
      "id": "class-templates_b46",
      "type": "paragraph",
      "html": "<p>Class templates can also have multiple template types. For example, if we wanted the two members of our <code>Pair</code> class to be able to have different types, we can define our <code>Pair</code> class template with two template types:</p>",
      "preview": "Class templates can also have multiple template types. For example, if we wanted",
      "canCheck": true
    },
    {
      "id": "class-templates_b47",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\ntemplate <typename T, typename U>\nstruct Pair\n{\n    T first{};\n    U second{};\n};\n\ntemplate <typename T, typename U>\nvoid print(Pair<T, U> p)\n{\n    std::cout << '[' << p.first << \", \" << p.second << ']';\n}\n\nint main()\n{\n    Pair<int, double> p1{ 1, 2.3 }; // a pair holding an int and a double\n    Pair<double, int> p2{ 4.5, 6 }; // a pair holding a double and an int\n    Pair<int, int> p3{ 7, 8 };      // a pair holding two ints\n\n    print(p2);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\ntemplate &lt;typename T, typename U&gt;\nstruct Pair\n{\n    T first{};\n    U second{};\n};\n\ntemplate &lt;typename T, typename U&gt;\nvoid print(Pair&lt;T, U&gt; p)\n{\n    std::cout &lt;&lt; '[' &lt;&lt; p.first &lt;&lt; \", \" &lt;&lt; p.second &lt;&lt; ']';\n}\n\nint main()\n{\n    Pair&lt;int, double&gt; p1{ 1, 2.3 }; // a pair holding an int and a double\n    Pair&lt;double, int&gt; p2{ 4.5, 6 }; // a pair holding a double and an int\n    Pair&lt;int, int&gt; p3{ 7, 8 };      // a pair holding two ints\n\n    print(p2);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b48",
      "type": "paragraph",
      "html": "<p>To define multiple template types, in our template parameter declaration, we separate each of our desired template types with a comma. In the above example we define two different template types, one named <code>T</code>, and one named <code>U</code>. The actual template type arguments for <code>T</code> and <code>U</code> can be different (as in the case of <code>p1</code> and <code>p2</code> above) or the same (as in the case of <code>p3</code>).</p>",
      "preview": "To define multiple template types, in our template parameter declaration, we sep",
      "canCheck": true
    },
    {
      "id": "class-templates_b49",
      "type": "section",
      "title": "Making a function template work with more than one class type",
      "html": "<h3 class=\"cpp-section-heading\">Making a function template work with more than one class type</h3>",
      "canCheck": false
    },
    {
      "id": "class-templates_b50",
      "type": "paragraph",
      "html": "<p>Consider the <code>print()</code> function template from the above example:</p>",
      "preview": "Consider the print() function template from the above example:",
      "canCheck": true
    },
    {
      "id": "class-templates_b51",
      "type": "code",
      "language": "cpp",
      "code": "template <typename T, typename U>\nvoid print(Pair<T, U> p)\n{\n    std::cout << '[' << p.first << \", \" << p.second << ']';\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">template &lt;typename T, typename U&gt;\nvoid print(Pair&lt;T, U&gt; p)\n{\n    std::cout &lt;&lt; '[' &lt;&lt; p.first &lt;&lt; \", \" &lt;&lt; p.second &lt;&lt; ']';\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b52",
      "type": "paragraph",
      "html": "<p>Because we’ve explicitly defined the function parameter as a <code>Pair&lt;T, U&gt;</code>, only arguments of type <code>Pair&lt;T, U&gt;</code> (or those that can be converted to a <code>Pair&lt;T, U&gt;</code>) will match. This is ideal if we only want to be able to call our function with a <code>Pair&lt;T, U&gt;</code> argument.</p>",
      "preview": "Because we’ve explicitly defined the function parameter as a Pair<T, U>, only ar",
      "canCheck": true
    },
    {
      "id": "class-templates_b53",
      "type": "paragraph",
      "html": "<p>In some cases, we may write function templates that we want to use with any type that will successfully compile. To do that, we simply use a type template parameter as the function parameter instead.</p>",
      "preview": "In some cases, we may write function templates that we want to use with any type",
      "canCheck": true
    },
    {
      "id": "class-templates_b54",
      "type": "paragraph",
      "html": "<p>For example:</p>",
      "preview": "For example:",
      "canCheck": true
    },
    {
      "id": "class-templates_b55",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\ntemplate <typename T, typename U>\nstruct Pair\n{\n    T first{};\n    U second{};\n};\n\nstruct Point\n{\n    int first{};\n    int second{};\n};\n\ntemplate <typename T>\nvoid print(T p) // type template parameter will match anything\n{\n    std::cout << '[' << p.first << \", \" << p.second << ']'; // will only compile if type has first and second members\n}\n\nint main()\n{\n    Pair<double, int> p1{ 4.5, 6 };\n    print(p1); // matches print(Pair<double, int>)\n\n    std::cout << '\\n';\n\n    Point p2 { 7, 8 };\n    print(p2); // matches print(Point)\n\n    std::cout << '\\n';\n    \n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\ntemplate &lt;typename T, typename U&gt;\nstruct Pair\n{\n    T first{};\n    U second{};\n};\n\nstruct Point\n{\n    int first{};\n    int second{};\n};\n\ntemplate &lt;typename T&gt;\nvoid print(T p) // type template parameter will match anything\n{\n    std::cout &lt;&lt; '[' &lt;&lt; p.first &lt;&lt; \", \" &lt;&lt; p.second &lt;&lt; ']'; // will only compile if type has first and second members\n}\n\nint main()\n{\n    Pair&lt;double, int&gt; p1{ 4.5, 6 };\n    print(p1); // matches print(Pair&lt;double, int&gt;)\n\n    std::cout &lt;&lt; '\\n';\n\n    Point p2 { 7, 8 };\n    print(p2); // matches print(Point)\n\n    std::cout &lt;&lt; '\\n';\n    \n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b56",
      "type": "paragraph",
      "html": "<p>In the above example, we’ve rewritten <code>print()</code> so that it has only a single type template parameter (<code>T</code>), which will match any type. The body of the function will compile successfully for any class type that has a <code>first</code> and <code>second</code> member. We demonstrate this by calling <code>print()</code> with an object of type <code>Pair&lt;double, int&gt;</code>, and then again with an object of type <code>Point</code>.</p>",
      "preview": "In the above example, we’ve rewritten print() so that it has only a single type ",
      "canCheck": true
    },
    {
      "id": "class-templates_b57",
      "type": "paragraph",
      "html": "<p>There is one case that can be misleading. Consider the following version of <code>print()</code>:</p>",
      "preview": "There is one case that can be misleading. Consider the following version of prin",
      "canCheck": true
    },
    {
      "id": "class-templates_b58",
      "type": "code",
      "language": "cpp",
      "code": "template <typename T, typename U>\nstruct Pair // defines a class type named Pair\n{\n    T first{};\n    U second{};\n};\n\ntemplate <typename Pair> // defines a type template parameter named Pair (shadows Pair class type)\nvoid print(Pair p)       // this refers to template parameter Pair, not class type Pair\n{\n    std::cout << '[' << p.first << \", \" << p.second << ']';\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">template &lt;typename T, typename U&gt;\nstruct Pair // defines a class type named Pair\n{\n    T first{};\n    U second{};\n};\n\ntemplate &lt;typename Pair&gt; // defines a type template parameter named Pair (shadows Pair class type)\nvoid print(Pair p)       // this refers to template parameter Pair, not class type Pair\n{\n    std::cout &lt;&lt; '[' &lt;&lt; p.first &lt;&lt; \", \" &lt;&lt; p.second &lt;&lt; ']';\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b59",
      "type": "paragraph",
      "html": "<p>You might expect that this function will only match when called with a <code>Pair</code> class type argument. But this version of <code>print()</code> is functionally identically to the prior version where the template parameter was named <code>T</code>, and will match with <em>any</em> type. The issue here is that when we define <code>Pair</code> as a type template parameter, it shadows other uses of the name <code>Pair</code> within the global scope. So within the function template, <code>Pair</code> refers to the template parameter <code>Pair</code>, not the class type <code>Pair</code>. And since a type template parameter will match to any type, this <code>Pair</code> matches to any argument type, not just those of class type <code>Pair</code>!</p>",
      "preview": "You might expect that this function will only match when called with a Pair clas",
      "canCheck": true
    },
    {
      "id": "class-templates_b60",
      "type": "paragraph",
      "html": "<p>This is a good reason to stick to simple template parameter names, such a <code>T</code>, <code>U</code>, <code>N</code>, as they are less likely to shadow a class type name.</p>",
      "preview": "This is a good reason to stick to simple template parameter names, such a T, U, ",
      "canCheck": true
    },
    {
      "id": "class-templates_b61",
      "type": "section",
      "title": "std::pair",
      "html": "<h3 class=\"cpp-section-heading\">std::pair</h3>",
      "canCheck": false
    },
    {
      "id": "class-templates_b62",
      "type": "paragraph",
      "html": "<p>Because working with pairs of data is common, the C++ standard library contains a class template named <code>std::pair</code> (in the <code>&lt;utility&gt;</code> header) that is defined identically to the <code>Pair</code> class template with multiple template types in the preceding section. In fact, we can swap out the <code>pair</code> struct we developed for <code>std::pair</code>:</p>",
      "preview": "Because working with pairs of data is common, the C++ standard library contains ",
      "canCheck": true
    },
    {
      "id": "class-templates_b63",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n#include <utility>\n\ntemplate <typename T, typename U>\nvoid print(std::pair<T, U> p)\n{\n    // the members of std::pair have predefined names `first` and `second`\n    std::cout << '[' << p.first << \", \" << p.second << ']';\n}\n\nint main()\n{\n    std::pair<int, double> p1{ 1, 2.3 }; // a pair holding an int and a double\n    std::pair<double, int> p2{ 4.5, 6 }; // a pair holding a double and an int\n    std::pair<int, int> p3{ 7, 8 };      // a pair holding two ints\n\n    print(p2);\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n#include &lt;utility&gt;\n\ntemplate &lt;typename T, typename U&gt;\nvoid print(std::pair&lt;T, U&gt; p)\n{\n    // the members of std::pair have predefined names `first` and `second`\n    std::cout &lt;&lt; '[' &lt;&lt; p.first &lt;&lt; \", \" &lt;&lt; p.second &lt;&lt; ']';\n}\n\nint main()\n{\n    std::pair&lt;int, double&gt; p1{ 1, 2.3 }; // a pair holding an int and a double\n    std::pair&lt;double, int&gt; p2{ 4.5, 6 }; // a pair holding a double and an int\n    std::pair&lt;int, int&gt; p3{ 7, 8 };      // a pair holding two ints\n\n    print(p2);\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b64",
      "type": "paragraph",
      "html": "<p>We developed our own <code>Pair</code> class in this lesson to show how things work, but in real code, you should favor <code>std::pair</code> over writing your own.</p>",
      "preview": "We developed our own Pair class in this lesson to show how things work, but in r",
      "canCheck": true
    },
    {
      "id": "class-templates_b65",
      "type": "section",
      "title": "Using class templates in multiple files",
      "html": "<h3 class=\"cpp-section-heading\">Using class templates in multiple files</h3>",
      "canCheck": false
    },
    {
      "id": "class-templates_b66",
      "type": "paragraph",
      "html": "<p>Just like function templates, class templates are typically defined in header files so they can be included into any code file that needs them. Both template definitions and type definitions are exempt from the one-definition rule, so this won’t cause problems:</p>",
      "preview": "Just like function templates, class templates are typically defined in header fi",
      "canCheck": true
    },
    {
      "id": "class-templates_b67",
      "type": "paragraph",
      "html": "<p>pair.h:</p>",
      "preview": "pair.h:",
      "canCheck": true
    },
    {
      "id": "class-templates_b68",
      "type": "code",
      "language": "cpp",
      "code": "#ifndef PAIR_H\n#define PAIR_H\n\ntemplate <typename T>\nstruct Pair\n{\n    T first{};\n    T second{};\n};\n\ntemplate <typename T>\nconstexpr T max(Pair<T> p)\n{\n    return (p.first < p.second ? p.second : p.first);\n}\n\n#endif",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#ifndef PAIR_H\n#define PAIR_H\n\ntemplate &lt;typename T&gt;\nstruct Pair\n{\n    T first{};\n    T second{};\n};\n\ntemplate &lt;typename T&gt;\nconstexpr T max(Pair&lt;T&gt; p)\n{\n    return (p.first &lt; p.second ? p.second : p.first);\n}\n\n#endif</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b69",
      "type": "paragraph",
      "html": "<p>foo.cpp:</p>",
      "preview": "foo.cpp:",
      "canCheck": true
    },
    {
      "id": "class-templates_b70",
      "type": "code",
      "language": "cpp",
      "code": "#include \"pair.h\"\n#include <iostream>\n\nvoid foo()\n{\n    Pair<int> p1{ 1, 2 };\n    std::cout << max(p1) << \" is larger\\n\";\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include \"pair.h\"\n#include &lt;iostream&gt;\n\nvoid foo()\n{\n    Pair&lt;int&gt; p1{ 1, 2 };\n    std::cout &lt;&lt; max(p1) &lt;&lt; \" is larger\\n\";\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "class-templates_b71",
      "type": "paragraph",
      "html": "<p>main.cpp:</p>",
      "preview": "main.cpp:",
      "canCheck": true
    },
    {
      "id": "class-templates_b72",
      "type": "code",
      "language": "cpp",
      "code": "#include \"pair.h\"\n#include <iostream>\n\nvoid foo(); // forward declaration for function foo()\n\nint main()\n{\n    Pair<double> p2 { 3.4, 5.6 };\n    std::cout << max(p2) << \" is larger\\n\";\n\n    foo();\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include \"pair.h\"\n#include &lt;iostream&gt;\n\nvoid foo(); // forward declaration for function foo()\n\nint main()\n{\n    Pair&lt;double&gt; p2 { 3.4, 5.6 };\n    std::cout &lt;&lt; max(p2) &lt;&lt; \" is larger\\n\";\n\n    foo();\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    }
  ]
}