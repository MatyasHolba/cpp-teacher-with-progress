{
  "index": 266,
  "chapterId": "21",
  "number": "21.11",
  "title": "21.11 — Overloading typecasts",
  "url": "https://www.learncpp.com/cpp-tutorial/overloading-typecasts/",
  "slug": "overloading-typecasts",
  "scrapedAt": "2026-09-15T16:30:54.691Z",
  "totalBlocks": 53,
  "totalCheckpoints": 49,
  "blocks": [
    {
      "id": "overloading-typecasts_b0",
      "type": "paragraph",
      "html": "<p>In lesson <a href=\"https://www.learncpp.com/cpp-tutorial/explicit-type-conversion-casting-and-static-cast/\">10.6 -- Explicit type conversion (casting) and static_cast</a>, you learned that C++ allows you to convert one data type to another. The following example shows an int being converted into a double:</p>",
      "preview": "In lesson 10.6 -- Explicit type conversion (casting) and static_cast, you learne",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b1",
      "type": "code",
      "language": "cpp",
      "code": "int n{ 5 };\nauto d{ static_cast<double>(n) }; // int cast to a double",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">int n{ 5 };\nauto d{ static_cast&lt;double&gt;(n) }; // int cast to a double</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b2",
      "type": "paragraph",
      "html": "<p>C++ already knows how to convert between the built-in data types. However, by default, C++ doesn’t know how to convert any of our program-defined classes.</p>",
      "preview": "C++ already knows how to convert between the built-in data types. However, by de",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b3",
      "type": "paragraph",
      "html": "<p>In lesson <a href=\"https://www.learncpp.com/cpp-tutorial/converting-constructors-and-the-explicit-keyword/\">14.16 -- Converting constructors and the explicit keyword</a>, we showed how we can use a converting constructor to create a class type object from another type of object. But this only works if the destination type is a class type that can be modified to add such a constructor. What if this is not the case?</p>",
      "preview": "In lesson 14.16 -- Converting constructors and the explicit keyword, we showed h",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b4",
      "type": "paragraph",
      "html": "<p>Take a look at the following class:</p>",
      "preview": "Take a look at the following class:",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b5",
      "type": "code",
      "language": "cpp",
      "code": "class Cents\n{\nprivate:\n    int m_cents{};\npublic:\n    Cents(int cents=0)\n        : m_cents{ cents }\n    {\n    }\n\n    int getCents() const { return m_cents; }\n    void setCents(int cents) { m_cents = cents; }\n};",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">class Cents\n{\nprivate:\n    int m_cents{};\npublic:\n    Cents(int cents=0)\n        : m_cents{ cents }\n    {\n    }\n\n    int getCents() const { return m_cents; }\n    void setCents(int cents) { m_cents = cents; }\n};</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b6",
      "type": "paragraph",
      "html": "<p>This class is pretty simple: it holds some number of cents as an integer, and provides access functions to get and set the number of cents. It also provides a constructor for converting an <code>int</code> into a <code>Cents</code>.</p>",
      "preview": "This class is pretty simple: it holds some number of cents as an integer, and pr",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b7",
      "type": "paragraph",
      "html": "<p>If we can convert an <code>int</code> into a <code>Cents</code> (via a constructor), then we might also want to provide a way to convert a <code>Cents</code> back into an <code>int</code>. In some cases, this might not be desirable, but it does make sense here.</p>",
      "preview": "If we can convert an int into a Cents (via a constructor), then we might also wa",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b8",
      "type": "callout",
      "calloutType": "advanced",
      "html": "<div class=\"cpp-note cpp-lightgraybackground\"><p class=\"cpp-note-title cpp-bottomline\">Author’s note</p><p>A poem:</p><p>Converting our ints into some Cents<br>Will use a constructor that assents<br>Of course Cents to ints also make sense<br>But the compiler errors and prevents.</p><p>To allow such conversion events<br>We give the compiler our consents<br>And then define how, for all intents<br>We can transform these type of contents.</p><p>So what’s the syntax that circumvents<br>The compiler’s static type defense?<br>We’ll detail just how quite soon, and hence<br>You’ll no longer be kept in suspense.</p></div>",
      "preview": "Author’s noteA poem:Converting our ints into some CentsWill use a constructor that assentsOf course ",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b9",
      "type": "paragraph",
      "html": "<p>One not-great way is to use a conversion function. In this example, we use member function <code>getCents()</code> to “convert” our <code>Cents</code> variable back into an <code>int</code> so we can print it using <code>printInt()</code>:</p>",
      "preview": "One not-great way is to use a conversion function. In this example, we use membe",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b10",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nvoid printInt(int value)\n{\n    std::cout << value;\n}\n\nint main()\n{\n    Cents cents{ 7 };\n    printInt(cents.getCents()); // print 7\n\n    std::cout << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nvoid printInt(int value)\n{\n    std::cout &lt;&lt; value;\n}\n\nint main()\n{\n    Cents cents{ 7 };\n    printInt(cents.getCents()); // print 7\n\n    std::cout &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b11",
      "type": "paragraph",
      "html": "<p>While this function produces the result we want, it’s not truly a conversion, as the compiler won’t understand that it should use this function for casts or implicit conversion. This also means that if we do a lot of <code>Cents</code> to <code>int</code> conversions, our code will be littered with calls to getCents(), which is messy.</p>",
      "preview": "While this function produces the result we want, it’s not truly a conversion, as",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b12",
      "type": "paragraph",
      "html": "<p>What else can we do?</p>",
      "preview": "What else can we do?",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b13",
      "type": "section",
      "title": "Overloading a typecast",
      "html": "<h3 class=\"cpp-section-heading\">Overloading a typecast</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-typecasts_b14",
      "type": "paragraph",
      "html": "<p>This is where overloading the typecast operators comes into play. Such a typecast can be used explicitly (via a cast) or implicitly by the compiler to perform conversions as needed.</p>",
      "preview": "This is where overloading the typecast operators comes into play. Such a typecas",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b15",
      "type": "paragraph",
      "html": "<p>Let’s show how we overload a typecast to define a conversion from <code>Cents</code> to <code>int</code>:</p>",
      "preview": "Let’s show how we overload a typecast to define a conversion from Cents to int:",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b16",
      "type": "code",
      "language": "cpp",
      "code": "class Cents\n{\nprivate:\n    int m_cents{};\npublic:\n    Cents(int cents=0)\n        : m_cents{ cents }\n    {\n    }\n\n    // Overloaded int cast\n    operator int() const { return m_cents; }\n\n    int getCents() const { return m_cents; }\n    void setCents(int cents) { m_cents = cents; }\n};",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">class Cents\n{\nprivate:\n    int m_cents{};\npublic:\n    Cents(int cents=0)\n        : m_cents{ cents }\n    {\n    }\n\n    // Overloaded int cast\n    operator int() const { return m_cents; }\n\n    int getCents() const { return m_cents; }\n    void setCents(int cents) { m_cents = cents; }\n};</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b17",
      "type": "paragraph",
      "html": "<p>To do so, we’ve written a new overloaded operator named <code>operator int()</code>. Note that there is a space between the word operator and the type we are casting to.</p>",
      "preview": "To do so, we’ve written a new overloaded operator named operator int(). Note tha",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b18",
      "type": "paragraph",
      "html": "<p>There are a few things worth noting here:</p>",
      "preview": "There are a few things worth noting here:",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b19",
      "type": "list",
      "html": "<ul><li>Overloaded typecasts must be non-static members, and should be <code>const</code> so they can be used with const objects.</li><li>Overloaded typecasts do not have explicit parameters, as there is no way to pass explicit arguments to them. They do still have a hidden <code>*this</code> parameter, pointing to the implicit object (which is the object to be converted).</li><li>Overloaded typecast do not declare a return type. The name of the conversion (e.g. int) is used as the return type, as it is the only return type allowed. This prevents redundancy in the declaration.</li></ul>",
      "preview": "Overloaded typecasts must be non-static members, and should be const so they can",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b20",
      "type": "paragraph",
      "html": "<p>Now in our example, we can call <code>printInt()</code> like this:</p>",
      "preview": "Now in our example, we can call printInt() like this:",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b21",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nint main()\n{\n    Cents cents{ 7 };\n    printInt(cents); // print 7\n\n    std::cout << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nint main()\n{\n    Cents cents{ 7 };\n    printInt(cents); // print 7\n\n    std::cout &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b22",
      "type": "paragraph",
      "html": "<p>The compiler will first note that function <code>printInt()</code> has an <code>int</code> parameter. Then it will note that variable <code>cents</code> is not an <code>int</code>. Finally, it will look to see if we’ve provided a way to convert a <code>Cents</code> into an <code>int</code>. Since we have, it will call our <code>operator int()</code> function, which returns an <code>int</code>, and the returned <code>int</code> will be passed to <code>printInt()</code>.</p>",
      "preview": "The compiler will first note that function printInt() has an int parameter. Then",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b23",
      "type": "paragraph",
      "html": "<p>Such typecasts can also be invoked explicitly via <code>static_cast</code>:</p>",
      "preview": "Such typecasts can also be invoked explicitly via static_cast:",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b24",
      "type": "code",
      "language": "cpp",
      "code": "std::cout << static_cast<int>(cents);",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">std::cout &lt;&lt; static_cast&lt;int&gt;(cents);</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b25",
      "type": "paragraph",
      "html": "<p>You can provide overloaded typecasts for any data type you wish, including your own program-defined data types!</p>",
      "preview": "You can provide overloaded typecasts for any data type you wish, including your ",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b26",
      "type": "paragraph",
      "html": "<p>Here’s a new class called <code>Dollars</code> that provides an overloaded <code>Cents</code> conversion:</p>",
      "preview": "Here’s a new class called Dollars that provides an overloaded Cents conversion:",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b27",
      "type": "code",
      "language": "cpp",
      "code": "class Dollars\n{\nprivate:\n    int m_dollars{};\npublic:\n    Dollars(int dollars=0)\n        : m_dollars{ dollars }\n    {\n    }\n\n     // Allow us to convert Dollars into Cents\n     operator Cents() const { return Cents{ m_dollars * 100 }; }\n};",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">class Dollars\n{\nprivate:\n    int m_dollars{};\npublic:\n    Dollars(int dollars=0)\n        : m_dollars{ dollars }\n    {\n    }\n\n     // Allow us to convert Dollars into Cents\n     operator Cents() const { return Cents{ m_dollars * 100 }; }\n};</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b28",
      "type": "paragraph",
      "html": "<p>This allows us to convert a <code>Dollars</code> object directly into a <code>Cents</code> object! This allows you to do something like this:</p>",
      "preview": "This allows us to convert a Dollars object directly into a Cents object! This al",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b29",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nclass Cents\n{\nprivate:\n    int m_cents{};\npublic:\n    Cents(int cents=0)\n        : m_cents{ cents }\n    {\n    }\n\n    // Overloaded int cast\n    operator int() const { return m_cents; }\n\n    int getCents() const { return m_cents; }\n    void setCents(int cents) { m_cents = cents; }\n};\n\nclass Dollars\n{\nprivate:\n    int m_dollars{};\npublic:\n    Dollars(int dollars=0)\n        : m_dollars{ dollars }\n    {\n    }\n\n    // Allow us to convert Dollars into Cents\n    operator Cents() const { return Cents { m_dollars * 100 }; }\n};\n\nvoid printCents(Cents cents)\n{\n    std::cout << cents; // cents will be implicitly cast to an int here\n}\n\nint main()\n{\n    Dollars dollars{ 9 };\n    printCents(dollars); // dollars will be implicitly cast to a Cents here\n\n    std::cout << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nclass Cents\n{\nprivate:\n    int m_cents{};\npublic:\n    Cents(int cents=0)\n        : m_cents{ cents }\n    {\n    }\n\n    // Overloaded int cast\n    operator int() const { return m_cents; }\n\n    int getCents() const { return m_cents; }\n    void setCents(int cents) { m_cents = cents; }\n};\n\nclass Dollars\n{\nprivate:\n    int m_dollars{};\npublic:\n    Dollars(int dollars=0)\n        : m_dollars{ dollars }\n    {\n    }\n\n    // Allow us to convert Dollars into Cents\n    operator Cents() const { return Cents { m_dollars * 100 }; }\n};\n\nvoid printCents(Cents cents)\n{\n    std::cout &lt;&lt; cents; // cents will be implicitly cast to an int here\n}\n\nint main()\n{\n    Dollars dollars{ 9 };\n    printCents(dollars); // dollars will be implicitly cast to a Cents here\n\n    std::cout &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b30",
      "type": "paragraph",
      "html": "<p>Consequently, this program will print the value:</p>",
      "preview": "Consequently, this program will print the value:",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b31",
      "type": "output",
      "text": "900\n",
      "html": "<pre>900\n</pre>",
      "canCheck": false
    },
    {
      "id": "overloading-typecasts_b32",
      "type": "paragraph",
      "html": "<p>which makes sense, since 9 dollars is 900 cents!</p>",
      "preview": "which makes sense, since 9 dollars is 900 cents!",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b33",
      "type": "paragraph",
      "html": "<p>Although this demonstrates that such a thing is possible, in this case, adding a converting constructor to <code>Dollars</code> (with a parameter of type <code>Cents</code>) is actually preferable. We’ll discuss why below.</p>",
      "preview": "Although this demonstrates that such a thing is possible, in this case, adding a",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b34",
      "type": "section",
      "title": "Explicit typecasts",
      "html": "<h3 class=\"cpp-section-heading\">Explicit typecasts</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-typecasts_b35",
      "type": "paragraph",
      "html": "<p>Just like we can make constructors <code>explicit</code> so that they can’t be used for implicit conversions, we can also make our overloaded typecasts <code>explicit</code> for the same reason. Explicit typecasts can only be invoked by casting (e.g. <code>static_cast</code>) or by a form of direct initialization (either parenthesis or brace). They are not considered when doing copy-initialization.</p>",
      "preview": "Just like we can make constructors explicit so that they can’t be used for impli",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b36",
      "type": "code",
      "language": "cpp",
      "code": "#include <iostream>\n\nclass Cents\n{\nprivate:\n    int m_cents{};\npublic:\n    Cents(int cents=0)\n        : m_cents{ cents }\n    {\n    }\n\n    explicit operator int() const { return m_cents; } // now marked as explicit\n\n    int getCents() const { return m_cents; }\n    void setCents(int cents) { m_cents = cents; }\n};\n\nclass Dollars\n{\nprivate:\n    int m_dollars{};\npublic:\n    Dollars(int dollars=0)\n        : m_dollars{ dollars }\n    {\n    }\n\n    operator Cents() const { return Cents { m_dollars * 100 }; }\n};\n\nvoid printCents(Cents cents)\n{\n//  std::cout << cents;                   // no longer works because cents won't implicit convert to an int\n    std::cout << static_cast<int>(cents); // we can use an explicit cast instead\n}\n\nint main()\n{\n    Dollars dollars{ 9 };\n    printCents(dollars); // implicit conversion from Dollars to Cents okay because its not marked as explicit\n\n    std::cout << '\\n';\n\n    return 0;\n}",
      "html": "<pre class=\"language-cpp line-numbers\"><code class=\"language-cpp match-braces\">#include &lt;iostream&gt;\n\nclass Cents\n{\nprivate:\n    int m_cents{};\npublic:\n    Cents(int cents=0)\n        : m_cents{ cents }\n    {\n    }\n\n    explicit operator int() const { return m_cents; } // now marked as explicit\n\n    int getCents() const { return m_cents; }\n    void setCents(int cents) { m_cents = cents; }\n};\n\nclass Dollars\n{\nprivate:\n    int m_dollars{};\npublic:\n    Dollars(int dollars=0)\n        : m_dollars{ dollars }\n    {\n    }\n\n    operator Cents() const { return Cents { m_dollars * 100 }; }\n};\n\nvoid printCents(Cents cents)\n{\n//  std::cout &lt;&lt; cents;                   // no longer works because cents won't implicit convert to an int\n    std::cout &lt;&lt; static_cast&lt;int&gt;(cents); // we can use an explicit cast instead\n}\n\nint main()\n{\n    Dollars dollars{ 9 };\n    printCents(dollars); // implicit conversion from Dollars to Cents okay because its not marked as explicit\n\n    std::cout &lt;&lt; '\\n';\n\n    return 0;\n}</code></pre>",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b37",
      "type": "paragraph",
      "html": "<p>Typecasts should be generally be marked as explicit. Exceptions can be made in cases where the conversion inexpensively converts to a similar user-defined type. Our <code>Dollars::operator Cents()</code> typecast was left non-explicit because there is no reason not to let a <code>Dollars</code> object be used anywhere a <code>Cents</code> is expected.</p>",
      "preview": "Typecasts should be generally be marked as explicit. Exceptions can be made in c",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b38",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>Just like single-parameter converting constructors should be marked as explicit, typecasts should be marked as explicit, except in cases where the type to be converted to is essentially synonymous with the destination type.</p></div>",
      "preview": "Best practiceJust like single-parameter converting constructors should be marked as explicit, typeca",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b39",
      "type": "section",
      "title": "When to use converting constructors vs overloaded typecasts",
      "html": "<h3 class=\"cpp-section-heading\">When to use converting constructors vs overloaded typecasts</h3>",
      "canCheck": false
    },
    {
      "id": "overloading-typecasts_b40",
      "type": "paragraph",
      "html": "<p>Overloaded typecasts and converting constructors perform similar functions:</p>",
      "preview": "Overloaded typecasts and converting constructors perform similar functions:",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b41",
      "type": "list",
      "html": "<ul><li>A converting constructor is a member function of class type B that defines how B is created from A.</li><li>An overloaded typecast is a member function of class type A that defines how A is converted to B.</li></ul>",
      "preview": "A converting constructor is a member function of class type B that defines how B",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b42",
      "type": "paragraph",
      "html": "<p>In both cases, we start with an A, and we end up with a B. The main difference between the two is whether A or B has ownership of how the conversion happens.</p>",
      "preview": "In both cases, we start with an A, and we end up with a B. The main difference b",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b43",
      "type": "paragraph",
      "html": "<p>Since both ways require defining a member function, they can only be used for class types that can be modified. If A is not a class type that can be modified, then you can’t use an overloaded typecast. If B is not a class type that can be modified, then you can’t use a converting constructor. If neither are class types that can be modified, then you will need to use a non-member conversion function instead.</p>",
      "preview": "Since both ways require defining a member function, they can only be used for cl",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b44",
      "type": "paragraph",
      "html": "<p>In cases where A and B are both class types that can be modified, we could use either. But since we need only one, which should we prefer?</p>",
      "preview": "In cases where A and B are both class types that can be modified, we could use e",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b45",
      "type": "paragraph",
      "html": "<p>In general, a converting constructor should be preferred to an overloaded typecast. All other things equal, it is cleaner for a class type to own its own construction, rather than rely on another class to create and initialize it.</p>",
      "preview": "In general, a converting constructor should be preferred to an overloaded typeca",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b46",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>When possible, prefer converting constructors, and avoid overloaded typecasts.</p></div>",
      "preview": "Best practiceWhen possible, prefer converting constructors, and avoid overloaded typecasts.",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b47",
      "type": "paragraph",
      "html": "<p>There are a few cases where an overloaded typecast should be used instead:</p>",
      "preview": "There are a few cases where an overloaded typecast should be used instead:",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b48",
      "type": "list",
      "html": "<ul><li>When providing a conversion to a fundamental type (since you can’t define constructors for these types). Most conventionally, these are used to provide a conversion to <code>bool</code> for cases where it makes sense to be able to use an object in a conditional statement.</li><li>When the conversion returns a reference or const reference.</li><li>When providing a conversion to a type you can’t add members to (e.g. a conversion to <code>std::vector</code>, since you can’t define constructors for these types either).</li><li>When you do not want the type being constructed to be aware of the type being converted from. This can be helpful for avoiding circular dependencies.</li></ul>",
      "preview": "When providing a conversion to a fundamental type (since you can’t define constr",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b49",
      "type": "paragraph",
      "html": "<p>For an example of that last bullet, <code>std::string</code> has a constructor to create a <code>std::string</code> from a <code>std::string_view</code>. This means <code>&lt;string&gt;</code> must include <code>&lt;string_view&gt;</code>. If <code>std::string_view</code> had a constructor to create a <code>std::string_view</code> from a <code>std::string</code>, then <code>&lt;string_view&gt;</code> would need to include <code>&lt;string&gt;</code>, and this would result in a circular dependency between headers.</p>",
      "preview": "For an example of that last bullet, std::string has a constructor to create a st",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b50",
      "type": "paragraph",
      "html": "<p>Instead, <code>std::string</code> has an overloaded typecast that handles conversion from <code>std::string</code> to <code>std::string_view</code> (which is fine, since it’s already including <code>&lt;string_view&gt;</code>). <code>std::string_view</code> does not know about <code>std::string</code> at all, and thus does not need to include <code>&lt;string&gt;</code>. In this way, the circular dependency is avoided.</p>",
      "preview": "Instead, std::string has an overloaded typecast that handles conversion from std",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b51",
      "type": "paragraph",
      "html": "<p>When a converting constructor and an overloaded typecast are both defined for the same conversion, both are considered during overload resolution. Depending on whether the overloaded typecast is const, the object being converted is const, and what type of cast or initialization is used (copy vs direct), either function might be chosen (which can result a typecast being selected over a converting constructor), or the result can be ambiguous (resulting in a compile error). For this reason, you should avoid defining both an overloaded typecast and a converting constructor that can serve the same conversion.</p>",
      "preview": "When a converting constructor and an overloaded typecast are both defined for th",
      "canCheck": true
    },
    {
      "id": "overloading-typecasts_b52",
      "type": "callout",
      "calloutType": "best-practice",
      "html": "<div class=\"cpp-note cpp-lightgreenbackground\"><p class=\"cpp-note-title cpp-bottomline\">Best practice</p><p>When you need to define how convert type A to type B:</p><ul><li>If B is a class type you can modify, prefer using a converting constructor to create B from A.</li><li>Otherwise, if A is a class type you can modify, use an overloaded typecast to convert A to B.</li><li>Otherwise use a non-member function to convert A to B.</li></ul></div>",
      "preview": "Best practiceWhen you need to define how convert type A to type B:If B is a class type you can modif",
      "canCheck": true
    }
  ]
}